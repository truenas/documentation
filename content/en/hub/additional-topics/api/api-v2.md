---
title: FreeNAS RESTful API v2.0
language_tabs:
  - shell: Shell
  - python: Python
  - java: Java
language_clients:
  - shell: ""
  - python: ""
  - java: ""
toc_footers: []
includes: []
search: true
highlight_theme: darkula
headingLevel: 2
generator: widdershins v4.0.1

---

<h1 id="freenas-restful-api">FreeNAS RESTful API v2.0</h1>

> Scroll down for code samples, example requests and responses. Select a language for code samples from the tabs above or the mobile navigation menu.

Base URLs:

* <a href="http://192.168.1.123/api/v2.0">http://192.168.1.123/api/v2.0</a>

# Authentication

- HTTP Authentication, scheme: basic 

<h1 id="freenas-restful-api-core">core</h1>

## post__core_bulk

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/core/bulk \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/core/bulk', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/core/bulk");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /core/bulk`

Will loop on a list of items for the given method, returning a list of
dicts containing a result and error key.

Result will be the message returned by the method being called,
or a string of an error, in which case the error key will be the
exception

> Body parameter

```json
{
  "method": "string",
  "params": []
}
```

<h3 id="post__core_bulk-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[core_bulk](#schemacore_bulk)|false|none|

<h3 id="post__core_bulk-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__core_debug

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/core/debug \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/core/debug', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/core/debug");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /core/debug`

Setup middlewared for remote debugging.

engines:
  - PTVS: Python Visual Studio
  - PYDEV: Python Dev (Eclipse/PyCharm)
  - REMOTE_PDB: Remote vanilla PDB (over TCP sockets)

options:
  - secret: password for PTVS
  - host: required for PYDEV, hostname of local computer (developer workstation)
  - local_path: required for PYDEV, path for middlewared source in local computer (e.g. /home/user/freenas/src/middlewared/middlewared
  - threaded: run debugger in a new thread instead of event loop

> Body parameter

```json
{
  "engine": "PTVS",
  "options": {}
}
```

<h3 id="post__core_debug-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[core_debug](#schemacore_debug)|false|none|

<h3 id="post__core_debug-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__core_download

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/core/download \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/core/download', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/core/download");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /core/download`

Core helper to call a job marked for download.

Returns the job id and the URL for download.

> Body parameter

```json
{
  "method": "string",
  "args": [],
  "filename": "string"
}
```

<h3 id="post__core_download-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[core_download](#schemacore_download)|false|none|

<h3 id="post__core_download-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__core_get_events

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/core/get_events

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/core/get_events')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/core/get_events");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /core/get_events`

Returns metadata for every possible event emitted from websocket server.

<h3 id="get__core_get_events-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__core_get_jobs

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/core/get_jobs

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/core/get_jobs')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/core/get_jobs");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /core/get_jobs`

Get the long running jobs.

<h3 id="get__core_get_jobs-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__core_get_jobs-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__core_get_methods

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/core/get_methods \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/core/get_methods', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/core/get_methods");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /core/get_methods`

Return methods metadata of every available service.

`service` parameter is optional and filters the result for a single service.

> Body parameter

```json
null
```

<h3 id="post__core_get_methods-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[core_get_methods_0](#schemacore_get_methods_0)|false|none|

<h3 id="post__core_get_methods-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__core_get_services

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/core/get_services

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/core/get_services')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/core/get_services");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /core/get_services`

Returns a list of all registered services.

<h3 id="get__core_get_services-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__core_job_abort

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/core/job_abort \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/core/job_abort', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/core/job_abort");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /core/job_abort`

> Body parameter

```json
0
```

<h3 id="post__core_job_abort-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[core_job_abort_0](#schemacore_job_abort_0)|false|none|

<h3 id="post__core_job_abort-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__core_job_update

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/core/job_update \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/core/job_update', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/core/job_update");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /core/job_update`

> Body parameter

```json
{
  "id": 0,
  "job-update": {}
}
```

<h3 id="post__core_job_update-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[core_job_update](#schemacore_job_update)|false|none|

<h3 id="post__core_job_update-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__core_job_wait

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/core/job_wait \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/core/job_wait', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/core/job_wait");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /core/job_wait`

> Body parameter

```json
0
```

<h3 id="post__core_job_wait-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[core_job_wait_0](#schemacore_job_wait_0)|false|none|

<h3 id="post__core_job_wait-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__core_ping

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/core/ping

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/core/ping')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/core/ping");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /core/ping`

Utility method which just returns "pong".
Can be used to keep connection/authtoken alive instead of using
"ping" protocol message.

<h3 id="get__core_ping-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__core_ping_remote

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/core/ping_remote \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/core/ping_remote', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/core/ping_remote");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /core/ping_remote`

Method that will send an ICMP echo request to "hostname"
and will wait up to "timeout" for a reply.

> Body parameter

```json
{}
```

<h3 id="post__core_ping_remote-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[core_ping_remote_0](#schemacore_ping_remote_0)|false|none|

<h3 id="post__core_ping_remote-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__core_resize_shell

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/core/resize_shell \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/core/resize_shell', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/core/resize_shell");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /core/resize_shell`

Resize terminal session (/websocket/shell) to cols x rows

> Body parameter

```json
{
  "id": "string",
  "cols": 0,
  "rows": 0
}
```

<h3 id="post__core_resize_shell-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[core_resize_shell](#schemacore_resize_shell)|false|none|

<h3 id="post__core_resize_shell-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__core_sessions

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/core/sessions

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/core/sessions')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/core/sessions");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /core/sessions`

Get currently open websocket sessions.

<h3 id="get__core_sessions-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__core_sessions-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-acme-dns-authenticator">acme.dns.authenticator</h1>

## get__acme_dns_authenticator

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/acme/dns/authenticator

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/acme/dns/authenticator')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/acme/dns/authenticator");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /acme/dns/authenticator`

<h3 id="get__acme_dns_authenticator-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__acme_dns_authenticator-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__acme_dns_authenticator

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/acme/dns/authenticator \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/acme/dns/authenticator', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/acme/dns/authenticator");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /acme/dns/authenticator`

Create a DNS Authenticator

Create a specific DNS Authenticator containing required authentication details for the said
provider to successfully connect with it

> Body parameter

```json
{}
```

<h3 id="post__acme_dns_authenticator-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[acme_dns_authenticator_create_0](#schemaacme_dns_authenticator_create_0)|false|none|

<h3 id="post__acme_dns_authenticator-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__acme_dns_authenticator_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/acme/dns/authenticator/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/acme/dns/authenticator/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/acme/dns/authenticator/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /acme/dns/authenticator/id/{id}`

Delete DNS Authenticator of `id`

<h3 id="delete__acme_dns_authenticator_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__acme_dns_authenticator_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__acme_dns_authenticator_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/acme/dns/authenticator/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/acme/dns/authenticator/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/acme/dns/authenticator/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /acme/dns/authenticator/id/{id}`

<h3 id="get__acme_dns_authenticator_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__acme_dns_authenticator_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__acme_dns_authenticator_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/acme/dns/authenticator/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/acme/dns/authenticator/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/acme/dns/authenticator/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /acme/dns/authenticator/id/{id}`

Update DNS Authenticator of `id`

> Body parameter

```json
{}
```

<h3 id="put__acme_dns_authenticator_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[acme_dns_authenticator_update_1](#schemaacme_dns_authenticator_update_1)|false|none|

<h3 id="put__acme_dns_authenticator_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__acme_dns_authenticator_authenticator_schemas

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/acme/dns/authenticator/authenticator_schemas

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/acme/dns/authenticator/authenticator_schemas')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/acme/dns/authenticator/authenticator_schemas");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /acme/dns/authenticator/authenticator_schemas`

Get the schemas for all DNS providers we support for ACME DNS Challenge and the respective attributes
required for connecting to them while validating a DNS Challenge

<h3 id="get__acme_dns_authenticator_authenticator_schemas-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-activedirectory">activedirectory</h1>

## get__activedirectory

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/activedirectory

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/activedirectory')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/activedirectory");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /activedirectory`

<h3 id="get__activedirectory-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__activedirectory

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/activedirectory \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/activedirectory', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/activedirectory");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /activedirectory`

Update active directory configuration.
`domainname` full DNS domain name of the Active Directory domain.

`bindname` username used to perform the intial domain join.

`bindpw` password used to perform the initial domain join. User-
provided credentials are used to obtain a kerberos ticket, which
is used to perform the actual domain join.

`verbose_logging` increase logging during the domain join process.

`use_default_domain` controls whether domain users and groups have
the pre-windows 2000 domain name prepended to the user account. When
enabled, the user appears as "administrator" rather than
"EXAMPLEdministrator"

`allow_trusted_doms` enable support for trusted domains. If this
parameter is enabled, then separate idmap backends _must_ be configured
for each trusted domain, and the idmap cache should be cleared.

`allow_dns_updates` during the domain join process, automatically
generate DNS entries in the AD domain for the NAS. If this is disabled,
then a domain administrator must manually add appropriate DNS entries
for the NAS. This parameter is recommended for TrueNAS HA servers.

`disable_freenas_cache` disables active caching of AD users and groups.
When disabled, only users cached in winbind's internal cache are
visible in GUI dropdowns. Disabling active caching is recommended
in environments with a large amount of users.

`site` AD site of which the NAS is a member. This parameter is auto-
detected during the domain join process. If no AD site is configured
for the subnet in which the NAS is configured, then this parameter
appears as 'Default-First-Site-Name'. Auto-detection is only performed
during the initial domain join.

`kerberos_realm` in which the server is located. This parameter is
automatically populated during the initial domain join. If the NAS has
an AD site configured and that site has multiple kerberos servers, then
the kerberos realm is automatically updated with a site-specific
configuration to use those servers. Auto-detection is only performed
during initial domain join.

`kerberos_principal` kerberos principal to use for AD-related
operations outside of Samba. After intial domain join, this field is
updated with the kerberos principal associated with the AD machine
account for the NAS.

`nss_info` controls how Winbind retrieves Name Service Information to
construct a user's home directory and login shell. This parameter
is only effective if the Active Directory Domain Controller supports
the Microsoft Services for Unix (SFU) LDAP schema.

`timeout` timeout value for winbind-related operations. This value may
need to be increased in  environments with high latencies for
communications with domain controllers or a large number of domain
controllers. Lowering the value may cause status checks to fail.

`dns_timeout` timeout value for DNS queries during the initial domain
join. This value is also set as the NETWORK_TIMEOUT in the ldap config
file.

`createcomputer` Active Directory Organizational Unit in which new
computer accounts are created.

The OU string is read from top to bottom without RDNs. Slashes ("/")
are used as delimiters, like `Computers/Servers/NAS`. The backslash
("\") is used to escape characters but not as a separator. Backslashes
are interpreted at multiple levels and might require doubling or even
quadrupling to take effect.

When this field is blank, new computer accounts are created in the
Active Directory default OU.

The Active Directory service is started after a configuration
update if the service was initially disabled, and the updated
configuration sets `enable` to `True`. The Active Directory
service is stopped if `enable` is changed to `False`. If the
configuration is updated, but the initial `enable` state is `True`, and
remains unchanged, then the samba server is only restarted.

During the domain join, a kerberos keytab for the newly-created AD
machine account is generated. It is used for all future
LDAP / AD interaction and the user-provided credentials are removed.

> Body parameter

```json
{}
```

<h3 id="put__activedirectory-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[activedirectory_update_0](#schemaactivedirectory_update_0)|false|none|

<h3 id="put__activedirectory-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__activedirectory_change_trust_account_pw

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/activedirectory/change_trust_account_pw

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/activedirectory/change_trust_account_pw')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/activedirectory/change_trust_account_pw");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /activedirectory/change_trust_account_pw`

Force an update of the AD machine account password. This can be used to
refresh the Kerberos principals in the server's system keytab.

<h3 id="get__activedirectory_change_trust_account_pw-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__activedirectory_domain_info

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/activedirectory/domain_info

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/activedirectory/domain_info')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/activedirectory/domain_info");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /activedirectory/domain_info`

Returns the following information about the currently joined domain:

`LDAP server` IP address of current LDAP server to which TrueNAS is connected.

`LDAP server name` DNS name of LDAP server to which TrueNAS is connected

`Realm` Kerberos realm

`LDAP port`

`Server time` timestamp.

`KDC server` Kerberos KDC to which TrueNAS is connected

`Server time offset` current time offset from DC.

`Last machine account password change`. timestamp

<h3 id="get__activedirectory_domain_info-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__activedirectory_get_spn_list

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/activedirectory/get_spn_list

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/activedirectory/get_spn_list')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/activedirectory/get_spn_list");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /activedirectory/get_spn_list`

Return list of kerberos SPN entries registered for the server's Active
Directory computer account. This may not reflect the state of the
server's current kerberos keytab.

<h3 id="get__activedirectory_get_spn_list-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__activedirectory_get_state

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/activedirectory/get_state

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/activedirectory/get_state')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/activedirectory/get_state");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /activedirectory/get_state`

Wrapper function for 'directoryservices.get_state'. Returns only the state of the
Active Directory service.

<h3 id="get__activedirectory_get_state-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__activedirectory_leave

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/activedirectory/leave \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/activedirectory/leave', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/activedirectory/leave");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /activedirectory/leave`

Leave Active Directory domain. This will remove computer
object from AD and clear relevant configuration data from
the NAS.
This requires credentials for appropriately-privileged user.
Credentials are used to obtain a kerberos ticket, which is
used to perform the actual removal from the domain.

> Body parameter

```json
{}
```

<h3 id="post__activedirectory_leave-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[activedirectory_leave_0](#schemaactivedirectory_leave_0)|false|none|

<h3 id="post__activedirectory_leave-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__activedirectory_nss_info_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/activedirectory/nss_info_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/activedirectory/nss_info_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/activedirectory/nss_info_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /activedirectory/nss_info_choices`

Returns list of available LDAP schema choices.

<h3 id="get__activedirectory_nss_info_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__activedirectory_started

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/activedirectory/started

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/activedirectory/started')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/activedirectory/started");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /activedirectory/started`

Issue a no-effect command to our DC. This checks if our secure channel connection to our
domain controller is still alive. It has much less impact than wbinfo -t.
Default winbind request timeout is 60 seconds, and can be adjusted by the smb4.conf parameter
'winbind request timeout ='

<h3 id="get__activedirectory_started-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-afp">afp</h1>

## get__afp

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/afp

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/afp')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/afp");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /afp`

<h3 id="get__afp-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__afp

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/afp \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/afp', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/afp");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /afp`

Update AFP service settings.

`bindip` is a list of IPs to bind AFP to. Leave blank (empty list) to bind to all
available IPs.

`map_acls` defines how to map the effective permissions of authenticated users.
RIGHTS - Unix-style permissions
MODE - ACLs
NONE - Do not map

`chmod_request` defines advanced permission control that deals with ACLs.
PRESERVE - Preserve ZFS ACEs for named users and groups or POSIX ACL group mask
SIMPLE - Change permission as requested without any extra steps
IGNORE - Permission change requests are ignored

> Body parameter

```json
{}
```

<h3 id="put__afp-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[afp_update_0](#schemaafp_update_0)|false|none|

<h3 id="put__afp-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__afp_bindip_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/afp/bindip_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/afp/bindip_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/afp/bindip_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /afp/bindip_choices`

List of valid choices for IP addresses to which to bind the AFP service.

<h3 id="get__afp_bindip_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-alert">alert</h1>

## post__alert_dismiss

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/alert/dismiss \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/alert/dismiss', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/alert/dismiss");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /alert/dismiss`

Dismiss `id` alert.

> Body parameter

```json
"string"
```

<h3 id="post__alert_dismiss-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[alert_dismiss_0](#schemaalert_dismiss_0)|false|none|

<h3 id="post__alert_dismiss-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__alert_list

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/alert/list

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/alert/list')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/alert/list");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /alert/list`

List all types of alerts including active/dismissed currently in the system.

<h3 id="get__alert_list-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__alert_list_categories

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/alert/list_categories

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/alert/list_categories')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/alert/list_categories");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /alert/list_categories`

List all types of alerts which the system can issue.

<h3 id="get__alert_list_categories-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__alert_list_policies

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/alert/list_policies

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/alert/list_policies')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/alert/list_policies");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /alert/list_policies`

List all alert policies which indicate the frequency of the alerts.

<h3 id="get__alert_list_policies-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__alert_restore

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/alert/restore \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/alert/restore', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/alert/restore");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /alert/restore`

Restore `id` alert which had been dismissed.

> Body parameter

```json
"string"
```

<h3 id="post__alert_restore-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[alert_restore_0](#schemaalert_restore_0)|false|none|

<h3 id="post__alert_restore-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-alertclasses">alertclasses</h1>

## get__alertclasses

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/alertclasses

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/alertclasses')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/alertclasses");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /alertclasses`

<h3 id="get__alertclasses-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__alertclasses

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/alertclasses \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/alertclasses', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/alertclasses");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /alertclasses`

Update default Alert settings.

> Body parameter

```json
{}
```

<h3 id="put__alertclasses-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[alertclasses_update_0](#schemaalertclasses_update_0)|false|none|

<h3 id="put__alertclasses-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-alertservice">alertservice</h1>

## get__alertservice

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/alertservice

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/alertservice')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/alertservice");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /alertservice`

<h3 id="get__alertservice-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__alertservice-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__alertservice

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/alertservice \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/alertservice', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/alertservice");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /alertservice`

Create an Alert Service of specified `type`.

If `enabled`, it sends alerts to the configured `type` of Alert Service.

> Body parameter

```json
{}
```

<h3 id="post__alertservice-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[alertservice_create_0](#schemaalertservice_create_0)|false|none|

<h3 id="post__alertservice-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__alertservice_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/alertservice/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/alertservice/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/alertservice/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /alertservice/id/{id}`

Delete Alert Service of `id`.

<h3 id="delete__alertservice_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__alertservice_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__alertservice_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/alertservice/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/alertservice/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/alertservice/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /alertservice/id/{id}`

<h3 id="get__alertservice_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__alertservice_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__alertservice_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/alertservice/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/alertservice/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/alertservice/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /alertservice/id/{id}`

Update Alert Service of `id`.

> Body parameter

```json
{}
```

<h3 id="put__alertservice_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[alertservice_update_1](#schemaalertservice_update_1)|false|none|

<h3 id="put__alertservice_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__alertservice_list_types

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/alertservice/list_types

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/alertservice/list_types')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/alertservice/list_types");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /alertservice/list_types`

List all types of supported Alert services which can be configured with the system.

<h3 id="get__alertservice_list_types-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__alertservice_test

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/alertservice/test \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/alertservice/test', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/alertservice/test");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /alertservice/test`

Send a test alert using `type` of Alert Service.

> Body parameter

```json
{}
```

<h3 id="post__alertservice_test-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[alertservice_test_0](#schemaalertservice_test_0)|false|none|

<h3 id="post__alertservice_test-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-api_key">api_key</h1>

## get__api_key

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/api_key

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/api_key')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/api_key");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /api_key`

<h3 id="get__api_key-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__api_key-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__api_key

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/api_key \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/api_key', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/api_key");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /api_key`

Creates API Key.

`name` is a user-readable name for key.

> Body parameter

```json
{}
```

<h3 id="post__api_key-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[api_key_create_0](#schemaapi_key_create_0)|false|none|

<h3 id="post__api_key-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__api_key_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/api_key/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/api_key/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/api_key/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /api_key/id/{id}`

Delete API Key `id`.

<h3 id="delete__api_key_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__api_key_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__api_key_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/api_key/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/api_key/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/api_key/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /api_key/id/{id}`

<h3 id="get__api_key_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__api_key_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__api_key_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/api_key/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/api_key/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/api_key/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /api_key/id/{id}`

Update API Key `id`.

Specify `reset: true` to reset this API Key.

> Body parameter

```json
{}
```

<h3 id="put__api_key_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[api_key_update_1](#schemaapi_key_update_1)|false|none|

<h3 id="put__api_key_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-auth">auth</h1>

## post__auth_check_user

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/auth/check_user \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/auth/check_user', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/auth/check_user");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /auth/check_user`

Verify username and password

> Body parameter

```json
{
  "username": "string",
  "password": "string"
}
```

<h3 id="post__auth_check_user-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[auth_check_user](#schemaauth_check_user)|false|none|

<h3 id="post__auth_check_user-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__auth_generate_token

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/auth/generate_token \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/auth/generate_token', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/auth/generate_token");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /auth/generate_token`

Generate a token to be used for authentication.

`ttl` stands for Time To Live, in seconds. The token will be invalidated if the connection
has been inactive for a time greater than this.

`attrs` is a general purpose object/dictionary to hold information about the token.

> Body parameter

```json
{
  "ttl": 600,
  "attrs": {}
}
```

<h3 id="post__auth_generate_token-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[auth_generate_token](#schemaauth_generate_token)|false|none|

<h3 id="post__auth_generate_token-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__auth_sessions

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/auth/sessions

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/auth/sessions')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/auth/sessions");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /auth/sessions`

Returns list of active auth sessions.

Example of return value:

[
    {
        "id": "NyhB1J5vjPjIV82yZ6caU12HLA1boDJcZNWuVQM4hQWuiyUWMGZTz2ElDp7Yk87d",
        "origin": "192.168.0.3:40392",
        "credentials": "TOKEN",
        "internal": False,
        "created_at": {"$date": 1545842426070}
    }
]

`credentials` can be `UNIX_SOCKET`, `ROOT_TCP_SOCKET`, `TRUENAS_NODE`, `LOGIN_PASSWORD` or `TOKEN`,
depending on what authentication method was used.

If you want to exclude all internal connections from the list, call this method with following arguments:

[
    [
        ["internal", "=", True]
    ]
]

<h3 id="get__auth_sessions-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__auth_sessions-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__auth_two_factor_auth

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/auth/two_factor_auth

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/auth/two_factor_auth')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/auth/two_factor_auth");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /auth/two_factor_auth`

Returns true if two factor authorization is required for authorizing user's login.

<h3 id="get__auth_two_factor_auth-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-auth-twofactor">auth.twofactor</h1>

## get__auth_twofactor

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/auth/twofactor

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/auth/twofactor')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/auth/twofactor");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /auth/twofactor`

<h3 id="get__auth_twofactor-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__auth_twofactor

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/auth/twofactor \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/auth/twofactor', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/auth/twofactor");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /auth/twofactor`

`otp_digits` represents number of allowed digits in the OTP.

`window` extends the validity to `window` many counter ticks before and after the current one.

`interval` is time duration in seconds specifying OTP expiration time from it's creation time.

> Body parameter

```json
{}
```

<h3 id="put__auth_twofactor-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[auth_twofactor_update_0](#schemaauth_twofactor_update_0)|false|none|

<h3 id="put__auth_twofactor-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__auth_twofactor_provisioning_uri

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/auth/twofactor/provisioning_uri

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/auth/twofactor/provisioning_uri')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/auth/twofactor/provisioning_uri");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /auth/twofactor/provisioning_uri`

Returns the provisioning URI for the OTP. This can then be encoded in a QR Code and used to
provision an OTP app like Google Authenticator.

<h3 id="get__auth_twofactor_provisioning_uri-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__auth_twofactor_renew_secret

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/auth/twofactor/renew_secret

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/auth/twofactor/renew_secret')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/auth/twofactor/renew_secret");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /auth/twofactor/renew_secret`

Generates a new secret for Two Factor Authentication. Returns boolean true on success.

<h3 id="get__auth_twofactor_renew_secret-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__auth_twofactor_verify

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/auth/twofactor/verify \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/auth/twofactor/verify', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/auth/twofactor/verify");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /auth/twofactor/verify`

Returns boolean true if provided `token` is successfully authenticated.

> Body parameter

```json
"string"
```

<h3 id="post__auth_twofactor_verify-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[auth_twofactor_verify_0](#schemaauth_twofactor_verify_0)|false|none|

<h3 id="post__auth_twofactor_verify-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-boot">boot</h1>

## post__boot_attach

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/boot/attach \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/boot/attach', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/boot/attach");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /boot/attach`

Attach a disk to the boot pool, turning a stripe into a mirror.

`expand` option will determine whether the new disk partition will be
         the maximum available or the same size as the current disk.

> Body parameter

```json
{
  "dev": "string",
  "options": {}
}
```

<h3 id="post__boot_attach-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[boot_attach](#schemaboot_attach)|false|none|

<h3 id="post__boot_attach-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__boot_detach

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/boot/detach \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/boot/detach', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/boot/detach");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /boot/detach`

Detach given `dev` from boot pool.

> Body parameter

```json
"string"
```

<h3 id="post__boot_detach-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[boot_detach_0](#schemaboot_detach_0)|false|none|

<h3 id="post__boot_detach-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__boot_get_disks

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/boot/get_disks

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/boot/get_disks')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/boot/get_disks");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /boot/get_disks`

Returns disks of the boot pool.

<h3 id="get__boot_get_disks-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__boot_get_scrub_interval

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/boot/get_scrub_interval

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/boot/get_scrub_interval')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/boot/get_scrub_interval");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /boot/get_scrub_interval`

Get Automatic Scrub Interval value in days.

<h3 id="get__boot_get_scrub_interval-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__boot_get_state

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/boot/get_state

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/boot/get_state')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/boot/get_state");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /boot/get_state`

Returns the current state of the boot pool, including all vdevs, properties and datasets.

<h3 id="get__boot_get_state-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__boot_replace

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/boot/replace \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/boot/replace', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/boot/replace");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /boot/replace`

Replace device `label` on boot pool with `dev`.

> Body parameter

```json
{
  "label": "string",
  "dev": "string"
}
```

<h3 id="post__boot_replace-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[boot_replace](#schemaboot_replace)|false|none|

<h3 id="post__boot_replace-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__boot_scrub

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/boot/scrub

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/boot/scrub')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/boot/scrub");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /boot/scrub`

Scrub on boot pool.

<h3 id="get__boot_scrub-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__boot_set_scrub_interval

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/boot/set_scrub_interval \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/boot/set_scrub_interval', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/boot/set_scrub_interval");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /boot/set_scrub_interval`

Set Automatic Scrub Interval value in days.

> Body parameter

```json
0
```

<h3 id="post__boot_set_scrub_interval-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[boot_set_scrub_interval_0](#schemaboot_set_scrub_interval_0)|false|none|

<h3 id="post__boot_set_scrub_interval-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-bootenv">bootenv</h1>

## get__bootenv

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/bootenv

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/bootenv')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/bootenv");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /bootenv`

Query all Boot Environments with `query-filters` and `query-options`.

<h3 id="get__bootenv-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__bootenv-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__bootenv

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/bootenv \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/bootenv', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/bootenv");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /bootenv`

Create a new boot environment using `name`.

If a new boot environment is desired which is a clone of another boot environment, `source` can be passed.
Then, a new boot environment of `name` is created using boot environment `source` by cloning it.

Ensure that `name` and `source` are valid boot environment names.

> Body parameter

```json
{}
```

<h3 id="post__bootenv-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[bootenv_create_0](#schemabootenv_create_0)|false|none|

<h3 id="post__bootenv-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__bootenv_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/bootenv/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/bootenv/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/bootenv/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /bootenv/id/{id}`

Delete `id` boot environment. This removes the clone from the system.

<h3 id="delete__bootenv_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|string|true|none|

<h3 id="delete__bootenv_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__bootenv_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/bootenv/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/bootenv/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/bootenv/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /bootenv/id/{id}`

Query all Boot Environments with `query-filters` and `query-options`.

<h3 id="get__bootenv_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__bootenv_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__bootenv_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/bootenv/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/bootenv/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/bootenv/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /bootenv/id/{id}`

Update `id` boot environment name with a new provided valid `name`.

> Body parameter

```json
{}
```

<h3 id="put__bootenv_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|string|true|none|
|body|body|[bootenv_update_1](#schemabootenv_update_1)|false|none|

<h3 id="put__bootenv_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__bootenv_id_{id}_activate

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/bootenv/id/{id}/activate \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/bootenv/id/{id}/activate', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/bootenv/id/{id}/activate");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /bootenv/id/{id}/activate`

Activates boot environment `id`.

> Body parameter

```json
{}
```

<h3 id="post__bootenv_id_{id}_activate-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|string|true|none|
|body|body|[bootenv_activate](#schemabootenv_activate)|false|none|

<h3 id="post__bootenv_id_{id}_activate-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__bootenv_id_{id}_set_attribute

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/bootenv/id/{id}/set_attribute \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/bootenv/id/{id}/set_attribute', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/bootenv/id/{id}/set_attribute");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /bootenv/id/{id}/set_attribute`

Sets attributes boot environment `id`.

Currently only `keep` attribute is allowed.

> Body parameter

```json
{}
```

<h3 id="post__bootenv_id_{id}_set_attribute-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|string|true|none|
|body|body|[bootenv_set_attribute_1](#schemabootenv_set_attribute_1)|false|none|

<h3 id="post__bootenv_id_{id}_set_attribute-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-certificate">certificate</h1>

## get__certificate

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/certificate

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/certificate')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/certificate");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /certificate`

<h3 id="get__certificate-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__certificate-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__certificate

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/certificate \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/certificate', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/certificate");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /certificate`

Create a new Certificate

Certificates are classified under following types and the necessary keywords to be passed
for `create_type` attribute to create the respective type of certificate

1) Internal Certificate                 -  CERTIFICATE_CREATE_INTERNAL

2) Imported Certificate                 -  CERTIFICATE_CREATE_IMPORTED

3) Certificate Signing Request          -  CERTIFICATE_CREATE_CSR

4) Imported Certificate Signing Request -  CERTIFICATE_CREATE_IMPORTED_CSR

5) ACME Certificate                     -  CERTIFICATE_CREATE_ACME

By default, created certs use RSA keys. If an Elliptic Curve Key is desired, it can be specified with the
`key_type` attribute. If the `ec_curve` attribute is not specified for the Elliptic Curve Key, then default to
using "BrainpoolP384R1" curve.

A type is selected by the Certificate Service based on `create_type`. The rest of the values in `data` are
validated accordingly and finally a certificate is made based on the selected type.

`cert_extensions` can be specified to set X509v3 extensions.

> Body parameter

```json
{}
```

<h3 id="post__certificate-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[certificate_create_0](#schemacertificate_create_0)|false|none|

<h3 id="post__certificate-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__certificate_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/certificate/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.delete('http://192.168.1.123/api/v2.0/certificate/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/certificate/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /certificate/id/{id}`

Delete certificate of `id`.

If the certificate is an ACME based certificate, certificate service will try to
revoke the certificate by updating it's status with the ACME server, if it fails an exception is raised
and the certificate is not deleted from the system. However, if `force` is set to True, certificate is deleted
from the system even if some error occurred while revoking the certificate with the ACME Server

> Body parameter

```json
false
```

<h3 id="delete__certificate_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[certificate_delete_1](#schemacertificate_delete_1)|false|none|

<h3 id="delete__certificate_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__certificate_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/certificate/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/certificate/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/certificate/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /certificate/id/{id}`

<h3 id="get__certificate_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__certificate_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__certificate_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/certificate/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/certificate/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/certificate/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /certificate/id/{id}`

Update certificate of `id`

Only name and revoked attribute can be updated.

When `revoked` is enabled, the specified cert `id` is revoked and if it belongs to a CA chain which
exists on this system, its serial number is added to the CA's certificate revocation list.

> Body parameter

```json
{}
```

<h3 id="put__certificate_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[certificate_update_1](#schemacertificate_update_1)|false|none|

<h3 id="put__certificate_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__certificate_acme_server_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/certificate/acme_server_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/certificate/acme_server_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/certificate/acme_server_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /certificate/acme_server_choices`

Dictionary of popular ACME Servers with their directory URI endpoints which we display automatically
in UI

<h3 id="get__certificate_acme_server_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__certificate_country_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/certificate/country_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/certificate/country_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/certificate/country_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /certificate/country_choices`

Returns country choices for creating a certificate/csr.

<h3 id="get__certificate_country_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__certificate_ec_curve_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/certificate/ec_curve_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/certificate/ec_curve_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/certificate/ec_curve_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /certificate/ec_curve_choices`

Dictionary of supported EC curves.

<h3 id="get__certificate_ec_curve_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__certificate_extended_key_usage_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/certificate/extended_key_usage_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/certificate/extended_key_usage_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/certificate/extended_key_usage_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /certificate/extended_key_usage_choices`

Dictionary of choices for `ExtendedKeyUsage` extension which can be passed over to `usages` attribute.

<h3 id="get__certificate_extended_key_usage_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__certificate_key_type_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/certificate/key_type_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/certificate/key_type_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/certificate/key_type_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /certificate/key_type_choices`

Dictionary of supported key types for certificates.

<h3 id="get__certificate_key_type_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__certificate_profiles

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/certificate/profiles

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/certificate/profiles')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/certificate/profiles");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /certificate/profiles`

Returns a dictionary of predefined options for specific use cases i.e openvpn client/server
configurations which can be used for creating certificates.

<h3 id="get__certificate_profiles-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-certificateauthority">certificateauthority</h1>

## get__certificateauthority

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/certificateauthority

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/certificateauthority')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/certificateauthority");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /certificateauthority`

<h3 id="get__certificateauthority-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__certificateauthority-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__certificateauthority

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/certificateauthority \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/certificateauthority', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/certificateauthority");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /certificateauthority`

Create a new Certificate Authority

Certificate Authorities are classified under following types with the necessary keywords to be passed
for `create_type` attribute to create the respective type of certificate authority

1) Internal Certificate Authority       -  CA_CREATE_INTERNAL

2) Imported Certificate Authority       -  CA_CREATE_IMPORTED

3) Intermediate Certificate Authority   -  CA_CREATE_INTERMEDIATE

Created certificate authorities use RSA keys by default. If an Elliptic Curve Key is desired, then it can be
specified with the `key_type` attribute. If the `ec_curve` attribute is not specified for the Elliptic
Curve Key, default to using "BrainpoolP384R1" curve.

A type is selected by the Certificate Authority Service based on `create_type`. The rest of the values
are validated accordingly and finally a certificate is made based on the selected type.

`cert_extensions` can be specified to set X509v3 extensions.

> Body parameter

```json
{}
```

<h3 id="post__certificateauthority-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[certificateauthority_create_0](#schemacertificateauthority_create_0)|false|none|

<h3 id="post__certificateauthority-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__certificateauthority_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/certificateauthority/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/certificateauthority/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/certificateauthority/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /certificateauthority/id/{id}`

Delete a Certificate Authority of `id`

<h3 id="delete__certificateauthority_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__certificateauthority_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__certificateauthority_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/certificateauthority/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/certificateauthority/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/certificateauthority/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /certificateauthority/id/{id}`

<h3 id="get__certificateauthority_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__certificateauthority_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__certificateauthority_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/certificateauthority/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/certificateauthority/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/certificateauthority/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /certificateauthority/id/{id}`

Update Certificate Authority of `id`

Only `name` and `revoked` attribute can be updated.

If `revoked` is enabled, the CA and its complete chain is marked as revoked and added to the CA's
certificate revocation list.

> Body parameter

```json
{}
```

<h3 id="put__certificateauthority_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[certificateauthority_update_1](#schemacertificateauthority_update_1)|false|none|

<h3 id="put__certificateauthority_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__certificateauthority_ca_sign_csr

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/certificateauthority/ca_sign_csr \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/certificateauthority/ca_sign_csr', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/certificateauthority/ca_sign_csr");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /certificateauthority/ca_sign_csr`

Sign CSR by Certificate Authority of `ca_id`

Sign CSR's and generate a certificate from it. `ca_id` provides which CA is to be used for signing
a CSR of `csr_cert_id` which exists in the system

`cert_extensions` can be specified if specific extensions are to be set in the newly signed certificate.

> Body parameter

```json
{}
```

<h3 id="post__certificateauthority_ca_sign_csr-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[certificateauthority_ca_sign_csr_0](#schemacertificateauthority_ca_sign_csr_0)|false|none|

<h3 id="post__certificateauthority_ca_sign_csr-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__certificateauthority_profiles

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/certificateauthority/profiles

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/certificateauthority/profiles')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/certificateauthority/profiles");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /certificateauthority/profiles`

Returns a dictionary of predefined options for specific use cases i.e OpenVPN certificate authority
configurations which can be used for creating certificate authorities.

<h3 id="get__certificateauthority_profiles-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-cloudsync">cloudsync</h1>

## get__cloudsync

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/cloudsync

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/cloudsync')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/cloudsync");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /cloudsync`

Query all Cloud Sync Tasks with `query-filters` and `query-options`.

<h3 id="get__cloudsync-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__cloudsync-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__cloudsync

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/cloudsync \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/cloudsync', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/cloudsync");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /cloudsync`

Creates a new cloud_sync entry.

> Body parameter

```json
{}
```

<h3 id="post__cloudsync-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[cloudsync_create_0](#schemacloudsync_create_0)|false|none|

<h3 id="post__cloudsync-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__cloudsync_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/cloudsync/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/cloudsync/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/cloudsync/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /cloudsync/id/{id}`

Deletes cloud_sync entry `id`.

<h3 id="delete__cloudsync_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__cloudsync_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__cloudsync_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/cloudsync/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/cloudsync/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/cloudsync/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /cloudsync/id/{id}`

Query all Cloud Sync Tasks with `query-filters` and `query-options`.

<h3 id="get__cloudsync_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__cloudsync_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__cloudsync_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/cloudsync/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/cloudsync/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/cloudsync/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /cloudsync/id/{id}`

Updates the cloud_sync entry `id` with `data`.

> Body parameter

```json
{}
```

<h3 id="put__cloudsync_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[cloudsync_update_1](#schemacloudsync_update_1)|false|none|

<h3 id="put__cloudsync_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__cloudsync_id_{id}_abort

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/cloudsync/id/{id}/abort \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/cloudsync/id/{id}/abort', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/cloudsync/id/{id}/abort");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /cloudsync/id/{id}/abort`

Aborts cloud sync task.

> Body parameter

```json
{}
```

<h3 id="post__cloudsync_id_{id}_abort-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[cloudsync_abort](#schemacloudsync_abort)|false|none|

<h3 id="post__cloudsync_id_{id}_abort-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__cloudsync_common_task_schema

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/cloudsync/common_task_schema

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/cloudsync/common_task_schema')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/cloudsync/common_task_schema");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /cloudsync/common_task_schema`

<h3 id="get__cloudsync_common_task_schema-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__cloudsync_list_buckets

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/cloudsync/list_buckets \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/cloudsync/list_buckets', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/cloudsync/list_buckets");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /cloudsync/list_buckets`

> Body parameter

```json
0
```

<h3 id="post__cloudsync_list_buckets-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[cloudsync_list_buckets_0](#schemacloudsync_list_buckets_0)|false|none|

<h3 id="post__cloudsync_list_buckets-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__cloudsync_list_directory

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/cloudsync/list_directory \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/cloudsync/list_directory', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/cloudsync/list_directory");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /cloudsync/list_directory`

List contents of a remote bucket / directory.

If remote supports buckets, path is constructed by two keys "bucket"/"folder" in `attributes`.
If remote does not support buckets, path is constructed using "folder" key only in `attributes`.
"folder" is directory name and "bucket" is bucket name for remote.

Path examples:

S3 Service
`bucketname/directory/name`

Dropbox Service
`directory/name`

`credentials` is a valid id of a Cloud Sync Credential which will be used to connect to the provider.

> Body parameter

```json
{}
```

<h3 id="post__cloudsync_list_directory-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[cloudsync_list_directory_0](#schemacloudsync_list_directory_0)|false|none|

<h3 id="post__cloudsync_list_directory-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__cloudsync_onedrive_list_drives

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/cloudsync/onedrive_list_drives \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/cloudsync/onedrive_list_drives', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/cloudsync/onedrive_list_drives");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /cloudsync/onedrive_list_drives`

Lists all available drives and their types for given Microsoft OneDrive credentials.

> Body parameter

```json
{}
```

<h3 id="post__cloudsync_onedrive_list_drives-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[cloudsync_onedrive_list_drives_0](#schemacloudsync_onedrive_list_drives_0)|false|none|

<h3 id="post__cloudsync_onedrive_list_drives-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__cloudsync_providers

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/cloudsync/providers

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/cloudsync/providers')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/cloudsync/providers");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /cloudsync/providers`

Returns a list of dictionaries of supported providers for Cloud Sync Tasks.

`credentials_schema` is JSON schema for credentials attributes.

`task_schema` is JSON schema for task attributes.

`buckets` is a boolean value which is set to "true" if provider supports buckets.

Example of a single provider:

[
    {
        "name": "AMAZON_CLOUD_DRIVE",
        "title": "Amazon Cloud Drive",
        "credentials_schema": [
            {
                "property": "client_id",
                "schema": {
                    "title": "Amazon Application Client ID",
                    "_required_": true,
                    "type": "string"
                }
            },
            {
                "property": "client_secret",
                "schema": {
                    "title": "Application Key",
                    "_required_": true,
                    "type": "string"
                }
            }
        ],
        "credentials_oauth": null,
        "buckets": false,
        "bucket_title": "Bucket",
        "task_schema": []
    }
]

<h3 id="get__cloudsync_providers-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__cloudsync_id_{id}_restore

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/cloudsync/id/{id}/restore \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/cloudsync/id/{id}/restore', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/cloudsync/id/{id}/restore");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /cloudsync/id/{id}/restore`

Create the opposite of cloud sync task `id` (PULL if it was PUSH and vice versa).

> Body parameter

```json
{}
```

<h3 id="post__cloudsync_id_{id}_restore-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[cloudsync_restore_1](#schemacloudsync_restore_1)|false|none|

<h3 id="post__cloudsync_id_{id}_restore-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__cloudsync_id_{id}_sync

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/cloudsync/id/{id}/sync \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/cloudsync/id/{id}/sync', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/cloudsync/id/{id}/sync");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /cloudsync/id/{id}/sync`

Run the cloud_sync job `id`, syncing the local data to remote.

> Body parameter

```json
{}
```

<h3 id="post__cloudsync_id_{id}_sync-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[cloudsync_sync_1](#schemacloudsync_sync_1)|false|none|

<h3 id="post__cloudsync_id_{id}_sync-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__cloudsync_sync_onetime

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/cloudsync/sync_onetime \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/cloudsync/sync_onetime', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/cloudsync/sync_onetime");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /cloudsync/sync_onetime`

Run cloud sync task without creating it.

> Body parameter

```json
{
  "cloud_sync_create": {},
  "cloud_sync_sync_options": {}
}
```

<h3 id="post__cloudsync_sync_onetime-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[cloudsync_sync_onetime](#schemacloudsync_sync_onetime)|false|none|

<h3 id="post__cloudsync_sync_onetime-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-cloudsync-credentials">cloudsync.credentials</h1>

## get__cloudsync_credentials

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/cloudsync/credentials

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/cloudsync/credentials')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/cloudsync/credentials");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /cloudsync/credentials`

<h3 id="get__cloudsync_credentials-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__cloudsync_credentials-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__cloudsync_credentials

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/cloudsync/credentials \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/cloudsync/credentials', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/cloudsync/credentials");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /cloudsync/credentials`

Create Cloud Sync Credentials.

`attributes` is a dictionary of valid values which will be used to authorize with the `provider`.

> Body parameter

```json
{}
```

<h3 id="post__cloudsync_credentials-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[cloudsync_credentials_create_0](#schemacloudsync_credentials_create_0)|false|none|

<h3 id="post__cloudsync_credentials-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__cloudsync_credentials_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/cloudsync/credentials/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/cloudsync/credentials/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/cloudsync/credentials/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /cloudsync/credentials/id/{id}`

Delete Cloud Sync Credentials of `id`.

<h3 id="delete__cloudsync_credentials_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__cloudsync_credentials_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__cloudsync_credentials_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/cloudsync/credentials/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/cloudsync/credentials/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/cloudsync/credentials/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /cloudsync/credentials/id/{id}`

<h3 id="get__cloudsync_credentials_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__cloudsync_credentials_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__cloudsync_credentials_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/cloudsync/credentials/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/cloudsync/credentials/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/cloudsync/credentials/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /cloudsync/credentials/id/{id}`

Update Cloud Sync Credentials of `id`.

> Body parameter

```json
{}
```

<h3 id="put__cloudsync_credentials_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[cloudsync_credentials_update_1](#schemacloudsync_credentials_update_1)|false|none|

<h3 id="put__cloudsync_credentials_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__cloudsync_credentials_verify

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/cloudsync/credentials/verify \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/cloudsync/credentials/verify', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/cloudsync/credentials/verify");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /cloudsync/credentials/verify`

Verify if `attributes` provided for `provider` are authorized by the `provider`.

> Body parameter

```json
{}
```

<h3 id="post__cloudsync_credentials_verify-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[cloudsync_credentials_verify_0](#schemacloudsync_credentials_verify_0)|false|none|

<h3 id="post__cloudsync_credentials_verify-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-config">config</h1>

## post__config_reset

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/config/reset \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/config/reset', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/config/reset");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /config/reset`

Reset database to configuration defaults.

If `reboot` is true this job will reboot the system after its completed with a delay of 10
seconds.

> Body parameter

```json
{}
```

<h3 id="post__config_reset-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[config_reset_0](#schemaconfig_reset_0)|false|none|

<h3 id="post__config_reset-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-cronjob">cronjob</h1>

## get__cronjob

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/cronjob

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/cronjob')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/cronjob");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /cronjob`

<h3 id="get__cronjob-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__cronjob-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__cronjob

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/cronjob \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/cronjob', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/cronjob");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /cronjob`

Create a new cron job.

`stderr` and `stdout` are boolean values which if `true`, represent that we would like to suppress
standard error / standard output respectively.

> Body parameter

```json
{}
```

<h3 id="post__cronjob-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[cronjob_create_0](#schemacronjob_create_0)|false|none|

<h3 id="post__cronjob-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__cronjob_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/cronjob/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/cronjob/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/cronjob/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /cronjob/id/{id}`

Delete cronjob of `id`.

<h3 id="delete__cronjob_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__cronjob_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__cronjob_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/cronjob/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/cronjob/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/cronjob/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /cronjob/id/{id}`

<h3 id="get__cronjob_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__cronjob_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__cronjob_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/cronjob/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/cronjob/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/cronjob/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /cronjob/id/{id}`

Update cronjob of `id`.

> Body parameter

```json
{}
```

<h3 id="put__cronjob_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[cronjob_update_1](#schemacronjob_update_1)|false|none|

<h3 id="put__cronjob_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__cronjob_run

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/cronjob/run \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/cronjob/run', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/cronjob/run");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /cronjob/run`

Job to run cronjob task of `id`.

> Body parameter

```json
{
  "id": 0,
  "skip_disabled": false
}
```

<h3 id="post__cronjob_run-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[cronjob_run](#schemacronjob_run)|false|none|

<h3 id="post__cronjob_run-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-device">device</h1>

## post__device_get_info

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/device/get_info \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/device/get_info', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/device/get_info");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /device/get_info`

Get info for SERIAL/DISK device types.

> Body parameter

```json
"SERIAL"
```

<h3 id="post__device_get_info-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[device_get_info_0](#schemadevice_get_info_0)|false|none|

#### Enumerated Values

|Parameter|Value|
|---|---|
|body|SERIAL|
|body|DISK|

<h3 id="post__device_get_info-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-directoryservices">directoryservices</h1>

## get__directoryservices_cache_refresh

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/directoryservices/cache_refresh

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/directoryservices/cache_refresh')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/directoryservices/cache_refresh");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /directoryservices/cache_refresh`

<h3 id="get__directoryservices_cache_refresh-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__directoryservices_get_state

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/directoryservices/get_state

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/directoryservices/get_state')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/directoryservices/get_state");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /directoryservices/get_state`

`DISABLED` Directory Service is disabled.

`FAULTED` Directory Service is enabled, but not HEALTHY. Review logs and generated alert
messages to debug the issue causing the service to be in a FAULTED state.

`LEAVING` Directory Service is in process of stopping.

`JOINING` Directory Service is in process of starting.

`HEALTHY` Directory Service is enabled, and last status check has passed.

<h3 id="get__directoryservices_get_state-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-disk">disk</h1>

## get__disk

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/disk

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/disk')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/disk");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /disk`

<h3 id="get__disk-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__disk-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__disk_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/disk/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/disk/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/disk/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /disk/id/{id}`

<h3 id="get__disk_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__disk_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__disk_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/disk/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/disk/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/disk/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /disk/id/{id}`

Update disk of `id`.

If extra options need to be passed to SMART which we don't already support, they can be passed by
`smartoptions`.

`critical`, `informational` and `difference` are integer values on which alerts for SMART are configured
if the disk temperature crosses the assigned threshold for each respective attribute.
If they are set to null, then SMARTD config values are used as defaults.

Email of log level LOG_CRIT is issued when disk temperature crosses `critical`.

Email of log level LOG_INFO is issued when disk temperature crosses `informational`.

If temperature of a disk changes by `difference` degree Celsius since the last report, SMART reports this.

> Body parameter

```json
{}
```

<h3 id="put__disk_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|string|true|none|
|body|body|[disk_update_1](#schemadisk_update_1)|false|none|

<h3 id="put__disk_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__disk_get_encrypted

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/disk/get_encrypted \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/disk/get_encrypted', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/disk/get_encrypted");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /disk/get_encrypted`

Get all geli providers

It might be an entire disk or a partition of type freebsd-zfs.

Before a geli encrypted pool can be imported, disks used in the pool should be decrypted
and then pool import can proceed as desired. In that case `unused` can be passed as `true`, to find out
which disks are geli encrypted but not being used by active ZFS pools.

> Body parameter

```json
{}
```

<h3 id="post__disk_get_encrypted-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[disk_get_encrypted_0](#schemadisk_get_encrypted_0)|false|none|

<h3 id="post__disk_get_encrypted-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__disk_get_unused

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/disk/get_unused \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/disk/get_unused', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/disk/get_unused");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /disk/get_unused`

Helper method to get all disks that are not in use, either by the boot
pool or the user pools.

> Body parameter

```json
false
```

<h3 id="post__disk_get_unused-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[disk_get_unused_0](#schemadisk_get_unused_0)|false|none|

<h3 id="post__disk_get_unused-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__disk_label_to_dev

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/disk/label_to_dev

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/disk/label_to_dev')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/disk/label_to_dev");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /disk/label_to_dev`

<h3 id="get__disk_label_to_dev-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__disk_overprovision

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/disk/overprovision \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/disk/overprovision', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/disk/overprovision");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /disk/overprovision`

Sets overprovision of disk `devname` to `size` gigabytes

> Body parameter

```json
{
  "devname": "string",
  "size": 0
}
```

<h3 id="post__disk_overprovision-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[disk_overprovision](#schemadisk_overprovision)|false|none|

<h3 id="post__disk_overprovision-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__disk_smart_attributes

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/disk/smart_attributes \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/disk/smart_attributes', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/disk/smart_attributes");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /disk/smart_attributes`

Returns S.M.A.R.T. attributes values for specified disk name.

> Body parameter

```json
"string"
```

<h3 id="post__disk_smart_attributes-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[disk_smart_attributes_0](#schemadisk_smart_attributes_0)|false|none|

<h3 id="post__disk_smart_attributes-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__disk_spindown

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/disk/spindown \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/disk/spindown', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/disk/spindown");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /disk/spindown`

Spin down disk by device name

> Body parameter

```json
"string"
```

<h3 id="post__disk_spindown-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[disk_spindown_0](#schemadisk_spindown_0)|false|none|

<h3 id="post__disk_spindown-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__disk_temperature

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/disk/temperature \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/disk/temperature', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/disk/temperature");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /disk/temperature`

Returns temperature for device `name` using specified S.M.A.R.T. `powermode`.

> Body parameter

```json
{
  "name": "string",
  "powermode": "NEVER"
}
```

<h3 id="post__disk_temperature-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[disk_temperature](#schemadisk_temperature)|false|none|

<h3 id="post__disk_temperature-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__disk_temperatures

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/disk/temperatures \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/disk/temperatures', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/disk/temperatures");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /disk/temperatures`

Returns temperatures for a list of devices (runs in parallel).
See `disk.temperature` documentation for more details.

> Body parameter

```json
{
  "names": [
    "string"
  ],
  "powermode": "NEVER"
}
```

<h3 id="post__disk_temperatures-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[disk_temperatures](#schemadisk_temperatures)|false|none|

<h3 id="post__disk_temperatures-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__disk_unoverprovision

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/disk/unoverprovision \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/disk/unoverprovision', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/disk/unoverprovision");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /disk/unoverprovision`

Removes overprovisioning of disk `devname`

> Body parameter

```json
"string"
```

<h3 id="post__disk_unoverprovision-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[disk_unoverprovision_0](#schemadisk_unoverprovision_0)|false|none|

<h3 id="post__disk_unoverprovision-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__disk_wipe

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/disk/wipe \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/disk/wipe', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/disk/wipe");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /disk/wipe`

Performs a wipe of a disk `dev`.
It can be of the following modes:
  - QUICK: clean the first few and last megabytes of every partition and disk
  - FULL: write whole disk with zero's
  - FULL_RANDOM: write whole disk with random bytes

> Body parameter

```json
{
  "dev": "string",
  "mode": "QUICK",
  "synccache": true,
  "swap_removal_options": {}
}
```

<h3 id="post__disk_wipe-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[disk_wipe](#schemadisk_wipe)|false|none|

<h3 id="post__disk_wipe-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-dns">dns</h1>

## get__dns_query

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/dns/query

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/dns/query')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/dns/query");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /dns/query`

Query Name Servers with `query-filters` and `query-options`.

<h3 id="get__dns_query-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__dns_query-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-dyndns">dyndns</h1>

## get__dyndns

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/dyndns

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/dyndns')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/dyndns");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /dyndns`

<h3 id="get__dyndns-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__dyndns

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/dyndns \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/dyndns', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/dyndns");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /dyndns`

Update dynamic dns service configuration.

`period` indicates how often the IP is checked in seconds.

`ssl` if set to true, makes sure that HTTPS is used for the connection to the server which updates the
DNS record.

> Body parameter

```json
{}
```

<h3 id="put__dyndns-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[dyndns_update_0](#schemadyndns_update_0)|false|none|

<h3 id="put__dyndns-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__dyndns_provider_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/dyndns/provider_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/dyndns/provider_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/dyndns/provider_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /dyndns/provider_choices`

List supported Dynamic DNS Service Providers.

<h3 id="get__dyndns_provider_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-ec2">ec2</h1>

## get__ec2_Meta

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/ec2/Meta

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/ec2/Meta')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/ec2/Meta");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /ec2/Meta`

<h3 id="get__ec2_meta-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__ec2_instance_id

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/ec2/instance_id

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/ec2/instance_id')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/ec2/instance_id");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /ec2/instance_id`

<h3 id="get__ec2_instance_id-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__ec2_set_ntp_servers

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/ec2/set_ntp_servers

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/ec2/set_ntp_servers')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/ec2/set_ntp_servers");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /ec2/set_ntp_servers`

<h3 id="get__ec2_set_ntp_servers-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__ec2_set_root_ssh_public_key

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/ec2/set_root_ssh_public_key

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/ec2/set_root_ssh_public_key')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/ec2/set_root_ssh_public_key");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /ec2/set_root_ssh_public_key`

<h3 id="get__ec2_set_root_ssh_public_key-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__ec2_setup

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/ec2/setup

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/ec2/setup')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/ec2/setup");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /ec2/setup`

<h3 id="get__ec2_setup-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-enclosure">enclosure</h1>

## get__enclosure

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/enclosure

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/enclosure')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/enclosure");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /enclosure`

<h3 id="get__enclosure-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__enclosure-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__enclosure_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/enclosure/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/enclosure/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/enclosure/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /enclosure/id/{id}`

<h3 id="get__enclosure_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__enclosure_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__enclosure_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/enclosure/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/enclosure/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/enclosure/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /enclosure/id/{id}`

> Body parameter

```json
{}
```

<h3 id="put__enclosure_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|string|true|none|
|body|body|[enclosure_update_1](#schemaenclosure_update_1)|false|none|

<h3 id="put__enclosure_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__enclosure_set_slot_status

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/enclosure/set_slot_status \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/enclosure/set_slot_status', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/enclosure/set_slot_status");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /enclosure/set_slot_status`

> Body parameter

```json
{
  "enclosure_id": "string",
  "slot": 0,
  "status": "CLEAR"
}
```

<h3 id="post__enclosure_set_slot_status-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[enclosure_set_slot_status](#schemaenclosure_set_slot_status)|false|none|

<h3 id="post__enclosure_set_slot_status-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-failover">failover</h1>

## get__failover

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/failover

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/failover')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/failover");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /failover`

<h3 id="get__failover-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__failover

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/failover \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/failover', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/failover");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /failover`

Update failover state.

`disabled` when true indicates that HA is disabled.
`master` sets the state of current node. Standby node will have the opposite value.

`timeout` is the time to WAIT until a failover occurs when a network
    event occurs on an interface that is marked critical for failover AND
    HA is enabled and working appropriately.

    The default time to wait is 2 seconds.
    **NOTE**
        This setting does NOT effect the `disabled` or `master` parameters.

> Body parameter

```json
{}
```

<h3 id="put__failover-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[failover_update_0](#schemafailover_update_0)|false|none|

<h3 id="put__failover-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__failover_call_remote

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/failover/call_remote \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/failover/call_remote', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/failover/call_remote");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /failover/call_remote`

Call a method in the other node.

> Body parameter

```json
{
  "method": "string",
  "args": [],
  "options": {}
}
```

<h3 id="post__failover_call_remote-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[failover_call_remote](#schemafailover_call_remote)|false|none|

<h3 id="post__failover_call_remote-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__failover_control

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/failover/control \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/failover/control', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/failover/control");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /failover/control`

> Body parameter

```json
{
  "action": "ENABLE",
  "options": {}
}
```

<h3 id="post__failover_control-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[failover_control](#schemafailover_control)|false|none|

<h3 id="post__failover_control-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__failover_force_master

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/failover/force_master

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/failover/force_master')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/failover/force_master");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /failover/force_master`

Force this controller to become MASTER.

<h3 id="get__failover_force_master-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__failover_hardware

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/failover/hardware

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/failover/hardware')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/failover/hardware");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /failover/hardware`

Returns the hardware type for an HA system.
  ECHOSTREAM
  ECHOWARP
  PUMA
  SBB
  ULTIMATE
  BHYVE
  MANUAL

<h3 id="get__failover_hardware-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__failover_in_progress

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/failover/in_progress

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/failover/in_progress')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/failover/in_progress");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /failover/in_progress`

Returns True if there is an ongoing failover event.

<h3 id="get__failover_in_progress-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__failover_licensed

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/failover/licensed

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/failover/licensed')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/failover/licensed");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /failover/licensed`

Checks whether this instance is licensed as a HA unit.

<h3 id="get__failover_licensed-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__failover_node

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/failover/node

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/failover/node')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/failover/node");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /failover/node`

Returns the slot position in the chassis that
the controller is located.
  A - First node
  B - Seconde Node
  MANUAL - slot position in chassis could not be determined

<h3 id="get__failover_node-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__failover_sync_from_peer

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/failover/sync_from_peer

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/failover/sync_from_peer')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/failover/sync_from_peer");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /failover/sync_from_peer`

Sync database and files from the other controller.

<h3 id="get__failover_sync_from_peer-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__failover_sync_to_peer

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/failover/sync_to_peer \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/failover/sync_to_peer', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/failover/sync_to_peer");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /failover/sync_to_peer`

Sync database and files to the other controller.

`reboot` as true will reboot the other controller after syncing.

> Body parameter

```json
{}
```

<h3 id="post__failover_sync_to_peer-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[failover_sync_to_peer_0](#schemafailover_sync_to_peer_0)|false|none|

<h3 id="post__failover_sync_to_peer-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__failover_unlock

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/failover/unlock \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/failover/unlock', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/failover/unlock");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /failover/unlock`

Unlock pools in HA, syncing passphrase between controllers and forcing this controller
to be MASTER importing the pools.

> Body parameter

```json
{}
```

<h3 id="post__failover_unlock-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[failover_unlock_0](#schemafailover_unlock_0)|false|none|

<h3 id="post__failover_unlock-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__failover_upgrade

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/failover/upgrade \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/failover/upgrade', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/failover/upgrade");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /failover/upgrade`

Upgrades both controllers.

Files will be downloaded to the Active Controller and then transferred to the Standby
Controller.

Upgrade process will start concurrently on both nodes.

Once both upgrades are applied, the Standby Controller will reboot. This job will wait for
that job to complete before finalizing.

A file can be uploaded to this end point. This end point is special, please refer to Jobs section in Websocket API documentation for details.

> Body parameter

```json
{}
```

<h3 id="post__failover_upgrade-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[failover_upgrade_0](#schemafailover_upgrade_0)|false|none|

<h3 id="post__failover_upgrade-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__failover_upgrade_finish

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/failover/upgrade_finish

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/failover/upgrade_finish')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/failover/upgrade_finish");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /failover/upgrade_finish`

Perform the last stage of an HA upgrade.

This will activate the new boot environment on the
Standby Controller and reboot it.

<h3 id="get__failover_upgrade_finish-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__failover_upgrade_pending

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/failover/upgrade_pending

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/failover/upgrade_pending')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/failover/upgrade_pending");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /failover/upgrade_pending`

Verify if HA upgrade is pending.

`upgrade_finish` needs to be called to finish
the HA upgrade process if this method returns true.

<h3 id="get__failover_upgrade_pending-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-fcport">fcport</h1>

## get__fcport

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/fcport

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/fcport')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/fcport");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /fcport`

<h3 id="get__fcport-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__fcport-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__fcport_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/fcport/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/fcport/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/fcport/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /fcport/id/{id}`

<h3 id="get__fcport_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__fcport_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__fcport_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/fcport/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/fcport/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/fcport/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /fcport/id/{id}`

> Body parameter

```json
{}
```

<h3 id="put__fcport_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|string|true|none|
|body|body|[fcport_update_1](#schemafcport_update_1)|false|none|

<h3 id="put__fcport_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-filesystem">filesystem</h1>

## post__filesystem_acl_is_trivial

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/filesystem/acl_is_trivial \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/filesystem/acl_is_trivial', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/filesystem/acl_is_trivial");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /filesystem/acl_is_trivial`

Returns True if the ACL can be fully expressed as a file mode without losing
any access rules, or if the path does not support NFSv4 ACLs (for example
a path on a tmpfs filesystem).

> Body parameter

```json
"string"
```

<h3 id="post__filesystem_acl_is_trivial-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[filesystem_acl_is_trivial_0](#schemafilesystem_acl_is_trivial_0)|false|none|

<h3 id="post__filesystem_acl_is_trivial-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__filesystem_chown

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/filesystem/chown \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/filesystem/chown', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/filesystem/chown");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /filesystem/chown`

Change owner or group of file at `path`.

`uid` and `gid` specify new owner of the file. If either
key is absent or None, then existing value on the file is not
changed.

`recursive` performs action recursively, but does
not traverse filesystem mount points.

If `traverse` and `recursive` are specified, then the chown
operation will traverse filesystem mount points.

> Body parameter

```json
{}
```

<h3 id="post__filesystem_chown-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[filesystem_chown_0](#schemafilesystem_chown_0)|false|none|

<h3 id="post__filesystem_chown-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__filesystem_default_acl_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/filesystem/default_acl_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/filesystem/default_acl_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/filesystem/default_acl_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /filesystem/default_acl_choices`

Get list of default ACL types.

<h3 id="get__filesystem_default_acl_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__filesystem_get_default_acl

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/filesystem/get_default_acl \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/filesystem/get_default_acl', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/filesystem/get_default_acl");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /filesystem/get_default_acl`

Returns a default ACL depending on the usage specified by `acl_type`.
If an admin group is defined, then an entry granting it full control will
be placed at the top of the ACL. Optionally may pass `share_type` to argument
to get share-specific template ACL.

> Body parameter

```json
{
  "acl_type": "OPEN",
  "share_type": "NONE"
}
```

<h3 id="post__filesystem_get_default_acl-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[filesystem_get_default_acl](#schemafilesystem_get_default_acl)|false|none|

<h3 id="post__filesystem_get_default_acl-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__filesystem_getacl

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/filesystem/getacl \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/filesystem/getacl', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/filesystem/getacl");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /filesystem/getacl`

Return ACL of a given path. This may return a POSIX1e ACL or a NFSv4 ACL. The acl type is indicated
by the `ACLType` key.

Errata about ACLType NFSv4:

`simplified` returns a shortened form of the ACL permset and flags.

`TRAVERSE` sufficient rights to traverse a directory, but not read contents.

`READ` sufficient rights to traverse a directory, and read file contents.

`MODIFIY` sufficient rights to traverse, read, write, and modify a file. Equivalent to modify_set.

`FULL_CONTROL` all permissions.

If the permisssions do not fit within one of the pre-defined simplified permissions types, then
the full ACL entry will be returned.

In all cases we replace USER_OBJ, GROUP_OBJ, and EVERYONE with owner@, group@, everyone@ for
consistency with getfacl and setfacl. If one of aforementioned special tags is used, 'id' must
be set to None.

An inheriting empty everyone@ ACE is appended to non-trivial ACLs in order to enforce Windows
expectations regarding permissions inheritance. This entry is removed from NT ACL returned
to SMB clients when 'ixnas' samba VFS module is enabled. We also remove it here to avoid confusion.

> Body parameter

```json
{
  "path": "string",
  "simplified": true
}
```

<h3 id="post__filesystem_getacl-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[filesystem_getacl](#schemafilesystem_getacl)|false|none|

<h3 id="post__filesystem_getacl-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__filesystem_listdir

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/filesystem/listdir \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/filesystem/listdir', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/filesystem/listdir");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /filesystem/listdir`

Get the contents of a directory.

Each entry of the list consists of:
  name(str): name of the file
  path(str): absolute path of the entry
  realpath(str): absolute real path of the entry (if SYMLINK)
  type(str): DIRECTORY | FILESYSTEM | SYMLINK | OTHER
  size(int): size of the entry
  mode(int): file mode/permission
  uid(int): user id of entry owner
  gid(int): group id of entry onwer
  acl(bool): extended ACL is present on file

> Body parameter

```json
{
  "path": "string",
  "query-filters": [],
  "query-options": {}
}
```

<h3 id="post__filesystem_listdir-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[filesystem_listdir](#schemafilesystem_listdir)|false|none|

<h3 id="post__filesystem_listdir-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__filesystem_setacl

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/filesystem/setacl \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/filesystem/setacl', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/filesystem/setacl");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /filesystem/setacl`

Set ACL of a given path. Takes the following parameters:
`path` full path to directory or file.

`dacl` "simplified" ACL here or a full ACL.

`uid` the desired UID of the file user. If set to None (the default), then user is not changed.

`gid` the desired GID of the file group. If set to None (the default), then group is not changed.

`recursive` apply the ACL recursively

`traverse` traverse filestem boundaries (ZFS datasets)

`strip` convert ACL to trivial. ACL is trivial if it can be expressed as a file mode without
losing any access rules.

`canonicalize` reorder ACL entries so that they are in concanical form as described
in the Microsoft documentation MS-DTYP 2.4.5 (ACL)

In all cases we replace USER_OBJ, GROUP_OBJ, and EVERYONE with owner@, group@, everyone@ for
consistency with getfacl and setfacl. If one of aforementioned special tags is used, 'id' must
be set to None.

An inheriting empty everyone@ ACE is appended to non-trivial ACLs in order to enforce Windows
expectations regarding permissions inheritance. This entry is removed from NT ACL returned
to SMB clients when 'ixnas' samba VFS module is enabled.

> Body parameter

```json
{}
```

<h3 id="post__filesystem_setacl-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[filesystem_setacl_0](#schemafilesystem_setacl_0)|false|none|

<h3 id="post__filesystem_setacl-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__filesystem_setperm

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/filesystem/setperm \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/filesystem/setperm', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/filesystem/setperm");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /filesystem/setperm`

Remove extended ACL from specified path.

If `mode` is specified then the mode will be applied to the
path and files and subdirectories depending on which `options` are
selected. Mode should be formatted as string representation of octal
permissions bits.

`uid` the desired UID of the file user. If set to None (the default), then user is not changed.

`gid` the desired GID of the file group. If set to None (the default), then group is not changed.

`stripacl` setperm will fail if an extended ACL is present on `path`,
unless `stripacl` is set to True.

`recursive` remove ACLs recursively, but do not traverse dataset
boundaries.

`traverse` remove ACLs from child datasets.

If no `mode` is set, and `stripacl` is True, then non-trivial ACLs
will be converted to trivial ACLs. An ACL is trivial if it can be
expressed as a file mode without losing any access rules.

> Body parameter

```json
{}
```

<h3 id="post__filesystem_setperm-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[filesystem_setperm_0](#schemafilesystem_setperm_0)|false|none|

<h3 id="post__filesystem_setperm-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__filesystem_stat

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/filesystem/stat \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/filesystem/stat', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/filesystem/stat");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /filesystem/stat`

Return the filesystem stat(2) for a given `path`.

> Body parameter

```json
"string"
```

<h3 id="post__filesystem_stat-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[filesystem_stat_0](#schemafilesystem_stat_0)|false|none|

<h3 id="post__filesystem_stat-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__filesystem_statfs

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/filesystem/statfs \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/filesystem/statfs', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/filesystem/statfs");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /filesystem/statfs`

Return stats from the filesystem of a given path.

Raises:
    CallError(ENOENT) - Path not found

> Body parameter

```json
"string"
```

<h3 id="post__filesystem_statfs-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[filesystem_statfs_0](#schemafilesystem_statfs_0)|false|none|

<h3 id="post__filesystem_statfs-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-ftp">ftp</h1>

## get__ftp

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/ftp

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/ftp')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/ftp");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /ftp`

<h3 id="get__ftp-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__ftp

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/ftp \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/ftp', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/ftp");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /ftp`

Update ftp service configuration.

`clients` is an integer value which sets the maximum number of simultaneous clients allowed. It defaults to 32.

`ipconnections` is an integer value which shows the maximum number of connections per IP address. It defaults
to 0 which equals to unlimited.

`timeout` is the maximum client idle time in seconds before client is disconnected.

`rootlogin` is a boolean value which when configured to true enables login as root. This is generally
discouraged because of the security risks.

`onlyanonymous` allows anonymous FTP logins with access to the directory specified by `anonpath`.

`banner` is a message displayed to local login users after they successfully authenticate. It is not displayed
to anonymous login users.

`filemask` sets the default permissions for newly created files which by default are 077.

`dirmask` sets the default permissions for newly created directories which by default are 077.

`resume` if set allows FTP clients to resume interrupted transfers.

`fxp` if set to true indicates that File eXchange Protocol is enabled. Generally it is discouraged as it
makes the server vulnerable to FTP bounce attacks.

`defaultroot` when set ensures that for local users, home directory access is only granted if the user
is a member of group wheel.

`ident` is a boolean value which when set to true indicates that IDENT authentication is required. If identd
is not running on the client, this can result in timeouts.

`masqaddress` is the public IP address or hostname which is set if FTP clients cannot connect through a
NAT device.

`localuserbw` is a positive integer value which indicates maximum upload bandwidth in KB/s for local user.
Default of zero indicates unlimited upload bandwidth ( from the FTP server configuration ).

`localuserdlbw` is a positive integer value which indicates maximum download bandwidth in KB/s for local user.
Default of zero indicates unlimited download bandwidth ( from the FTP server configuration ).

`anonuserbw` is a positive integer value which indicates maximum upload bandwidth in KB/s for anonymous user.
Default of zero indicates unlimited upload bandwidth ( from the FTP server configuration ).

`anonuserdlbw` is a positive integer value which indicates maximum download bandwidth in KB/s for anonymous
user. Default of zero indicates unlimited download bandwidth ( from the FTP server configuration ).

`tls` is a boolean value which when set indicates that encrypted connections are enabled. This requires a
certificate to be configured first with the certificate service and the id of certificate is passed on in
`ssltls_certificate`.

`tls_policy` defines whether the control channel, data channel, both channels, or neither channel of an FTP
session must occur over SSL/TLS.

`tls_opt_enable_diags` is a boolean value when set, logs verbosely. This is helpful when troubleshooting a
connection.

`options` is a string used to add proftpd(8) parameters not covered by ftp service.

> Body parameter

```json
{}
```

<h3 id="put__ftp-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[ftp_update_0](#schemaftp_update_0)|false|none|

<h3 id="put__ftp-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-group">group</h1>

## get__group

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/group

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/group')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/group");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /group`

Query groups with `query-filters` and `query-options`. As a performance optimization, only local groups
will be queried by default.

Groups from directory services such as NIS, LDAP, or Active Directory will be included in query results
if the option `{'extra': {'search_dscache': True}}` is specified.

<h3 id="get__group-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__group-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__group

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/group \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/group', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/group");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /group`

Create a new group.

If `gid` is not provided it is automatically filled with the next one available.

`allow_duplicate_gid` allows distinct group names to share the same gid.

`users` is a list of user ids (`id` attribute from `user.query`).

`smb` specifies whether the group should be mapped into an NT group.

> Body parameter

```json
{}
```

<h3 id="post__group-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[group_create_0](#schemagroup_create_0)|false|none|

<h3 id="post__group-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__group_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/group/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.delete('http://192.168.1.123/api/v2.0/group/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/group/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /group/id/{id}`

Delete group `id`.

The `delete_users` option deletes all users that have this group as their primary group.

> Body parameter

```json
{}
```

<h3 id="delete__group_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[group_delete_1](#schemagroup_delete_1)|false|none|

<h3 id="delete__group_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__group_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/group/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/group/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/group/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /group/id/{id}`

Query groups with `query-filters` and `query-options`. As a performance optimization, only local groups
will be queried by default.

Groups from directory services such as NIS, LDAP, or Active Directory will be included in query results
if the option `{'extra': {'search_dscache': True}}` is specified.

<h3 id="get__group_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__group_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__group_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/group/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/group/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/group/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /group/id/{id}`

Update attributes of an existing group.

> Body parameter

```json
{}
```

<h3 id="put__group_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[group_update_1](#schemagroup_update_1)|false|none|

<h3 id="put__group_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__group_get_group_obj

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/group/get_group_obj \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/group/get_group_obj', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/group/get_group_obj");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /group/get_group_obj`

Returns dictionary containing information from struct grp for the group specified by either
the groupname or gid. Bypasses group cache.

> Body parameter

```json
{}
```

<h3 id="post__group_get_group_obj-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[group_get_group_obj_0](#schemagroup_get_group_obj_0)|false|none|

<h3 id="post__group_get_group_obj-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__group_get_next_gid

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/group/get_next_gid

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/group/get_next_gid')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/group/get_next_gid");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /group/get_next_gid`

Get the next available/free gid.

<h3 id="get__group_get_next_gid-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-idmap">idmap</h1>

## get__idmap

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/idmap

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/idmap')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/idmap");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /idmap`

<h3 id="get__idmap-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__idmap-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__idmap

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/idmap \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/idmap', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/idmap");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /idmap`

Create a new IDMAP domain. These domains must be unique. This table
will be automatically populated after joining an Active Directory domain
if "allow trusted domains" is set to True in the AD service configuration.
There are three default system domains: DS_TYPE_ACTIVEDIRECTORY, DS_TYPE_LDAP, DS_TYPE_DEFAULT_DOMAIN.
The system domains correspond with the idmap settings under Active Directory, LDAP, and SMB
respectively.

`name` the pre-windows 2000 domain name.

`DNS_domain_name` DNS name of the domain.

`idmap_backend` provides a plugin interface for Winbind to use varying
backends to store SID/uid/gid mapping tables. The correct setting
depends on the environment in which the NAS is deployed.

`range_low` and `range_high` specify the UID and GID range for which this backend is authoritative.

`certificate_id` references the certificate ID of the SSL certificate to use for certificate-based
authentication to a remote LDAP server. This parameter is not supported for all idmap backends as some
backends will generate SID to ID mappings algorithmically without causing network traffic.

`options` are additional parameters that are backend-dependent:

`AD` idmap backend options:
`unix_primary_group` If True, the primary group membership is fetched from the LDAP attributes (gidNumber).
If False, the primary group membership is calculated via the "primaryGroupID" LDAP attribute.

`unix_nss_info` if True winbind will retrieve the login shell and home directory from the LDAP attributes.
If False or if the AD LDAP entry lacks the SFU attributes the smb4.conf parameters `template shell` and `template homedir` are used.

`schema_mode` Defines the schema that idmap_ad should use when querying Active Directory regarding user and group information.
This can be either the RFC2307 schema support included in Windows 2003 R2 or the Service for Unix (SFU) schema.
For SFU 3.0 or 3.5 please choose "SFU", for SFU 2.0 please choose "SFU20". The behavior of primary group membership is
controlled by the unix_primary_group option.

`AUTORID` idmap backend options:
`readonly` sets the module to read-only mode. No new ranges will be allocated and new mappings
will not be created in the idmap pool.

`ignore_builtin` ignores mapping requests for the BUILTIN domain.

`LDAP` idmap backend options:
`ldap_base_dn` defines the directory base suffix to use for SID/uid/gid mapping entries.

`ldap_user_dn` defines the user DN to be used for authentication.

`ldap_url` specifies the LDAP server to use for SID/uid/gid map entries.

`ssl` specifies whether to encrypt the LDAP transport for the idmap backend.

`NSS` idmap backend options:
`linked_service` specifies the auxiliary directory service ID provider.

`RFC2307` idmap backend options:
`domain` specifies the domain for which the idmap backend is being created. Numeric id, short-form
domain name, or long-form DNS domain name of the domain may be specified. Entry must be entered as
it appears in `idmap.domain`.

`range_low` and `range_high` specify the UID and GID range for which this backend is authoritative.

`ldap_server` defines the type of LDAP server to use. This can either be an LDAP server provided
by the Active Directory Domain (ad) or a stand-alone LDAP server.

`bind_path_user` specfies the search base where user objects can be found in the LDAP server.

`bind_path_group` specifies the search base where group objects can be found in the LDAP server.

`user_cn` query cn attribute instead of uid attribute for the user name in LDAP.

`realm` append @realm to cn for groups (and users if user_cn is set) in LDAP queries.

`ldmap_domain` when using the LDAP server in the Active Directory server, this allows one to
specify the domain where to access the Active Directory server. This allows using trust relationships
while keeping all RFC 2307 records in one place. This parameter is optional, the default is to access
the AD server in the current domain to query LDAP records.

`ldap_url` when using a stand-alone LDAP server, this parameter specifies the LDAP URL for accessing the LDAP server.

`ldap_user_dn` defines the user DN to be used for authentication.

`ldap_user_dn_password` is the password to be used for LDAP authentication.

`realm` defines the realm to use in the user and group names. This is only required when using cn_realm together with
 a stand-alone ldap server.

`RID` backend options:
`sssd_compat` generate idmap low range based on same algorithm that SSSD uses by default.

> Body parameter

```json
{}
```

<h3 id="post__idmap-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[idmap_create_0](#schemaidmap_create_0)|false|none|

<h3 id="post__idmap-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__idmap_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/idmap/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/idmap/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/idmap/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /idmap/id/{id}`

Delete a domain by id. Deletion of default system domains is not permitted.

<h3 id="delete__idmap_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__idmap_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__idmap_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/idmap/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/idmap/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/idmap/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /idmap/id/{id}`

<h3 id="get__idmap_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__idmap_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__idmap_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/idmap/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/idmap/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/idmap/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /idmap/id/{id}`

Update a domain by id.

> Body parameter

```json
{}
```

<h3 id="put__idmap_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[idmap_update_1](#schemaidmap_update_1)|false|none|

<h3 id="put__idmap_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__idmap_backend_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/idmap/backend_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/idmap/backend_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/idmap/backend_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /idmap/backend_choices`

Returns array of valid idmap backend choices per directory service.

<h3 id="get__idmap_backend_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__idmap_backend_options

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/idmap/backend_options

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/idmap/backend_options')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/idmap/backend_options");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /idmap/backend_options`

This returns full information about idmap backend options. Not all
`options` are valid for every backend.

<h3 id="get__idmap_backend_options-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__idmap_clear_idmap_cache

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/idmap/clear_idmap_cache

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/idmap/clear_idmap_cache')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/idmap/clear_idmap_cache");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /idmap/clear_idmap_cache`

Stop samba, remove the winbindd_cache.tdb file, start samba, flush samba's cache.
This should be performed after finalizing idmap changes.

<h3 id="get__idmap_clear_idmap_cache-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__idmap_options_choices

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/idmap/options_choices \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/idmap/options_choices', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/idmap/options_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /idmap/options_choices`

Returns a list of supported keys for the specified idmap backend.

> Body parameter

```json
"AD"
```

<h3 id="post__idmap_options_choices-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[idmap_options_choices_0](#schemaidmap_options_choices_0)|false|none|

#### Enumerated Values

|Parameter|Value|
|---|---|
|body|AD|
|body|AUTORID|
|body|LDAP|
|body|NSS|
|body|RFC2307|
|body|RID|
|body|TDB|

<h3 id="post__idmap_options_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-initshutdownscript">initshutdownscript</h1>

## get__initshutdownscript

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/initshutdownscript

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/initshutdownscript')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/initshutdownscript");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /initshutdownscript`

<h3 id="get__initshutdownscript-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__initshutdownscript-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__initshutdownscript

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/initshutdownscript \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/initshutdownscript', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/initshutdownscript");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /initshutdownscript`

Create an initshutdown script task.

`type` indicates if a command or script should be executed at `when`.

There are three choices for `when`:

1) PREINIT - This is early in the boot process before all the services / rc scripts have started
2) POSTINIT - This is late in the boot process when most of the services / rc scripts have started
3) SHUTDOWN - This is on shutdown

`timeout` is an integer value which indicates time in seconds which the system should wait for the execution
of script/command. It should be noted that a hard limit for a timeout is configured by the base OS, so when
a script/command is set to execute on SHUTDOWN, the hard limit configured by the base OS is changed adding
the timeout specified by script/command so it can be ensured that it executes as desired and is not interrupted
by the base OS's limit.

> Body parameter

```json
{}
```

<h3 id="post__initshutdownscript-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[initshutdownscript_create_0](#schemainitshutdownscript_create_0)|false|none|

<h3 id="post__initshutdownscript-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__initshutdownscript_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/initshutdownscript/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/initshutdownscript/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/initshutdownscript/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /initshutdownscript/id/{id}`

Delete init/shutdown task of `id`.

<h3 id="delete__initshutdownscript_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__initshutdownscript_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__initshutdownscript_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/initshutdownscript/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/initshutdownscript/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/initshutdownscript/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /initshutdownscript/id/{id}`

<h3 id="get__initshutdownscript_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__initshutdownscript_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__initshutdownscript_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/initshutdownscript/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/initshutdownscript/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/initshutdownscript/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /initshutdownscript/id/{id}`

Update initshutdown script task of `id`.

> Body parameter

```json
{}
```

<h3 id="put__initshutdownscript_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[initshutdownscript_update_1](#schemainitshutdownscript_update_1)|false|none|

<h3 id="put__initshutdownscript_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-interface">interface</h1>

## get__interface

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/interface

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/interface')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/interface");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /interface`

Query Interfaces with `query-filters` and `query-options`

<h3 id="get__interface-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__interface-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__interface

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/interface \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/interface', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/interface");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /interface`

Create virtual interfaces (Link Aggregation, VLAN)

For BRIDGE `type` the following attribute is required: bridge_members.

For LINK_AGGREGATION `type` the following attributes are required: lag_ports,
lag_protocol.

For VLAN `type` the following attributes are required: vlan_parent_interface,
vlan_tag and vlan_pcp.

> Body parameter

```json
{}
```

<h3 id="post__interface-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[interface_create_0](#schemainterface_create_0)|false|none|

<h3 id="post__interface-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__interface_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/interface/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/interface/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/interface/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /interface/id/{id}`

Delete Interface of `id`.

<h3 id="delete__interface_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|string|true|none|

<h3 id="delete__interface_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__interface_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/interface/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/interface/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/interface/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /interface/id/{id}`

Query Interfaces with `query-filters` and `query-options`

<h3 id="get__interface_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__interface_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__interface_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/interface/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/interface/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/interface/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /interface/id/{id}`

Update Interface of `id`.

> Body parameter

```json
{}
```

<h3 id="put__interface_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|string|true|none|
|body|body|[interface_update_1](#schemainterface_update_1)|false|none|

<h3 id="put__interface_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__interface_bridge_members_choices

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/interface/bridge_members_choices \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/interface/bridge_members_choices', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/interface/bridge_members_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /interface/bridge_members_choices`

Return available interface choices for `bridge_members` attribute.

`id` is the name of the bridge interface to update or null for a new
bridge interface.

> Body parameter

```json
null
```

<h3 id="post__interface_bridge_members_choices-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[interface_bridge_members_choices_0](#schemainterface_bridge_members_choices_0)|false|none|

<h3 id="post__interface_bridge_members_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__interface_checkin

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/interface/checkin

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/interface/checkin')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/interface/checkin");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /interface/checkin`

After interfaces changes are committed with checkin timeout this method needs to be called
within that timeout limit to prevent reverting the changes.

This is to ensure user verifies the changes went as planned and its working.

<h3 id="get__interface_checkin-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__interface_checkin_waiting

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/interface/checkin_waiting

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/interface/checkin_waiting')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/interface/checkin_waiting");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /interface/checkin_waiting`

Returns wether or not we are waiting user to checkin the applied network changes
before they are rolled back.
Value is in number of seconds or null.

<h3 id="get__interface_checkin_waiting-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__interface_choices

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/interface/choices \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/interface/choices', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/interface/choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /interface/choices`

Choices of available network interfaces.

`bridge_members` will include BRIDGE members.
`lag_ports` will include LINK_AGGREGATION ports.
`vlan_parent` will include VLAN parent interface.
`exclude` is a list of interfaces prefix to remove.
`include` is a list of interfaces that should not be removed.

> Body parameter

```json
{}
```

<h3 id="post__interface_choices-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[interface_choices_0](#schemainterface_choices_0)|false|none|

<h3 id="post__interface_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__interface_commit

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/interface/commit \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/interface/commit', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/interface/commit");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /interface/commit`

Commit/apply pending interfaces changes.

`rollback` as true (default) will rollback changes in case they fail to apply.
`checkin_timeout` is the time in seconds it will wait for the checkin call to acknowledge
the interfaces changes happened as planned from the user. If checkin does not happen
within this period of time the changes will get reverted.

> Body parameter

```json
{}
```

<h3 id="post__interface_commit-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[interface_commit_0](#schemainterface_commit_0)|false|none|

<h3 id="post__interface_commit-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__interface_has_pending_changes

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/interface/has_pending_changes

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/interface/has_pending_changes')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/interface/has_pending_changes");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /interface/has_pending_changes`

Returns whether there are pending interfaces changes to be applied or not.

<h3 id="get__interface_has_pending_changes-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__interface_ip_in_use

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/interface/ip_in_use \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/interface/ip_in_use', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/interface/ip_in_use");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /interface/ip_in_use`

Get all IPv4 / Ipv6 from all valid interfaces, excluding tap and epair.

`loopback` will return loopback interface addresses.

`any` will return wildcard addresses (0.0.0.0 and ::).

`static` when enabled will ensure we only return static ip's configured.

Returns a list of dicts - eg -

[
    {
        "type": "INET6",
        "address": "fe80::5054:ff:fe16:4aac",
        "netmask": 64
    },
    {
        "type": "INET",
        "address": "192.168.122.148",
        "netmask": 24,
        "broadcast": "192.168.122.255"
    },
]

> Body parameter

```json
{}
```

<h3 id="post__interface_ip_in_use-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[interface_ip_in_use_0](#schemainterface_ip_in_use_0)|false|none|

<h3 id="post__interface_ip_in_use-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__interface_lag_ports_choices

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/interface/lag_ports_choices \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/interface/lag_ports_choices', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/interface/lag_ports_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /interface/lag_ports_choices`

Return available interface choices for `lag_ports` attribute.

`id` is the name of the LAG interface to update or null for a new
LAG interface.

> Body parameter

```json
null
```

<h3 id="post__interface_lag_ports_choices-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[interface_lag_ports_choices_0](#schemainterface_lag_ports_choices_0)|false|none|

<h3 id="post__interface_lag_ports_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__interface_lag_setup

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/interface/lag_setup

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/interface/lag_setup')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/interface/lag_setup");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /interface/lag_setup`

<h3 id="get__interface_lag_setup-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__interface_rollback

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/interface/rollback

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/interface/rollback')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/interface/rollback");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /interface/rollback`

Rollback pending interfaces changes.

<h3 id="get__interface_rollback-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__interface_vlan_parent_interface_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/interface/vlan_parent_interface_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/interface/vlan_parent_interface_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/interface/vlan_parent_interface_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /interface/vlan_parent_interface_choices`

Return available interface choices for `vlan_parent_interface` attribute.

<h3 id="get__interface_vlan_parent_interface_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__interface_vlan_setup

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/interface/vlan_setup

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/interface/vlan_setup')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/interface/vlan_setup");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /interface/vlan_setup`

<h3 id="get__interface_vlan_setup-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-ipmi">ipmi</h1>

## get__ipmi

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/ipmi

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/ipmi')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/ipmi");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /ipmi`

Query all IPMI Channels with `query-filters` and `query-options`.

<h3 id="get__ipmi-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__ipmi-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__ipmi_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/ipmi/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/ipmi/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/ipmi/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /ipmi/id/{id}`

Query all IPMI Channels with `query-filters` and `query-options`.

<h3 id="get__ipmi_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__ipmi_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__ipmi_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/ipmi/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/ipmi/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/ipmi/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /ipmi/id/{id}`

Update `id` IPMI Configuration.

`ipaddress` is a valid ip which will be used to connect to the IPMI interface.

`netmask` is the subnet mask associated with `ipaddress`.

`dhcp` is a boolean value which if unset means that `ipaddress`, `netmask` and `gateway` must be set.

> Body parameter

```json
{}
```

<h3 id="put__ipmi_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[ipmi_update_1](#schemaipmi_update_1)|false|none|

<h3 id="put__ipmi_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__ipmi_channels

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/ipmi/channels

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/ipmi/channels')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/ipmi/channels");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /ipmi/channels`

Return a list with the IPMI channels available.

<h3 id="get__ipmi_channels-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__ipmi_identify

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/ipmi/identify \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/ipmi/identify', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/ipmi/identify");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /ipmi/identify`

Turn on IPMI chassis identify light.

To turn off specify 0 as `seconds`.

> Body parameter

```json
{}
```

<h3 id="post__ipmi_identify-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[ipmi_identify_0](#schemaipmi_identify_0)|false|none|

<h3 id="post__ipmi_identify-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__ipmi_is_loaded

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/ipmi/is_loaded

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/ipmi/is_loaded')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/ipmi/is_loaded");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /ipmi/is_loaded`

Returns a boolean true value indicating if ipmi device is loaded.

<h3 id="get__ipmi_is_loaded-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-iscsi-auth">iscsi.auth</h1>

## get__iscsi_auth

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/iscsi/auth

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/iscsi/auth')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/auth");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /iscsi/auth`

<h3 id="get__iscsi_auth-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__iscsi_auth-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__iscsi_auth

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/iscsi/auth \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/iscsi/auth', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/auth");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /iscsi/auth`

Create an iSCSI Authorized Access.

`tag` should be unique among all configured iSCSI Authorized Accesses.

`secret` and `peersecret` should have length between 12-16 letters inclusive.

`peeruser` and `peersecret` are provided only when configuring mutual CHAP. `peersecret` should not be
similar to `secret`.

> Body parameter

```json
{}
```

<h3 id="post__iscsi_auth-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[iscsi_auth_create_0](#schemaiscsi_auth_create_0)|false|none|

<h3 id="post__iscsi_auth-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__iscsi_auth_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/iscsi/auth/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/iscsi/auth/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/auth/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /iscsi/auth/id/{id}`

Delete iSCSI Authorized Access of `id`.

<h3 id="delete__iscsi_auth_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__iscsi_auth_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__iscsi_auth_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/iscsi/auth/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/iscsi/auth/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/auth/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /iscsi/auth/id/{id}`

<h3 id="get__iscsi_auth_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__iscsi_auth_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__iscsi_auth_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/iscsi/auth/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/iscsi/auth/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/auth/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /iscsi/auth/id/{id}`

Update iSCSI Authorized Access of `id`.

> Body parameter

```json
{}
```

<h3 id="put__iscsi_auth_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[iscsi_auth_update_1](#schemaiscsi_auth_update_1)|false|none|

<h3 id="put__iscsi_auth_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-iscsi-extent">iscsi.extent</h1>

## get__iscsi_extent

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/iscsi/extent

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/iscsi/extent')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/extent");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /iscsi/extent`

<h3 id="get__iscsi_extent-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__iscsi_extent-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__iscsi_extent

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/iscsi/extent \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/iscsi/extent', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/extent");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /iscsi/extent`

Create an iSCSI Extent.

When `type` is set to FILE, attribute `filesize` is used and it represents number of bytes. `filesize` if
not zero should be a multiple of `blocksize`. `path` is a required attribute with `type` set as FILE and it
should be ensured that it does not come under a jail root.

With `type` being set to DISK, a valid ZVOL or DISK should be provided.

`insecure_tpc` when enabled allows an initiator to bypass normal access control and access any scannable
target. This allows xcopy operations otherwise blocked by access control.

`xen` is a boolean value which is set to true if Xen is being used as the iSCSI initiator.

`ro` when set to true prevents the initiator from writing to this LUN.

> Body parameter

```json
{}
```

<h3 id="post__iscsi_extent-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[iscsi_extent_create_0](#schemaiscsi_extent_create_0)|false|none|

<h3 id="post__iscsi_extent-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__iscsi_extent_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/iscsi/extent/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.delete('http://192.168.1.123/api/v2.0/iscsi/extent/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/extent/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /iscsi/extent/id/{id}`

Delete iSCSI Extent of `id`.

If `id` iSCSI Extent's `type` was configured to FILE, `remove` can be set to remove the configured file.

> Body parameter

```json
{
  "remove": false,
  "force": false
}
```

<h3 id="delete__iscsi_extent_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[iscsi_extent_delete](#schemaiscsi_extent_delete)|false|none|

<h3 id="delete__iscsi_extent_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__iscsi_extent_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/iscsi/extent/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/iscsi/extent/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/extent/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /iscsi/extent/id/{id}`

<h3 id="get__iscsi_extent_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__iscsi_extent_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__iscsi_extent_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/iscsi/extent/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/iscsi/extent/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/extent/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /iscsi/extent/id/{id}`

Update iSCSI Extent of `id`.

> Body parameter

```json
{}
```

<h3 id="put__iscsi_extent_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[iscsi_extent_update_1](#schemaiscsi_extent_update_1)|false|none|

<h3 id="put__iscsi_extent_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__iscsi_extent_disk_choices

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/iscsi/extent/disk_choices \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/iscsi/extent/disk_choices', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/extent/disk_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /iscsi/extent/disk_choices`

Exclude will exclude the path from being in the used_zvols list,
allowing the user to keep the same item on update

> Body parameter

```json
[]
```

<h3 id="post__iscsi_extent_disk_choices-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[iscsi_extent_disk_choices_0](#schemaiscsi_extent_disk_choices_0)|false|none|

<h3 id="post__iscsi_extent_disk_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-iscsi-global">iscsi.global</h1>

## get__iscsi_global

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/iscsi/global

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/iscsi/global')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/global");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /iscsi/global`

<h3 id="get__iscsi_global-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__iscsi_global

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/iscsi/global \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/iscsi/global', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/global");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /iscsi/global`

`alua` is a no-op for FreeNAS.

> Body parameter

```json
{}
```

<h3 id="put__iscsi_global-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[iscsi_global_update_0](#schemaiscsi_global_update_0)|false|none|

<h3 id="put__iscsi_global-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__iscsi_global_alua_enabled

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/iscsi/global/alua_enabled

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/iscsi/global/alua_enabled')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/global/alua_enabled");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /iscsi/global/alua_enabled`

Returns whether iSCSI ALUA is enabled or not.

<h3 id="get__iscsi_global_alua_enabled-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__iscsi_global_sessions

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/iscsi/global/sessions

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/iscsi/global/sessions')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/global/sessions");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /iscsi/global/sessions`

Get a list of currently running iSCSI sessions. This includes initiator and target names
and the unique connection IDs.

<h3 id="get__iscsi_global_sessions-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__iscsi_global_sessions-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-iscsi-initiator">iscsi.initiator</h1>

## get__iscsi_initiator

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/iscsi/initiator

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/iscsi/initiator')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/initiator");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /iscsi/initiator`

<h3 id="get__iscsi_initiator-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__iscsi_initiator-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__iscsi_initiator

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/iscsi/initiator \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/iscsi/initiator', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/initiator");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /iscsi/initiator`

Create an iSCSI Initiator.

`initiators` is a list of initiator hostnames which are authorized to access an iSCSI Target. To allow all
possible initiators, `initiators` can be left empty.

`auth_network` is a list of IP/CIDR addresses which are allowed to use this initiator. If all networks are
to be allowed, this field should be left empty.

> Body parameter

```json
{}
```

<h3 id="post__iscsi_initiator-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[iscsi_initiator_create_0](#schemaiscsi_initiator_create_0)|false|none|

<h3 id="post__iscsi_initiator-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__iscsi_initiator_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/iscsi/initiator/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/iscsi/initiator/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/initiator/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /iscsi/initiator/id/{id}`

Delete iSCSI initiator of `id`.

<h3 id="delete__iscsi_initiator_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__iscsi_initiator_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__iscsi_initiator_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/iscsi/initiator/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/iscsi/initiator/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/initiator/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /iscsi/initiator/id/{id}`

<h3 id="get__iscsi_initiator_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__iscsi_initiator_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__iscsi_initiator_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/iscsi/initiator/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/iscsi/initiator/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/initiator/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /iscsi/initiator/id/{id}`

Update iSCSI initiator of `id`.

> Body parameter

```json
{}
```

<h3 id="put__iscsi_initiator_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[iscsi_initiator_update_1](#schemaiscsi_initiator_update_1)|false|none|

<h3 id="put__iscsi_initiator_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-iscsi-portal">iscsi.portal</h1>

## get__iscsi_portal

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/iscsi/portal

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/iscsi/portal')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/portal");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /iscsi/portal`

<h3 id="get__iscsi_portal-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__iscsi_portal-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__iscsi_portal

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/iscsi/portal \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/iscsi/portal', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/portal");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /iscsi/portal`

Create a new iSCSI Portal.

`discovery_authgroup` is required for CHAP and CHAP_MUTUAL.

> Body parameter

```json
{}
```

<h3 id="post__iscsi_portal-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[iscsi_portal_create_0](#schemaiscsi_portal_create_0)|false|none|

<h3 id="post__iscsi_portal-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__iscsi_portal_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/iscsi/portal/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/iscsi/portal/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/portal/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /iscsi/portal/id/{id}`

Delete iSCSI Portal `id`.

<h3 id="delete__iscsi_portal_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__iscsi_portal_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__iscsi_portal_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/iscsi/portal/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/iscsi/portal/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/portal/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /iscsi/portal/id/{id}`

<h3 id="get__iscsi_portal_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__iscsi_portal_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__iscsi_portal_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/iscsi/portal/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/iscsi/portal/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/portal/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /iscsi/portal/id/{id}`

Update iSCSI Portal `id`.

> Body parameter

```json
{}
```

<h3 id="put__iscsi_portal_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[iscsi_portal_update_1](#schemaiscsi_portal_update_1)|false|none|

<h3 id="put__iscsi_portal_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__iscsi_portal_listen_ip_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/iscsi/portal/listen_ip_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/iscsi/portal/listen_ip_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/portal/listen_ip_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /iscsi/portal/listen_ip_choices`

Returns possible choices for `listen.ip` attribute of portal create and update.

<h3 id="get__iscsi_portal_listen_ip_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-iscsi-target">iscsi.target</h1>

## get__iscsi_target

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/iscsi/target

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/iscsi/target')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/target");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /iscsi/target`

<h3 id="get__iscsi_target-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__iscsi_target-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__iscsi_target

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/iscsi/target \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/iscsi/target', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/target");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /iscsi/target`

Create an iSCSI Target.

`groups` is a list of group dictionaries which provide information related to using a `portal`, `initiator`,
`authmethod` and `auth` with this target. `auth` represents a valid iSCSI Authorized Access and defaults to
null.

> Body parameter

```json
{}
```

<h3 id="post__iscsi_target-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[iscsi_target_create_0](#schemaiscsi_target_create_0)|false|none|

<h3 id="post__iscsi_target-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__iscsi_target_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/iscsi/target/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.delete('http://192.168.1.123/api/v2.0/iscsi/target/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/target/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /iscsi/target/id/{id}`

Delete iSCSI Target of `id`.

Deleting an iSCSI Target makes sure we delete all Associated Targets which use `id` iSCSI Target.

> Body parameter

```json
false
```

<h3 id="delete__iscsi_target_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[iscsi_target_delete_1](#schemaiscsi_target_delete_1)|false|none|

<h3 id="delete__iscsi_target_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__iscsi_target_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/iscsi/target/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/iscsi/target/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/target/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /iscsi/target/id/{id}`

<h3 id="get__iscsi_target_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__iscsi_target_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__iscsi_target_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/iscsi/target/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/iscsi/target/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/target/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /iscsi/target/id/{id}`

Update iSCSI Target of `id`.

> Body parameter

```json
{}
```

<h3 id="put__iscsi_target_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[iscsi_target_update_1](#schemaiscsi_target_update_1)|false|none|

<h3 id="put__iscsi_target_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-iscsi-targetextent">iscsi.targetextent</h1>

## get__iscsi_targetextent

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/iscsi/targetextent

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/iscsi/targetextent')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/targetextent");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /iscsi/targetextent`

<h3 id="get__iscsi_targetextent-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__iscsi_targetextent-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__iscsi_targetextent

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/iscsi/targetextent \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/iscsi/targetextent', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/targetextent");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /iscsi/targetextent`

Create an Associated Target.

`lunid` will be automatically assigned if it is not provided based on the `target`.

> Body parameter

```json
{}
```

<h3 id="post__iscsi_targetextent-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[iscsi_targetextent_create_0](#schemaiscsi_targetextent_create_0)|false|none|

<h3 id="post__iscsi_targetextent-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__iscsi_targetextent_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/iscsi/targetextent/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.delete('http://192.168.1.123/api/v2.0/iscsi/targetextent/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/targetextent/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /iscsi/targetextent/id/{id}`

Delete Associated Target of `id`.

> Body parameter

```json
false
```

<h3 id="delete__iscsi_targetextent_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[iscsi_targetextent_delete_1](#schemaiscsi_targetextent_delete_1)|false|none|

<h3 id="delete__iscsi_targetextent_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__iscsi_targetextent_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/iscsi/targetextent/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/iscsi/targetextent/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/targetextent/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /iscsi/targetextent/id/{id}`

<h3 id="get__iscsi_targetextent_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__iscsi_targetextent_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__iscsi_targetextent_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/iscsi/targetextent/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/iscsi/targetextent/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/iscsi/targetextent/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /iscsi/targetextent/id/{id}`

Update Associated Target of `id`.

> Body parameter

```json
{}
```

<h3 id="put__iscsi_targetextent_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[iscsi_targetextent_update_1](#schemaiscsi_targetextent_update_1)|false|none|

<h3 id="put__iscsi_targetextent_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-jail">jail</h1>

## get__jail

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/jail

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/jail')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/jail");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /jail`

Query all jails with `query-filters` and `query-options`.

<h3 id="get__jail-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__jail-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__jail

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/jail \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/jail', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/jail");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /jail`

Creates a jail.

> Body parameter

```json
{}
```

<h3 id="post__jail-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[jail_create_0](#schemajail_create_0)|false|none|

<h3 id="post__jail-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__jail_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/jail/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.delete('http://192.168.1.123/api/v2.0/jail/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/jail/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /jail/id/{id}`

Takes a jail and destroys it.

> Body parameter

```json
{}
```

<h3 id="delete__jail_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|string|true|none|
|body|body|[jail_delete_1](#schemajail_delete_1)|false|none|

<h3 id="delete__jail_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__jail_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/jail/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/jail/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/jail/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /jail/id/{id}`

Query all jails with `query-filters` and `query-options`.

<h3 id="get__jail_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__jail_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__jail_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/jail/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/jail/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/jail/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /jail/id/{id}`

Sets a jail property.

> Body parameter

```json
{}
```

<h3 id="put__jail_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|string|true|none|
|body|body|[jail_update_1](#schemajail_update_1)|false|none|

<h3 id="put__jail_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__jail_activate

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/jail/activate \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/jail/activate', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/jail/activate");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /jail/activate`

Activates a pool for iocage usage, and deactivates the rest.

> Body parameter

```json
"string"
```

<h3 id="post__jail_activate-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[jail_activate_0](#schemajail_activate_0)|false|none|

<h3 id="post__jail_activate-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__jail_clean

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/jail/clean \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/jail/clean', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/jail/clean");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /jail/clean`

Cleans all iocage datasets of ds_type

> Body parameter

```json
"ALL"
```

<h3 id="post__jail_clean-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[jail_clean_0](#schemajail_clean_0)|false|none|

#### Enumerated Values

|Parameter|Value|
|---|---|
|body|ALL|
|body|JAIL|
|body|TEMPLATE|
|body|RELEASE|

<h3 id="post__jail_clean-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__jail_id_{id}_clone

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/jail/id/{id}/clone \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/jail/id/{id}/clone', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/jail/id/{id}/clone");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /jail/id/{id}/clone`

> Body parameter

```json
{}
```

<h3 id="post__jail_id_{id}_clone-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|string|true|none|
|body|body|[jail_clone_1](#schemajail_clone_1)|false|none|

<h3 id="post__jail_id_{id}_clone-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__jail_default_configuration

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/jail/default_configuration

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/jail/default_configuration')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/jail/default_configuration");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /jail/default_configuration`

Retrieve default configuration for iocage jails.

<h3 id="get__jail_default_configuration-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__jail_exec

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/jail/exec \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/jail/exec', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/jail/exec");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /jail/exec`

Issues a command inside a jail.

> Body parameter

```json
{
  "jail": "string",
  "command": [
    null
  ],
  "options": {}
}
```

<h3 id="post__jail_exec-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[jail_exec](#schemajail_exec)|false|none|

<h3 id="post__jail_exec-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__jail_export

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/jail/export \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/jail/export', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/jail/export");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /jail/export`

Export jail to compressed file.

> Body parameter

```json
{}
```

<h3 id="post__jail_export-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[jail_export_0](#schemajail_export_0)|false|none|

<h3 id="post__jail_export-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__jail_fetch

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/jail/fetch \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/jail/fetch', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/jail/fetch");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /jail/fetch`

Fetches a release or plugin.

> Body parameter

```json
{}
```

<h3 id="post__jail_fetch-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[jail_fetch_0](#schemajail_fetch_0)|false|none|

<h3 id="post__jail_fetch-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__jail_fstab

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/jail/fstab \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/jail/fstab', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/jail/fstab");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /jail/fstab`

Manipulate a jails fstab

> Body parameter

```json
{
  "jail": "string",
  "options": {}
}
```

<h3 id="post__jail_fstab-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[jail_fstab](#schemajail_fstab)|false|none|

<h3 id="post__jail_fstab-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__jail_get_activated_pool

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/jail/get_activated_pool

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/jail/get_activated_pool')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/jail/get_activated_pool");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /jail/get_activated_pool`

Returns the activated pool if there is one, or None

<h3 id="get__jail_get_activated_pool-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__jail_import_image

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/jail/import_image \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/jail/import_image', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/jail/import_image");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /jail/import_image`

Import jail from compressed file.

`compression algorithm`: None indicates that middlewared is to automatically determine
which compression algorithm to use based on the compressed file extension. If multiple copies are found, an
exception is raised.

`path` is the directory where the exported jail lives. It defaults to the iocage images dataset.

> Body parameter

```json
{}
```

<h3 id="post__jail_import_image-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[jail_import_image_0](#schemajail_import_image_0)|false|none|

<h3 id="post__jail_import_image-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__jail_interface_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/jail/interface_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/jail/interface_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/jail/interface_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /jail/interface_choices`

Returns a dictionary of interface choices which can be used with creating/updating jails.

<h3 id="get__jail_interface_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__jail_rc_action

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/jail/rc_action \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/jail/rc_action', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/jail/rc_action");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /jail/rc_action`

Does specified action on rc enabled (boot=on) jails

> Body parameter

```json
"START"
```

<h3 id="post__jail_rc_action-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[jail_rc_action_0](#schemajail_rc_action_0)|false|none|

#### Enumerated Values

|Parameter|Value|
|---|---|
|body|START|
|body|STOP|
|body|RESTART|

<h3 id="post__jail_rc_action-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__jail_releases_choices

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/jail/releases_choices \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/jail/releases_choices', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/jail/releases_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /jail/releases_choices`

List installed or available releases which can be downloaded.

> Body parameter

```json
false
```

<h3 id="post__jail_releases_choices-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[jail_releases_choices_0](#schemajail_releases_choices_0)|false|none|

<h3 id="post__jail_releases_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__jail_restart

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/jail/restart \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/jail/restart', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/jail/restart");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /jail/restart`

Takes a jail and restarts it.

> Body parameter

```json
"string"
```

<h3 id="post__jail_restart-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[jail_restart_0](#schemajail_restart_0)|false|none|

<h3 id="post__jail_restart-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__jail_start

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/jail/start \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/jail/start', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/jail/start");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /jail/start`

Takes a jail and starts it.

> Body parameter

```json
"string"
```

<h3 id="post__jail_start-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[jail_start_0](#schemajail_start_0)|false|none|

<h3 id="post__jail_start-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__jail_stop

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/jail/stop \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/jail/stop', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/jail/stop");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /jail/stop`

Takes a jail and stops it.

> Body parameter

```json
{
  "jail": "string",
  "force": false
}
```

<h3 id="post__jail_stop-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[jail_stop](#schemajail_stop)|false|none|

<h3 id="post__jail_stop-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__jail_update_defaults

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/jail/update_defaults \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/jail/update_defaults', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/jail/update_defaults");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /jail/update_defaults`

Update default properties for iocage which will remain true for all jails moving on i.e nat_backend

> Body parameter

```json
{}
```

<h3 id="post__jail_update_defaults-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[jail_update_defaults_0](#schemajail_update_defaults_0)|false|none|

<h3 id="post__jail_update_defaults-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__jail_update_to_latest_patch

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/jail/update_to_latest_patch \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/jail/update_to_latest_patch', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/jail/update_to_latest_patch");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /jail/update_to_latest_patch`

Updates specified jail to latest patch level.

> Body parameter

```json
{
  "jail": "string",
  "update_pkgs": false
}
```

<h3 id="post__jail_update_to_latest_patch-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[jail_update_to_latest_patch](#schemajail_update_to_latest_patch)|false|none|

<h3 id="post__jail_update_to_latest_patch-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-kerberos">kerberos</h1>

## get__kerberos

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/kerberos

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/kerberos')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/kerberos");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /kerberos`

<h3 id="get__kerberos-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__kerberos

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/kerberos \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/kerberos', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/kerberos");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /kerberos`

`appdefaults_aux` add parameters to "appdefaults" section of the krb5.conf file.

`libdefaults_aux` add parameters to "libdefaults" section of the krb5.conf file.

> Body parameter

```json
{}
```

<h3 id="put__kerberos-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[kerberos_update_0](#schemakerberos_update_0)|false|none|

<h3 id="put__kerberos-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-kerberos-keytab">kerberos.keytab</h1>

## get__kerberos_keytab

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/kerberos/keytab

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/kerberos/keytab')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/kerberos/keytab");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /kerberos/keytab`

<h3 id="get__kerberos_keytab-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__kerberos_keytab-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__kerberos_keytab

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/kerberos/keytab \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/kerberos/keytab', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/kerberos/keytab");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /kerberos/keytab`

Create a kerberos keytab. Uploaded keytab files will be merged with the system
keytab under /etc/krb5.keytab.

`file` b64encoded kerberos keytab
`name` name for kerberos keytab

> Body parameter

```json
{}
```

<h3 id="post__kerberos_keytab-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[kerberos_keytab_create_0](#schemakerberos_keytab_create_0)|false|none|

<h3 id="post__kerberos_keytab-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__kerberos_keytab_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/kerberos/keytab/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/kerberos/keytab/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/kerberos/keytab/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /kerberos/keytab/id/{id}`

Delete kerberos keytab by id, and force regeneration of
system keytab.

<h3 id="delete__kerberos_keytab_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__kerberos_keytab_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__kerberos_keytab_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/kerberos/keytab/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/kerberos/keytab/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/kerberos/keytab/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /kerberos/keytab/id/{id}`

<h3 id="get__kerberos_keytab_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__kerberos_keytab_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__kerberos_keytab_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/kerberos/keytab/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/kerberos/keytab/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/kerberos/keytab/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /kerberos/keytab/id/{id}`

Update kerberos keytab by id.

> Body parameter

```json
{}
```

<h3 id="put__kerberos_keytab_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[kerberos_keytab_update_1](#schemakerberos_keytab_update_1)|false|none|

<h3 id="put__kerberos_keytab_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__kerberos_keytab_system_keytab_list

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/kerberos/keytab/system_keytab_list

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/kerberos/keytab/system_keytab_list')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/kerberos/keytab/system_keytab_list");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /kerberos/keytab/system_keytab_list`

Returns content of system keytab (/etc/krb5.keytab).

<h3 id="get__kerberos_keytab_system_keytab_list-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-kerberos-realm">kerberos.realm</h1>

## get__kerberos_realm

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/kerberos/realm

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/kerberos/realm')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/kerberos/realm");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /kerberos/realm`

<h3 id="get__kerberos_realm-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__kerberos_realm-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__kerberos_realm

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/kerberos/realm \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/kerberos/realm', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/kerberos/realm");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /kerberos/realm`

Create a new kerberos realm. This will be automatically populated during the
domain join process in an Active Directory environment. Kerberos realm names
are case-sensitive, but convention is to only use upper-case.

Entries for kdc, admin_server, and kpasswd_server are not required.
If they are unpopulated, then kerberos will use DNS srv records to
discover the correct servers. The option to hard-code them is provided
due to AD site discovery. Kerberos has no concept of Active Directory
sites. This means that middleware performs the site discovery and
sets the kerberos configuration based on the AD site.

> Body parameter

```json
{}
```

<h3 id="post__kerberos_realm-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[kerberos_realm_create_0](#schemakerberos_realm_create_0)|false|none|

<h3 id="post__kerberos_realm-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__kerberos_realm_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/kerberos/realm/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/kerberos/realm/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/kerberos/realm/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /kerberos/realm/id/{id}`

Delete a kerberos realm by ID.

<h3 id="delete__kerberos_realm_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__kerberos_realm_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__kerberos_realm_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/kerberos/realm/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/kerberos/realm/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/kerberos/realm/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /kerberos/realm/id/{id}`

<h3 id="get__kerberos_realm_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__kerberos_realm_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__kerberos_realm_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/kerberos/realm/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/kerberos/realm/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/kerberos/realm/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /kerberos/realm/id/{id}`

Update a kerberos realm by id. This will be automatically populated during the
domain join process in an Active Directory environment. Kerberos realm names
are case-sensitive, but convention is to only use upper-case.

> Body parameter

```json
{}
```

<h3 id="put__kerberos_realm_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[kerberos_realm_update_1](#schemakerberos_realm_update_1)|false|none|

<h3 id="put__kerberos_realm_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-keychaincredential">keychaincredential</h1>

## get__keychaincredential

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/keychaincredential

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/keychaincredential')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/keychaincredential");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /keychaincredential`

<h3 id="get__keychaincredential-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__keychaincredential-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__keychaincredential

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/keychaincredential \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/keychaincredential', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/keychaincredential");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /keychaincredential`

Create a Keychain Credential

Create a Keychain Credential of any type.
Every Keychain Credential has a `name` which is used to distinguish it from others.
The following `type`s are supported:
 * `SSH_KEY_PAIR`
   Which `attributes` are:
   * `private_key`
   * `public_key` (which can be omitted and thus automatically derived from private key)
   At least one attribute is required.

 * `SSH_CREDENTIALS`
   Which `attributes` are:
   * `host`
   * `port` (default 22)
   * `username` (default root)
   * `private_key` (Keychain Credential ID)
   * `remote_host_key` (you can use `keychaincredential.remote_ssh_host_key_scan` do discover it)
   * `cipher`: one of `STANDARD`, `FAST`, or `DISABLED` (last requires special support from both SSH server and
     client)
   * `connect_timeout` (default 10)

> Body parameter

```json
{}
```

<h3 id="post__keychaincredential-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[keychaincredential_create_0](#schemakeychaincredential_create_0)|false|none|

<h3 id="post__keychaincredential-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__keychaincredential_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/keychaincredential/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.delete('http://192.168.1.123/api/v2.0/keychaincredential/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/keychaincredential/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /keychaincredential/id/{id}`

Delete Keychain Credential with specific `id`

> Body parameter

```json
{}
```

<h3 id="delete__keychaincredential_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[keychaincredential_delete_1](#schemakeychaincredential_delete_1)|false|none|

<h3 id="delete__keychaincredential_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__keychaincredential_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/keychaincredential/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/keychaincredential/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/keychaincredential/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /keychaincredential/id/{id}`

<h3 id="get__keychaincredential_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__keychaincredential_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__keychaincredential_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/keychaincredential/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/keychaincredential/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/keychaincredential/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /keychaincredential/id/{id}`

Update a Keychain Credential with specific `id`

Please note that you can't change `type`

Also you must specify full `attributes` value

See the documentation for `create` method for information on payload contents

> Body parameter

```json
{}
```

<h3 id="put__keychaincredential_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[keychaincredential_update_1](#schemakeychaincredential_update_1)|false|none|

<h3 id="put__keychaincredential_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__keychaincredential_generate_ssh_key_pair

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/keychaincredential/generate_ssh_key_pair

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/keychaincredential/generate_ssh_key_pair')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/keychaincredential/generate_ssh_key_pair");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /keychaincredential/generate_ssh_key_pair`

Generate a public/private key pair

Generate a public/private key pair (useful for `SSH_KEY_PAIR` type)

<h3 id="get__keychaincredential_generate_ssh_key_pair-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__keychaincredential_remote_ssh_host_key_scan

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/keychaincredential/remote_ssh_host_key_scan \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/keychaincredential/remote_ssh_host_key_scan', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/keychaincredential/remote_ssh_host_key_scan");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /keychaincredential/remote_ssh_host_key_scan`

Discover a remote host key

Discover a remote host key (useful for `SSH_CREDENTIALS`)

> Body parameter

```json
{}
```

<h3 id="post__keychaincredential_remote_ssh_host_key_scan-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[keychaincredential_remote_ssh_host_key_scan_0](#schemakeychaincredential_remote_ssh_host_key_scan_0)|false|none|

<h3 id="post__keychaincredential_remote_ssh_host_key_scan-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__keychaincredential_remote_ssh_semiautomatic_setup

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/keychaincredential/remote_ssh_semiautomatic_setup \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/keychaincredential/remote_ssh_semiautomatic_setup', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/keychaincredential/remote_ssh_semiautomatic_setup");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /keychaincredential/remote_ssh_semiautomatic_setup`

Perform semi-automatic SSH connection setup with other FreeNAS machine

Perform semi-automatic SSH connection setup with other FreeNAS machine. It creates a `SSH_CREDENTIALS`
credential with specified `name` that can be used to connect to FreeNAS machine with specified `url` and
temporary auth `token`. Other FreeNAS machine adds `private_key` to allowed `username`'s private keys. Other
`SSH_CREDENTIALS` attributes such as `cipher` and `connect_timeout` can be specified as well.

> Body parameter

```json
{}
```

<h3 id="post__keychaincredential_remote_ssh_semiautomatic_setup-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[keychaincredential_remote_ssh_semiautomatic_setup_0](#schemakeychaincredential_remote_ssh_semiautomatic_setup_0)|false|none|

<h3 id="post__keychaincredential_remote_ssh_semiautomatic_setup-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__keychaincredential_used_by

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/keychaincredential/used_by \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/keychaincredential/used_by', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/keychaincredential/used_by");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /keychaincredential/used_by`

Returns list of objects that use this credential.

> Body parameter

```json
0
```

<h3 id="post__keychaincredential_used_by-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[keychaincredential_used_by_0](#schemakeychaincredential_used_by_0)|false|none|

<h3 id="post__keychaincredential_used_by-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-kmip">kmip</h1>

## get__kmip

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/kmip

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/kmip')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/kmip");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /kmip`

<h3 id="get__kmip-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__kmip

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/kmip \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/kmip', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/kmip");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /kmip`

Update KMIP Server Configuration.

System currently authenticates connection with remote KMIP Server with a TLS handshake. `certificate` and
`certificate_authority` determine the certs which will be used to initiate the TLS handshake with `server`.

`validate` is enabled by default. When enabled, system will test connection to `server` making sure
it's reachable.

`manage_zfs_keys`/`manage_sed_disks` when enabled will sync keys from local database to remote KMIP server.
When disabled, if there are any keys left to be retrieved from the KMIP server,
it will sync them back to local database.

`enabled` if true, cannot be set to disabled if there are existing keys pending to be synced. However users
can still perform this action by enabling `force_clear`.

`change_server` is a boolean field which allows users to migrate data between two KMIP servers. System
will first migrate keys from old KMIP server to local database and then migrate the keys from local database
to new KMIP server. If it is unable to retrieve all the keys from old server, this will fail. Users can bypass
this by enabling `force_clear`.

`force_clear` is a boolean option which when enabled will in this case remove all
pending keys to be synced from database. It should be used with extreme caution as users may end up with
not having ZFS dataset or SED disks keys leaving them locked forever. It is disabled by default.

> Body parameter

```json
{}
```

<h3 id="put__kmip-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[kmip_update_0](#schemakmip_update_0)|false|none|

<h3 id="put__kmip-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__kmip_clear_sync_pending_keys

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/kmip/clear_sync_pending_keys

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/kmip/clear_sync_pending_keys')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/kmip/clear_sync_pending_keys");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /kmip/clear_sync_pending_keys`

Clear all keys which are pending to be synced between KMIP server and TN database.

For ZFS/SED keys, we remove the UID from local database with which we are able to retrieve ZFS/SED keys.
It should be used with caution.

<h3 id="get__kmip_clear_sync_pending_keys-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__kmip_kmip_sync_pending

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/kmip/kmip_sync_pending

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/kmip/kmip_sync_pending')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/kmip/kmip_sync_pending");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /kmip/kmip_sync_pending`

Returns true or false based on if there are keys which are to be synced from local database to remote KMIP
server or vice versa.

<h3 id="get__kmip_kmip_sync_pending-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__kmip_sync_keys

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/kmip/sync_keys

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/kmip/sync_keys')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/kmip/sync_keys");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /kmip/sync_keys`

Sync ZFS/SED keys between KMIP Server and TN database.

<h3 id="get__kmip_sync_keys-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-ldap">ldap</h1>

## get__ldap

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/ldap

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/ldap')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/ldap");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /ldap`

<h3 id="get__ldap-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__ldap

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/ldap \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/ldap', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/ldap");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /ldap`

`hostname` list of ip addresses or hostnames of LDAP servers with
which to communicate in order of preference. Failover only occurs
if the current LDAP server is unresponsive.

`basedn` specifies the default base DN to use when performing ldap
operations. The base must be specified as a Distinguished Name in LDAP
format.

`binddn` specifies the default bind DN to use when performing ldap
operations. The bind DN must be specified as a Distinguished Name in
LDAP format.

`anonbind` use anonymous authentication.

`ssl` establish SSL/TLS-protected connections to the LDAP server(s).
GSSAPI signing is disabled on SSL/TLS-protected connections if
kerberos authentication is used.

`certificate` LDAPs client certificate to be used for certificate-
based authentication.

`validate_certificates` specifies whether to perform checks on server
certificates in a TLS session. If enabled, TLS_REQCERT demand is set.
The server certificate is requested. If no certificate is provided or
if a bad certificate is provided, the session is immediately terminated.
If disabled, TLS_REQCERT allow is set. The server certificate is
requested, but all errors are ignored.

`kerberos_realm` in which the server is located. This parameter is
only required for SASL GSSAPI authentication to the remote LDAP server.

`kerberos_principal` kerberos principal to use for SASL GSSAPI
authentication to the remote server. If `kerberos_realm` is specified
without a keytab, then the `binddn` and `bindpw` are used to
perform to obtain the ticket necessary for GSSAPI authentication.

`timeout` specifies  a  timeout  (in  seconds) after which calls to
synchronous LDAP APIs will abort if no response is received.

`dns_timeout` specifies the timeout (in seconds) after which the
poll(2)/select(2) following a connect(2) returns in case of no activity
for openldap. For nslcd this specifies the time limit (in seconds) to
use when connecting to the directory server. This directly impacts the
length of time that the LDAP service tries before failing over to
a secondary LDAP URI.

`has_samba_schema` determines whether to configure samba to use the
ldapsam passdb backend to provide SMB access to LDAP users. This feature
requires the presence of Samba LDAP schema extensions on the remote
LDAP server.

> Body parameter

```json
{}
```

<h3 id="put__ldap-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[ldap_update_0](#schemaldap_update_0)|false|none|

<h3 id="put__ldap-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__ldap_get_state

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/ldap/get_state

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/ldap/get_state')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/ldap/get_state");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /ldap/get_state`

Wrapper function for 'directoryservices.get_state'. Returns only the state of the
LDAP service.

<h3 id="get__ldap_get_state-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__ldap_schema_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/ldap/schema_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/ldap/schema_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/ldap/schema_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /ldap/schema_choices`

Returns list of available LDAP schema choices.

<h3 id="get__ldap_schema_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__ldap_ssl_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/ldap/ssl_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/ldap/ssl_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/ldap/ssl_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /ldap/ssl_choices`

Returns list of SSL choices.

<h3 id="get__ldap_ssl_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-lldp">lldp</h1>

## get__lldp

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/lldp

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/lldp')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/lldp");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /lldp`

<h3 id="get__lldp-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__lldp

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/lldp \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/lldp', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/lldp");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /lldp`

Update LLDP Service Configuration.

`country` is a two letter ISO 3166 country code required for LLDP location support.

`location` is an optional attribute specifying the physical location of the host.

> Body parameter

```json
{}
```

<h3 id="put__lldp-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[lldp_update_0](#schemalldp_update_0)|false|none|

<h3 id="put__lldp-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__lldp_country_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/lldp/country_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/lldp/country_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/lldp/country_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /lldp/country_choices`

Returns country choices for LLDP.

<h3 id="get__lldp_country_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-mail">mail</h1>

## get__mail

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/mail

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/mail')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/mail");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /mail`

<h3 id="get__mail-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__mail

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/mail \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/mail', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/mail");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /mail`

Update Mail Service Configuration.

`fromemail` is used as a sending address which the mail server will use for sending emails.

`outgoingserver` is the hostname or IP address of SMTP server used for sending an email.

`security` is type of encryption desired.

`smtp` is a boolean value which when set indicates that SMTP authentication has been enabled and `user`/`pass`
are required attributes now.

> Body parameter

```json
{}
```

<h3 id="put__mail-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[mail_update_0](#schemamail_update_0)|false|none|

<h3 id="put__mail-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__mail_send

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/mail/send \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/mail/send', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/mail/send");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /mail/send`

Sends mail using configured mail settings.

`text` will be formatted to HTML using Markdown and rendered using default E-Mail template.
You can put your own HTML using `html`. If `html` is null, no HTML MIME part will be added to E-Mail.

If `attachments` is true, a list compromised of the following dict is required
via HTTP upload:
  - headers(list)
    - name(str)
    - value(str)
    - params(dict)
  - content (str)

[
 {
  "headers": [
   {
    "name": "Content-Transfer-Encoding",
    "value": "base64"
   },
   {
    "name": "Content-Type",
    "value": "application/octet-stream",
    "params": {
     "name": "test.txt"
    }
   }
  ],
  "content": "dGVzdAo="
 }
]

A file can be uploaded to this end point. This end point is special, please refer to Jobs section in Websocket API documentation for details.

> Body parameter

```json
{
  "mail_message": {},
  "mail_update": {}
}
```

<h3 id="post__mail_send-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[mail_send](#schemamail_send)|false|none|

<h3 id="post__mail_send-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-multipath">multipath</h1>

## get__multipath

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/multipath

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/multipath')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/multipath");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /multipath`

Get multipaths and their consumers.

<h3 id="get__multipath-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__multipath-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__multipath_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/multipath/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/multipath/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/multipath/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /multipath/id/{id}`

Get multipaths and their consumers.

<h3 id="get__multipath_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__multipath_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-network-configuration">network.configuration</h1>

## get__network_configuration

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/network/configuration

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/network/configuration')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/network/configuration");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /network/configuration`

<h3 id="get__network_configuration-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__network_configuration

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/network/configuration \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/network/configuration', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/network/configuration");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /network/configuration`

Update Network Configuration Service configuration.

`ipv4gateway` if set is used instead of the default gateway provided by DHCP.

`nameserver1` is primary DNS server.

`nameserver2` is secondary DNS server.

`nameserver3` is tertiary DNS server.

`httpproxy` attribute must be provided if a proxy is to be used for network operations.

`netwait_enabled` is a boolean attribute which when set indicates that network services will not start at
boot unless they are able to ping the addresses listed in `netwait_ip` list.

`service_announcement` determines the broadcast protocols that will be used to advertise the server.
`netbios` enables the NetBIOS name server (NBNS), which starts concurrently with the SMB service. SMB clients
will only perform NBNS lookups if SMB1 is enabled. NBNS may be required for legacy SMB clients.
`mdns` enables multicast DNS service announcements for enabled services. `wsd` enables Web Service
Discovery support.

> Body parameter

```json
{}
```

<h3 id="put__network_configuration-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[network_configuration_update_0](#schemanetwork_configuration_update_0)|false|none|

<h3 id="put__network_configuration-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-network-general">network.general</h1>

## get__network_general_summary

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/network/general/summary

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/network/general/summary')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/network/general/summary");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /network/general/summary`

Retrieve general information for current Network.

Returns a dictionary. For example:

<h3 id="get__network_general_summary-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-nfs">nfs</h1>

## get__nfs

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/nfs

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/nfs')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/nfs");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /nfs`

<h3 id="get__nfs-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__nfs

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/nfs \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/nfs', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/nfs");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /nfs`

Update NFS Service Configuration.

`servers` represents number of servers to create.

When `allow_nonroot` is set, it allows non-root mount requests to be served.

`bindip` is a list of IP's on which NFS will listen for requests. When it is unset/empty, NFS listens on
all available addresses.

`v4` when set means that we switch from NFSv3 to NFSv4.

`v4_v3owner` when set means that system will use NFSv3 ownership model for NFSv4.

`v4_krb` will force NFS shares to fail if the Kerberos ticket is unavailable.

`v4_domain` overrides the default DNS domain name for NFSv4.

`mountd_port` specifies the port mountd(8) binds to.

`rpcstatd_port` specifies the port rpc.statd(8) binds to.

`rpclockd_port` specifies the port rpclockd_port(8) binds to.

> Body parameter

```json
{}
```

<h3 id="put__nfs-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[nfs_update_0](#schemanfs_update_0)|false|none|

<h3 id="put__nfs-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__nfs_bindip_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/nfs/bindip_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/nfs/bindip_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/nfs/bindip_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /nfs/bindip_choices`

Returns ip choices for NFS service to use

<h3 id="get__nfs_bindip_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-nis">nis</h1>

## get__nis

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/nis

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/nis')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/nis");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /nis`

<h3 id="get__nis-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__nis

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/nis \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/nis', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/nis");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /nis`

Update NIS Service Configuration.

`domain` is the name of NIS domain.

`servers` is a list of hostnames/IP addresses.

`secure_mode` when enabled sets ypbind(8) to refuse binding to any NIS server not running as root on a
TCP port over 1024.

`manycast` when enabled sets ypbind(8) to bind to the server that responds the fastest.

`enable` enables and starts the NIS service. The NIS service is disabled when this
value is changed to False.

> Body parameter

```json
{}
```

<h3 id="put__nis-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[nis_update_0](#schemanis_update_0)|false|none|

<h3 id="put__nis-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__nis_get_state

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/nis/get_state

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/nis/get_state')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/nis/get_state");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /nis/get_state`

Wrapper function for 'directoryservices.get_state'. Returns only the state of the
NIS service.

<h3 id="get__nis_get_state-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-openvpn-client">openvpn.client</h1>

## get__openvpn_client

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/openvpn/client

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/openvpn/client')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/openvpn/client");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /openvpn/client`

<h3 id="get__openvpn_client-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__openvpn_client

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/openvpn/client \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/openvpn/client', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/openvpn/client");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /openvpn/client`

Update OpenVPN Client configuration.

`remote` can be a valid ip address / domain which openvpn will try to connect to.

`nobind` must be enabled if OpenVPN client / server are to run concurrently.

> Body parameter

```json
{}
```

<h3 id="put__openvpn_client-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[openvpn_client_update_0](#schemaopenvpn_client_update_0)|false|none|

<h3 id="put__openvpn_client-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__openvpn_client_authentication_algorithm_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/openvpn/client/authentication_algorithm_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/openvpn/client/authentication_algorithm_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/openvpn/client/authentication_algorithm_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /openvpn/client/authentication_algorithm_choices`

Returns a dictionary of valid authentication algorithms which can be used with OpenVPN server.

<h3 id="get__openvpn_client_authentication_algorithm_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__openvpn_client_cipher_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/openvpn/client/cipher_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/openvpn/client/cipher_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/openvpn/client/cipher_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /openvpn/client/cipher_choices`

Returns a dictionary of valid ciphers which can be used with OpenVPN server.

<h3 id="get__openvpn_client_cipher_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-openvpn-server">openvpn.server</h1>

## get__openvpn_server

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/openvpn/server

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/openvpn/server')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/openvpn/server");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /openvpn/server`

<h3 id="get__openvpn_server-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__openvpn_server

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/openvpn/server \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/openvpn/server', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/openvpn/server");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /openvpn/server`

Update OpenVPN Server configuration.

When `tls_crypt_auth_enabled` is enabled and `tls_crypt_auth` not provided, a static key is automatically
generated to be used with OpenVPN server.

> Body parameter

```json
{}
```

<h3 id="put__openvpn_server-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[openvpn_server_update_0](#schemaopenvpn_server_update_0)|false|none|

<h3 id="put__openvpn_server-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__openvpn_server_authentication_algorithm_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/openvpn/server/authentication_algorithm_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/openvpn/server/authentication_algorithm_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/openvpn/server/authentication_algorithm_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /openvpn/server/authentication_algorithm_choices`

Returns a dictionary of valid authentication algorithms which can be used with OpenVPN server.

<h3 id="get__openvpn_server_authentication_algorithm_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__openvpn_server_cipher_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/openvpn/server/cipher_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/openvpn/server/cipher_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/openvpn/server/cipher_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /openvpn/server/cipher_choices`

Returns a dictionary of valid ciphers which can be used with OpenVPN server.

<h3 id="get__openvpn_server_cipher_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__openvpn_server_client_configuration_generation

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/openvpn/server/client_configuration_generation \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/openvpn/server/client_configuration_generation', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/openvpn/server/client_configuration_generation");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /openvpn/server/client_configuration_generation`

Returns a configuration for OpenVPN client which can be used with any client to connect to FN/TN OpenVPN
server.

`client_certificate_id` should be a valid certificate issued for use with OpenVPN client service.

`server_address` if specified auto-fills the remote directive in the OpenVPN configuration enabling the end
user to use the file without making any edits to connect to OpenVPN server.

> Body parameter

```json
{
  "client_certificate_id": 0,
  "server_address": "string"
}
```

<h3 id="post__openvpn_server_client_configuration_generation-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[openvpn_server_client_configuration_generation](#schemaopenvpn_server_client_configuration_generation)|false|none|

<h3 id="post__openvpn_server_client_configuration_generation-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__openvpn_server_renew_static_key

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/openvpn/server/renew_static_key

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/openvpn/server/renew_static_key')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/openvpn/server/renew_static_key");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /openvpn/server/renew_static_key`

Reset OpenVPN server's TLS static key which will be used to encrypt/authenticate control channel packets.

<h3 id="get__openvpn_server_renew_static_key-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-plugin">plugin</h1>

## get__plugin

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/plugin

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/plugin')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/plugin");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /plugin`

Query installed plugins with `query-filters` and `query-options`.

<h3 id="get__plugin-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__plugin-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__plugin

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/plugin \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/plugin', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/plugin");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /plugin`

Create a Plugin.

`plugin_name` is the name of the plugin specified by the INDEX file in "plugin_repository" and it's JSON
file.

`jail_name` is the name of the jail that will manage the plugin. Required.

`props` is a list of jail properties that the user manually sets. Plugins should always set the jail
networking capability with DHCP, IP Address, or NAT properties. i.e dhcp=1 / ip4_addr="192.168.0.2" / nat=1

`plugin_repository` is a git URI that fetches data for `plugin_name`.

`branch` is the FreeNAS repository branch to use as the base for the `plugin_repository`. The default is to
use the current system version. Example: 11.3-RELEASE.

> Body parameter

```json
{}
```

<h3 id="post__plugin-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[plugin_create_0](#schemaplugin_create_0)|false|none|

<h3 id="post__plugin-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__plugin_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/plugin/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/plugin/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/plugin/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /plugin/id/{id}`

Delete plugin `id`.

<h3 id="delete__plugin_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|string|true|none|

<h3 id="delete__plugin_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__plugin_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/plugin/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/plugin/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/plugin/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /plugin/id/{id}`

Query installed plugins with `query-filters` and `query-options`.

<h3 id="get__plugin_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__plugin_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__plugin_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/plugin/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/plugin/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/plugin/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /plugin/id/{id}`

Update plugin `id`.

> Body parameter

```json
{}
```

<h3 id="put__plugin_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|string|true|none|
|body|body|[plugin_update_1](#schemaplugin_update_1)|false|none|

<h3 id="put__plugin_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__plugin_available

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/plugin/available \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/plugin/available', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/plugin/available");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /plugin/available`

List available plugins which can be fetched for `plugin_repository`.

> Body parameter

```json
{}
```

<h3 id="post__plugin_available-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[plugin_available_0](#schemaplugin_available_0)|false|none|

<h3 id="post__plugin_available-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__plugin_branches_choices

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/plugin/branches_choices \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/plugin/branches_choices', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/plugin/branches_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /plugin/branches_choices`

> Body parameter

```json
null
```

<h3 id="post__plugin_branches_choices-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[plugin_branches_choices_0](#schemaplugin_branches_choices_0)|false|none|

<h3 id="post__plugin_branches_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__plugin_defaults

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/plugin/defaults \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/plugin/defaults', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/plugin/defaults");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /plugin/defaults`

Retrieve default properties specified for `plugin` in the plugin's manifest.

When `refresh` is specified, `plugin_repository` is updated before retrieving plugin's default properties.

> Body parameter

```json
{}
```

<h3 id="post__plugin_defaults-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[plugin_defaults_0](#schemaplugin_defaults_0)|false|none|

<h3 id="post__plugin_defaults-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__plugin_official_repositories

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/plugin/official_repositories

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/plugin/official_repositories')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/plugin/official_repositories");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /plugin/official_repositories`

List officially supported plugin repositories.

<h3 id="get__plugin_official_repositories-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__plugin_retrieve_versions_for_repos

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/plugin/retrieve_versions_for_repos

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/plugin/retrieve_versions_for_repos')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/plugin/retrieve_versions_for_repos");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /plugin/retrieve_versions_for_repos`

<h3 id="get__plugin_retrieve_versions_for_repos-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-pool">pool</h1>

## get__pool

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/pool

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/pool')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /pool`

<h3 id="get__pool-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__pool-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool`

Create a new ZFS Pool.

`topology` is a object which requires at least one `data` entry.
All of `data` entries (vdevs) require to be of the same type.

`deduplication` when set to ON or VERIFY makes sure that no block of data is duplicated in the pool. When
VERIFY is specified, if two blocks have similar signatures, byte to byte comparison is performed to ensure that
the blocks are identical. This should be used in special circumstances as it carries a significant overhead.

`encryption` when enabled will create an ZFS encrypted root dataset for `name` pool.

`encryption_options` specifies configuration for encryption of root dataset for `name` pool.
`encryption_options.passphrase` must be specified if encryption for root dataset is desired with a passphrase
as a key.
Otherwise a hex encoded key can be specified by providing `encryption_options.key`.
`encryption_options.generate_key` when enabled automatically generates the key to be used
for dataset encryption.

It should be noted that keys are stored by the system for automatic locking/unlocking
on import/export of encrypted datasets. If that is not desired, dataset should be created
with a passphrase as a key.

Example of `topology`:

    {
        "data": [
            {"type": "RAIDZ1", "disks": ["da1", "da2", "da3"]}
        ],
        "cache": [
            {"type": "STRIPE", "disks": ["da4"]}
        ],
        "log": [
            {"type": "STRIPE", "disks": ["da5"]}
        ],
        "spares": ["da6"]
    }

> Body parameter

```json
{}
```

<h3 id="post__pool-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[pool_create_0](#schemapool_create_0)|false|none|

<h3 id="post__pool-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__pool_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/pool/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/pool/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /pool/id/{id}`

<h3 id="get__pool_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__pool_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__pool_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/pool/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/pool/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /pool/id/{id}`

Update pool of `id`, adding the new topology.

The `type` of `data` must be the same of existing vdevs.

> Body parameter

```json
{}
```

<h3 id="put__pool_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[pool_update_1](#schemapool_update_1)|false|none|

<h3 id="put__pool_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_attach

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/attach \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/attach', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/attach");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/attach`

For TrueNAS Core/Enterprise platform, if the `oid` pool is passphrase GELI encrypted, `passphrase`
must be specified for this operation to succeed.

`target_vdev` is the GUID of the vdev where the disk needs to be attached. In case of STRIPED vdev, this
is the STRIPED disk GUID which will be converted to mirror. If `target_vdev` is mirror, it will be converted
into a n-way mirror.

> Body parameter

```json
{
  "oid": 0,
  "pool_attach": {}
}
```

<h3 id="post__pool_attach-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[pool_attach](#schemapool_attach)|false|none|

<h3 id="post__pool_attach-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_id_{id}_attachments

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/id/{id}/attachments \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/id/{id}/attachments', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/id/{id}/attachments");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/id/{id}/attachments`

Return a list of services dependent of this pool.

Responsible for telling the user whether there is a related
share, asking for confirmation.

> Body parameter

```json
{}
```

<h3 id="post__pool_id_{id}_attachments-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[pool_attachments](#schemapool_attachments)|false|none|

<h3 id="post__pool_id_{id}_attachments-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_id_{id}_detach

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/id/{id}/detach \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/id/{id}/detach', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/id/{id}/detach");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/id/{id}/detach`

Detach a disk from pool of id `id`.

`label` is the vdev guid or device name.

> Body parameter

```json
{}
```

<h3 id="post__pool_id_{id}_detach-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[pool_detach_1](#schemapool_detach_1)|false|none|

<h3 id="post__pool_id_{id}_detach-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_id_{id}_download_encryption_key

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/id/{id}/download_encryption_key \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/id/{id}/download_encryption_key', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/id/{id}/download_encryption_key");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/id/{id}/download_encryption_key`

Download encryption key for a given pool `id`.

> Body parameter

```json
"geli.key"
```

<h3 id="post__pool_id_{id}_download_encryption_key-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[pool_download_encryption_key_1](#schemapool_download_encryption_key_1)|false|none|

<h3 id="post__pool_id_{id}_download_encryption_key-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_id_{id}_expand

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/id/{id}/expand \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/id/{id}/expand', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/id/{id}/expand");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/id/{id}/expand`

Expand pool to fit all available disk space.

> Body parameter

```json
{}
```

<h3 id="post__pool_id_{id}_expand-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[pool_expand_1](#schemapool_expand_1)|false|none|

<h3 id="post__pool_id_{id}_expand-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_id_{id}_export

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/id/{id}/export \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/id/{id}/export', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/id/{id}/export");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/id/{id}/export`

Export pool of `id`.

`cascade` will delete all attachments of the given pool (`pool.attachments`).
`restart_services` will restart services that have open files on given pool.
`destroy` will also PERMANENTLY destroy the pool/data.

> Body parameter

```json
{}
```

<h3 id="post__pool_id_{id}_export-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[pool_export_1](#schemapool_export_1)|false|none|

<h3 id="post__pool_id_{id}_export-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_filesystem_choices

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/filesystem_choices \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/filesystem_choices', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/filesystem_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/filesystem_choices`

Returns all available datasets, except system datasets.

> Body parameter

```json
[
  "FILESYSTEM",
  "VOLUME"
]
```

<h3 id="post__pool_filesystem_choices-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[pool_filesystem_choices_0](#schemapool_filesystem_choices_0)|false|none|

<h3 id="post__pool_filesystem_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_id_{id}_get_disks

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/id/{id}/get_disks \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/id/{id}/get_disks', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/id/{id}/get_disks");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/id/{id}/get_disks`

Get all disks in use by pools.
If `id` is provided only the disks from the given pool `id` will be returned.

> Body parameter

```json
{}
```

<h3 id="post__pool_id_{id}_get_disks-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[pool_get_disks](#schemapool_get_disks)|false|none|

<h3 id="post__pool_id_{id}_get_disks-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_import_disk

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/import_disk \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/import_disk', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/import_disk");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/import_disk`

Import a disk, by copying its content to a pool.

> Body parameter

```json
{
  "device": "string",
  "fs_type": "string",
  "fs_options": {},
  "dst_path": "string"
}
```

<h3 id="post__pool_import_disk-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[pool_import_disk](#schemapool_import_disk)|false|none|

<h3 id="post__pool_import_disk-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_import_disk_autodetect_fs_type

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/import_disk_autodetect_fs_type \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/import_disk_autodetect_fs_type', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/import_disk_autodetect_fs_type");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/import_disk_autodetect_fs_type`

Autodetect filesystem type for `pool.import_disk`.

> Body parameter

```json
"string"
```

<h3 id="post__pool_import_disk_autodetect_fs_type-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[pool_import_disk_autodetect_fs_type_0](#schemapool_import_disk_autodetect_fs_type_0)|false|none|

<h3 id="post__pool_import_disk_autodetect_fs_type-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__pool_import_disk_msdosfs_locales

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/pool/import_disk_msdosfs_locales

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/pool/import_disk_msdosfs_locales')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/import_disk_msdosfs_locales");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /pool/import_disk_msdosfs_locales`

Get a list of locales for msdosfs type to be used in `pool.import_disk`.

<h3 id="get__pool_import_disk_msdosfs_locales-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__pool_import_find

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/pool/import_find

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/pool/import_find')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/import_find");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /pool/import_find`

Returns a job id which can be used to retrieve a list of pools available for
import with the following details as a result of the job:
name, guid, status, hostname.

<h3 id="get__pool_import_find-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_import_pool

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/import_pool \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/import_pool', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/import_pool");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/import_pool`

Import a pool found with `pool.import_find`.

If a `name` is specified the pool will be imported using that new name.

`passphrase` is required while importing an encrypted pool. In that case this method needs to
be called using /_upload/ endpoint with the encryption key.

If `enable_attachments` is set to true, attachments that were disabled during pool export will be
re-enabled.

Errors:
    ENOENT - Pool not found

A file can be uploaded to this end point. This end point is special, please refer to Jobs section in Websocket API documentation for details.

> Body parameter

```json
{}
```

<h3 id="post__pool_import_pool-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[pool_import_pool_0](#schemapool_import_pool_0)|false|none|

<h3 id="post__pool_import_pool-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_id_{id}_is_upgraded

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/id/{id}/is_upgraded \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/id/{id}/is_upgraded', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/id/{id}/is_upgraded");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/id/{id}/is_upgraded`

Returns whether or not the pool of `id` is on the latest version and with all feature
flags enabled.

> Body parameter

```json
{}
```

<h3 id="post__pool_id_{id}_is_upgraded-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[pool_is_upgraded](#schemapool_is_upgraded)|false|none|

<h3 id="post__pool_id_{id}_is_upgraded-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_id_{id}_lock

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/id/{id}/lock \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/id/{id}/lock', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/id/{id}/lock");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/id/{id}/lock`

Lock encrypted pool `id`.

> Body parameter

```json
"string"
```

<h3 id="post__pool_id_{id}_lock-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[pool_lock_1](#schemapool_lock_1)|false|none|

<h3 id="post__pool_id_{id}_lock-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_id_{id}_offline

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/id/{id}/offline \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/id/{id}/offline', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/id/{id}/offline");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/id/{id}/offline`

Offline a disk from pool of id `id`.

`label` is the vdev guid or device name.

> Body parameter

```json
{}
```

<h3 id="post__pool_id_{id}_offline-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[pool_offline_1](#schemapool_offline_1)|false|none|

<h3 id="post__pool_id_{id}_offline-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_id_{id}_online

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/id/{id}/online \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/id/{id}/online', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/id/{id}/online");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/id/{id}/online`

Online a disk from pool of id `id`.

`label` is the vdev guid or device name.

> Body parameter

```json
{}
```

<h3 id="post__pool_id_{id}_online-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[pool_online_1](#schemapool_online_1)|false|none|

<h3 id="post__pool_id_{id}_online-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_id_{id}_passphrase

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/id/{id}/passphrase \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/id/{id}/passphrase', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/id/{id}/passphrase");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/id/{id}/passphrase`

Create/Change/Remove passphrase for an encrypted pool.

Setting passphrase to null will remove the passphrase.
`admin_password` is required when changing or removing passphrase.

> Body parameter

```json
{}
```

<h3 id="post__pool_id_{id}_passphrase-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[pool_passphrase_1](#schemapool_passphrase_1)|false|none|

<h3 id="post__pool_id_{id}_passphrase-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_id_{id}_processes

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/id/{id}/processes \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/id/{id}/processes', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/id/{id}/processes");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/id/{id}/processes`

Returns a list of running processes using this pool.

> Body parameter

```json
{}
```

<h3 id="post__pool_id_{id}_processes-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[pool_processes](#schemapool_processes)|false|none|

<h3 id="post__pool_id_{id}_processes-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_id_{id}_recoverykey_rm

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/id/{id}/recoverykey_rm \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/id/{id}/recoverykey_rm', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/id/{id}/recoverykey_rm");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/id/{id}/recoverykey_rm`

Remove recovery key for encrypted pool `id`.

> Body parameter

```json
{}
```

<h3 id="post__pool_id_{id}_recoverykey_rm-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[pool_recoverykey_rm_1](#schemapool_recoverykey_rm_1)|false|none|

<h3 id="post__pool_id_{id}_recoverykey_rm-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_id_{id}_rekey

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/id/{id}/rekey \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/id/{id}/rekey', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/id/{id}/rekey");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/id/{id}/rekey`

Rekey encrypted pool `id`.

> Body parameter

```json
{}
```

<h3 id="post__pool_id_{id}_rekey-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[pool_rekey_1](#schemapool_rekey_1)|false|none|

<h3 id="post__pool_id_{id}_rekey-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_id_{id}_remove

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/id/{id}/remove \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/id/{id}/remove', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/id/{id}/remove");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/id/{id}/remove`

Remove a disk from pool of id `id`.

`label` is the vdev guid or device name.

Error codes:

    EZFS_NOSPC(2032): out of space to remove a device
    EZFS_NODEVICE(2017): no such device in pool
    EZFS_NOREPLICAS(2019): no valid replicas

> Body parameter

```json
{}
```

<h3 id="post__pool_id_{id}_remove-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[pool_remove_1](#schemapool_remove_1)|false|none|

<h3 id="post__pool_id_{id}_remove-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_id_{id}_replace

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/id/{id}/replace \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/id/{id}/replace', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/id/{id}/replace");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/id/{id}/replace`

Replace a disk on a pool.

`label` is the ZFS guid or a device name
`disk` is the identifier of a disk
`passphrase` is only valid for TrueNAS Core/Enterprise platform where pool is GELI encrypted

> Body parameter

```json
{}
```

<h3 id="post__pool_id_{id}_replace-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[pool_replace_1](#schemapool_replace_1)|false|none|

<h3 id="post__pool_id_{id}_replace-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_id_{id}_scrub

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/id/{id}/scrub \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/id/{id}/scrub', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/id/{id}/scrub");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/id/{id}/scrub`

Performs a scrub action to pool of `id`.

`action` can be either of "START", "STOP" or "PAUSE".

> Body parameter

```json
"START"
```

<h3 id="post__pool_id_{id}_scrub-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[pool_scrub_1](#schemapool_scrub_1)|false|none|

#### Enumerated Values

|Parameter|Value|
|---|---|
|body|START|
|body|STOP|
|body|PAUSE|

<h3 id="post__pool_id_{id}_scrub-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_id_{id}_unlock

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/id/{id}/unlock \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/id/{id}/unlock', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/id/{id}/unlock");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/id/{id}/unlock`

Unlock encrypted pool `id`.

`passphrase` is required of a recovery key is not provided.

If `recoverykey` is true this method expects the recovery key file to be uploaded using
the /_upload/ endpoint.

`services_restart` is a list of services to be restarted when the pool gets unlocked.
Said list be be retrieve using `pool.unlock_services_restart_choices`.

A file can be uploaded to this end point. This end point is special, please refer to Jobs section in Websocket API documentation for details.

> Body parameter

```json
{}
```

<h3 id="post__pool_id_{id}_unlock-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[pool_unlock_1](#schemapool_unlock_1)|false|none|

<h3 id="post__pool_id_{id}_unlock-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_unlock_services_restart_choices

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/unlock_services_restart_choices \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/unlock_services_restart_choices', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/unlock_services_restart_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/unlock_services_restart_choices`

Get a mapping of services identifiers and labels that can be restart
on volume unlock.

> Body parameter

```json
0
```

<h3 id="post__pool_unlock_services_restart_choices-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[pool_unlock_services_restart_choices_0](#schemapool_unlock_services_restart_choices_0)|false|none|

<h3 id="post__pool_unlock_services_restart_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_id_{id}_upgrade

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/id/{id}/upgrade \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/id/{id}/upgrade', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/id/{id}/upgrade");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/id/{id}/upgrade`

Upgrade pool of `id` to latest version with all feature flags.

> Body parameter

```json
{}
```

<h3 id="post__pool_id_{id}_upgrade-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[pool_upgrade](#schemapool_upgrade)|false|none|

<h3 id="post__pool_id_{id}_upgrade-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-pool-dataset">pool.dataset</h1>

## get__pool_dataset

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/pool/dataset

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/pool/dataset')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/dataset");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /pool/dataset`

Query Pool Datasets with `query-filters` and `query-options`.

We provide two ways to retrieve datasets. The first is a flat structure (default), where
all datasets in the system are returned as separate objects which contain all data
there is for their children. This retrieval type is slightly slower because of duplicates in each object.
The second type is hierarchical, where only top level datasets are returned in the list. They contain all the
children in the `children` key. This retrieval type is slightly faster.
These options are controlled by the `query-options.extra.flat` attribute (default true).

<h3 id="get__pool_dataset-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__pool_dataset-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_dataset

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/dataset \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/dataset', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/dataset");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/dataset`

Creates a dataset/zvol.

`volsize` is required for type=VOLUME and is supposed to be a multiple of the block size.
`sparse` and `volblocksize` are only used for type=VOLUME.

`encryption` when enabled will create an ZFS encrypted root dataset for `name` pool.
There are 2 cases where ZFS encryption is not allowed for a dataset:
1) Pool in question is GELI encrypted.
2) If the parent dataset is encrypted with a passphrase and `name` is being created
   with a key for encrypting the dataset.

`encryption_options` specifies configuration for encryption of dataset for `name` pool.
`encryption_options.passphrase` must be specified if encryption for dataset is desired with a passphrase
as a key.
Otherwise a hex encoded key can be specified by providing `encryption_options.key`.
`encryption_options.generate_key` when enabled automatically generates the key to be used
for dataset encryption.

It should be noted that keys are stored by the system for automatic locking/unlocking
on import/export of encrypted datasets. If that is not desired, dataset should be created
with a passphrase as a key.

> Body parameter

```json
{}
```

<h3 id="post__pool_dataset-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[pool_dataset_create_0](#schemapool_dataset_create_0)|false|none|

<h3 id="post__pool_dataset-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__pool_dataset_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/pool/dataset/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.delete('http://192.168.1.123/api/v2.0/pool/dataset/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/dataset/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /pool/dataset/id/{id}`

Delete dataset/zvol `id`.

`recursive` will also delete/destroy all children datasets.
`force` will force delete busy datasets.

> Body parameter

```json
{}
```

<h3 id="delete__pool_dataset_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|string|true|none|
|body|body|[pool_dataset_delete_1](#schemapool_dataset_delete_1)|false|none|

<h3 id="delete__pool_dataset_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__pool_dataset_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/pool/dataset/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/pool/dataset/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/dataset/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /pool/dataset/id/{id}`

Query Pool Datasets with `query-filters` and `query-options`.

We provide two ways to retrieve datasets. The first is a flat structure (default), where
all datasets in the system are returned as separate objects which contain all data
there is for their children. This retrieval type is slightly slower because of duplicates in each object.
The second type is hierarchical, where only top level datasets are returned in the list. They contain all the
children in the `children` key. This retrieval type is slightly faster.
These options are controlled by the `query-options.extra.flat` attribute (default true).

<h3 id="get__pool_dataset_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__pool_dataset_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__pool_dataset_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/pool/dataset/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/pool/dataset/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/dataset/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /pool/dataset/id/{id}`

Updates a dataset/zvol `id`.

> Body parameter

```json
{}
```

<h3 id="put__pool_dataset_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|string|true|none|
|body|body|[pool_dataset_update_1](#schemapool_dataset_update_1)|false|none|

<h3 id="put__pool_dataset_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_dataset_id_{id}_attachments

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/dataset/id/{id}/attachments \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/dataset/id/{id}/attachments', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/dataset/id/{id}/attachments");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/dataset/id/{id}/attachments`

Return a list of services dependent of this dataset.

Responsible for telling the user whether there is a related
share, asking for confirmation.

Example return value:
[
  {
    "type": "NFS Share",
    "service": "nfs",
    "attachments": ["/mnt/tank/work"]
  }
]

> Body parameter

```json
{}
```

<h3 id="post__pool_dataset_id_{id}_attachments-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|string|true|none|
|body|body|[pool_dataset_attachments](#schemapool_dataset_attachments)|false|none|

<h3 id="post__pool_dataset_id_{id}_attachments-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_dataset_change_key

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/dataset/change_key \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/dataset/change_key', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/dataset/change_key");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/dataset/change_key`

Change encryption properties for `id` encrypted dataset.

Changing dataset encryption to use passphrase instead of a key is not allowed if:

1) It has encrypted roots as children which are encrypted with a key
2) If it is a root dataset where the system dataset is located

A file can be uploaded to this end point. This end point is special, please refer to Jobs section in Websocket API documentation for details.

> Body parameter

```json
{
  "id": "string",
  "change_key_options": {}
}
```

<h3 id="post__pool_dataset_change_key-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[pool_dataset_change_key](#schemapool_dataset_change_key)|false|none|

<h3 id="post__pool_dataset_change_key-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__pool_dataset_encryption_algorithm_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/pool/dataset/encryption_algorithm_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/pool/dataset/encryption_algorithm_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/dataset/encryption_algorithm_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /pool/dataset/encryption_algorithm_choices`

Retrieve encryption algorithms supported for ZFS dataset encryption.

<h3 id="get__pool_dataset_encryption_algorithm_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_dataset_encryption_summary

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/dataset/encryption_summary \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/dataset/encryption_summary', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/dataset/encryption_summary");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/dataset/encryption_summary`

Retrieve summary of all encrypted roots under `id`.

Keys/passphrase can be supplied to check if the keys are valid.

It should be noted that there are 2 keys which show if a recursive unlock operation is
done for `id`, which dataset will be unlocked and if not why it won't be unlocked. The keys
namely are "unlock_successful" and "unlock_error". The former is a boolean value showing if unlock
would succeed/fail. The latter is description why it failed if it failed.

If a dataset is already unlocked, it will show up as true for "unlock_successful" regardless of what
key user provided as the unlock keys in the output are to reflect what a real unlock operation would
behave. If user is interested in seeing if a provided key is valid or not, then the key to look out for
in the output is "valid_key" which based on what system has in database or if a user provided one, validates
the key and sets a boolean value for the dataset.

Example output:
[
    {
        "name": "vol",
        "key_format": "PASSPHRASE",
        "key_present_in_database": false,
        "valid_key": true,
        "locked": true,
        "unlock_error": null,
        "unlock_successful": true
    },
    {
        "name": "vol/c1/d1",
        "key_format": "PASSPHRASE",
        "key_present_in_database": false,
        "valid_key": false,
        "locked": true,
        "unlock_error": "Provided key is invalid",
        "unlock_successful": false
    },
    {
        "name": "vol/c",
        "key_format": "PASSPHRASE",
        "key_present_in_database": false,
        "valid_key": false,
        "locked": true,
        "unlock_error": "Key not provided",
        "unlock_successful": false
    },
    {
        "name": "vol/c/d2",
        "key_format": "PASSPHRASE",
        "key_present_in_database": false,
        "valid_key": false,
        "locked": true,
        "unlock_error": "Child cannot be unlocked when parent "vol/c" is locked and provided key is invalid",
        "unlock_successful": false
    }
]

A file can be uploaded to this end point. This end point is special, please refer to Jobs section in Websocket API documentation for details.

> Body parameter

```json
{
  "id": "string",
  "encryption_root_summary_options": {}
}
```

<h3 id="post__pool_dataset_encryption_summary-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[pool_dataset_encryption_summary](#schemapool_dataset_encryption_summary)|false|none|

<h3 id="post__pool_dataset_encryption_summary-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_dataset_id_{id}_get_quota

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/dataset/id/{id}/get_quota \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/dataset/id/{id}/get_quota', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/dataset/id/{id}/get_quota");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/dataset/id/{id}/get_quota`

Return a list of the specified `quota_type` of  quotas on the ZFS dataset `ds`.
Support `query-filters` and `query-options`. used_bytes and used_percentage
may not instantly update as space is used.

When quota_type is not DATASET, each quota entry has these fields:

`id` - the uid or gid to which the quota applies.

`name` - the user or group name to which the quota applies. Value is
null if the id in the quota cannot be resolved to a user or group. This
indicates that the user or group does not exist on the server.

`quota` - the quota size in bytes.

`used_bytes` - the amount of bytes the user has written to the dataset.
A value of zero means unlimited.

`used_percentage` - the percentage of the user or group quota consumed.

`obj_quota` - the number of objects that may be owned by `id`.
A value of zero means unlimited.

'obj_used` - the nubmer of objects currently owned by `id`.

`obj_used_percent` - the percentage of the `obj_quota` currently used.

> Body parameter

```json
{
  "quota_type": "USER",
  "query-filters": [],
  "query-options": {}
}
```

<h3 id="post__pool_dataset_id_{id}_get_quota-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|string|true|none|
|body|body|[pool_dataset_get_quota](#schemapool_dataset_get_quota)|false|none|

<h3 id="post__pool_dataset_id_{id}_get_quota-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_dataset_inherit_parent_encryption_properties

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/dataset/inherit_parent_encryption_properties \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/dataset/inherit_parent_encryption_properties', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/dataset/inherit_parent_encryption_properties");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/dataset/inherit_parent_encryption_properties`

Allows inheriting parent's encryption root discarding its current encryption settings. This
can only be done where `id` has an encrypted parent and `id` itself is an encryption root.

> Body parameter

```json
"string"
```

<h3 id="post__pool_dataset_inherit_parent_encryption_properties-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[pool_dataset_inherit_parent_encryption_properties_0](#schemapool_dataset_inherit_parent_encryption_properties_0)|false|none|

<h3 id="post__pool_dataset_inherit_parent_encryption_properties-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_dataset_lock

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/dataset/lock \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/dataset/lock', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/dataset/lock");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/dataset/lock`

Locks `id` dataset. It will unmount the dataset and its children before locking.

> Body parameter

```json
{
  "id": "string",
  "lock_options": {}
}
```

<h3 id="post__pool_dataset_lock-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[pool_dataset_lock](#schemapool_dataset_lock)|false|none|

<h3 id="post__pool_dataset_lock-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_dataset_id_{id}_permission

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/dataset/id/{id}/permission \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/dataset/id/{id}/permission', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/dataset/id/{id}/permission");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/dataset/id/{id}/permission`

Set permissions for a dataset `id`. Permissions may be specified as
either a posix `mode` or an nfsv4 `acl`. Setting mode will fail if the
dataset has an existing nfsv4 acl. In this case, the option `stripacl`
must be set to `True`.

> Body parameter

```json
{}
```

<h3 id="post__pool_dataset_id_{id}_permission-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|string|true|none|
|body|body|[pool_dataset_permission_1](#schemapool_dataset_permission_1)|false|none|

<h3 id="post__pool_dataset_id_{id}_permission-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_dataset_id_{id}_processes

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/dataset/id/{id}/processes \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/dataset/id/{id}/processes', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/dataset/id/{id}/processes");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/dataset/id/{id}/processes`

Return a list of processes using this dataset.

Example return value:

[
  {
    "pid": 2520,
    "name": "smbd",
    "service": "cifs"
  },
  {
    "pid": 97778,
    "name": "minio",
    "cmdline": "/usr/local/bin/minio -C /usr/local/etc/minio server --address=0.0.0.0:9000 --quiet /mnt/tank/wk"
  }
]

> Body parameter

```json
{}
```

<h3 id="post__pool_dataset_id_{id}_processes-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|string|true|none|
|body|body|[pool_dataset_processes](#schemapool_dataset_processes)|false|none|

<h3 id="post__pool_dataset_id_{id}_processes-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_dataset_id_{id}_promote

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/dataset/id/{id}/promote \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/dataset/id/{id}/promote', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/dataset/id/{id}/promote");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/dataset/id/{id}/promote`

Promote the cloned dataset `id`.

> Body parameter

```json
{}
```

<h3 id="post__pool_dataset_id_{id}_promote-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|string|true|none|
|body|body|[pool_dataset_promote](#schemapool_dataset_promote)|false|none|

<h3 id="post__pool_dataset_id_{id}_promote-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_dataset_recommended_zvol_blocksize

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/dataset/recommended_zvol_blocksize \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/dataset/recommended_zvol_blocksize', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/dataset/recommended_zvol_blocksize");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/dataset/recommended_zvol_blocksize`

Helper method to get recommended size for a new zvol (dataset of type VOLUME).

> Body parameter

```json
"string"
```

<h3 id="post__pool_dataset_recommended_zvol_blocksize-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[pool_dataset_recommended_zvol_blocksize_0](#schemapool_dataset_recommended_zvol_blocksize_0)|false|none|

<h3 id="post__pool_dataset_recommended_zvol_blocksize-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_dataset_id_{id}_set_quota

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/dataset/id/{id}/set_quota \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/dataset/id/{id}/set_quota', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/dataset/id/{id}/set_quota");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/dataset/id/{id}/set_quota`

There are three over-arching types of quotas for ZFS datasets.
1) dataset quotas and refquotas. If a DATASET quota type is specified in
this API call, then the API acts as a wrapper for `pool.dataset.update`.

2) User and group quotas. These limit the amount of disk space consumed
by files that are owned by the specified users or groups. If the respective
"object quota" type is specfied, then the quota limits the number of objects
that may be owned by the specified user or group.

3) Project quotas. These limit the amount of disk space consumed by files
that are owned by the specified project. Project quotas are not yet implemended.

This API allows users to set multiple quotas simultaneously by submitting a
list of quotas. The list may contain all supported quota types.

`ds` the name of the target ZFS dataset.

`quotas` specifies a list of `quota_entry` entries to apply to dataset.

`quota_entry` entries have these required parameters:

`quota_type`: specifies the type of quota to apply to the dataset. Possible
values are USER, USEROBJ, GROUP, GROUPOBJ, and DATASET. USEROBJ and GROUPOBJ
quotas limit the number of objects consumed by the specified user or group.

`id`: the uid, gid, or name to which the quota applies. If quota_type is
'DATASET', then `id` must be either `QUOTA` or `REFQUOTA`.

`quota_value`: the quota size in bytes. Setting a value of `0` removes
the user or group quota.

> Body parameter

```json
[
  {
    "quota_type": "USER",
    "id": "0",
    "quota_value": 0
  }
]
```

<h3 id="post__pool_dataset_id_{id}_set_quota-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|string|true|none|
|body|body|[pool_dataset_set_quota_1](#schemapool_dataset_set_quota_1)|false|none|

<h3 id="post__pool_dataset_id_{id}_set_quota-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_dataset_unlock

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/dataset/unlock \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/dataset/unlock', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/dataset/unlock");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/dataset/unlock`

Unlock `id` dataset.

If `id` dataset is not encrypted an exception will be raised. There is one exception:
when `id` is a root dataset and `unlock_options.recursive` is specified, encryption
validation will not be performed for `id`. This allow unlocking encrypted children the `id` pool.

For datasets which are encrypted with a passphrase, include the passphrase with
`unlock_options.datasets`.

Uploading a json file which contains encrypted dataset keys can be specified with
`unlock_options.key_file`. The format is similar to that used for exporting encrypted dataset keys.

A file can be uploaded to this end point. This end point is special, please refer to Jobs section in Websocket API documentation for details.

> Body parameter

```json
{
  "id": "string",
  "unlock_options": {}
}
```

<h3 id="post__pool_dataset_unlock-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[pool_dataset_unlock](#schemapool_dataset_unlock)|false|none|

<h3 id="post__pool_dataset_unlock-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-pool-dataset-userprop">pool.dataset.userprop</h1>

## get__pool_dataset_userprop

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/pool/dataset/userprop

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/pool/dataset/userprop')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/dataset/userprop");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /pool/dataset/userprop`

Query all user properties for ZFS datasets.

<h3 id="get__pool_dataset_userprop-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__pool_dataset_userprop-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_dataset_userprop

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/dataset/userprop \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/dataset/userprop', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/dataset/userprop");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/dataset/userprop`

Create a user property for a given `id` dataset.

> Body parameter

```json
{}
```

<h3 id="post__pool_dataset_userprop-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[pool_dataset_userprop_create_0](#schemapool_dataset_userprop_create_0)|false|none|

<h3 id="post__pool_dataset_userprop-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__pool_dataset_userprop_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/pool/dataset/userprop/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.delete('http://192.168.1.123/api/v2.0/pool/dataset/userprop/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/dataset/userprop/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /pool/dataset/userprop/id/{id}`

Delete user property `dataset_user_prop_delete.name` for `id` dataset.

> Body parameter

```json
{}
```

<h3 id="delete__pool_dataset_userprop_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|string|true|none|
|body|body|[pool_dataset_userprop_delete_1](#schemapool_dataset_userprop_delete_1)|false|none|

<h3 id="delete__pool_dataset_userprop_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__pool_dataset_userprop_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/pool/dataset/userprop/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/pool/dataset/userprop/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/dataset/userprop/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /pool/dataset/userprop/id/{id}`

Query all user properties for ZFS datasets.

<h3 id="get__pool_dataset_userprop_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__pool_dataset_userprop_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__pool_dataset_userprop_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/pool/dataset/userprop/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/pool/dataset/userprop/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/dataset/userprop/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /pool/dataset/userprop/id/{id}`

Update `dataset_user_prop_update.name` user property for `id` dataset.

> Body parameter

```json
{}
```

<h3 id="put__pool_dataset_userprop_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|string|true|none|
|body|body|[pool_dataset_userprop_update_1](#schemapool_dataset_userprop_update_1)|false|none|

<h3 id="put__pool_dataset_userprop_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-pool-resilver">pool.resilver</h1>

## get__pool_resilver

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/pool/resilver

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/pool/resilver')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/resilver");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /pool/resilver`

<h3 id="get__pool_resilver-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__pool_resilver

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/pool/resilver \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/pool/resilver', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/resilver");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /pool/resilver`

Configure Pool Resilver Priority.

If `begin` time is greater than `end` time it means it will rollover the day, e.g.
begin = "19:00", end = "05:00" will increase pool resilver priority from 19:00 of one day
until 05:00 of the next day.

`weekday` follows crontab(5) values 0-7 (0 or 7 is Sun).

> Body parameter

```json
{}
```

<h3 id="put__pool_resilver-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[pool_resilver_update_0](#schemapool_resilver_update_0)|false|none|

<h3 id="put__pool_resilver-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-pool-scrub">pool.scrub</h1>

## get__pool_scrub

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/pool/scrub

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/pool/scrub')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/scrub");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /pool/scrub`

<h3 id="get__pool_scrub-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__pool_scrub-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_scrub

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/scrub \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/scrub', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/scrub");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/scrub`

Create a scrub task for a pool.

`threshold` refers to the minimum amount of time in days has to be passed before
a scrub can run again.

> Body parameter

```json
{}
```

<h3 id="post__pool_scrub-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[pool_scrub_create_0](#schemapool_scrub_create_0)|false|none|

<h3 id="post__pool_scrub-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__pool_scrub_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/pool/scrub/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/pool/scrub/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/scrub/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /pool/scrub/id/{id}`

Delete scrub task of `id`.

<h3 id="delete__pool_scrub_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__pool_scrub_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__pool_scrub_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/pool/scrub/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/pool/scrub/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/scrub/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /pool/scrub/id/{id}`

<h3 id="get__pool_scrub_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__pool_scrub_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__pool_scrub_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/pool/scrub/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/pool/scrub/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/scrub/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /pool/scrub/id/{id}`

Update scrub task of `id`.

> Body parameter

```json
{}
```

<h3 id="put__pool_scrub_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[pool_scrub_update_1](#schemapool_scrub_update_1)|false|none|

<h3 id="put__pool_scrub_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_scrub_run

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/scrub/run \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/scrub/run', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/scrub/run");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/scrub/run`

Initiate a scrub of a pool `name` if last scrub was performed more than `threshold` days before.

> Body parameter

```json
{
  "name": "string",
  "threshold": 35
}
```

<h3 id="post__pool_scrub_run-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[pool_scrub_run](#schemapool_scrub_run)|false|none|

<h3 id="post__pool_scrub_run-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-pool-snapshottask">pool.snapshottask</h1>

## get__pool_snapshottask

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/pool/snapshottask

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/pool/snapshottask')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/snapshottask");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /pool/snapshottask`

<h3 id="get__pool_snapshottask-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__pool_snapshottask-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_snapshottask

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/snapshottask \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/snapshottask', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/snapshottask");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/snapshottask`

Create a Periodic Snapshot Task

Create a Periodic Snapshot Task that will take snapshots of specified `dataset` at specified `schedule`.
Recursive snapshots can be created if `recursive` flag is enabled. You can `exclude` specific child datasets
from snapshot.
Snapshots will be automatically destroyed after a certain amount of time, specified by
`lifetime_value` and `lifetime_unit`.
Snapshots will be named according to `naming_schema` which is a `strftime`-like template for snapshot name
and must contain `%Y`, `%m`, `%d`, `%H` and `%M`.

> Body parameter

```json
{}
```

<h3 id="post__pool_snapshottask-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[pool_snapshottask_create_0](#schemapool_snapshottask_create_0)|false|none|

<h3 id="post__pool_snapshottask-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__pool_snapshottask_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/pool/snapshottask/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/pool/snapshottask/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/snapshottask/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /pool/snapshottask/id/{id}`

Delete a Periodic Snapshot Task with specific `id`

<h3 id="delete__pool_snapshottask_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__pool_snapshottask_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__pool_snapshottask_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/pool/snapshottask/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/pool/snapshottask/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/snapshottask/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /pool/snapshottask/id/{id}`

<h3 id="get__pool_snapshottask_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__pool_snapshottask_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__pool_snapshottask_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/pool/snapshottask/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/pool/snapshottask/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/snapshottask/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /pool/snapshottask/id/{id}`

Update a Periodic Snapshot Task with specific `id`

See the documentation for `create` method for information on payload contents

> Body parameter

```json
{}
```

<h3 id="put__pool_snapshottask_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[pool_snapshottask_update_1](#schemapool_snapshottask_update_1)|false|none|

<h3 id="put__pool_snapshottask_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__pool_snapshottask_id_{id}_run

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/pool/snapshottask/id/{id}/run \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/pool/snapshottask/id/{id}/run', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/pool/snapshottask/id/{id}/run");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /pool/snapshottask/id/{id}/run`

Execute a Periodic Snapshot Task of `id`.

> Body parameter

```json
{}
```

<h3 id="post__pool_snapshottask_id_{id}_run-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[pool_snapshottask_run](#schemapool_snapshottask_run)|false|none|

<h3 id="post__pool_snapshottask_id_{id}_run-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-replication">replication</h1>

## get__replication

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/replication

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/replication')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/replication");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /replication`

<h3 id="get__replication-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__replication-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__replication

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/replication \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/replication', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/replication");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /replication`

Create a Replication Task

Create a Replication Task that will push or pull ZFS snapshots to or from remote host..

* `name` specifies a name for replication task
* `direction` specifies whether task will `PUSH` or `PULL` snapshots
* `transport` is a method of snapshots transfer:
  * `SSH` transfers snapshots via SSH connection. This method is supported everywhere but does not achieve
    great performance
    `ssh_credentials` is a required field for this transport (Keychain Credential ID of type `SSH_CREDENTIALS`)
  * `SSH+NETCAT` uses unencrypted connection for data transfer. This can only be used in trusted networks
    and requires a port (specified by range from `netcat_active_side_port_min` to `netcat_active_side_port_max`)
    to be open on `netcat_active_side`
    `ssh_credentials` is also required for control connection
  * `LOCAL` replicates to or from localhost
* `source_datasets` is a non-empty list of datasets to replicate snapshots from
* `target_dataset` is a dataset to put snapshots into. It must exist on target side
* `recursive` and `exclude` have the same meaning as for Periodic Snapshot Task
* `properties` control whether we should send dataset properties along with snapshots
* `periodic_snapshot_tasks` is a list of periodic snapshot task IDs that are sources of snapshots for this
  replication task. Only push replication tasks can be bound to periodic snapshot tasks.
* `naming_schema` is a list of naming schemas for pull replication
* `also_include_naming_schema` is a list of naming schemas for push replication
* `auto` allows replication to run automatically on schedule or after bound periodic snapshot task
* `schedule` is a schedule to run replication task. Only `auto` replication tasks without bound periodic
  snapshot tasks can have a schedule
* `restrict_schedule` restricts when replication task with bound periodic snapshot tasks runs. For example,
  you can have periodic snapshot tasks that run every 15 minutes, but only run replication task every hour.
* Enabling `only_matching_schedule` will only replicate snapshots that match `schedule` or
  `restrict_schedule`
* `allow_from_scratch` will destroy all snapshots on target side and replicate everything from scratch if none
  of the snapshots on target side matches source snapshots
* `readonly` controls destination datasets readonly property:
  * `SET` will set all destination datasets to readonly=on after finishing the replication
  * `REQUIRE` will require all existing destination datasets to have readonly=on property
  * `IGNORE` will avoid this kind of behavior
* `hold_pending_snapshots` will prevent source snapshots from being deleted by retention of replication fails
  for some reason
* `retention_policy` specifies how to delete old snapshots on target side:
  * `SOURCE` deletes snapshots that are absent on source side
  * `CUSTOM` deletes snapshots that are older than `lifetime_value` and `lifetime_unit`
  * `NONE` does not delete any snapshots
* `compression` compresses SSH stream. Available only for SSH transport
* `speed_limit` limits speed of SSH stream. Available only for SSH transport
* `large_block`, `embed` and `compressed` are various ZFS stream flag documented in `man zfs send`
* `retries` specifies number of retries before considering replication failed

> Body parameter

```json
{}
```

<h3 id="post__replication-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[replication_create_0](#schemareplication_create_0)|false|none|

<h3 id="post__replication-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__replication_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/replication/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/replication/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/replication/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /replication/id/{id}`

Delete a Replication Task with specific `id`

<h3 id="delete__replication_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__replication_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__replication_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/replication/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/replication/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/replication/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /replication/id/{id}`

<h3 id="get__replication_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__replication_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__replication_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/replication/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/replication/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/replication/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /replication/id/{id}`

Update a Replication Task with specific `id`

See the documentation for `create` method for information on payload contents

> Body parameter

```json
{}
```

<h3 id="put__replication_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[replication_update_1](#schemareplication_update_1)|false|none|

<h3 id="put__replication_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__replication_count_eligible_manual_snapshots

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/replication/count_eligible_manual_snapshots \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/replication/count_eligible_manual_snapshots', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/replication/count_eligible_manual_snapshots");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /replication/count_eligible_manual_snapshots`

Count how many existing snapshots of `dataset` match `naming_schema`.

> Body parameter

```json
{
  "datasets": [
    "string"
  ],
  "naming_schema": [
    "string"
  ],
  "transport": "SSH",
  "ssh_credentials": null
}
```

<h3 id="post__replication_count_eligible_manual_snapshots-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[replication_count_eligible_manual_snapshots](#schemareplication_count_eligible_manual_snapshots)|false|none|

<h3 id="post__replication_count_eligible_manual_snapshots-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__replication_create_dataset

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/replication/create_dataset \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/replication/create_dataset', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/replication/create_dataset");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /replication/create_dataset`

Creates dataset on remote side

Accepts `dataset` name, `transport` and SSH credentials ID (for non-local transport)

> Body parameter

```json
{
  "dataset": "string",
  "transport": "SSH",
  "ssh_credentials": null
}
```

<h3 id="post__replication_create_dataset-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[replication_create_dataset](#schemareplication_create_dataset)|false|none|

<h3 id="post__replication_create_dataset-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__replication_list_datasets

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/replication/list_datasets \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/replication/list_datasets', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/replication/list_datasets");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /replication/list_datasets`

List datasets on remote side

Accepts `transport` and SSH credentials ID (for non-local transport)

> Body parameter

```json
{
  "transport": "SSH",
  "ssh_credentials": null
}
```

<h3 id="post__replication_list_datasets-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[replication_list_datasets](#schemareplication_list_datasets)|false|none|

<h3 id="post__replication_list_datasets-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__replication_list_naming_schemas

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/replication/list_naming_schemas

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/replication/list_naming_schemas')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/replication/list_naming_schemas");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /replication/list_naming_schemas`

List all naming schemas used in periodic snapshot and replication tasks.

<h3 id="get__replication_list_naming_schemas-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__replication_id_{id}_restore

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/replication/id/{id}/restore \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/replication/id/{id}/restore', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/replication/id/{id}/restore");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /replication/id/{id}/restore`

Create the opposite of replication task `id` (PULL if it was PUSH and vice versa).

> Body parameter

```json
{}
```

<h3 id="post__replication_id_{id}_restore-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[replication_restore_1](#schemareplication_restore_1)|false|none|

<h3 id="post__replication_id_{id}_restore-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__replication_id_{id}_run

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/replication/id/{id}/run \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/replication/id/{id}/run', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/replication/id/{id}/run");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /replication/id/{id}/run`

Run Replication Task of `id`.

> Body parameter

```json
{}
```

<h3 id="post__replication_id_{id}_run-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[replication_run](#schemareplication_run)|false|none|

<h3 id="post__replication_id_{id}_run-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__replication_target_unmatched_snapshots

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/replication/target_unmatched_snapshots \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/replication/target_unmatched_snapshots', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/replication/target_unmatched_snapshots");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /replication/target_unmatched_snapshots`

Check if target has any snapshots that do not exist on source.

> Body parameter

```json
{
  "direction": "PUSH",
  "source_datasets": [
    "string"
  ],
  "target_dataset": "string",
  "transport": "SSH",
  "ssh_credentials": null
}
```

<h3 id="post__replication_target_unmatched_snapshots-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[replication_target_unmatched_snapshots](#schemareplication_target_unmatched_snapshots)|false|none|

<h3 id="post__replication_target_unmatched_snapshots-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-reporting">reporting</h1>

## get__reporting

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/reporting

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/reporting')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/reporting");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /reporting`

<h3 id="get__reporting-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__reporting

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/reporting \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/reporting', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/reporting");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /reporting`

Configure Reporting Database settings.

If `cpu_in_percentage` is `true`, collectd reports CPU usage in percentage instead of "jiffies".

`graphite` specifies a destination hostname or IP for collectd data sent by the Graphite plugin..

`graphite_separateinstances` corresponds to collectd SeparateInstances option.

`graph_age` specifies the maximum age of stored graphs in months. `graph_points` is the number of points for
each hourly, daily, weekly, etc. graph. Changing these requires destroying the current reporting database,
so when these fields are changed, an additional `confirm_rrd_destroy: true` flag must be present.

> Body parameter

```json
{}
```

<h3 id="put__reporting-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[reporting_update_0](#schemareporting_update_0)|false|none|

<h3 id="put__reporting-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__reporting_get_data

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/reporting/get_data \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/reporting/get_data', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/reporting/get_data");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /reporting/get_data`

Get reporting data for given graphs.

List of possible graphs can be retrieved using `reporting.graphs` call.

For the time period of the graph either `unit` and `page` OR `start` and `end` should be
used, not both.

`aggregate` will return aggregate available data for each graph (e.g. min, max, mean).

> Body parameter

```json
{
  "graphs": [
    {}
  ],
  "reporting_query": {}
}
```

<h3 id="post__reporting_get_data-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[reporting_get_data](#schemareporting_get_data)|false|none|

<h3 id="post__reporting_get_data-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__reporting_graphs

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/reporting/graphs

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/reporting/graphs')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/reporting/graphs");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /reporting/graphs`

<h3 id="get__reporting_graphs-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__reporting_graphs-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-route">route</h1>

## post__route_ipv4gw_reachable

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/route/ipv4gw_reachable \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/route/ipv4gw_reachable', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/route/ipv4gw_reachable");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /route/ipv4gw_reachable`

Get the IPv4 gateway and verify if it is reachable by any interface.

Returns:
    bool: True if the gateway is reachable or otherwise False.

> Body parameter

```json
"string"
```

<h3 id="post__route_ipv4gw_reachable-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[route_ipv4gw_reachable_0](#schemaroute_ipv4gw_reachable_0)|false|none|

<h3 id="post__route_ipv4gw_reachable-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__route_system_routes

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/route/system_routes

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/route/system_routes')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/route/system_routes");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /route/system_routes`

Get current/applied network routes.

<h3 id="get__route_system_routes-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__route_system_routes-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-rsyncd">rsyncd</h1>

## get__rsyncd

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/rsyncd

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/rsyncd')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/rsyncd");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /rsyncd`

<h3 id="get__rsyncd-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__rsyncd

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/rsyncd \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/rsyncd', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/rsyncd");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /rsyncd`

Update Rsyncd Service Configuration.

`auxiliary` attribute can be used to pass on any additional parameters from rsyncd.conf(5).

> Body parameter

```json
{}
```

<h3 id="put__rsyncd-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[rsyncd_update_0](#schemarsyncd_update_0)|false|none|

<h3 id="put__rsyncd-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-rsyncmod">rsyncmod</h1>

## get__rsyncmod

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/rsyncmod

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/rsyncmod')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/rsyncmod");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /rsyncmod`

<h3 id="get__rsyncmod-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__rsyncmod-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__rsyncmod

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/rsyncmod \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/rsyncmod', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/rsyncmod");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /rsyncmod`

Create a Rsyncmod module.

`path` represents the path to a dataset. Path length is limited to 1023 characters maximum as per the limit
enforced by FreeBSD. It is possible that we reach this max length recursively while transferring data. In that
case, the user must ensure the maximum path will not be too long or modify the recursed path to shorter
than the limit.

`maxconn` is an integer value representing the maximum number of simultaneous connections. Zero represents
unlimited.

`hostsallow` is a list of patterns to match hostname/ip address of a connecting client. If list is empty,
all hosts are allowed.

`hostsdeny` is a list of patterns to match hostname/ip address of a connecting client. If the pattern is
matched, access is denied to the client. If no client should be denied, this should be left empty.

`auxiliary` attribute can be used to pass on any additional parameters from rsyncd.conf(5).

> Body parameter

```json
{}
```

<h3 id="post__rsyncmod-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[rsyncmod_create_0](#schemarsyncmod_create_0)|false|none|

<h3 id="post__rsyncmod-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__rsyncmod_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/rsyncmod/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/rsyncmod/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/rsyncmod/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /rsyncmod/id/{id}`

Delete Rsyncmod module of `id`.

<h3 id="delete__rsyncmod_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__rsyncmod_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__rsyncmod_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/rsyncmod/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/rsyncmod/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/rsyncmod/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /rsyncmod/id/{id}`

<h3 id="get__rsyncmod_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__rsyncmod_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__rsyncmod_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/rsyncmod/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/rsyncmod/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/rsyncmod/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /rsyncmod/id/{id}`

Update Rsyncmod module of `id`.

> Body parameter

```json
{}
```

<h3 id="put__rsyncmod_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[rsyncmod_update_1](#schemarsyncmod_update_1)|false|none|

<h3 id="put__rsyncmod_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-rsynctask">rsynctask</h1>

## get__rsynctask

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/rsynctask

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/rsynctask')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/rsynctask");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /rsynctask`

<h3 id="get__rsynctask-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__rsynctask-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__rsynctask

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/rsynctask \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/rsynctask', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/rsynctask");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /rsynctask`

Create a Rsync Task.

See the comment in Rsyncmod about `path` length limits.

`remotehost` is ip address or hostname of the remote system. If username differs on the remote host,
"username@remote_host" format should be used.

`mode` represents different operating mechanisms for Rsync i.e Rsync Module mode / Rsync SSH mode.

`remotemodule` is the name of remote module, this attribute should be specified when `mode` is set to MODULE.

`remotepath` specifies the path on the remote system.

`validate_rpath` is a boolean which when sets validates the existence of the remote path.

`direction` specifies if data should be PULLED or PUSHED from the remote system.

`compress` when set reduces the size of the data which is to be transmitted.

`archive` when set makes rsync run recursively, preserving symlinks, permissions, modification times, group,
and special files.

`delete` when set deletes files in the destination directory which do not exist in the source directory.

`preserveperm` when set preserves original file permissions.

> Body parameter

```json
{}
```

<h3 id="post__rsynctask-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[rsynctask_create_0](#schemarsynctask_create_0)|false|none|

<h3 id="post__rsynctask-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__rsynctask_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/rsynctask/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/rsynctask/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/rsynctask/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /rsynctask/id/{id}`

Delete Rsync Task of `id`.

<h3 id="delete__rsynctask_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__rsynctask_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__rsynctask_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/rsynctask/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/rsynctask/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/rsynctask/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /rsynctask/id/{id}`

<h3 id="get__rsynctask_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__rsynctask_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__rsynctask_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/rsynctask/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/rsynctask/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/rsynctask/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /rsynctask/id/{id}`

Update Rsync Task of `id`.

> Body parameter

```json
{}
```

<h3 id="put__rsynctask_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[rsynctask_update_1](#schemarsynctask_update_1)|false|none|

<h3 id="put__rsynctask_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__rsynctask_id_{id}_run

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/rsynctask/id/{id}/run \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/rsynctask/id/{id}/run', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/rsynctask/id/{id}/run");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /rsynctask/id/{id}/run`

Job to run rsync task of `id`.

Output is saved to job log excerpt (not syslog).

> Body parameter

```json
{}
```

<h3 id="post__rsynctask_id_{id}_run-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[rsynctask_run](#schemarsynctask_run)|false|none|

<h3 id="post__rsynctask_id_{id}_run-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-s3">s3</h1>

## get__s3

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/s3

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/s3')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/s3");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /s3`

<h3 id="get__s3-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__s3

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/s3 \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/s3', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/s3");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /s3`

Update S3 Service Configuration.

`access_key` must only contain alphanumeric characters and should be between 5 and 20 characters.

`secret_key` must only contain alphanumeric characters and should be between 8 and 40 characters.

`browser` when set, enables the web user interface for the S3 Service.

`certificate` is a valid certificate id which exists in the system. This is used to enable secure
S3 connections.

> Body parameter

```json
{}
```

<h3 id="put__s3-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[s3_update_0](#schemas3_update_0)|false|none|

<h3 id="put__s3-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__s3_bindip_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/s3/bindip_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/s3/bindip_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/s3/bindip_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /s3/bindip_choices`

Return ip choices for S3 service to use.

<h3 id="get__s3_bindip_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-sensor">sensor</h1>

## get__sensor_query

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/sensor/query

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/sensor/query')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/sensor/query");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /sensor/query`

<h3 id="get__sensor_query-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__sensor_query-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-service">service</h1>

## get__service

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/service

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/service')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/service");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /service`

Query all system services with `query-filters` and `query-options`.

<h3 id="get__service-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__service-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__service_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/service/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/service/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/service/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /service/id/{id}`

Query all system services with `query-filters` and `query-options`.

<h3 id="get__service_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__service_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__service_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/service/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/service/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/service/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /service/id/{id}`

Update service entry of `id_or_name`.

Currently it only accepts `enable` option which means whether the
service should start on boot.

> Body parameter

```json
{}
```

<h3 id="put__service_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|string|true|none|
|body|body|[service_update_1](#schemaservice_update_1)|false|none|

<h3 id="put__service_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__service_reload

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/service/reload \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/service/reload', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/service/reload");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /service/reload`

Reload the service specified by `service`.

> Body parameter

```json
{
  "service": "string",
  "service-control": {}
}
```

<h3 id="post__service_reload-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[service_reload](#schemaservice_reload)|false|none|

<h3 id="post__service_reload-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__service_restart

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/service/restart \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/service/restart', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/service/restart");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /service/restart`

Restart the service specified by `service`.

> Body parameter

```json
{
  "service": "string",
  "service-control": {}
}
```

<h3 id="post__service_restart-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[service_restart](#schemaservice_restart)|false|none|

<h3 id="post__service_restart-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__service_start

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/service/start \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/service/start', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/service/start");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /service/start`

Start the service specified by `service`.

> Body parameter

```json
{
  "service": "string",
  "service-control": {}
}
```

<h3 id="post__service_start-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[service_start](#schemaservice_start)|false|none|

<h3 id="post__service_start-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__service_started

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/service/started

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/service/started')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/service/started");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /service/started`

Test if service specified by `service` has been started.

<h3 id="get__service_started-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__service_stop

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/service/stop \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/service/stop', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/service/stop");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /service/stop`

Stop the service specified by `service`.

> Body parameter

```json
{
  "service": "string",
  "service-control": {}
}
```

<h3 id="post__service_stop-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[service_stop](#schemaservice_stop)|false|none|

<h3 id="post__service_stop-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__service_terminate_process

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/service/terminate_process \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/service/terminate_process', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/service/terminate_process");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /service/terminate_process`

Terminate process by `pid`.

First send `TERM` signal, then, if was not terminated in `timeout` seconds, send `KILL` signal.

Returns `true` is process has been successfully terminated with `TERM` and `false` if we had to use `KILL`.

> Body parameter

```json
{
  "pid": 0,
  "timeout": 10
}
```

<h3 id="post__service_terminate_process-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[service_terminate_process](#schemaservice_terminate_process)|false|none|

<h3 id="post__service_terminate_process-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-sharing">sharing</h1>

## get__sharing

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/sharing

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/sharing')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/sharing");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /sharing`

<h3 id="get__sharing-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__sharing-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__sharing_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/sharing/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/sharing/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/sharing/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /sharing/id/{id}`

<h3 id="get__sharing_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__sharing_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-sharing-afp">sharing.afp</h1>

## get__sharing_afp

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/sharing/afp

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/sharing/afp')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/sharing/afp");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /sharing/afp`

<h3 id="get__sharing_afp-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__sharing_afp-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__sharing_afp

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/sharing/afp \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/sharing/afp', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/sharing/afp");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /sharing/afp`

Create AFP share.

`allow`, `deny`, `ro`, and `rw` are lists of users and groups. Groups are designated by
an @ prefix.

`hostsallow` and `hostsdeny` are lists of hosts and/or networks.

> Body parameter

```json
{}
```

<h3 id="post__sharing_afp-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[sharing_afp_create_0](#schemasharing_afp_create_0)|false|none|

<h3 id="post__sharing_afp-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__sharing_afp_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/sharing/afp/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/sharing/afp/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/sharing/afp/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /sharing/afp/id/{id}`

Delete AFP share `id`.

<h3 id="delete__sharing_afp_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__sharing_afp_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__sharing_afp_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/sharing/afp/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/sharing/afp/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/sharing/afp/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /sharing/afp/id/{id}`

<h3 id="get__sharing_afp_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__sharing_afp_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__sharing_afp_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/sharing/afp/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/sharing/afp/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/sharing/afp/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /sharing/afp/id/{id}`

Update AFP share `id`.

> Body parameter

```json
{}
```

<h3 id="put__sharing_afp_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[sharing_afp_update_1](#schemasharing_afp_update_1)|false|none|

<h3 id="put__sharing_afp_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-sharing-nfs">sharing.nfs</h1>

## get__sharing_nfs

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/sharing/nfs

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/sharing/nfs')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/sharing/nfs");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /sharing/nfs`

<h3 id="get__sharing_nfs-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__sharing_nfs-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__sharing_nfs

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/sharing/nfs \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/sharing/nfs', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/sharing/nfs");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /sharing/nfs`

Create a NFS Share.

`paths` is a list of valid paths which are configured to be shared on this share.

`networks` is a list of authorized networks that are allowed to access the share having format
"network/mask" CIDR notation. If empty, all networks are allowed.

`hosts` is a list of IP's/hostnames which are allowed to access the share. If empty, all IP's/hostnames are
allowed.

`alldirs` is a boolean value which when set indicates that the client can mount any subdirectories of the
selected pool or dataset.

> Body parameter

```json
{}
```

<h3 id="post__sharing_nfs-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[sharing_nfs_create_0](#schemasharing_nfs_create_0)|false|none|

<h3 id="post__sharing_nfs-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__sharing_nfs_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/sharing/nfs/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/sharing/nfs/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/sharing/nfs/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /sharing/nfs/id/{id}`

Delete NFS Share of `id`.

<h3 id="delete__sharing_nfs_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__sharing_nfs_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__sharing_nfs_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/sharing/nfs/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/sharing/nfs/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/sharing/nfs/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /sharing/nfs/id/{id}`

<h3 id="get__sharing_nfs_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__sharing_nfs_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__sharing_nfs_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/sharing/nfs/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/sharing/nfs/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/sharing/nfs/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /sharing/nfs/id/{id}`

Update NFS Share of `id`.

> Body parameter

```json
{}
```

<h3 id="put__sharing_nfs_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[sharing_nfs_update_1](#schemasharing_nfs_update_1)|false|none|

<h3 id="put__sharing_nfs_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__sharing_nfs_human_identifier

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/sharing/nfs/human_identifier

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/sharing/nfs/human_identifier')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/sharing/nfs/human_identifier");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /sharing/nfs/human_identifier`

<h3 id="get__sharing_nfs_human_identifier-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-sharing-smb">sharing.smb</h1>

## get__sharing_smb

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/sharing/smb

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/sharing/smb')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/sharing/smb");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /sharing/smb`

<h3 id="get__sharing_smb-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__sharing_smb-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__sharing_smb

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/sharing/smb \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/sharing/smb', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/sharing/smb");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /sharing/smb`

Create a SMB Share.

`purpose` applies common configuration presets depending on intended purpose.

`timemachine` when set, enables Time Machine backups for this share.

`ro` when enabled, prohibits write access to the share.

`guestok` when enabled, allows access to this share without a password.

`hostsallow` is a list of hostnames / IP addresses which have access to this share.

`hostsdeny` is a list of hostnames / IP addresses which are not allowed access to this share. If a handful
of hostnames are to be only allowed access, `hostsdeny` can be passed "ALL" which means that it will deny
access to ALL hostnames except for the ones which have been listed in `hostsallow`.

`acl` enables support for storing the SMB Security Descriptor as a Filesystem ACL.

`streams` enables support for storing alternate datastreams as filesystem extended attributes.

`fsrvp` enables support for the filesystem remote VSS protocol. This allows clients to create
ZFS snapshots through RPC.

`shadowcopy` enables support for the volume shadow copy service.

`auxsmbconf` is a string of additional smb4.conf parameters not covered by the system's API.

> Body parameter

```json
{}
```

<h3 id="post__sharing_smb-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[sharing_smb_create_0](#schemasharing_smb_create_0)|false|none|

<h3 id="post__sharing_smb-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__sharing_smb_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/sharing/smb/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/sharing/smb/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/sharing/smb/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /sharing/smb/id/{id}`

Delete SMB Share of `id`. This will forcibly disconnect SMB clients
that are accessing the share.

<h3 id="delete__sharing_smb_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__sharing_smb_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__sharing_smb_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/sharing/smb/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/sharing/smb/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/sharing/smb/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /sharing/smb/id/{id}`

<h3 id="get__sharing_smb_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__sharing_smb_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__sharing_smb_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/sharing/smb/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/sharing/smb/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/sharing/smb/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /sharing/smb/id/{id}`

Update SMB Share of `id`.

> Body parameter

```json
{}
```

<h3 id="put__sharing_smb_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[sharing_smb_update_1](#schemasharing_smb_update_1)|false|none|

<h3 id="put__sharing_smb_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__sharing_smb_presets

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/sharing/smb/presets

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/sharing/smb/presets')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/sharing/smb/presets");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /sharing/smb/presets`

Retrieve pre-defined configuration sets for specific use-cases. These parameter
combinations are often non-obvious, but beneficial in these scenarios.

<h3 id="get__sharing_smb_presets-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-sharing-webdav">sharing.webdav</h1>

## get__sharing_webdav

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/sharing/webdav

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/sharing/webdav')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/sharing/webdav");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /sharing/webdav`

<h3 id="get__sharing_webdav-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__sharing_webdav-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__sharing_webdav

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/sharing/webdav \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/sharing/webdav', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/sharing/webdav");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /sharing/webdav`

Create a Webdav Share.

`ro` when enabled prohibits users from writing to this share.

`perm` when enabled automatically recursively changes the ownership of this share to
webdav ( user and group both ).

> Body parameter

```json
{}
```

<h3 id="post__sharing_webdav-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[sharing_webdav_create_0](#schemasharing_webdav_create_0)|false|none|

<h3 id="post__sharing_webdav-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__sharing_webdav_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/sharing/webdav/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/sharing/webdav/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/sharing/webdav/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /sharing/webdav/id/{id}`

Update Webdav Share of `id`.

<h3 id="delete__sharing_webdav_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__sharing_webdav_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__sharing_webdav_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/sharing/webdav/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/sharing/webdav/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/sharing/webdav/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /sharing/webdav/id/{id}`

<h3 id="get__sharing_webdav_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__sharing_webdav_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__sharing_webdav_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/sharing/webdav/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/sharing/webdav/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/sharing/webdav/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /sharing/webdav/id/{id}`

Update Webdav Share of `id`.

> Body parameter

```json
{}
```

<h3 id="put__sharing_webdav_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[sharing_webdav_update_1](#schemasharing_webdav_update_1)|false|none|

<h3 id="put__sharing_webdav_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-smart">smart</h1>

## get__smart

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/smart

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/smart')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/smart");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /smart`

<h3 id="get__smart-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__smart

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/smart \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/smart', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/smart");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /smart`

Update SMART Service Configuration.

`interval` is an integer value in minutes which defines how often smartd activates to check if any tests
are configured to run.

`critical`, `informational` and `difference` are integer values on which alerts for SMART are configured if
the disks temperature crosses the assigned threshold for each respective attribute. They default to 0 which
indicates they are disabled.

> Body parameter

```json
{}
```

<h3 id="put__smart-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[smart_update_0](#schemasmart_update_0)|false|none|

<h3 id="put__smart-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-smart-test">smart.test</h1>

## get__smart_test

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/smart/test

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/smart/test')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/smart/test");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /smart/test`

<h3 id="get__smart_test-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__smart_test-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__smart_test

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/smart/test \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/smart/test', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/smart/test");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /smart/test`

Create a SMART Test Task.

`disks` is a list of valid disks which should be monitored in this task.

`type` is specified to represent the type of SMART test to be executed.

`all_disks` when enabled sets the task to cover all disks in which case `disks` is not required.

> Body parameter

```json
{}
```

<h3 id="post__smart_test-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[smart_test_create_0](#schemasmart_test_create_0)|false|none|

<h3 id="post__smart_test-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__smart_test_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/smart/test/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/smart/test/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/smart/test/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /smart/test/id/{id}`

Delete SMART Test Task of `id`.

<h3 id="delete__smart_test_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__smart_test_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__smart_test_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/smart/test/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/smart/test/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/smart/test/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /smart/test/id/{id}`

<h3 id="get__smart_test_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__smart_test_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__smart_test_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/smart/test/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/smart/test/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/smart/test/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /smart/test/id/{id}`

Update SMART Test Task of `id`.

> Body parameter

```json
{}
```

<h3 id="put__smart_test_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[smart_test_update_1](#schemasmart_test_update_1)|false|none|

<h3 id="put__smart_test_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__smart_test_manual_test

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/smart/test/manual_test \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/smart/test/manual_test', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/smart/test/manual_test");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /smart/test/manual_test`

Run manual SMART tests for `disks`.

`type` indicates what type of SMART test will be ran and must be specified.

> Body parameter

```json
[
  {}
]
```

<h3 id="post__smart_test_manual_test-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[smart_test_manual_test_0](#schemasmart_test_manual_test_0)|false|none|

<h3 id="post__smart_test_manual_test-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__smart_test_results

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/smart/test/results

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/smart/test/results')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/smart/test/results");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /smart/test/results`

Get disk(s) S.M.A.R.T. test(s) results.

<h3 id="get__smart_test_results-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__smart_test_results-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-smb">smb</h1>

## get__smb

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/smb

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/smb')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/smb");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /smb`

<h3 id="get__smb-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__smb

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/smb \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/smb', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/smb");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /smb`

Update SMB Service Configuration.

`netbiosname` defaults to the original hostname of the system.

`workgroup` and `netbiosname` should have different values.

`enable_smb1` allows legacy SMB clients to connect to the server when enabled.

`localmaster` when set, determines if the system participates in a browser election.

`domain_logons` is used to provide netlogin service for older Windows clients if enabled.

`guest` attribute is specified to select the account to be used for guest access. It defaults to "nobody".

`nullpw` when enabled allows the users to authorize access without a password.

`hostlookup` when enabled, allows using hostnames rather then IP addresses in "hostsallow"/"hostsdeny" fields
of SMB Shares.

> Body parameter

```json
{}
```

<h3 id="put__smb-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[smb_update_0](#schemasmb_update_0)|false|none|

<h3 id="put__smb-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__smb_bindip_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/smb/bindip_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/smb/bindip_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/smb/bindip_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /smb/bindip_choices`

List of valid choices for IP addresses to which to bind the SMB service.
Addresses assigned by DHCP are excluded from the results.

<h3 id="get__smb_bindip_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__smb_domain_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/smb/domain_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/smb/domain_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/smb/domain_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /smb/domain_choices`

List of domains visible to winbindd. Returns empty list if winbindd is
stopped.

<h3 id="get__smb_domain_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__smb_get_remote_acl

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/smb/get_remote_acl \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/smb/get_remote_acl', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/smb/get_remote_acl");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /smb/get_remote_acl`

Retrieves an ACL from a remote SMB server.

`server` IP Address or hostname of the remote server

`share` Share name

`path` path on the remote SMB server. Use "" to separate path components

`username` username to use for authentication

`password` password to use for authentication

`use_kerberos` use credentials to get a kerberos ticket for authentication.
AD only.

`output_format` format for resulting ACL data. Choices are either 'SMB',
which will present the information as a Windows SD or 'LOCAL', which formats
the ACL information according local filesystem of the TrueNAS server.

> Body parameter

```json
{}
```

<h3 id="post__smb_get_remote_acl-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[smb_get_remote_acl_0](#schemasmb_get_remote_acl_0)|false|none|

<h3 id="post__smb_get_remote_acl-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__smb_status

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/smb/status \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/smb/status', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/smb/status");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /smb/status`

Returns SMB server status (sessions, open files, locks, notifications).

`info_level` type of information requests. Defaults to ALL.

`status_options` additional options to filter query results. Supported
values are as follows: `verbose` gives more verbose status output
`fast` causes smbstatus to not check if the status data is valid by
checking if the processes that the status data refer to all still
exist. This speeds up execution on busy systems and clusters but
might display stale data of processes that died without cleaning up
properly. `restrict_user` specifies the limits results to the specified
user.

> Body parameter

```json
{
  "info_level": "ALL",
  "query-filters": [],
  "query-options": {},
  "status_options": {}
}
```

<h3 id="post__smb_status-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[smb_status](#schemasmb_status)|false|none|

<h3 id="post__smb_status-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__smb_unixcharset_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/smb/unixcharset_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/smb/unixcharset_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/smb/unixcharset_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /smb/unixcharset_choices`

<h3 id="get__smb_unixcharset_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-smb-sharesec">smb.sharesec</h1>

## get__smb_sharesec

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/smb/sharesec

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/smb/sharesec')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/smb/sharesec");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /smb/sharesec`

Use query-filters to search the SMB share ACLs present on server.

<h3 id="get__smb_sharesec-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__smb_sharesec-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__smb_sharesec

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/smb/sharesec \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/smb/sharesec', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/smb/sharesec");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /smb/sharesec`

Update the ACL on a given SMB share. Will write changes to both
/var/db/system/samba4/share_info.tdb and the configuration file.
Since an SMB share will _always_ have an ACL present, there is little
distinction between the `create` and `update` methods apart from arguments.

`share_name` - name of SMB share.

`share_acl` a list of ACL entries (dictionaries) with the following keys:

`ae_who_sid` who the ACL entry applies to expressed as a Windows SID

`ae_who_name` who the ACL entry applies to expressed as a name. `ae_who_name` is
a dictionary containing the following keys: `domain` that the user is a member of,
`name` username in the domain. The domain for local users is the netbios name of
the FreeNAS server.

`ae_perm` string representation of the permissions granted to the user or group.
`FULL` grants read, write, execute, delete, write acl, and change owner.
`CHANGE` grants read, write, execute, and delete.
`READ` grants read and execute.

`ae_type` can be ALLOWED or DENIED.

> Body parameter

```json
{}
```

<h3 id="post__smb_sharesec-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[smb_sharesec_create_0](#schemasmb_sharesec_create_0)|false|none|

<h3 id="post__smb_sharesec-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__smb_sharesec_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/smb/sharesec/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/smb/sharesec/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/smb/sharesec/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /smb/sharesec/id/{id}`

Replace share ACL for the specified SMB share with the samba default ACL of S-1-1-0/FULL
(Everyone - Full Control). In this case, access will be fully determined
by the underlying filesystem ACLs and smb4.conf parameters governing access control
and permissions.
Share can be deleted by name or numerical by numerical index.

<h3 id="delete__smb_sharesec_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|string|true|none|

<h3 id="delete__smb_sharesec_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__smb_sharesec_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/smb/sharesec/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/smb/sharesec/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/smb/sharesec/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /smb/sharesec/id/{id}`

Use query-filters to search the SMB share ACLs present on server.

<h3 id="get__smb_sharesec_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__smb_sharesec_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__smb_sharesec_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/smb/sharesec/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/smb/sharesec/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/smb/sharesec/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /smb/sharesec/id/{id}`

Update the ACL on the share specified by the numerical index `id`. Will write changes
to both /var/db/system/samba4/share_info.tdb and the configuration file.

> Body parameter

```json
{}
```

<h3 id="put__smb_sharesec_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[smb_sharesec_update_1](#schemasmb_sharesec_update_1)|false|none|

<h3 id="put__smb_sharesec_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__smb_sharesec_getacl

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/smb/sharesec/getacl \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/smb/sharesec/getacl', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/smb/sharesec/getacl");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /smb/sharesec/getacl`

View the ACL information for `share_name`. The share ACL is distinct from filesystem
ACLs which can be viewed by calling `filesystem.getacl`. `ae_who_name` will appear
as `None` if the SMB service is stopped or if winbind is unable  to resolve the SID
to a name.

If the `option` `resolve_sids` is set to `False` then the returned ACL will not
contain names.

> Body parameter

```json
{
  "share_name": "string",
  "options": {}
}
```

<h3 id="post__smb_sharesec_getacl-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[smb_sharesec_getacl](#schemasmb_sharesec_getacl)|false|none|

<h3 id="post__smb_sharesec_getacl-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__smb_sharesec_synchronize_acls

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/smb/sharesec/synchronize_acls

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/smb/sharesec/synchronize_acls')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/smb/sharesec/synchronize_acls");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /smb/sharesec/synchronize_acls`

Synchronize the share ACL stored in the config database with Samba's running
configuration as reflected in the share_info.tdb file.

The only situation in which the configuration stored in the database will
overwrite samba's running configuration is if share_info.tdb is empty. Samba
fakes a single S-1-1-0:ALLOW/0x0/FULL entry in the absence of an entry for a
share in share_info.tdb.

<h3 id="get__smb_sharesec_synchronize_acls-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-snmp">snmp</h1>

## get__snmp

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/snmp

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/snmp')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/snmp");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /snmp`

<h3 id="get__snmp-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__snmp

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/snmp \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/snmp', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/snmp");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /snmp`

Update SNMP Service Configuration.

`v3` when set enables SNMP version 3.

`v3_username`, `v3_authtype`, `v3_password`, `v3_privproto` and `v3_privpassphrase` are only used when `v3`
is enabled.

> Body parameter

```json
{}
```

<h3 id="put__snmp-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[snmp_update_0](#schemasnmp_update_0)|false|none|

<h3 id="put__snmp-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-ssh">ssh</h1>

## get__ssh

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/ssh

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/ssh')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/ssh");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /ssh`

<h3 id="get__ssh-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__ssh

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/ssh \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/ssh', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/ssh");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /ssh`

Update settings of SSH daemon service.

If `bindiface` is empty it will listen for all available addresses.

> Body parameter

```json
{}
```

<h3 id="put__ssh-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[ssh_update_0](#schemassh_update_0)|false|none|

<h3 id="put__ssh-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__ssh_bindiface_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/ssh/bindiface_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/ssh/bindiface_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/ssh/bindiface_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /ssh/bindiface_choices`

Available choices for the bindiface attribute of SSH service.

<h3 id="get__ssh_bindiface_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-staticroute">staticroute</h1>

## get__staticroute

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/staticroute

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/staticroute')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/staticroute");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /staticroute`

<h3 id="get__staticroute-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__staticroute-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__staticroute

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/staticroute \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/staticroute', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/staticroute");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /staticroute`

Create a Static Route.

Address families of `gateway` and `destination` should match when creating a static route.

`description` is an optional attribute for any notes regarding the static route.

> Body parameter

```json
{}
```

<h3 id="post__staticroute-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[staticroute_create_0](#schemastaticroute_create_0)|false|none|

<h3 id="post__staticroute-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__staticroute_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/staticroute/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/staticroute/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/staticroute/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /staticroute/id/{id}`

Delete Static Route of `id`.

<h3 id="delete__staticroute_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__staticroute_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__staticroute_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/staticroute/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/staticroute/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/staticroute/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /staticroute/id/{id}`

<h3 id="get__staticroute_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__staticroute_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__staticroute_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/staticroute/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/staticroute/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/staticroute/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /staticroute/id/{id}`

Update Static Route of `id`.

> Body parameter

```json
{}
```

<h3 id="put__staticroute_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[staticroute_update_1](#schemastaticroute_update_1)|false|none|

<h3 id="put__staticroute_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-stats">stats</h1>

## post__stats_get_data

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/stats/get_data \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/stats/get_data', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/stats/get_data");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /stats/get_data`

Get data points from rrd files.

> Body parameter

```json
{
  "stats_list": [
    {}
  ],
  "stats-filter": {}
}
```

<h3 id="post__stats_get_data-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[stats_get_data](#schemastats_get_data)|false|none|

<h3 id="post__stats_get_data-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__stats_get_dataset_info

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/stats/get_dataset_info \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/stats/get_dataset_info', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/stats/get_dataset_info");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /stats/get_dataset_info`

Returns info about a given dataset from some source.

> Body parameter

```json
{
  "source": "string",
  "type": "string"
}
```

<h3 id="post__stats_get_dataset_info-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[stats_get_dataset_info](#schemastats_get_dataset_info)|false|none|

<h3 id="post__stats_get_dataset_info-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__stats_get_sources

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/stats/get_sources

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/stats/get_sources')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/stats/get_sources");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /stats/get_sources`

Returns an object with all available sources tried with metric datasets.

<h3 id="get__stats_get_sources-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-support">support</h1>

## get__support

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/support

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/support')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/support");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /support`

<h3 id="get__support-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__support

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/support \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/support', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/support");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /support`

Update Proactive Support settings.

> Body parameter

```json
{}
```

<h3 id="put__support-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[support_update_0](#schemasupport_update_0)|false|none|

<h3 id="put__support-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__support_fetch_categories

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/support/fetch_categories \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/support/fetch_categories', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/support/fetch_categories");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /support/fetch_categories`

Fetch all the categories available for `username` using `password`.
Returns a dict with the category name as a key and id as value.

> Body parameter

```json
{
  "username": "string",
  "password": "string"
}
```

<h3 id="post__support_fetch_categories-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[support_fetch_categories](#schemasupport_fetch_categories)|false|none|

<h3 id="post__support_fetch_categories-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__support_fields

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/support/fields

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/support/fields')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/support/fields");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /support/fields`

Returns list of pairs of field names and field titles for Proactive Support.

<h3 id="get__support_fields-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__support_is_available

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/support/is_available

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/support/is_available')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/support/is_available");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /support/is_available`

Returns whether Proactive Support is available for this product type and current license.

<h3 id="get__support_is_available-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__support_is_available_and_enabled

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/support/is_available_and_enabled

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/support/is_available_and_enabled')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/support/is_available_and_enabled");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /support/is_available_and_enabled`

Returns whether Proactive Support is available and enabled.

<h3 id="get__support_is_available_and_enabled-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__support_new_ticket

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/support/new_ticket \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/support/new_ticket', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/support/new_ticket");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /support/new_ticket`

Creates a new ticket for support.
This is done using the support proxy API.
For FreeNAS it will be created on Redmine and for TrueNAS on SupportSuite.

For FreeNAS `criticality`, `environment`, `phone`, `name` and `email` attributes are not required.
For TrueNAS `username`, `password` and `type` attributes are not required.

> Body parameter

```json
{}
```

<h3 id="post__support_new_ticket-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[support_new_ticket_0](#schemasupport_new_ticket_0)|false|none|

<h3 id="post__support_new_ticket-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-system">system</h1>

## get__system_boot_id

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/system/boot_id

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/system/boot_id')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/boot_id");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /system/boot_id`

Returns an unique boot identifier.

It is supposed to be unique every system boot.

<h3 id="get__system_boot_id-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__system_environment

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/system/environment

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/system/environment')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/environment");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /system/environment`

Return environment in which product is running. Possible values:
- DEFAULT
- EC2

<h3 id="get__system_environment-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__system_feature_enabled

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/system/feature_enabled \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/system/feature_enabled', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/feature_enabled");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /system/feature_enabled`

Returns whether the `feature` is enabled or not

> Body parameter

```json
"DEDUP"
```

<h3 id="post__system_feature_enabled-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[system_feature_enabled_0](#schemasystem_feature_enabled_0)|false|none|

#### Enumerated Values

|Parameter|Value|
|---|---|
|body|DEDUP|
|body|FIBRECHANNEL|
|body|JAILS|
|body|VM|

<h3 id="post__system_feature_enabled-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__system_info

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/system/info

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/system/info')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/info");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /system/info`

Returns basic system information.

<h3 id="get__system_info-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__system_is_freenas

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/system/is_freenas

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/system/is_freenas')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/is_freenas");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /system/is_freenas`

FreeNAS is now TrueNAS CORE.

DEPRECATED: Use `system.product_type`

<h3 id="get__system_is_freenas-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__system_license_update

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/system/license_update \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/system/license_update', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/license_update");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /system/license_update`

Update license file.

> Body parameter

```json
"string"
```

<h3 id="post__system_license_update-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[system_license_update_0](#schemasystem_license_update_0)|false|none|

<h3 id="post__system_license_update-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__system_product_name

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/system/product_name

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/system/product_name')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/product_name");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /system/product_name`

Returns name of the product we are using.

<h3 id="get__system_product_name-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__system_product_type

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/system/product_type

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/system/product_type')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/product_type");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /system/product_type`

Returns the type of the product.

CORE - TrueNAS Core, community version
ENTERPRISE - TrueNAS Enterprise, appliance version
SCALE - TrueNAS SCALE

<h3 id="get__system_product_type-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__system_ready

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/system/ready

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/system/ready')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/ready");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /system/ready`

Returns whether the system completed boot and is ready to use

<h3 id="get__system_ready-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__system_reboot

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/system/reboot \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/system/reboot', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/reboot");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /system/reboot`

Reboots the operating system.

Emits an "added" event of name "system" and id "reboot".

> Body parameter

```json
{}
```

<h3 id="post__system_reboot-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[system_reboot_0](#schemasystem_reboot_0)|false|none|

<h3 id="post__system_reboot-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__system_shutdown

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/system/shutdown \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/system/shutdown', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/shutdown");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /system/shutdown`

Shuts down the operating system.

An "added" event of name "system" and id "shutdown" is emitted when shutdown is initiated.

> Body parameter

```json
{}
```

<h3 id="post__system_shutdown-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[system_shutdown_0](#schemasystem_shutdown_0)|false|none|

<h3 id="post__system_shutdown-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__system_state

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/system/state

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/system/state')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/state");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /system/state`

Returns system state:
"BOOTING" - System is booting
"READY" - System completed boot and is ready to use
"SHUTTING_DOWN" - System is shutting down

<h3 id="get__system_state-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__system_version

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/system/version

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/system/version')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/version");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /system/version`

Returns software version of the system.

<h3 id="get__system_version-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-system-advanced">system.advanced</h1>

## get__system_advanced

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/system/advanced

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/system/advanced')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/advanced");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /system/advanced`

<h3 id="get__system_advanced-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__system_advanced

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/system/advanced \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/system/advanced', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/advanced");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /system/advanced`

Update System Advanced Service Configuration.

`consolemenu` should be disabled if the menu at console is not desired. It will default to standard login
in the console if disabled.

`autotune` when enabled executes autotune script which attempts to optimize the system based on the installed
hardware.

When `syslogserver` is defined, logs of `sysloglevel` or above are sent.

> Body parameter

```json
{}
```

<h3 id="put__system_advanced-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[system_advanced_update_0](#schemasystem_advanced_update_0)|false|none|

<h3 id="put__system_advanced-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__system_advanced_sed_global_password

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/system/advanced/sed_global_password

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/system/advanced/sed_global_password')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/advanced/sed_global_password");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /system/advanced/sed_global_password`

Returns configured global SED password.

<h3 id="get__system_advanced_sed_global_password-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__system_advanced_serial_port_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/system/advanced/serial_port_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/system/advanced/serial_port_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/advanced/serial_port_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /system/advanced/serial_port_choices`

Get available choices for `serialport`.

<h3 id="get__system_advanced_serial_port_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-system-general">system.general</h1>

## get__system_general

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/system/general

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/system/general')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/general");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /system/general`

<h3 id="get__system_general-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__system_general

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/system/general \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/system/general', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/general");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /system/general`

Update System General Service Configuration.

`ui_certificate` is used to enable HTTPS access to the system. If `ui_certificate` is not configured on boot,
it is automatically created by the system.

`ui_httpsredirect` when set, makes sure that all HTTP requests are converted to HTTPS requests to better
enhance security.

`ui_address` and `ui_v6address` are a list of valid ipv4/ipv6 addresses respectively which the system will
listen on.

`syslogserver` and `sysloglevel` are deprecated fields as of 11.3
and will be permanently moved to system.advanced.update for 12.0

> Body parameter

```json
{}
```

<h3 id="put__system_general-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[system_general_update_0](#schemasystem_general_update_0)|false|none|

<h3 id="put__system_general-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__system_general_country_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/system/general/country_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/system/general/country_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/general/country_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /system/general/country_choices`

Returns country choices.

<h3 id="get__system_general_country_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__system_general_kbdmap_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/system/general/kbdmap_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/system/general/kbdmap_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/general/kbdmap_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /system/general/kbdmap_choices`

Returns kbdmap choices.

<h3 id="get__system_general_kbdmap_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__system_general_language_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/system/general/language_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/system/general/language_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/general/language_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /system/general/language_choices`

Returns language choices.

<h3 id="get__system_general_language_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__system_general_local_url

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/system/general/local_url

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/system/general/local_url')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/general/local_url");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /system/general/local_url`

Returns configured local url in the format of protocol://host:port

<h3 id="get__system_general_local_url-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__system_general_timezone_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/system/general/timezone_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/system/general/timezone_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/general/timezone_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /system/general/timezone_choices`

Returns time zone choices.

<h3 id="get__system_general_timezone_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__system_general_ui_address_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/system/general/ui_address_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/system/general/ui_address_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/general/ui_address_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /system/general/ui_address_choices`

Returns UI ipv4 address choices.

<h3 id="get__system_general_ui_address_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__system_general_ui_certificate_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/system/general/ui_certificate_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/system/general/ui_certificate_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/general/ui_certificate_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /system/general/ui_certificate_choices`

Return choices of certificates which can be used for `ui_certificate`.

<h3 id="get__system_general_ui_certificate_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__system_general_ui_httpsprotocols_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/system/general/ui_httpsprotocols_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/system/general/ui_httpsprotocols_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/general/ui_httpsprotocols_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /system/general/ui_httpsprotocols_choices`

Returns available HTTPS protocols.

<h3 id="get__system_general_ui_httpsprotocols_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__system_general_ui_restart

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/system/general/ui_restart

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/system/general/ui_restart')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/general/ui_restart");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /system/general/ui_restart`

Restart HTTP server to use latest UI settings.

<h3 id="get__system_general_ui_restart-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__system_general_ui_v6address_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/system/general/ui_v6address_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/system/general/ui_v6address_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/general/ui_v6address_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /system/general/ui_v6address_choices`

Returns UI ipv6 address choices.

<h3 id="get__system_general_ui_v6address_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-system-ntpserver">system.ntpserver</h1>

## get__system_ntpserver

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/system/ntpserver

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/system/ntpserver')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/ntpserver");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /system/ntpserver`

<h3 id="get__system_ntpserver-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__system_ntpserver-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__system_ntpserver

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/system/ntpserver \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/system/ntpserver', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/ntpserver");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /system/ntpserver`

Add an NTP Server.

`address` specifies the hostname/IP address of the NTP server.

`burst` when enabled makes sure that if server is reachable, sends a burst of eight packets instead of one.
This is designed to improve timekeeping quality with the server command.

`iburst` when enabled speeds up the initial synchronization, taking seconds rather than minutes.

`prefer` marks the specified server as preferred. When all other things are equal, this host is chosen
for synchronization acquisition with the server command. It is recommended that they be used for servers with
time monitoring hardware.

`minpoll` is minimum polling time in seconds. It must be a power of 2 and less than `maxpoll`.

`maxpoll` is maximum polling time in seconds. It must be a power of 2 and greater than `minpoll`.

`force` when enabled forces the addition of NTP server even if it is currently unreachable.

> Body parameter

```json
{}
```

<h3 id="post__system_ntpserver-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[system_ntpserver_create_0](#schemasystem_ntpserver_create_0)|false|none|

<h3 id="post__system_ntpserver-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__system_ntpserver_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/system/ntpserver/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/system/ntpserver/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/ntpserver/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /system/ntpserver/id/{id}`

Delete NTP server of `id`.

<h3 id="delete__system_ntpserver_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__system_ntpserver_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__system_ntpserver_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/system/ntpserver/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/system/ntpserver/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/ntpserver/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /system/ntpserver/id/{id}`

<h3 id="get__system_ntpserver_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__system_ntpserver_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__system_ntpserver_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/system/ntpserver/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/system/ntpserver/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/ntpserver/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /system/ntpserver/id/{id}`

Update NTP server of `id`.

> Body parameter

```json
{}
```

<h3 id="put__system_ntpserver_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[system_ntpserver_update_1](#schemasystem_ntpserver_update_1)|false|none|

<h3 id="put__system_ntpserver_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__system_ntpserver_test_ntp_server

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/system/ntpserver/test_ntp_server

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/system/ntpserver/test_ntp_server')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/system/ntpserver/test_ntp_server");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /system/ntpserver/test_ntp_server`

<h3 id="get__system_ntpserver_test_ntp_server-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-systemdataset">systemdataset</h1>

## get__systemdataset

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/systemdataset

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/systemdataset')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/systemdataset");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /systemdataset`

<h3 id="get__systemdataset-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__systemdataset

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/systemdataset \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/systemdataset', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/systemdataset");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /systemdataset`

Update System Dataset Service Configuration.

`pool` is the name of a valid pool configured in the system which will be used to host the system dataset.

`pool_exclude` can be specified to make sure that we don't place the system dataset on that pool if `pool`
is not provided.

> Body parameter

```json
{}
```

<h3 id="put__systemdataset-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[systemdataset_update_0](#schemasystemdataset_update_0)|false|none|

<h3 id="put__systemdataset-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-taskpath">taskpath</h1>

## get__taskpath

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/taskpath

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/taskpath')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/taskpath");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /taskpath`

<h3 id="get__taskpath-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__taskpath-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__taskpath_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/taskpath/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/taskpath/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/taskpath/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /taskpath/id/{id}`

<h3 id="get__taskpath_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__taskpath_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-tftp">tftp</h1>

## get__tftp

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/tftp

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/tftp')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/tftp");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /tftp`

<h3 id="get__tftp-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__tftp

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/tftp \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/tftp', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/tftp");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /tftp`

Update TFTP Service Configuration.

`newfiles` when set enables network devices to send files to the system.

`username` sets the user account which will be used to access `directory`. It should be ensured `username`
has access to `directory`.

> Body parameter

```json
{}
```

<h3 id="put__tftp-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[tftp_update_0](#schematftp_update_0)|false|none|

<h3 id="put__tftp-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-truecommand">truecommand</h1>

## get__truecommand

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/truecommand

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/truecommand')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/truecommand");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /truecommand`

<h3 id="get__truecommand-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__truecommand

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/truecommand \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/truecommand', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/truecommand");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /truecommand`

Update Truecommand service settings.

`api_key` is a valid API key generated by iX Portal.

> Body parameter

```json
{}
```

<h3 id="put__truecommand-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[truecommand_update_0](#schematruecommand_update_0)|false|none|

<h3 id="put__truecommand-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-truenas">truenas</h1>

## get__truenas_accept_eula

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/truenas/accept_eula

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/truenas/accept_eula')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/truenas/accept_eula");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /truenas/accept_eula`

Accept TrueNAS EULA.

<h3 id="get__truenas_accept_eula-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__truenas_get_chassis_hardware

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/truenas/get_chassis_hardware

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/truenas/get_chassis_hardware')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/truenas/get_chassis_hardware");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /truenas/get_chassis_hardware`

Returns what type of hardware this is, detected from dmidecode.

TRUENAS-X10-HA-D
TRUENAS-X10-S
TRUENAS-X20-HA-D
TRUENAS-X20-S
TRUENAS-M40-HA
TRUENAS-M40-S
TRUENAS-M50-HA
TRUENAS-M50-S
TRUENAS-M60-HA
TRUENAS-M60-S
TRUENAS-Z20-S
TRUENAS-Z20-HA-D
TRUENAS-Z30-HA-D
TRUENAS-Z30-S
TRUENAS-Z35-HA-D
TRUENAS-Z35-S
TRUENAS-Z50-HA-D
TRUENAS-Z50-S

Nothing in dmidecode but a M, X or Z class machine:
(Note this means production didn't burn the hardware model
into SMBIOS. We can detect this case by looking at the
motherboard)
TRUENAS-M
TRUENAS-X
TRUENAS-Z

Detected by the motherboard model:
TRUENAS-SBB

Pretty much anything else with a SM X8 board:
(X8DTH was popular but there are a few other boards out there)
TRUENAS-SM

Really NFI about hardware at this point.  TrueNAS on a Dell?
TRUENAS-UNKNOWN

<h3 id="get__truenas_get_chassis_hardware-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__truenas_get_customer_information

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/truenas/get_customer_information

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/truenas/get_customer_information')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/truenas/get_customer_information");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /truenas/get_customer_information`

Returns stored customer information.

<h3 id="get__truenas_get_customer_information-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__truenas_get_eula

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/truenas/get_eula

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/truenas/get_eula')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/truenas/get_eula");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /truenas/get_eula`

Returns the TrueNAS End-User License Agreement (EULA).

<h3 id="get__truenas_get_eula-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__truenas_is_eula_accepted

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/truenas/is_eula_accepted

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/truenas/is_eula_accepted')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/truenas/is_eula_accepted");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /truenas/is_eula_accepted`

Returns whether the EULA is accepted or not.

<h3 id="get__truenas_is_eula_accepted-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__truenas_is_production

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/truenas/is_production

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/truenas/is_production')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/truenas/is_production");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /truenas/is_production`

Returns if system is marked as production.

<h3 id="get__truenas_is_production-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__truenas_set_production

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/truenas/set_production \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/truenas/set_production', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/truenas/set_production");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /truenas/set_production`

Sets system production state and optionally sends initial debug.

> Body parameter

```json
{
  "production": true,
  "attach_debug": false
}
```

<h3 id="post__truenas_set_production-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[truenas_set_production](#schematruenas_set_production)|false|none|

<h3 id="post__truenas_set_production-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__truenas_update_customer_information

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/truenas/update_customer_information \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/truenas/update_customer_information', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/truenas/update_customer_information");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /truenas/update_customer_information`

Updates customer information.

> Body parameter

```json
{}
```

<h3 id="post__truenas_update_customer_information-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[truenas_update_customer_information_0](#schematruenas_update_customer_information_0)|false|none|

<h3 id="post__truenas_update_customer_information-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-tunable">tunable</h1>

## get__tunable

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/tunable

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/tunable')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/tunable");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /tunable`

<h3 id="get__tunable-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__tunable-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__tunable

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/tunable \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/tunable', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/tunable");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /tunable`

Create a Tunable.

`var` represents name of the sysctl/loader/rc variable.

`type` for SCALE should be one of the following:
1) SYSCTL     -     Configure `var` for sysctl(8)

`type` for CORE/ENTERPRISE should be one of the following:
1) LOADER     -     Configure `var` for loader(8)
2) RC         -     Configure `var` for rc(8)
3) SYSCTL     -     Configure `var` for sysctl(8)

> Body parameter

```json
{}
```

<h3 id="post__tunable-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[tunable_create_0](#schematunable_create_0)|false|none|

<h3 id="post__tunable-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__tunable_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/tunable/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/tunable/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/tunable/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /tunable/id/{id}`

Delete Tunable of `id`.

<h3 id="delete__tunable_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__tunable_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__tunable_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/tunable/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/tunable/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/tunable/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /tunable/id/{id}`

<h3 id="get__tunable_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__tunable_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__tunable_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/tunable/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/tunable/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/tunable/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /tunable/id/{id}`

Update Tunable of `id`.

> Body parameter

```json
{}
```

<h3 id="put__tunable_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[tunable_update_1](#schematunable_update_1)|false|none|

<h3 id="put__tunable_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__tunable_tunable_type_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/tunable/tunable_type_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/tunable/tunable_type_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/tunable/tunable_type_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /tunable/tunable_type_choices`

Retrieve tunable type choices supported in the system

<h3 id="get__tunable_tunable_type_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-update">update</h1>

## post__update_check_available

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/update/check_available \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/update/check_available', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/update/check_available");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /update/check_available`

Checks if there is an update available from update server.

status:
  - REBOOT_REQUIRED: an update has already been applied
  - AVAILABLE: an update is available
  - UNAVAILABLE: no update available
  - HA_UNAVAILABLE: HA is non-functional

> Body parameter

```json
{}
```

<h3 id="post__update_check_available-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[update_check_available_0](#schemaupdate_check_available_0)|false|none|

<h3 id="post__update_check_available-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__update_download

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/update/download

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/update/download')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/update/download");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /update/download`

Download updates using selected train.

<h3 id="get__update_download-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__update_get_auto_download

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/update/get_auto_download

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/update/get_auto_download')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/update/get_auto_download");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /update/get_auto_download`

Returns if update auto-download is enabled.

<h3 id="get__update_get_auto_download-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__update_get_pending

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/update/get_pending \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/update/get_pending', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/update/get_pending");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /update/get_pending`

Gets a list of packages already downloaded and ready to be applied.
Each entry of the lists consists of type of operation and name of it, e.g.

  {
    "operation": "upgrade",
    "name": "baseos-11.0 -> baseos-11.1"
  }

> Body parameter

```json
null
```

<h3 id="post__update_get_pending-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[update_get_pending_0](#schemaupdate_get_pending_0)|false|none|

<h3 id="post__update_get_pending-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__update_get_trains

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/update/get_trains

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/update/get_trains')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/update/get_trains");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /update/get_trains`

Returns available trains dict and the currently configured train as well as the
train of currently booted environment.

<h3 id="get__update_get_trains-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__update_manual

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/update/manual \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/update/manual', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/update/manual");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /update/manual`

Apply manual update of file `path`.

> Body parameter

```json
"string"
```

<h3 id="post__update_manual-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[update_manual_0](#schemaupdate_manual_0)|false|none|

<h3 id="post__update_manual-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__update_set_auto_download

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/update/set_auto_download \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/update/set_auto_download', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/update/set_auto_download");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /update/set_auto_download`

Sets if update auto-download is enabled.

> Body parameter

```json
true
```

<h3 id="post__update_set_auto_download-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[update_set_auto_download_0](#schemaupdate_set_auto_download_0)|false|none|

<h3 id="post__update_set_auto_download-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__update_set_train

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/update/set_train \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/update/set_train', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/update/set_train");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /update/set_train`

Set an update train to be used by default in updates.

> Body parameter

```json
"string"
```

<h3 id="post__update_set_train-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[update_set_train_0](#schemaupdate_set_train_0)|false|none|

<h3 id="post__update_set_train-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__update_update

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/update/update \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/update/update', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/update/update");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /update/update`

Downloads (if not already in cache) and apply an update.

> Body parameter

```json
{}
```

<h3 id="post__update_update-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[update_update_0](#schemaupdate_update_0)|false|none|

<h3 id="post__update_update-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-ups">ups</h1>

## get__ups

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/ups

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/ups')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/ups");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /ups`

<h3 id="get__ups-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__ups

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/ups \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/ups', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/ups");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /ups`

Update UPS Service Configuration.

`emailnotify` when enabled, sends out notifications of different UPS events via email.

`powerdown` when enabled, sets UPS to power off after shutting down the system.

`nocommwarntime` is a value in seconds which makes UPS Service wait the specified seconds before alerting that
the Service cannot reach configured UPS.

`shutdowntimer` is a value in seconds which tells the Service to wait specified seconds for the UPS before
initiating a shutdown. This only applies when `shutdown` is set to "BATT".

`shutdowncmd` is the command which is executed to initiate a shutdown. It defaults to "poweroff".

`toemail` is a list of valid email id's on which notification emails are sent.

> Body parameter

```json
{}
```

<h3 id="put__ups-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[ups_update_0](#schemaups_update_0)|false|none|

<h3 id="put__ups-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__ups_driver_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/ups/driver_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/ups/driver_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/ups/driver_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /ups/driver_choices`

Returns choices of UPS drivers supported by the system.

<h3 id="get__ups_driver_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__ups_port_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/ups/port_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/ups/port_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/ups/port_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /ups/port_choices`

<h3 id="get__ups_port_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-user">user</h1>

## get__user

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/user

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/user')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/user");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /user`

Query users with `query-filters` and `query-options`. As a performance optimization, only local users
will be queried by default.

Users from directory services such as NIS, LDAP, or Active Directory will be included in query results
if the option `{'extra': {'search_dscache': True}}` is specified.

<h3 id="get__user-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__user-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__user

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/user \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/user', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/user");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /user`

Create a new user.

If `uid` is not provided it is automatically filled with the next one available.

`group` is required if `group_create` is false.

`password` is required if `password_disabled` is false.

Available choices for `shell` can be retrieved with `user.shell_choices`.

`attributes` is a general-purpose object for storing arbitrary user information.

`smb` specifies whether the user should be allowed access to SMB shares. User
willl also automatically be added to the `builtin_users` group.

> Body parameter

```json
{}
```

<h3 id="post__user-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[user_create_0](#schemauser_create_0)|false|none|

<h3 id="post__user-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__user_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/user/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.delete('http://192.168.1.123/api/v2.0/user/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/user/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /user/id/{id}`

Delete user `id`.

The `delete_group` option deletes the user primary group if it is not being used by
any other user.

> Body parameter

```json
{}
```

<h3 id="delete__user_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[user_delete_1](#schemauser_delete_1)|false|none|

<h3 id="delete__user_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__user_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/user/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/user/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/user/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /user/id/{id}`

Query users with `query-filters` and `query-options`. As a performance optimization, only local users
will be queried by default.

Users from directory services such as NIS, LDAP, or Active Directory will be included in query results
if the option `{'extra': {'search_dscache': True}}` is specified.

<h3 id="get__user_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__user_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__user_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/user/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/user/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/user/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /user/id/{id}`

Update attributes of an existing user.

> Body parameter

```json
{}
```

<h3 id="put__user_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[user_update_1](#schemauser_update_1)|false|none|

<h3 id="put__user_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__user_get_next_uid

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/user/get_next_uid

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/user/get_next_uid')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/user/get_next_uid");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /user/get_next_uid`

Get the next available/free uid.

<h3 id="get__user_get_next_uid-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__user_get_user_obj

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/user/get_user_obj \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/user/get_user_obj', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/user/get_user_obj");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /user/get_user_obj`

Returns dictionary containing information from struct passwd for the user specified by either
the username or uid. Bypasses user cache.

> Body parameter

```json
{}
```

<h3 id="post__user_get_user_obj-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[user_get_user_obj_0](#schemauser_get_user_obj_0)|false|none|

<h3 id="post__user_get_user_obj-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__user_has_root_password

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/user/has_root_password

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/user/has_root_password')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/user/has_root_password");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /user/has_root_password`

Return whether the root user has a valid password set.

This is used when the system is installed without a password and must be set on
first use/login.

<h3 id="get__user_has_root_password-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__user_id_{id}_pop_attribute

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/user/id/{id}/pop_attribute \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/user/id/{id}/pop_attribute', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/user/id/{id}/pop_attribute");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /user/id/{id}/pop_attribute`

Remove user general purpose `attributes` dictionary `key`.

> Body parameter

```json
"string"
```

<h3 id="post__user_id_{id}_pop_attribute-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[user_pop_attribute_1](#schemauser_pop_attribute_1)|false|none|

<h3 id="post__user_id_{id}_pop_attribute-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__user_id_{id}_set_attribute

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/user/id/{id}/set_attribute \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/user/id/{id}/set_attribute', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/user/id/{id}/set_attribute");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /user/id/{id}/set_attribute`

Set user general purpose `attributes` dictionary `key` to `value`.

e.g. Setting key="foo" value="var" will result in {"attributes": {"foo": "bar"}}

> Body parameter

```json
{
  "key": "string",
  "value": "string"
}
```

<h3 id="post__user_id_{id}_set_attribute-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[user_set_attribute](#schemauser_set_attribute)|false|none|

<h3 id="post__user_id_{id}_set_attribute-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__user_shell_choices

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/user/shell_choices \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/user/shell_choices', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/user/shell_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /user/shell_choices`

Return the available shell choices to be used in `user.create` and `user.update`.

If `user_id` is provided, shell choices are filtered to ensure the user can access the shell choices provided.

> Body parameter

```json
null
```

<h3 id="post__user_shell_choices-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[user_shell_choices_0](#schemauser_shell_choices_0)|false|none|

<h3 id="post__user_shell_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-vm">vm</h1>

## get__vm

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/vm

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/vm')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /vm`

<h3 id="get__vm-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__vm-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__vm

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/vm \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/vm', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /vm`

Create a Virtual Machine (VM).

`grubconfig` may either be a path for the grub.cfg file or the actual content
of the file to be used with GRUB bootloader.

`devices` is a list of virtualized hardware to add to the newly created Virtual Machine.
Failure to attach a device destroys the VM and any resources allocated by the VM devices.

Maximum of 16 guest virtual CPUs are allowed. By default, every virtual CPU is configured as a
separate package. Multiple cores can be configured per CPU by specifying `cores` attributes.
`vcpus` specifies total number of CPU sockets. `cores` specifies number of cores per socket. `threads`
specifies number of threads per core.

`shutdown_timeout` indicates the time in seconds the system waits for the VM to cleanly shutdown. During system
shutdown, if the VM hasn't exited after a hardware shutdown signal has been sent by the system within
`shutdown_timeout` seconds, system initiates poweroff for the VM to stop it.

> Body parameter

```json
{}
```

<h3 id="post__vm-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[vm_create_0](#schemavm_create_0)|false|none|

<h3 id="post__vm-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__vm_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/vm/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.delete('http://192.168.1.123/api/v2.0/vm/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /vm/id/{id}`

Delete a VM.

> Body parameter

```json
{}
```

<h3 id="delete__vm_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[vm_delete_1](#schemavm_delete_1)|false|none|

<h3 id="delete__vm_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__vm_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/vm/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/vm/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /vm/id/{id}`

<h3 id="get__vm_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__vm_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__vm_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/vm/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/vm/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /vm/id/{id}`

Update all information of a specific VM.

`devices` is a list of virtualized hardware to attach to the virtual machine. If `devices` is not present,
no change is made to devices. If either the device list order or data stored by the device changes when the
attribute is passed, these actions are taken:

1) If there is no device in the `devices` list which was previously attached to the VM, that device is
   removed from the virtual machine.
2) Devices are updated in the `devices` list when they contain a valid `id` attribute that corresponds to
   an existing device.
3) Devices that do not have an `id` attribute are created and attached to `id` VM.

> Body parameter

```json
{}
```

<h3 id="put__vm_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[vm_update_1](#schemavm_update_1)|false|none|

<h3 id="put__vm_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__vm_id_{id}_clone

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/vm/id/{id}/clone \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/vm/id/{id}/clone', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm/id/{id}/clone");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /vm/id/{id}/clone`

Clone the VM `id`.

`name` is an optional parameter for the cloned VM.
If not provided it will append the next number available to the VM name.

> Body parameter

```json
null
```

<h3 id="post__vm_id_{id}_clone-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[vm_clone_1](#schemavm_clone_1)|false|none|

<h3 id="post__vm_id_{id}_clone-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__vm_flags

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/vm/flags

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/vm/flags')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm/flags");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /vm/flags`

Returns a dictionary with CPU flags for bhyve.

<h3 id="get__vm_flags-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__vm_get_attached_iface

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/vm/get_attached_iface \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/vm/get_attached_iface', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm/get_attached_iface");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /vm/get_attached_iface`

Get the attached physical interfaces from a given guest.

Returns:
    list: will return a list with all attached phisycal interfaces or otherwise False.

> Body parameter

```json
0
```

<h3 id="post__vm_get_attached_iface-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[vm_get_attached_iface_0](#schemavm_get_attached_iface_0)|false|none|

<h3 id="post__vm_get_attached_iface-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__vm_get_available_memory

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/vm/get_available_memory \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/vm/get_available_memory', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm/get_available_memory");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /vm/get_available_memory`

Get the current maximum amount of available memory to be allocated for VMs.

If `overcommit` is true only the current used memory of running VMs will be accounted for.
If false all memory (including unused) of runnings VMs will be accounted for.

This will include memory shrinking ZFS ARC to the minimum.

Memory is of course a very "volatile" resource, values may change abruptly between a
second but I deem it good enough to give the user a clue about how much memory is
available at the current moment and if a VM should be allowed to be launched.

> Body parameter

```json
false
```

<h3 id="post__vm_get_available_memory-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[vm_get_available_memory_0](#schemavm_get_available_memory_0)|false|none|

<h3 id="post__vm_get_available_memory-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__vm_get_console

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/vm/get_console \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/vm/get_console', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm/get_console");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /vm/get_console`

Get the console device from a given guest.

Returns:
    str: with the device path or False.

> Body parameter

```json
0
```

<h3 id="post__vm_get_console-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[vm_get_console_0](#schemavm_get_console_0)|false|none|

<h3 id="post__vm_get_console-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__vm_get_vmemory_in_use

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/vm/get_vmemory_in_use

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/vm/get_vmemory_in_use')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm/get_vmemory_in_use");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /vm/get_vmemory_in_use`

The total amount of virtual memory in MB used by guests

    Returns a dict with the following information:
        RNP - Running but not provisioned
        PRD - Provisioned but not running
        RPRD - Running and provisioned

<h3 id="get__vm_get_vmemory_in_use-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__vm_get_vnc

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/vm/get_vnc \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/vm/get_vnc', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm/get_vnc");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /vm/get_vnc`

Get the vnc devices from a given guest.

Returns:
    list(dict): with all attributes of the vnc device or an empty list.

> Body parameter

```json
0
```

<h3 id="post__vm_get_vnc-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[vm_get_vnc_0](#schemavm_get_vnc_0)|false|none|

<h3 id="post__vm_get_vnc-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__vm_get_vnc_ipv4

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/vm/get_vnc_ipv4

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/vm/get_vnc_ipv4')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm/get_vnc_ipv4");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /vm/get_vnc_ipv4`

Get all available IPv4 address in the system.

Returns:
   list: will return a list of available IPv4 address.

<h3 id="get__vm_get_vnc_ipv4-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__vm_identify_hypervisor

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/vm/identify_hypervisor

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/vm/identify_hypervisor')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm/identify_hypervisor");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /vm/identify_hypervisor`

Identify Hypervisors that might work nested with bhyve.

Returns:
        bool: True if compatible otherwise False.

<h3 id="get__vm_identify_hypervisor-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__vm_id_{id}_poweroff

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/vm/id/{id}/poweroff \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/vm/id/{id}/poweroff', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm/id/{id}/poweroff");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /vm/id/{id}/poweroff`

> Body parameter

```json
{}
```

<h3 id="post__vm_id_{id}_poweroff-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[vm_poweroff](#schemavm_poweroff)|false|none|

<h3 id="post__vm_id_{id}_poweroff-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__vm_random_mac

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/vm/random_mac

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/vm/random_mac')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm/random_mac");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /vm/random_mac`

Create a random mac address.

Returns:
    str: with six groups of two hexadecimal digits

<h3 id="get__vm_random_mac-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__vm_id_{id}_restart

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/vm/id/{id}/restart \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/vm/id/{id}/restart', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm/id/{id}/restart");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /vm/id/{id}/restart`

Restart a VM.

> Body parameter

```json
{}
```

<h3 id="post__vm_id_{id}_restart-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[vm_restart](#schemavm_restart)|false|none|

<h3 id="post__vm_id_{id}_restart-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__vm_id_{id}_start

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/vm/id/{id}/start \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/vm/id/{id}/start', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm/id/{id}/start");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /vm/id/{id}/start`

Start a VM.

options.overcommit defaults to false, meaning VMs are not allowed to
start if there is not enough available memory to hold all configured VMs.
If true, VM starts even if there is not enough memory for all configured VMs.

Error codes:

    ENOMEM(12): not enough free memory to run the VM without overcommit

> Body parameter

```json
{}
```

<h3 id="post__vm_id_{id}_start-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[vm_start_1](#schemavm_start_1)|false|none|

<h3 id="post__vm_id_{id}_start-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__vm_id_{id}_status

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/vm/id/{id}/status \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/vm/id/{id}/status', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm/id/{id}/status");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /vm/id/{id}/status`

Get the status of a VM.

Returns a dict:
    - state, RUNNING or STOPPED
    - pid, process id if RUNNING

> Body parameter

```json
{}
```

<h3 id="post__vm_id_{id}_status-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[vm_status](#schemavm_status)|false|none|

<h3 id="post__vm_id_{id}_status-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__vm_id_{id}_stop

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/vm/id/{id}/stop \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/vm/id/{id}/stop', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm/id/{id}/stop");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /vm/id/{id}/stop`

Stops a VM.

For unresponsive guests who have exceeded the `shutdown_timeout` defined by the user and have become
unresponsive, they required to be powered down using `vm.poweroff`. `vm.stop` is only going to send a
shutdown signal to the guest and wait the desired `shutdown_timeout` value before tearing down guest vmemory.

`force_after_timeout` when supplied, it will initiate poweroff for the VM forcing it to exit if it has
not already stopped within the specified `shutdown_timeout`.

> Body parameter

```json
{}
```

<h3 id="post__vm_id_{id}_stop-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[vm_stop_1](#schemavm_stop_1)|false|none|

<h3 id="post__vm_id_{id}_stop-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__vm_vnc_port_wizard

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/vm/vnc_port_wizard

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/vm/vnc_port_wizard')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm/vnc_port_wizard");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /vm/vnc_port_wizard`

It returns the next available VNC PORT and WEB VNC PORT.

Returns a dict with two keys vnc_port and vnc_web.

<h3 id="get__vm_vnc_port_wizard-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-vm-device">vm.device</h1>

## get__vm_device

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/vm/device

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/vm/device')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm/device");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /vm/device`

<h3 id="get__vm_device-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__vm_device-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__vm_device

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/vm/device \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/vm/device', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm/device");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /vm/device`

Create a new device for the VM of id `vm`.

If `dtype` is the `RAW` type and a new raw file is to be created, `attributes.exists` will be passed as false.
This means the API handles creating the raw file and raises the appropriate exception if file creation fails.

If `dtype` is of `DISK` type and a new Zvol is to be created, `attributes.create_zvol` will be passed as
true with valid `attributes.zvol_name` and `attributes.zvol_volsize` values.

> Body parameter

```json
{}
```

<h3 id="post__vm_device-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[vm_device_create_0](#schemavm_device_create_0)|false|none|

<h3 id="post__vm_device-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__vm_device_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/vm/device/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.delete('http://192.168.1.123/api/v2.0/vm/device/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm/device/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /vm/device/id/{id}`

Delete a VM device of `id`.

> Body parameter

```json
{}
```

<h3 id="delete__vm_device_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[vm_device_delete_1](#schemavm_device_delete_1)|false|none|

<h3 id="delete__vm_device_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__vm_device_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/vm/device/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/vm/device/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm/device/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /vm/device/id/{id}`

<h3 id="get__vm_device_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__vm_device_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__vm_device_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/vm/device/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/vm/device/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm/device/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /vm/device/id/{id}`

Update a VM device of `id`.

Pass `attributes.size` to resize a `dtype` `RAW` device. The raw file will be resized.

> Body parameter

```json
{}
```

<h3 id="put__vm_device_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[vm_device_update_1](#schemavm_device_update_1)|false|none|

<h3 id="put__vm_device_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__vm_device_nic_attach_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/vm/device/nic_attach_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/vm/device/nic_attach_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm/device/nic_attach_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /vm/device/nic_attach_choices`

Available choices for NIC Attach attribute.

<h3 id="get__vm_device_nic_attach_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__vm_device_pptdev_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/vm/device/pptdev_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/vm/device/pptdev_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm/device/pptdev_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /vm/device/pptdev_choices`

Available choices for PCI passthru device.

<h3 id="get__vm_device_pptdev_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__vm_device_vnc_bind_choices

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/vm/device/vnc_bind_choices

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/vm/device/vnc_bind_choices')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vm/device/vnc_bind_choices");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /vm/device/vnc_bind_choices`

Available choices for VNC Bind attribute.

<h3 id="get__vm_device_vnc_bind_choices-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-vmware">vmware</h1>

## get__vmware

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/vmware

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/vmware')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vmware");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /vmware`

<h3 id="get__vmware-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__vmware-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__vmware

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/vmware \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/vmware', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vmware");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /vmware`

Create VMWare snapshot.

`hostname` is a valid IP address / hostname of a VMWare host. When clustering, this is the vCenter server for
the cluster.

`username` and `password` are the credentials used to authorize access to the VMWare host.

`datastore` is a valid datastore name which exists on the VMWare host.

> Body parameter

```json
{}
```

<h3 id="post__vmware-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[vmware_create_0](#schemavmware_create_0)|false|none|

<h3 id="post__vmware-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__vmware_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/vmware/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/vmware/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vmware/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /vmware/id/{id}`

Delete VMWare snapshot of `id`.

<h3 id="delete__vmware_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__vmware_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__vmware_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/vmware/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/vmware/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vmware/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /vmware/id/{id}`

<h3 id="get__vmware_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__vmware_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__vmware_id_{id}

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/vmware/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/vmware/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vmware/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /vmware/id/{id}`

Update VMWare snapshot of `id`.

> Body parameter

```json
{}
```

<h3 id="put__vmware_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|
|body|body|[vmware_update_1](#schemavmware_update_1)|false|none|

<h3 id="put__vmware_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__vmware_dataset_has_vms

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/vmware/dataset_has_vms \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/vmware/dataset_has_vms', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vmware/dataset_has_vms");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /vmware/dataset_has_vms`

Returns "true" if `dataset` is configured with a VMWare snapshot

> Body parameter

```json
{
  "dataset": "string",
  "recursive": true
}
```

<h3 id="post__vmware_dataset_has_vms-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[vmware_dataset_has_vms](#schemavmware_dataset_has_vms)|false|none|

<h3 id="post__vmware_dataset_has_vms-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__vmware_get_datastores

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/vmware/get_datastores \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/vmware/get_datastores', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vmware/get_datastores");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /vmware/get_datastores`

Get datastores from VMWare.

> Body parameter

```json
{}
```

<h3 id="post__vmware_get_datastores-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[vmware_get_datastores_0](#schemavmware_get_datastores_0)|false|none|

<h3 id="post__vmware_get_datastores-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__vmware_get_virtual_machines

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/vmware/get_virtual_machines \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/vmware/get_virtual_machines', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vmware/get_virtual_machines");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /vmware/get_virtual_machines`

Returns Virtual Machines on the VMWare host identified by `pk`.

> Body parameter

```json
0
```

<h3 id="post__vmware_get_virtual_machines-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[vmware_get_virtual_machines_0](#schemavmware_get_virtual_machines_0)|false|none|

<h3 id="post__vmware_get_virtual_machines-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__vmware_match_datastores_with_datasets

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/vmware/match_datastores_with_datasets \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/vmware/match_datastores_with_datasets', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/vmware/match_datastores_with_datasets");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /vmware/match_datastores_with_datasets`

Requests datastores from vCenter server and tries to match them with local filesystems.

Returns a list of datastores, a list of local filesystems and guessed relationship between them.

> Body parameter

```json
{}
```

<h3 id="post__vmware_match_datastores_with_datasets-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[vmware_match_datastores_with_datasets_0](#schemavmware_match_datastores_with_datasets_0)|false|none|

<h3 id="post__vmware_match_datastores_with_datasets-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-webdav">webdav</h1>

## get__webdav

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/webdav

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/webdav')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/webdav");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /webdav`

<h3 id="get__webdav-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## put__webdav

> Code samples

```shell
# You can also use wget
curl -X PUT http://192.168.1.123/api/v2.0/webdav \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.put('http://192.168.1.123/api/v2.0/webdav', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/webdav");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("PUT");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`PUT /webdav`

Update Webdav Service Configuration.

`protocol` specifies which protocol should be used for connecting to Webdav Serivce. Value of "HTTPHTTPS"
allows both HTTP and HTTPS connections to the share.

`certssl` is a valid id of a certificate configured in the system. This is required if HTTPS connection is
desired with Webdave Service.

There are 3 types of Authentication supported with Webdav:
1) NONE      -   No authentication is required
2) BASIC     -   Password is sent over the network as plaintext
3) DIGEST    -   Hash of the password is sent over the network

`htauth` should be one of the valid types described above.

> Body parameter

```json
{}
```

<h3 id="put__webdav-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[webdav_update_0](#schemawebdav_update_0)|false|none|

<h3 id="put__webdav-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-webui-image">webui.image</h1>

## get__webui_image

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/webui/image

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/webui/image')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/webui/image");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /webui/image`

<h3 id="get__webui_image-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__webui_image-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__webui_image_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/webui/image/id/{id}

```

```python
import requests

r = requests.delete('http://192.168.1.123/api/v2.0/webui/image/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/webui/image/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /webui/image/id/{id}`

Remove the database entry, and then the item if it exists

<h3 id="delete__webui_image_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|integer|true|none|

<h3 id="delete__webui_image_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__webui_image_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/webui/image/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/webui/image/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/webui/image/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /webui/image/id/{id}`

<h3 id="get__webui_image_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__webui_image_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

<h1 id="freenas-restful-api-zfs-snapshot">zfs.snapshot</h1>

## get__zfs_snapshot

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/zfs/snapshot

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/zfs/snapshot')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/zfs/snapshot");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /zfs/snapshot`

Query all ZFS Snapshots with `query-filters` and `query-options`.

<h3 id="get__zfs_snapshot-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|

<h3 id="get__zfs_snapshot-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__zfs_snapshot

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/zfs/snapshot \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/zfs/snapshot', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/zfs/snapshot");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /zfs/snapshot`

Take a snapshot from a given dataset.

Returns:
    bool: True if succeed otherwise False.

> Body parameter

```json
{}
```

<h3 id="post__zfs_snapshot-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[zfs_snapshot_create_0](#schemazfs_snapshot_create_0)|false|none|

<h3 id="post__zfs_snapshot-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## delete__zfs_snapshot_id_{id}

> Code samples

```shell
# You can also use wget
curl -X DELETE http://192.168.1.123/api/v2.0/zfs/snapshot/id/{id} \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.delete('http://192.168.1.123/api/v2.0/zfs/snapshot/id/{id}', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/zfs/snapshot/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("DELETE");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`DELETE /zfs/snapshot/id/{id}`

Delete snapshot of name `id`.

`options.defer` will defer the deletion of snapshot.

> Body parameter

```json
{}
```

<h3 id="delete__zfs_snapshot_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|id|path|string|true|none|
|body|body|[zfs_snapshot_delete_1](#schemazfs_snapshot_delete_1)|false|none|

<h3 id="delete__zfs_snapshot_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## get__zfs_snapshot_id_{id}

> Code samples

```shell
# You can also use wget
curl -X GET http://192.168.1.123/api/v2.0/zfs/snapshot/id/{id}

```

```python
import requests

r = requests.get('http://192.168.1.123/api/v2.0/zfs/snapshot/id/{id}')

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/zfs/snapshot/id/{id}");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("GET");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`GET /zfs/snapshot/id/{id}`

Query all ZFS Snapshots with `query-filters` and `query-options`.

<h3 id="get__zfs_snapshot_id_{id}-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|limit|query|integer|false|none|
|offset|query|integer|false|none|
|count|query|boolean|false|none|
|sort|query|string|false|none|
|id|path|array[any]|true|none|

<h3 id="get__zfs_snapshot_id_{id}-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__zfs_snapshot_clone

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/zfs/snapshot/clone \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/zfs/snapshot/clone', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/zfs/snapshot/clone");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /zfs/snapshot/clone`

Clone a given snapshot to a new dataset.

Returns:
    bool: True if succeed otherwise False.

> Body parameter

```json
{}
```

<h3 id="post__zfs_snapshot_clone-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[zfs_snapshot_clone_0](#schemazfs_snapshot_clone_0)|false|none|

<h3 id="post__zfs_snapshot_clone-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__zfs_snapshot_remove

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/zfs/snapshot/remove \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/zfs/snapshot/remove', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/zfs/snapshot/remove");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /zfs/snapshot/remove`

Remove a snapshot from a given dataset.

Returns:
    bool: True if succeed otherwise False.

> Body parameter

```json
{}
```

<h3 id="post__zfs_snapshot_remove-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[zfs_snapshot_remove_0](#schemazfs_snapshot_remove_0)|false|none|

<h3 id="post__zfs_snapshot_remove-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

## post__zfs_snapshot_rollback

> Code samples

```shell
# You can also use wget
curl -X POST http://192.168.1.123/api/v2.0/zfs/snapshot/rollback \
  -H 'Content-Type: application/json'

```

```python
import requests
headers = {
  'Content-Type': 'application/json'
}

r = requests.post('http://192.168.1.123/api/v2.0/zfs/snapshot/rollback', headers = headers)

print(r.json())

```

```java
URL obj = new URL("http://192.168.1.123/api/v2.0/zfs/snapshot/rollback");
HttpURLConnection con = (HttpURLConnection) obj.openConnection();
con.setRequestMethod("POST");
int responseCode = con.getResponseCode();
BufferedReader in = new BufferedReader(
    new InputStreamReader(con.getInputStream()));
String inputLine;
StringBuffer response = new StringBuffer();
while ((inputLine = in.readLine()) != null) {
    response.append(inputLine);
}
in.close();
System.out.println(response.toString());

```

`POST /zfs/snapshot/rollback`

Rollback to a given snapshot `id`.

`options.recursive` will destroy any snapshots and bookmarks more recent than the one
specified.

`options.recursive_clones` is just like `recursive` but will also destroy any clones.

`options.force` will force unmount of any clones.

> Body parameter

```json
{
  "id": "string",
  "options": {}
}
```

<h3 id="post__zfs_snapshot_rollback-parameters">Parameters</h3>

|Name|In|Type|Required|Description|
|---|---|---|---|---|
|body|body|[zfs_snapshot_rollback](#schemazfs_snapshot_rollback)|false|none|

<h3 id="post__zfs_snapshot_rollback-responses">Responses</h3>

|Status|Meaning|Description|Schema|
|---|---|---|---|
|200|[OK](https://tools.ietf.org/html/rfc7231#section-6.3.1)|Operation succeeded|None|
|401|[Unauthorized](https://tools.ietf.org/html/rfc7235#section-3.1)|No authorization for this endpoint|None|

<aside class="warning">
To perform this operation, you must be authenticated by means of one of the following methods:
basic
</aside>

# Schemas

<h2 id="tocS_core_bulk_0">core_bulk_0</h2>

<a id="schemacore_bulk_0"></a>
<a id="schema_core_bulk_0"></a>
<a id="tocScore_bulk_0"></a>
<a id="tocscore_bulk_0"></a>

```json
"string"

```

method

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|method|string|false|none|none|

<h2 id="tocS_core_bulk_1">core_bulk_1</h2>

<a id="schemacore_bulk_1"></a>
<a id="schema_core_bulk_1"></a>
<a id="tocScore_bulk_1"></a>
<a id="tocscore_bulk_1"></a>

```json
[]

```

params

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|params|[any]|false|none|none|

<h2 id="tocS_core_bulk">core_bulk</h2>

<a id="schemacore_bulk"></a>
<a id="schema_core_bulk"></a>
<a id="tocScore_bulk"></a>
<a id="tocscore_bulk"></a>

```json
{
  "method": "string",
  "params": []
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|method|[core_bulk_0](#schemacore_bulk_0)|false|none|none|
|params|[core_bulk_1](#schemacore_bulk_1)|false|none|none|

<h2 id="tocS_core_debug_0">core_debug_0</h2>

<a id="schemacore_debug_0"></a>
<a id="schema_core_debug_0"></a>
<a id="tocScore_debug_0"></a>
<a id="tocscore_debug_0"></a>

```json
"PTVS"

```

engine

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|engine|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|engine|PTVS|
|engine|PYDEV|
|engine|REMOTE_PDB|

<h2 id="tocS_core_debug_1">core_debug_1</h2>

<a id="schemacore_debug_1"></a>
<a id="schema_core_debug_1"></a>
<a id="tocScore_debug_1"></a>
<a id="tocscore_debug_1"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|secret|string|false|none|none|
|bind_address|string|false|none|none|
|bind_port|integer|false|none|none|
|host|string|false|none|none|
|wait_attach|boolean|false|none|none|
|local_path|string|false|none|none|
|threaded|boolean|false|none|none|

<h2 id="tocS_core_debug">core_debug</h2>

<a id="schemacore_debug"></a>
<a id="schema_core_debug"></a>
<a id="tocScore_debug"></a>
<a id="tocscore_debug"></a>

```json
{
  "engine": "PTVS",
  "options": {}
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|engine|[core_debug_0](#schemacore_debug_0)|false|none|none|
|options|[core_debug_1](#schemacore_debug_1)|false|none|none|

<h2 id="tocS_core_download_0">core_download_0</h2>

<a id="schemacore_download_0"></a>
<a id="schema_core_download_0"></a>
<a id="tocScore_download_0"></a>
<a id="tocscore_download_0"></a>

```json
"string"

```

method

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|method|string|false|none|none|

<h2 id="tocS_core_download_1">core_download_1</h2>

<a id="schemacore_download_1"></a>
<a id="schema_core_download_1"></a>
<a id="tocScore_download_1"></a>
<a id="tocscore_download_1"></a>

```json
[]

```

args

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|args|[any]|false|none|none|

<h2 id="tocS_core_download_2">core_download_2</h2>

<a id="schemacore_download_2"></a>
<a id="schema_core_download_2"></a>
<a id="tocScore_download_2"></a>
<a id="tocscore_download_2"></a>

```json
"string"

```

filename

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|filename|string|false|none|none|

<h2 id="tocS_core_download">core_download</h2>

<a id="schemacore_download"></a>
<a id="schema_core_download"></a>
<a id="tocScore_download"></a>
<a id="tocscore_download"></a>

```json
{
  "method": "string",
  "args": [],
  "filename": "string"
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|method|[core_download_0](#schemacore_download_0)|false|none|none|
|args|[core_download_1](#schemacore_download_1)|false|none|none|
|filename|[core_download_2](#schemacore_download_2)|false|none|none|

<h2 id="tocS_core_get_methods_0">core_get_methods_0</h2>

<a id="schemacore_get_methods_0"></a>
<a id="schema_core_get_methods_0"></a>
<a id="tocScore_get_methods_0"></a>
<a id="tocscore_get_methods_0"></a>

```json
null

```

service

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|service|stringÂ¦null|false|none|none|

<h2 id="tocS_core_job_abort_0">core_job_abort_0</h2>

<a id="schemacore_job_abort_0"></a>
<a id="schema_core_job_abort_0"></a>
<a id="tocScore_job_abort_0"></a>
<a id="tocscore_job_abort_0"></a>

```json
0

```

id

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|id|integer|false|none|none|

<h2 id="tocS_core_job_update_0">core_job_update_0</h2>

<a id="schemacore_job_update_0"></a>
<a id="schema_core_job_update_0"></a>
<a id="tocScore_job_update_0"></a>
<a id="tocscore_job_update_0"></a>

```json
0

```

id

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|id|integer|false|none|none|

<h2 id="tocS_core_job_update_1">core_job_update_1</h2>

<a id="schemacore_job_update_1"></a>
<a id="schema_core_job_update_1"></a>
<a id="tocScore_job_update_1"></a>
<a id="tocscore_job_update_1"></a>

```json
{}

```

job-update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|progress|object|false|none|none|

<h2 id="tocS_core_job_update">core_job_update</h2>

<a id="schemacore_job_update"></a>
<a id="schema_core_job_update"></a>
<a id="tocScore_job_update"></a>
<a id="tocscore_job_update"></a>

```json
{
  "id": 0,
  "job-update": {}
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|id|[core_job_update_0](#schemacore_job_update_0)|false|none|none|
|job-update|[core_job_update_1](#schemacore_job_update_1)|false|none|none|

<h2 id="tocS_core_job_wait_0">core_job_wait_0</h2>

<a id="schemacore_job_wait_0"></a>
<a id="schema_core_job_wait_0"></a>
<a id="tocScore_job_wait_0"></a>
<a id="tocscore_job_wait_0"></a>

```json
0

```

id

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|id|integer|false|none|none|

<h2 id="tocS_core_ping_remote_0">core_ping_remote_0</h2>

<a id="schemacore_ping_remote_0"></a>
<a id="schema_core_ping_remote_0"></a>
<a id="tocScore_ping_remote_0"></a>
<a id="tocscore_ping_remote_0"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|type|string|false|none|none|
|hostname|string|false|none|none|
|timeout|integer|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|type|ICMP|
|type|ICMPV4|
|type|ICMPV6|

<h2 id="tocS_core_resize_shell_0">core_resize_shell_0</h2>

<a id="schemacore_resize_shell_0"></a>
<a id="schema_core_resize_shell_0"></a>
<a id="tocScore_resize_shell_0"></a>
<a id="tocscore_resize_shell_0"></a>

```json
"string"

```

id

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|id|string|false|none|none|

<h2 id="tocS_core_resize_shell_1">core_resize_shell_1</h2>

<a id="schemacore_resize_shell_1"></a>
<a id="schema_core_resize_shell_1"></a>
<a id="tocScore_resize_shell_1"></a>
<a id="tocscore_resize_shell_1"></a>

```json
0

```

cols

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|cols|integer|false|none|none|

<h2 id="tocS_core_resize_shell_2">core_resize_shell_2</h2>

<a id="schemacore_resize_shell_2"></a>
<a id="schema_core_resize_shell_2"></a>
<a id="tocScore_resize_shell_2"></a>
<a id="tocscore_resize_shell_2"></a>

```json
0

```

rows

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|rows|integer|false|none|none|

<h2 id="tocS_core_resize_shell">core_resize_shell</h2>

<a id="schemacore_resize_shell"></a>
<a id="schema_core_resize_shell"></a>
<a id="tocScore_resize_shell"></a>
<a id="tocscore_resize_shell"></a>

```json
{
  "id": "string",
  "cols": 0,
  "rows": 0
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|id|[core_resize_shell_0](#schemacore_resize_shell_0)|false|none|none|
|cols|[core_resize_shell_1](#schemacore_resize_shell_1)|false|none|none|
|rows|[core_resize_shell_2](#schemacore_resize_shell_2)|false|none|none|

<h2 id="tocS_acme_dns_authenticator_create_0">acme_dns_authenticator_create_0</h2>

<a id="schemaacme_dns_authenticator_create_0"></a>
<a id="schema_acme_dns_authenticator_create_0"></a>
<a id="tocSacme_dns_authenticator_create_0"></a>
<a id="tocsacme_dns_authenticator_create_0"></a>

```json
{}

```

dns_authenticator_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|authenticator|string|false|none|none|
|name|string|false|none|none|
|attributes|object|false|none|none|

<h2 id="tocS_acme_dns_authenticator_update_1">acme_dns_authenticator_update_1</h2>

<a id="schemaacme_dns_authenticator_update_1"></a>
<a id="schema_acme_dns_authenticator_update_1"></a>
<a id="tocSacme_dns_authenticator_update_1"></a>
<a id="tocsacme_dns_authenticator_update_1"></a>

```json
{}

```

dns_authenticator_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|
|attributes|object|false|none|none|

<h2 id="tocS_activedirectory_update_0">activedirectory_update_0</h2>

<a id="schemaactivedirectory_update_0"></a>
<a id="schema_activedirectory_update_0"></a>
<a id="tocSactivedirectory_update_0"></a>
<a id="tocsactivedirectory_update_0"></a>

```json
{}

```

activedirectory_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|domainname|string|false|none|none|
|bindname|string|false|none|none|
|bindpw|string|false|none|none|
|verbose_logging|boolean|false|none|none|
|use_default_domain|boolean|false|none|none|
|allow_trusted_doms|boolean|false|none|none|
|allow_dns_updates|boolean|false|none|none|
|disable_freenas_cache|boolean|false|none|none|
|restrict_pam|boolean|false|none|none|
|site|stringÂ¦null|false|none|none|
|kerberos_realm|integerÂ¦null|false|none|none|
|kerberos_principal|stringÂ¦null|false|none|none|
|timeout|integer|false|none|none|
|dns_timeout|integer|false|none|none|
|nss_info|stringÂ¦null|false|none|none|
|createcomputer|string|false|none|none|
|netbiosname|string|false|none|none|
|netbiosname_b|string|false|none|none|
|netbiosalias|[any]|false|none|none|
|enable|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|nss_info|SFU|
|nss_info|SFU20|
|nss_info|RFC2307|

<h2 id="tocS_activedirectory_leave_0">activedirectory_leave_0</h2>

<a id="schemaactivedirectory_leave_0"></a>
<a id="schema_activedirectory_leave_0"></a>
<a id="tocSactivedirectory_leave_0"></a>
<a id="tocsactivedirectory_leave_0"></a>

```json
{}

```

leave_ad

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|username|string|false|none|none|
|password|string|false|none|none|

<h2 id="tocS_afp_update_0">afp_update_0</h2>

<a id="schemaafp_update_0"></a>
<a id="schema_afp_update_0"></a>
<a id="tocSafp_update_0"></a>
<a id="tocsafp_update_0"></a>

```json
{}

```

afp_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|guest|boolean|false|none|none|
|guest_user|string|false|none|none|
|bindip|[string]|false|none|none|
|connections_limit|integer|false|none|none|
|dbpath|string|false|none|none|
|global_aux|string|false|none|none|
|map_acls|string|false|none|none|
|chmod_request|string|false|none|none|
|loglevel|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|map_acls|RIGHTS|
|map_acls|MODE|
|map_acls|NONE|
|chmod_request|PRESERVE|
|chmod_request|SIMPLE|
|chmod_request|IGNORE|
|loglevel|NONE|
|loglevel|MINIMUM|
|loglevel|NORMAL|
|loglevel|FULL|
|loglevel|DEBUG|

<h2 id="tocS_alert_dismiss_0">alert_dismiss_0</h2>

<a id="schemaalert_dismiss_0"></a>
<a id="schema_alert_dismiss_0"></a>
<a id="tocSalert_dismiss_0"></a>
<a id="tocsalert_dismiss_0"></a>

```json
"string"

```

uuid

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|uuid|string|false|none|none|

<h2 id="tocS_alert_restore_0">alert_restore_0</h2>

<a id="schemaalert_restore_0"></a>
<a id="schema_alert_restore_0"></a>
<a id="tocSalert_restore_0"></a>
<a id="tocsalert_restore_0"></a>

```json
"string"

```

uuid

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|uuid|string|false|none|none|

<h2 id="tocS_alertclasses_update_0">alertclasses_update_0</h2>

<a id="schemaalertclasses_update_0"></a>
<a id="schema_alertclasses_update_0"></a>
<a id="tocSalertclasses_update_0"></a>
<a id="tocsalertclasses_update_0"></a>

```json
{}

```

alert_classes_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|classes|object|false|none|none|

<h2 id="tocS_alertservice_create_0">alertservice_create_0</h2>

<a id="schemaalertservice_create_0"></a>
<a id="schema_alertservice_create_0"></a>
<a id="tocSalertservice_create_0"></a>
<a id="tocsalertservice_create_0"></a>

```json
{}

```

alert_service_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|
|type|string|false|none|none|
|attributes|object|false|none|none|
|level|string|false|none|none|
|enabled|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|level|INFO|
|level|NOTICE|
|level|WARNING|
|level|ERROR|
|level|CRITICAL|
|level|ALERT|
|level|EMERGENCY|

<h2 id="tocS_alertservice_update_1">alertservice_update_1</h2>

<a id="schemaalertservice_update_1"></a>
<a id="schema_alertservice_update_1"></a>
<a id="tocSalertservice_update_1"></a>
<a id="tocsalertservice_update_1"></a>

```json
{}

```

alert_service_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|
|type|string|false|none|none|
|attributes|object|false|none|none|
|level|string|false|none|none|
|enabled|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|level|INFO|
|level|NOTICE|
|level|WARNING|
|level|ERROR|
|level|CRITICAL|
|level|ALERT|
|level|EMERGENCY|

<h2 id="tocS_alertservice_test_0">alertservice_test_0</h2>

<a id="schemaalertservice_test_0"></a>
<a id="schema_alertservice_test_0"></a>
<a id="tocSalertservice_test_0"></a>
<a id="tocsalertservice_test_0"></a>

```json
{}

```

alert_service_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|
|type|string|false|none|none|
|attributes|object|false|none|none|
|level|string|false|none|none|
|enabled|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|level|INFO|
|level|NOTICE|
|level|WARNING|
|level|ERROR|
|level|CRITICAL|
|level|ALERT|
|level|EMERGENCY|

<h2 id="tocS_api_key_create_0">api_key_create_0</h2>

<a id="schemaapi_key_create_0"></a>
<a id="schema_api_key_create_0"></a>
<a id="tocSapi_key_create_0"></a>
<a id="tocsapi_key_create_0"></a>

```json
{}

```

api_key_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|

<h2 id="tocS_api_key_update_1">api_key_update_1</h2>

<a id="schemaapi_key_update_1"></a>
<a id="schema_api_key_update_1"></a>
<a id="tocSapi_key_update_1"></a>
<a id="tocsapi_key_update_1"></a>

```json
{}

```

api_key_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|
|reset|boolean|false|none|none|

<h2 id="tocS_auth_check_user_0">auth_check_user_0</h2>

<a id="schemaauth_check_user_0"></a>
<a id="schema_auth_check_user_0"></a>
<a id="tocSauth_check_user_0"></a>
<a id="tocsauth_check_user_0"></a>

```json
"string"

```

username

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|username|string|false|none|none|

<h2 id="tocS_auth_check_user_1">auth_check_user_1</h2>

<a id="schemaauth_check_user_1"></a>
<a id="schema_auth_check_user_1"></a>
<a id="tocSauth_check_user_1"></a>
<a id="tocsauth_check_user_1"></a>

```json
"string"

```

password

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|password|string|false|none|none|

<h2 id="tocS_auth_check_user">auth_check_user</h2>

<a id="schemaauth_check_user"></a>
<a id="schema_auth_check_user"></a>
<a id="tocSauth_check_user"></a>
<a id="tocsauth_check_user"></a>

```json
{
  "username": "string",
  "password": "string"
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|username|[auth_check_user_0](#schemaauth_check_user_0)|false|none|none|
|password|[auth_check_user_1](#schemaauth_check_user_1)|false|none|none|

<h2 id="tocS_auth_generate_token_0">auth_generate_token_0</h2>

<a id="schemaauth_generate_token_0"></a>
<a id="schema_auth_generate_token_0"></a>
<a id="tocSauth_generate_token_0"></a>
<a id="tocsauth_generate_token_0"></a>

```json
600

```

ttl

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|ttl|integerÂ¦null|false|none|none|

<h2 id="tocS_auth_generate_token_1">auth_generate_token_1</h2>

<a id="schemaauth_generate_token_1"></a>
<a id="schema_auth_generate_token_1"></a>
<a id="tocSauth_generate_token_1"></a>
<a id="tocsauth_generate_token_1"></a>

```json
{}

```

attrs

### Properties

*None*

<h2 id="tocS_auth_generate_token">auth_generate_token</h2>

<a id="schemaauth_generate_token"></a>
<a id="schema_auth_generate_token"></a>
<a id="tocSauth_generate_token"></a>
<a id="tocsauth_generate_token"></a>

```json
{
  "ttl": 600,
  "attrs": {}
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|ttl|[auth_generate_token_0](#schemaauth_generate_token_0)|false|none|none|
|attrs|[auth_generate_token_1](#schemaauth_generate_token_1)|false|none|none|

<h2 id="tocS_auth_twofactor_update_0">auth_twofactor_update_0</h2>

<a id="schemaauth_twofactor_update_0"></a>
<a id="schema_auth_twofactor_update_0"></a>
<a id="tocSauth_twofactor_update_0"></a>
<a id="tocsauth_twofactor_update_0"></a>

```json
{}

```

auth_twofactor_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|enabled|boolean|false|none|none|
|otp_digits|integer|false|none|none|
|window|integer|false|none|none|
|interval|integer|false|none|none|
|services|object|false|none|none|
|Â» ssh|boolean|false|none|none|

<h2 id="tocS_auth_twofactor_verify_0">auth_twofactor_verify_0</h2>

<a id="schemaauth_twofactor_verify_0"></a>
<a id="schema_auth_twofactor_verify_0"></a>
<a id="tocSauth_twofactor_verify_0"></a>
<a id="tocsauth_twofactor_verify_0"></a>

```json
"string"

```

token

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|token|stringÂ¦null|false|none|none|

<h2 id="tocS_boot_attach_0">boot_attach_0</h2>

<a id="schemaboot_attach_0"></a>
<a id="schema_boot_attach_0"></a>
<a id="tocSboot_attach_0"></a>
<a id="tocsboot_attach_0"></a>

```json
"string"

```

dev

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|dev|string|false|none|none|

<h2 id="tocS_boot_attach_1">boot_attach_1</h2>

<a id="schemaboot_attach_1"></a>
<a id="schema_boot_attach_1"></a>
<a id="tocSboot_attach_1"></a>
<a id="tocsboot_attach_1"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|expand|boolean|false|none|none|

<h2 id="tocS_boot_attach">boot_attach</h2>

<a id="schemaboot_attach"></a>
<a id="schema_boot_attach"></a>
<a id="tocSboot_attach"></a>
<a id="tocsboot_attach"></a>

```json
{
  "dev": "string",
  "options": {}
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|dev|[boot_attach_0](#schemaboot_attach_0)|false|none|none|
|options|[boot_attach_1](#schemaboot_attach_1)|false|none|none|

<h2 id="tocS_boot_detach_0">boot_detach_0</h2>

<a id="schemaboot_detach_0"></a>
<a id="schema_boot_detach_0"></a>
<a id="tocSboot_detach_0"></a>
<a id="tocsboot_detach_0"></a>

```json
"string"

```

dev

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|dev|string|false|none|none|

<h2 id="tocS_boot_replace_0">boot_replace_0</h2>

<a id="schemaboot_replace_0"></a>
<a id="schema_boot_replace_0"></a>
<a id="tocSboot_replace_0"></a>
<a id="tocsboot_replace_0"></a>

```json
"string"

```

label

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|label|string|false|none|none|

<h2 id="tocS_boot_replace_1">boot_replace_1</h2>

<a id="schemaboot_replace_1"></a>
<a id="schema_boot_replace_1"></a>
<a id="tocSboot_replace_1"></a>
<a id="tocsboot_replace_1"></a>

```json
"string"

```

dev

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|dev|string|false|none|none|

<h2 id="tocS_boot_replace">boot_replace</h2>

<a id="schemaboot_replace"></a>
<a id="schema_boot_replace"></a>
<a id="tocSboot_replace"></a>
<a id="tocsboot_replace"></a>

```json
{
  "label": "string",
  "dev": "string"
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|label|[boot_replace_0](#schemaboot_replace_0)|false|none|none|
|dev|[boot_replace_1](#schemaboot_replace_1)|false|none|none|

<h2 id="tocS_boot_set_scrub_interval_0">boot_set_scrub_interval_0</h2>

<a id="schemaboot_set_scrub_interval_0"></a>
<a id="schema_boot_set_scrub_interval_0"></a>
<a id="tocSboot_set_scrub_interval_0"></a>
<a id="tocsboot_set_scrub_interval_0"></a>

```json
0

```

interval

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|interval|integer|false|none|none|

<h2 id="tocS_bootenv_create_0">bootenv_create_0</h2>

<a id="schemabootenv_create_0"></a>
<a id="schema_bootenv_create_0"></a>
<a id="tocSbootenv_create_0"></a>
<a id="tocsbootenv_create_0"></a>

```json
{}

```

bootenv_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|
|source|string|false|none|none|

<h2 id="tocS_bootenv_update_1">bootenv_update_1</h2>

<a id="schemabootenv_update_1"></a>
<a id="schema_bootenv_update_1"></a>
<a id="tocSbootenv_update_1"></a>
<a id="tocsbootenv_update_1"></a>

```json
{}

```

bootenv_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|

<h2 id="tocS_bootenv_activate">bootenv_activate</h2>

<a id="schemabootenv_activate"></a>
<a id="schema_bootenv_activate"></a>
<a id="tocSbootenv_activate"></a>
<a id="tocsbootenv_activate"></a>

```json
{}

```

### Properties

*None*

<h2 id="tocS_bootenv_set_attribute_1">bootenv_set_attribute_1</h2>

<a id="schemabootenv_set_attribute_1"></a>
<a id="schema_bootenv_set_attribute_1"></a>
<a id="tocSbootenv_set_attribute_1"></a>
<a id="tocsbootenv_set_attribute_1"></a>

```json
{}

```

attributes

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|keep|boolean|false|none|none|

<h2 id="tocS_certificate_create_0">certificate_create_0</h2>

<a id="schemacertificate_create_0"></a>
<a id="schema_certificate_create_0"></a>
<a id="tocScertificate_create_0"></a>
<a id="tocscertificate_create_0"></a>

```json
{}

```

certificate_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|tos|boolean|false|none|none|
|dns_mapping|object|false|none|none|
|csr_id|integer|false|none|none|
|signedby|integer|false|none|none|
|key_length|integer|false|none|none|
|renew_days|integer|false|none|none|
|type|integer|false|none|none|
|lifetime|integer|false|none|none|
|serial|integer|false|none|none|
|acme_directory_uri|string|false|none|none|
|certificate|string|false|none|none|
|city|string|false|none|none|
|common|stringÂ¦null|false|none|none|
|country|string|false|none|none|
|CSR|string|false|none|none|
|ec_curve|string|false|none|none|
|email|string|false|none|none|
|key_type|string|false|none|none|
|name|string|false|none|none|
|organization|string|false|none|none|
|organizational_unit|string|false|none|none|
|passphrase|string|false|none|none|
|privatekey|string|false|none|none|
|state|string|false|none|none|
|create_type|string|false|none|none|
|digest_algorithm|string|false|none|none|
|san|[string]|false|none|none|
|cert_extensions|object|false|none|none|
|Â» BasicConstraints|object|false|none|none|
|Â»Â» ca|boolean|false|none|none|
|Â»Â» enabled|boolean|false|none|none|
|Â»Â» path_length|integerÂ¦null|false|none|none|
|Â»Â» extension_critical|boolean|false|none|none|
|Â» AuthorityKeyIdentifier|object|false|none|none|
|Â»Â» authority_cert_issuer|boolean|false|none|none|
|Â»Â» enabled|boolean|false|none|none|
|Â»Â» extension_critical|boolean|false|none|none|
|Â» ExtendedKeyUsage|object|false|none|none|
|Â»Â» usages|[string]|false|none|none|
|Â»Â» enabled|boolean|false|none|none|
|Â»Â» extension_critical|boolean|false|none|none|
|Â» KeyUsage|object|false|none|none|
|Â»Â» enabled|boolean|false|none|none|
|Â»Â» digital_signature|boolean|false|none|none|
|Â»Â» content_commitment|boolean|false|none|none|
|Â»Â» key_encipherment|boolean|false|none|none|
|Â»Â» data_encipherment|boolean|false|none|none|
|Â»Â» key_agreement|boolean|false|none|none|
|Â»Â» key_cert_sign|boolean|false|none|none|
|Â»Â» crl_sign|boolean|false|none|none|
|Â»Â» encipher_only|boolean|false|none|none|
|Â»Â» decipher_only|boolean|false|none|none|
|Â»Â» extension_critical|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|ec_curve|BrainpoolP512R1|
|ec_curve|BrainpoolP384R1|
|ec_curve|BrainpoolP256R1|
|ec_curve|SECP256K1|
|ec_curve|ed25519|
|key_type|RSA|
|key_type|EC|
|create_type|CERTIFICATE_CREATE_INTERNAL|
|create_type|CERTIFICATE_CREATE_IMPORTED|
|create_type|CERTIFICATE_CREATE_CSR|
|create_type|CERTIFICATE_CREATE_IMPORTED_CSR|
|create_type|CERTIFICATE_CREATE_ACME|
|digest_algorithm|SHA1|
|digest_algorithm|SHA224|
|digest_algorithm|SHA256|
|digest_algorithm|SHA384|
|digest_algorithm|SHA512|

<h2 id="tocS_certificate_delete_1">certificate_delete_1</h2>

<a id="schemacertificate_delete_1"></a>
<a id="schema_certificate_delete_1"></a>
<a id="tocScertificate_delete_1"></a>
<a id="tocscertificate_delete_1"></a>

```json
false

```

force

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|force|boolean|false|none|none|

<h2 id="tocS_certificate_update_1">certificate_update_1</h2>

<a id="schemacertificate_update_1"></a>
<a id="schema_certificate_update_1"></a>
<a id="tocScertificate_update_1"></a>
<a id="tocscertificate_update_1"></a>

```json
{}

```

certificate_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|revoked|boolean|false|none|none|
|name|string|false|none|none|

<h2 id="tocS_certificateauthority_create_0">certificateauthority_create_0</h2>

<a id="schemacertificateauthority_create_0"></a>
<a id="schema_certificateauthority_create_0"></a>
<a id="tocScertificateauthority_create_0"></a>
<a id="tocscertificateauthority_create_0"></a>

```json
{}

```

certificate_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|tos|boolean|false|none|none|
|csr_id|integer|false|none|none|
|signedby|integer|false|none|none|
|key_length|integer|false|none|none|
|renew_days|integer|false|none|none|
|type|integer|false|none|none|
|lifetime|integer|false|none|none|
|serial|integer|false|none|none|
|acme_directory_uri|string|false|none|none|
|certificate|string|false|none|none|
|city|string|false|none|none|
|common|stringÂ¦null|false|none|none|
|country|string|false|none|none|
|CSR|string|false|none|none|
|ec_curve|string|false|none|none|
|email|string|false|none|none|
|key_type|string|false|none|none|
|name|string|false|none|none|
|organization|string|false|none|none|
|organizational_unit|string|false|none|none|
|passphrase|string|false|none|none|
|privatekey|string|false|none|none|
|state|string|false|none|none|
|create_type|string|false|none|none|
|digest_algorithm|string|false|none|none|
|san|[string]|false|none|none|
|cert_extensions|object|false|none|none|
|Â» BasicConstraints|object|false|none|none|
|Â»Â» ca|boolean|false|none|none|
|Â»Â» enabled|boolean|false|none|none|
|Â»Â» path_length|integerÂ¦null|false|none|none|
|Â»Â» extension_critical|boolean|false|none|none|
|Â» AuthorityKeyIdentifier|object|false|none|none|
|Â»Â» authority_cert_issuer|boolean|false|none|none|
|Â»Â» enabled|boolean|false|none|none|
|Â»Â» extension_critical|boolean|false|none|none|
|Â» ExtendedKeyUsage|object|false|none|none|
|Â»Â» usages|[string]|false|none|none|
|Â»Â» enabled|boolean|false|none|none|
|Â»Â» extension_critical|boolean|false|none|none|
|Â» KeyUsage|object|false|none|none|
|Â»Â» enabled|boolean|false|none|none|
|Â»Â» digital_signature|boolean|false|none|none|
|Â»Â» content_commitment|boolean|false|none|none|
|Â»Â» key_encipherment|boolean|false|none|none|
|Â»Â» data_encipherment|boolean|false|none|none|
|Â»Â» key_agreement|boolean|false|none|none|
|Â»Â» key_cert_sign|boolean|false|none|none|
|Â»Â» crl_sign|boolean|false|none|none|
|Â»Â» encipher_only|boolean|false|none|none|
|Â»Â» decipher_only|boolean|false|none|none|
|Â»Â» extension_critical|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|ec_curve|BrainpoolP512R1|
|ec_curve|BrainpoolP384R1|
|ec_curve|BrainpoolP256R1|
|ec_curve|SECP256K1|
|ec_curve|ed25519|
|key_type|RSA|
|key_type|EC|
|create_type|CA_CREATE_INTERNAL|
|create_type|CA_CREATE_IMPORTED|
|create_type|CA_CREATE_INTERMEDIATE|
|digest_algorithm|SHA1|
|digest_algorithm|SHA224|
|digest_algorithm|SHA256|
|digest_algorithm|SHA384|
|digest_algorithm|SHA512|

<h2 id="tocS_certificateauthority_update_1">certificateauthority_update_1</h2>

<a id="schemacertificateauthority_update_1"></a>
<a id="schema_certificateauthority_update_1"></a>
<a id="tocScertificateauthority_update_1"></a>
<a id="tocscertificateauthority_update_1"></a>

```json
{}

```

ca_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|revoked|boolean|false|none|none|
|ca_id|integer|false|none|none|
|csr_cert_id|integer|false|none|none|
|create_type|string|false|none|none|
|name|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|create_type|CA_SIGN_CSR|

<h2 id="tocS_certificateauthority_ca_sign_csr_0">certificateauthority_ca_sign_csr_0</h2>

<a id="schemacertificateauthority_ca_sign_csr_0"></a>
<a id="schema_certificateauthority_ca_sign_csr_0"></a>
<a id="tocScertificateauthority_ca_sign_csr_0"></a>
<a id="tocscertificateauthority_ca_sign_csr_0"></a>

```json
{}

```

ca_sign_csr

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|ca_id|integer|false|none|none|
|csr_cert_id|integer|false|none|none|
|name|string|false|none|none|
|cert_extensions|object|false|none|none|
|Â» BasicConstraints|object|false|none|none|
|Â»Â» ca|boolean|false|none|none|
|Â»Â» enabled|boolean|false|none|none|
|Â»Â» path_length|integerÂ¦null|false|none|none|
|Â»Â» extension_critical|boolean|false|none|none|
|Â» AuthorityKeyIdentifier|object|false|none|none|
|Â»Â» authority_cert_issuer|boolean|false|none|none|
|Â»Â» enabled|boolean|false|none|none|
|Â»Â» extension_critical|boolean|false|none|none|
|Â» ExtendedKeyUsage|object|false|none|none|
|Â»Â» usages|[string]|false|none|none|
|Â»Â» enabled|boolean|false|none|none|
|Â»Â» extension_critical|boolean|false|none|none|
|Â» KeyUsage|object|false|none|none|
|Â»Â» enabled|boolean|false|none|none|
|Â»Â» digital_signature|boolean|false|none|none|
|Â»Â» content_commitment|boolean|false|none|none|
|Â»Â» key_encipherment|boolean|false|none|none|
|Â»Â» data_encipherment|boolean|false|none|none|
|Â»Â» key_agreement|boolean|false|none|none|
|Â»Â» key_cert_sign|boolean|false|none|none|
|Â»Â» crl_sign|boolean|false|none|none|
|Â»Â» encipher_only|boolean|false|none|none|
|Â»Â» decipher_only|boolean|false|none|none|
|Â»Â» extension_critical|boolean|false|none|none|

<h2 id="tocS_cloudsync_create_0">cloudsync_create_0</h2>

<a id="schemacloudsync_create_0"></a>
<a id="schema_cloudsync_create_0"></a>
<a id="tocScloudsync_create_0"></a>
<a id="tocscloudsync_create_0"></a>

```json
{}

```

cloud_sync_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|description|string|false|none|none|
|direction|string|false|none|none|
|transfer_mode|string|false|none|none|
|path|string|false|none|none|
|credentials|integer|false|none|none|
|encryption|boolean|false|none|none|
|filename_encryption|boolean|false|none|none|
|encryption_password|string|false|none|none|
|encryption_salt|string|false|none|none|
|schedule|object|false|none|none|
|Â» minute|string|false|none|none|
|Â» hour|string|false|none|none|
|Â» dom|string|false|none|none|
|Â» month|string|false|none|none|
|Â» dow|string|false|none|none|
|follow_symlinks|boolean|false|none|none|
|transfers|integerÂ¦null|false|none|none|
|bwlimit|[object]|false|none|none|
|exclude|[string]|false|none|none|
|attributes|object|false|none|none|
|snapshot|boolean|false|none|none|
|pre_script|string|false|none|none|
|post_script|string|false|none|none|
|args|string|false|none|none|
|enabled|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|direction|PUSH|
|direction|PULL|
|transfer_mode|SYNC|
|transfer_mode|COPY|
|transfer_mode|MOVE|

<h2 id="tocS_cloudsync_update_1">cloudsync_update_1</h2>

<a id="schemacloudsync_update_1"></a>
<a id="schema_cloudsync_update_1"></a>
<a id="tocScloudsync_update_1"></a>
<a id="tocscloudsync_update_1"></a>

```json
{}

```

cloud_sync_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|description|string|false|none|none|
|direction|string|false|none|none|
|transfer_mode|string|false|none|none|
|path|string|false|none|none|
|credentials|integer|false|none|none|
|encryption|boolean|false|none|none|
|filename_encryption|boolean|false|none|none|
|encryption_password|string|false|none|none|
|encryption_salt|string|false|none|none|
|schedule|object|false|none|none|
|Â» minute|string|false|none|none|
|Â» hour|string|false|none|none|
|Â» dom|string|false|none|none|
|Â» month|string|false|none|none|
|Â» dow|string|false|none|none|
|follow_symlinks|boolean|false|none|none|
|transfers|integerÂ¦null|false|none|none|
|bwlimit|[object]|false|none|none|
|exclude|[string]|false|none|none|
|attributes|object|false|none|none|
|snapshot|boolean|false|none|none|
|pre_script|string|false|none|none|
|post_script|string|false|none|none|
|args|string|false|none|none|
|enabled|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|direction|PUSH|
|direction|PULL|
|transfer_mode|SYNC|
|transfer_mode|COPY|
|transfer_mode|MOVE|

<h2 id="tocS_cloudsync_abort">cloudsync_abort</h2>

<a id="schemacloudsync_abort"></a>
<a id="schema_cloudsync_abort"></a>
<a id="tocScloudsync_abort"></a>
<a id="tocscloudsync_abort"></a>

```json
{}

```

### Properties

*None*

<h2 id="tocS_cloudsync_list_buckets_0">cloudsync_list_buckets_0</h2>

<a id="schemacloudsync_list_buckets_0"></a>
<a id="schema_cloudsync_list_buckets_0"></a>
<a id="tocScloudsync_list_buckets_0"></a>
<a id="tocscloudsync_list_buckets_0"></a>

```json
0

```

credentials_id

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|credentials_id|integer|false|none|none|

<h2 id="tocS_cloudsync_list_directory_0">cloudsync_list_directory_0</h2>

<a id="schemacloudsync_list_directory_0"></a>
<a id="schema_cloudsync_list_directory_0"></a>
<a id="tocScloudsync_list_directory_0"></a>
<a id="tocscloudsync_list_directory_0"></a>

```json
{}

```

cloud_sync_ls

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|credentials|integer|false|none|none|
|encryption|boolean|false|none|none|
|filename_encryption|boolean|false|none|none|
|encryption_password|string|false|none|none|
|encryption_salt|string|false|none|none|
|attributes|object|false|none|none|
|args|string|false|none|none|

<h2 id="tocS_cloudsync_onedrive_list_drives_0">cloudsync_onedrive_list_drives_0</h2>

<a id="schemacloudsync_onedrive_list_drives_0"></a>
<a id="schema_cloudsync_onedrive_list_drives_0"></a>
<a id="tocScloudsync_onedrive_list_drives_0"></a>
<a id="tocscloudsync_onedrive_list_drives_0"></a>

```json
{}

```

onedrive_list_drives

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|client_id|string|false|none|none|
|client_secret|string|false|none|none|
|token|string|false|none|none|

<h2 id="tocS_cloudsync_restore_1">cloudsync_restore_1</h2>

<a id="schemacloudsync_restore_1"></a>
<a id="schema_cloudsync_restore_1"></a>
<a id="tocScloudsync_restore_1"></a>
<a id="tocscloudsync_restore_1"></a>

```json
{}

```

cloud_sync_restore

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|description|string|false|none|none|
|transfer_mode|string|false|none|none|
|path|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|transfer_mode|SYNC|
|transfer_mode|COPY|

<h2 id="tocS_cloudsync_sync_1">cloudsync_sync_1</h2>

<a id="schemacloudsync_sync_1"></a>
<a id="schema_cloudsync_sync_1"></a>
<a id="tocScloudsync_sync_1"></a>
<a id="tocscloudsync_sync_1"></a>

```json
{}

```

cloud_sync_sync_options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|dry_run|boolean|false|none|none|

<h2 id="tocS_cloudsync_sync_onetime_0">cloudsync_sync_onetime_0</h2>

<a id="schemacloudsync_sync_onetime_0"></a>
<a id="schema_cloudsync_sync_onetime_0"></a>
<a id="tocScloudsync_sync_onetime_0"></a>
<a id="tocscloudsync_sync_onetime_0"></a>

```json
{}

```

cloud_sync_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|description|string|false|none|none|
|direction|string|false|none|none|
|transfer_mode|string|false|none|none|
|path|string|false|none|none|
|credentials|integer|false|none|none|
|encryption|boolean|false|none|none|
|filename_encryption|boolean|false|none|none|
|encryption_password|string|false|none|none|
|encryption_salt|string|false|none|none|
|schedule|object|false|none|none|
|Â» minute|string|false|none|none|
|Â» hour|string|false|none|none|
|Â» dom|string|false|none|none|
|Â» month|string|false|none|none|
|Â» dow|string|false|none|none|
|follow_symlinks|boolean|false|none|none|
|transfers|integerÂ¦null|false|none|none|
|bwlimit|[object]|false|none|none|
|exclude|[string]|false|none|none|
|attributes|object|false|none|none|
|snapshot|boolean|false|none|none|
|pre_script|string|false|none|none|
|post_script|string|false|none|none|
|args|string|false|none|none|
|enabled|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|direction|PUSH|
|direction|PULL|
|transfer_mode|SYNC|
|transfer_mode|COPY|
|transfer_mode|MOVE|

<h2 id="tocS_cloudsync_sync_onetime_1">cloudsync_sync_onetime_1</h2>

<a id="schemacloudsync_sync_onetime_1"></a>
<a id="schema_cloudsync_sync_onetime_1"></a>
<a id="tocScloudsync_sync_onetime_1"></a>
<a id="tocscloudsync_sync_onetime_1"></a>

```json
{}

```

cloud_sync_sync_options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|dry_run|boolean|false|none|none|

<h2 id="tocS_cloudsync_sync_onetime">cloudsync_sync_onetime</h2>

<a id="schemacloudsync_sync_onetime"></a>
<a id="schema_cloudsync_sync_onetime"></a>
<a id="tocScloudsync_sync_onetime"></a>
<a id="tocscloudsync_sync_onetime"></a>

```json
{
  "cloud_sync_create": {},
  "cloud_sync_sync_options": {}
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|cloud_sync_create|[cloudsync_sync_onetime_0](#schemacloudsync_sync_onetime_0)|false|none|none|
|cloud_sync_sync_options|[cloudsync_sync_onetime_1](#schemacloudsync_sync_onetime_1)|false|none|none|

<h2 id="tocS_cloudsync_credentials_create_0">cloudsync_credentials_create_0</h2>

<a id="schemacloudsync_credentials_create_0"></a>
<a id="schema_cloudsync_credentials_create_0"></a>
<a id="tocScloudsync_credentials_create_0"></a>
<a id="tocscloudsync_credentials_create_0"></a>

```json
{}

```

cloud_sync_credentials_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|
|provider|string|false|none|none|
|attributes|object|false|none|none|

<h2 id="tocS_cloudsync_credentials_update_1">cloudsync_credentials_update_1</h2>

<a id="schemacloudsync_credentials_update_1"></a>
<a id="schema_cloudsync_credentials_update_1"></a>
<a id="tocScloudsync_credentials_update_1"></a>
<a id="tocscloudsync_credentials_update_1"></a>

```json
{}

```

cloud_sync_credentials_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|
|provider|string|false|none|none|
|attributes|object|false|none|none|

<h2 id="tocS_cloudsync_credentials_verify_0">cloudsync_credentials_verify_0</h2>

<a id="schemacloudsync_credentials_verify_0"></a>
<a id="schema_cloudsync_credentials_verify_0"></a>
<a id="tocScloudsync_credentials_verify_0"></a>
<a id="tocscloudsync_credentials_verify_0"></a>

```json
{}

```

cloud_sync_credentials_verify

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|provider|string|false|none|none|
|attributes|object|false|none|none|

<h2 id="tocS_config_reset_0">config_reset_0</h2>

<a id="schemaconfig_reset_0"></a>
<a id="schema_config_reset_0"></a>
<a id="tocSconfig_reset_0"></a>
<a id="tocsconfig_reset_0"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|reboot|boolean|false|none|none|

<h2 id="tocS_cronjob_create_0">cronjob_create_0</h2>

<a id="schemacronjob_create_0"></a>
<a id="schema_cronjob_create_0"></a>
<a id="tocScronjob_create_0"></a>
<a id="tocscronjob_create_0"></a>

```json
{}

```

cron_job_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|enabled|boolean|false|none|none|
|stderr|boolean|false|none|none|
|stdout|boolean|false|none|none|
|schedule|object|false|none|none|
|Â» minute|string|false|none|none|
|Â» hour|string|false|none|none|
|Â» dom|string|false|none|none|
|Â» month|string|false|none|none|
|Â» dow|string|false|none|none|
|command|string|false|none|none|
|description|string|false|none|none|
|user|string|false|none|none|

<h2 id="tocS_cronjob_update_1">cronjob_update_1</h2>

<a id="schemacronjob_update_1"></a>
<a id="schema_cronjob_update_1"></a>
<a id="tocScronjob_update_1"></a>
<a id="tocscronjob_update_1"></a>

```json
{}

```

cron_job_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|enabled|boolean|false|none|none|
|stderr|boolean|false|none|none|
|stdout|boolean|false|none|none|
|schedule|object|false|none|none|
|Â» minute|string|false|none|none|
|Â» hour|string|false|none|none|
|Â» dom|string|false|none|none|
|Â» month|string|false|none|none|
|Â» dow|string|false|none|none|
|command|string|false|none|none|
|description|string|false|none|none|
|user|string|false|none|none|

<h2 id="tocS_cronjob_run_0">cronjob_run_0</h2>

<a id="schemacronjob_run_0"></a>
<a id="schema_cronjob_run_0"></a>
<a id="tocScronjob_run_0"></a>
<a id="tocscronjob_run_0"></a>

```json
0

```

id

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|id|integer|false|none|none|

<h2 id="tocS_cronjob_run_1">cronjob_run_1</h2>

<a id="schemacronjob_run_1"></a>
<a id="schema_cronjob_run_1"></a>
<a id="tocScronjob_run_1"></a>
<a id="tocscronjob_run_1"></a>

```json
false

```

skip_disabled

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|skip_disabled|boolean|false|none|none|

<h2 id="tocS_cronjob_run">cronjob_run</h2>

<a id="schemacronjob_run"></a>
<a id="schema_cronjob_run"></a>
<a id="tocScronjob_run"></a>
<a id="tocscronjob_run"></a>

```json
{
  "id": 0,
  "skip_disabled": false
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|id|[cronjob_run_0](#schemacronjob_run_0)|false|none|none|
|skip_disabled|[cronjob_run_1](#schemacronjob_run_1)|false|none|none|

<h2 id="tocS_device_get_info_0">device_get_info_0</h2>

<a id="schemadevice_get_info_0"></a>
<a id="schema_device_get_info_0"></a>
<a id="tocSdevice_get_info_0"></a>
<a id="tocsdevice_get_info_0"></a>

```json
"SERIAL"

```

type

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|type|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|type|SERIAL|
|type|DISK|

<h2 id="tocS_disk_update_1">disk_update_1</h2>

<a id="schemadisk_update_1"></a>
<a id="schema_disk_update_1"></a>
<a id="tocSdisk_update_1"></a>
<a id="tocsdisk_update_1"></a>

```json
{}

```

disk_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|togglesmart|boolean|false|none|none|
|acousticlevel|string|false|none|none|
|advpowermgmt|string|false|none|none|
|description|string|false|none|none|
|hddstandby|string|false|none|none|
|hddstandby_force|boolean|false|none|none|
|passwd|string|false|none|none|
|smartoptions|string|false|none|none|
|critical|integerÂ¦null|false|none|none|
|difference|integerÂ¦null|false|none|none|
|informational|integerÂ¦null|false|none|none|
|enclosure|object|false|none|none|
|Â» number|integer|false|none|none|
|Â» slot|integer|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|acousticlevel|DISABLED|
|acousticlevel|MINIMUM|
|acousticlevel|MEDIUM|
|acousticlevel|MAXIMUM|
|advpowermgmt|DISABLED|
|advpowermgmt|1|
|advpowermgmt|64|
|advpowermgmt|127|
|advpowermgmt|128|
|advpowermgmt|192|
|advpowermgmt|254|
|hddstandby|ALWAYS ON|
|hddstandby|5|
|hddstandby|10|
|hddstandby|20|
|hddstandby|30|
|hddstandby|60|
|hddstandby|120|
|hddstandby|180|
|hddstandby|240|
|hddstandby|300|
|hddstandby|330|

<h2 id="tocS_disk_get_encrypted_0">disk_get_encrypted_0</h2>

<a id="schemadisk_get_encrypted_0"></a>
<a id="schema_disk_get_encrypted_0"></a>
<a id="tocSdisk_get_encrypted_0"></a>
<a id="tocsdisk_get_encrypted_0"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|unused|boolean|false|none|none|

<h2 id="tocS_disk_get_unused_0">disk_get_unused_0</h2>

<a id="schemadisk_get_unused_0"></a>
<a id="schema_disk_get_unused_0"></a>
<a id="tocSdisk_get_unused_0"></a>
<a id="tocsdisk_get_unused_0"></a>

```json
false

```

join_partitions

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|join_partitions|boolean|false|none|none|

<h2 id="tocS_disk_overprovision_0">disk_overprovision_0</h2>

<a id="schemadisk_overprovision_0"></a>
<a id="schema_disk_overprovision_0"></a>
<a id="tocSdisk_overprovision_0"></a>
<a id="tocsdisk_overprovision_0"></a>

```json
"string"

```

devname

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|devname|string|false|none|none|

<h2 id="tocS_disk_overprovision_1">disk_overprovision_1</h2>

<a id="schemadisk_overprovision_1"></a>
<a id="schema_disk_overprovision_1"></a>
<a id="tocSdisk_overprovision_1"></a>
<a id="tocsdisk_overprovision_1"></a>

```json
0

```

size

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|size|integer|false|none|none|

<h2 id="tocS_disk_overprovision">disk_overprovision</h2>

<a id="schemadisk_overprovision"></a>
<a id="schema_disk_overprovision"></a>
<a id="tocSdisk_overprovision"></a>
<a id="tocsdisk_overprovision"></a>

```json
{
  "devname": "string",
  "size": 0
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|devname|[disk_overprovision_0](#schemadisk_overprovision_0)|false|none|none|
|size|[disk_overprovision_1](#schemadisk_overprovision_1)|false|none|none|

<h2 id="tocS_disk_smart_attributes_0">disk_smart_attributes_0</h2>

<a id="schemadisk_smart_attributes_0"></a>
<a id="schema_disk_smart_attributes_0"></a>
<a id="tocSdisk_smart_attributes_0"></a>
<a id="tocsdisk_smart_attributes_0"></a>

```json
"string"

```

name

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|

<h2 id="tocS_disk_spindown_0">disk_spindown_0</h2>

<a id="schemadisk_spindown_0"></a>
<a id="schema_disk_spindown_0"></a>
<a id="tocSdisk_spindown_0"></a>
<a id="tocsdisk_spindown_0"></a>

```json
"string"

```

disk

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|disk|string|false|none|none|

<h2 id="tocS_disk_temperature_0">disk_temperature_0</h2>

<a id="schemadisk_temperature_0"></a>
<a id="schema_disk_temperature_0"></a>
<a id="tocSdisk_temperature_0"></a>
<a id="tocsdisk_temperature_0"></a>

```json
"string"

```

name

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|

<h2 id="tocS_disk_temperature_1">disk_temperature_1</h2>

<a id="schemadisk_temperature_1"></a>
<a id="schema_disk_temperature_1"></a>
<a id="tocSdisk_temperature_1"></a>
<a id="tocsdisk_temperature_1"></a>

```json
"NEVER"

```

powermode

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|powermode|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|powermode|NEVER|
|powermode|SLEEP|
|powermode|STANDBY|
|powermode|IDLE|

<h2 id="tocS_disk_temperature">disk_temperature</h2>

<a id="schemadisk_temperature"></a>
<a id="schema_disk_temperature"></a>
<a id="tocSdisk_temperature"></a>
<a id="tocsdisk_temperature"></a>

```json
{
  "name": "string",
  "powermode": "NEVER"
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|[disk_temperature_0](#schemadisk_temperature_0)|false|none|none|
|powermode|[disk_temperature_1](#schemadisk_temperature_1)|false|none|none|

<h2 id="tocS_disk_temperatures_0">disk_temperatures_0</h2>

<a id="schemadisk_temperatures_0"></a>
<a id="schema_disk_temperatures_0"></a>
<a id="tocSdisk_temperatures_0"></a>
<a id="tocsdisk_temperatures_0"></a>

```json
[
  "string"
]

```

names

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|names|[string]|false|none|none|

<h2 id="tocS_disk_temperatures_1">disk_temperatures_1</h2>

<a id="schemadisk_temperatures_1"></a>
<a id="schema_disk_temperatures_1"></a>
<a id="tocSdisk_temperatures_1"></a>
<a id="tocsdisk_temperatures_1"></a>

```json
"NEVER"

```

powermode

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|powermode|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|powermode|NEVER|
|powermode|SLEEP|
|powermode|STANDBY|
|powermode|IDLE|

<h2 id="tocS_disk_temperatures">disk_temperatures</h2>

<a id="schemadisk_temperatures"></a>
<a id="schema_disk_temperatures"></a>
<a id="tocSdisk_temperatures"></a>
<a id="tocsdisk_temperatures"></a>

```json
{
  "names": [
    "string"
  ],
  "powermode": "NEVER"
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|names|[disk_temperatures_0](#schemadisk_temperatures_0)|false|none|none|
|powermode|[disk_temperatures_1](#schemadisk_temperatures_1)|false|none|none|

<h2 id="tocS_disk_unoverprovision_0">disk_unoverprovision_0</h2>

<a id="schemadisk_unoverprovision_0"></a>
<a id="schema_disk_unoverprovision_0"></a>
<a id="tocSdisk_unoverprovision_0"></a>
<a id="tocsdisk_unoverprovision_0"></a>

```json
"string"

```

devname

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|devname|string|false|none|none|

<h2 id="tocS_disk_wipe_0">disk_wipe_0</h2>

<a id="schemadisk_wipe_0"></a>
<a id="schema_disk_wipe_0"></a>
<a id="tocSdisk_wipe_0"></a>
<a id="tocsdisk_wipe_0"></a>

```json
"string"

```

dev

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|dev|string|false|none|none|

<h2 id="tocS_disk_wipe_1">disk_wipe_1</h2>

<a id="schemadisk_wipe_1"></a>
<a id="schema_disk_wipe_1"></a>
<a id="tocSdisk_wipe_1"></a>
<a id="tocsdisk_wipe_1"></a>

```json
"QUICK"

```

mode

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|mode|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|mode|QUICK|
|mode|FULL|
|mode|FULL_RANDOM|

<h2 id="tocS_disk_wipe_2">disk_wipe_2</h2>

<a id="schemadisk_wipe_2"></a>
<a id="schema_disk_wipe_2"></a>
<a id="tocSdisk_wipe_2"></a>
<a id="tocsdisk_wipe_2"></a>

```json
true

```

synccache

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|synccache|boolean|false|none|none|

<h2 id="tocS_disk_wipe_3">disk_wipe_3</h2>

<a id="schemadisk_wipe_3"></a>
<a id="schema_disk_wipe_3"></a>
<a id="tocSdisk_wipe_3"></a>
<a id="tocsdisk_wipe_3"></a>

```json
{}

```

swap_removal_options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|configure_swap|boolean|false|none|none|

<h2 id="tocS_disk_wipe">disk_wipe</h2>

<a id="schemadisk_wipe"></a>
<a id="schema_disk_wipe"></a>
<a id="tocSdisk_wipe"></a>
<a id="tocsdisk_wipe"></a>

```json
{
  "dev": "string",
  "mode": "QUICK",
  "synccache": true,
  "swap_removal_options": {}
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|dev|[disk_wipe_0](#schemadisk_wipe_0)|false|none|none|
|mode|[disk_wipe_1](#schemadisk_wipe_1)|false|none|none|
|synccache|[disk_wipe_2](#schemadisk_wipe_2)|false|none|none|
|swap_removal_options|[disk_wipe_3](#schemadisk_wipe_3)|false|none|none|

<h2 id="tocS_dyndns_update_0">dyndns_update_0</h2>

<a id="schemadyndns_update_0"></a>
<a id="schema_dyndns_update_0"></a>
<a id="tocSdyndns_update_0"></a>
<a id="tocsdyndns_update_0"></a>

```json
{}

```

dyndns_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|provider|string|false|none|none|
|checkip_ssl|boolean|false|none|none|
|checkip_server|string|false|none|none|
|checkip_path|string|false|none|none|
|ssl|boolean|false|none|none|
|custom_ddns_server|string|false|none|none|
|custom_ddns_path|string|false|none|none|
|domain|[string]|false|none|none|
|username|string|false|none|none|
|password|string|false|none|none|
|period|integer|false|none|none|

<h2 id="tocS_enclosure_update_1">enclosure_update_1</h2>

<a id="schemaenclosure_update_1"></a>
<a id="schema_enclosure_update_1"></a>
<a id="tocSenclosure_update_1"></a>
<a id="tocsenclosure_update_1"></a>

```json
{}

```

enclosure_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|label|string|false|none|none|

<h2 id="tocS_enclosure_set_slot_status_0">enclosure_set_slot_status_0</h2>

<a id="schemaenclosure_set_slot_status_0"></a>
<a id="schema_enclosure_set_slot_status_0"></a>
<a id="tocSenclosure_set_slot_status_0"></a>
<a id="tocsenclosure_set_slot_status_0"></a>

```json
"string"

```

enclosure_id

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|enclosure_id|string|false|none|none|

<h2 id="tocS_enclosure_set_slot_status_1">enclosure_set_slot_status_1</h2>

<a id="schemaenclosure_set_slot_status_1"></a>
<a id="schema_enclosure_set_slot_status_1"></a>
<a id="tocSenclosure_set_slot_status_1"></a>
<a id="tocsenclosure_set_slot_status_1"></a>

```json
0

```

slot

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|slot|integer|false|none|none|

<h2 id="tocS_enclosure_set_slot_status_2">enclosure_set_slot_status_2</h2>

<a id="schemaenclosure_set_slot_status_2"></a>
<a id="schema_enclosure_set_slot_status_2"></a>
<a id="tocSenclosure_set_slot_status_2"></a>
<a id="tocsenclosure_set_slot_status_2"></a>

```json
"CLEAR"

```

status

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|status|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|status|CLEAR|
|status|FAULT|
|status|IDENTIFY|

<h2 id="tocS_enclosure_set_slot_status">enclosure_set_slot_status</h2>

<a id="schemaenclosure_set_slot_status"></a>
<a id="schema_enclosure_set_slot_status"></a>
<a id="tocSenclosure_set_slot_status"></a>
<a id="tocsenclosure_set_slot_status"></a>

```json
{
  "enclosure_id": "string",
  "slot": 0,
  "status": "CLEAR"
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|enclosure_id|[enclosure_set_slot_status_0](#schemaenclosure_set_slot_status_0)|false|none|none|
|slot|[enclosure_set_slot_status_1](#schemaenclosure_set_slot_status_1)|false|none|none|
|status|[enclosure_set_slot_status_2](#schemaenclosure_set_slot_status_2)|false|none|none|

<h2 id="tocS_failover_update_0">failover_update_0</h2>

<a id="schemafailover_update_0"></a>
<a id="schema_failover_update_0"></a>
<a id="tocSfailover_update_0"></a>
<a id="tocsfailover_update_0"></a>

```json
{}

```

failover_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|disabled|boolean|false|none|none|
|timeout|integer|false|none|none|
|master|booleanÂ¦null|false|none|none|

<h2 id="tocS_failover_call_remote_0">failover_call_remote_0</h2>

<a id="schemafailover_call_remote_0"></a>
<a id="schema_failover_call_remote_0"></a>
<a id="tocSfailover_call_remote_0"></a>
<a id="tocsfailover_call_remote_0"></a>

```json
"string"

```

method

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|method|string|false|none|none|

<h2 id="tocS_failover_call_remote_1">failover_call_remote_1</h2>

<a id="schemafailover_call_remote_1"></a>
<a id="schema_failover_call_remote_1"></a>
<a id="tocSfailover_call_remote_1"></a>
<a id="tocsfailover_call_remote_1"></a>

```json
[]

```

args

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|args|[any]|false|none|none|

<h2 id="tocS_failover_call_remote_2">failover_call_remote_2</h2>

<a id="schemafailover_call_remote_2"></a>
<a id="schema_failover_call_remote_2"></a>
<a id="tocSfailover_call_remote_2"></a>
<a id="tocsfailover_call_remote_2"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|timeout|integer|false|none|none|
|job|boolean|false|none|none|
|job_return|booleanÂ¦null|false|none|none|
|callback|any|false|none|none|

anyOf

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|Â» *anonymous*|string|false|none|none|

or

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|Â» *anonymous*|integer|false|none|none|

or

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|Â» *anonymous*|boolean|false|none|none|

or

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|Â» *anonymous*|object|false|none|none|

or

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|Â» *anonymous*|array|false|none|none|

<h2 id="tocS_failover_call_remote">failover_call_remote</h2>

<a id="schemafailover_call_remote"></a>
<a id="schema_failover_call_remote"></a>
<a id="tocSfailover_call_remote"></a>
<a id="tocsfailover_call_remote"></a>

```json
{
  "method": "string",
  "args": [],
  "options": {}
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|method|[failover_call_remote_0](#schemafailover_call_remote_0)|false|none|none|
|args|[failover_call_remote_1](#schemafailover_call_remote_1)|false|none|none|
|options|[failover_call_remote_2](#schemafailover_call_remote_2)|false|none|none|

<h2 id="tocS_failover_control_0">failover_control_0</h2>

<a id="schemafailover_control_0"></a>
<a id="schema_failover_control_0"></a>
<a id="tocSfailover_control_0"></a>
<a id="tocsfailover_control_0"></a>

```json
"ENABLE"

```

action

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|action|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|action|ENABLE|
|action|DISABLE|

<h2 id="tocS_failover_control_1">failover_control_1</h2>

<a id="schemafailover_control_1"></a>
<a id="schema_failover_control_1"></a>
<a id="tocSfailover_control_1"></a>
<a id="tocsfailover_control_1"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|active|boolean|false|none|none|

<h2 id="tocS_failover_control">failover_control</h2>

<a id="schemafailover_control"></a>
<a id="schema_failover_control"></a>
<a id="tocSfailover_control"></a>
<a id="tocsfailover_control"></a>

```json
{
  "action": "ENABLE",
  "options": {}
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|action|[failover_control_0](#schemafailover_control_0)|false|none|none|
|options|[failover_control_1](#schemafailover_control_1)|false|none|none|

<h2 id="tocS_failover_sync_to_peer_0">failover_sync_to_peer_0</h2>

<a id="schemafailover_sync_to_peer_0"></a>
<a id="schema_failover_sync_to_peer_0"></a>
<a id="tocSfailover_sync_to_peer_0"></a>
<a id="tocsfailover_sync_to_peer_0"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|reboot|boolean|false|none|none|

<h2 id="tocS_failover_unlock_0">failover_unlock_0</h2>

<a id="schemafailover_unlock_0"></a>
<a id="schema_failover_unlock_0"></a>
<a id="tocSfailover_unlock_0"></a>
<a id="tocsfailover_unlock_0"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|pools|[object]|false|none|none|
|datasets|[object]|false|none|none|

<h2 id="tocS_failover_upgrade_0">failover_upgrade_0</h2>

<a id="schemafailover_upgrade_0"></a>
<a id="schema_failover_upgrade_0"></a>
<a id="tocSfailover_upgrade_0"></a>
<a id="tocsfailover_upgrade_0"></a>

```json
{}

```

failover_upgrade

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|train|string|false|none|none|

<h2 id="tocS_fcport_update_1">fcport_update_1</h2>

<a id="schemafcport_update_1"></a>
<a id="schema_fcport_update_1"></a>
<a id="tocSfcport_update_1"></a>
<a id="tocsfcport_update_1"></a>

```json
{}

```

fcport_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|mode|string|false|none|none|
|target|integerÂ¦null|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|mode|INITIATOR|
|mode|TARGET|
|mode|DISABLED|

<h2 id="tocS_filesystem_acl_is_trivial_0">filesystem_acl_is_trivial_0</h2>

<a id="schemafilesystem_acl_is_trivial_0"></a>
<a id="schema_filesystem_acl_is_trivial_0"></a>
<a id="tocSfilesystem_acl_is_trivial_0"></a>
<a id="tocsfilesystem_acl_is_trivial_0"></a>

```json
"string"

```

path

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|path|string|false|none|none|

<h2 id="tocS_filesystem_chown_0">filesystem_chown_0</h2>

<a id="schemafilesystem_chown_0"></a>
<a id="schema_filesystem_chown_0"></a>
<a id="tocSfilesystem_chown_0"></a>
<a id="tocsfilesystem_chown_0"></a>

```json
{}

```

filesystem_ownership

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|path|string|false|none|none|
|uid|integerÂ¦null|false|none|none|
|gid|integerÂ¦null|false|none|none|
|options|object|false|none|none|
|Â» recursive|boolean|false|none|none|
|Â» traverse|boolean|false|none|none|

<h2 id="tocS_filesystem_get_default_acl_0">filesystem_get_default_acl_0</h2>

<a id="schemafilesystem_get_default_acl_0"></a>
<a id="schema_filesystem_get_default_acl_0"></a>
<a id="tocSfilesystem_get_default_acl_0"></a>
<a id="tocsfilesystem_get_default_acl_0"></a>

```json
"OPEN"

```

acl_type

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|acl_type|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|acl_type|OPEN|
|acl_type|RESTRICTED|
|acl_type|HOME|
|acl_type|DOMAIN_HOME|

<h2 id="tocS_filesystem_get_default_acl_1">filesystem_get_default_acl_1</h2>

<a id="schemafilesystem_get_default_acl_1"></a>
<a id="schema_filesystem_get_default_acl_1"></a>
<a id="tocSfilesystem_get_default_acl_1"></a>
<a id="tocsfilesystem_get_default_acl_1"></a>

```json
"NONE"

```

share_type

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|share_type|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|share_type|NONE|
|share_type|AFP|
|share_type|SMB|
|share_type|NFS|

<h2 id="tocS_filesystem_get_default_acl">filesystem_get_default_acl</h2>

<a id="schemafilesystem_get_default_acl"></a>
<a id="schema_filesystem_get_default_acl"></a>
<a id="tocSfilesystem_get_default_acl"></a>
<a id="tocsfilesystem_get_default_acl"></a>

```json
{
  "acl_type": "OPEN",
  "share_type": "NONE"
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|acl_type|[filesystem_get_default_acl_0](#schemafilesystem_get_default_acl_0)|false|none|none|
|share_type|[filesystem_get_default_acl_1](#schemafilesystem_get_default_acl_1)|false|none|none|

<h2 id="tocS_filesystem_getacl_0">filesystem_getacl_0</h2>

<a id="schemafilesystem_getacl_0"></a>
<a id="schema_filesystem_getacl_0"></a>
<a id="tocSfilesystem_getacl_0"></a>
<a id="tocsfilesystem_getacl_0"></a>

```json
"string"

```

path

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|path|string|false|none|none|

<h2 id="tocS_filesystem_getacl_1">filesystem_getacl_1</h2>

<a id="schemafilesystem_getacl_1"></a>
<a id="schema_filesystem_getacl_1"></a>
<a id="tocSfilesystem_getacl_1"></a>
<a id="tocsfilesystem_getacl_1"></a>

```json
true

```

simplified

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|simplified|boolean|false|none|none|

<h2 id="tocS_filesystem_getacl">filesystem_getacl</h2>

<a id="schemafilesystem_getacl"></a>
<a id="schema_filesystem_getacl"></a>
<a id="tocSfilesystem_getacl"></a>
<a id="tocsfilesystem_getacl"></a>

```json
{
  "path": "string",
  "simplified": true
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|path|[filesystem_getacl_0](#schemafilesystem_getacl_0)|false|none|none|
|simplified|[filesystem_getacl_1](#schemafilesystem_getacl_1)|false|none|none|

<h2 id="tocS_filesystem_listdir_0">filesystem_listdir_0</h2>

<a id="schemafilesystem_listdir_0"></a>
<a id="schema_filesystem_listdir_0"></a>
<a id="tocSfilesystem_listdir_0"></a>
<a id="tocsfilesystem_listdir_0"></a>

```json
"string"

```

path

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|path|string|false|none|none|

<h2 id="tocS_filesystem_listdir_1">filesystem_listdir_1</h2>

<a id="schemafilesystem_listdir_1"></a>
<a id="schema_filesystem_listdir_1"></a>
<a id="tocSfilesystem_listdir_1"></a>
<a id="tocsfilesystem_listdir_1"></a>

```json
[]

```

query-filters

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|query-filters|[any]|false|none|none|

<h2 id="tocS_filesystem_listdir_2">filesystem_listdir_2</h2>

<a id="schemafilesystem_listdir_2"></a>
<a id="schema_filesystem_listdir_2"></a>
<a id="tocSfilesystem_listdir_2"></a>
<a id="tocsfilesystem_listdir_2"></a>

```json
{}

```

query-options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|relationships|boolean|false|none|none|
|extend|stringÂ¦null|false|none|none|
|extend_context|stringÂ¦null|false|none|none|
|prefix|stringÂ¦null|false|none|none|
|extra|object|false|none|none|
|order_by|[any]|false|none|none|
|select|[any]|false|none|none|
|count|boolean|false|none|none|
|get|boolean|false|none|none|
|offset|integer|false|none|none|
|limit|integer|false|none|none|

<h2 id="tocS_filesystem_listdir">filesystem_listdir</h2>

<a id="schemafilesystem_listdir"></a>
<a id="schema_filesystem_listdir"></a>
<a id="tocSfilesystem_listdir"></a>
<a id="tocsfilesystem_listdir"></a>

```json
{
  "path": "string",
  "query-filters": [],
  "query-options": {}
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|path|[filesystem_listdir_0](#schemafilesystem_listdir_0)|false|none|none|
|query-filters|[filesystem_listdir_1](#schemafilesystem_listdir_1)|false|none|none|
|query-options|[filesystem_listdir_2](#schemafilesystem_listdir_2)|false|none|none|

<h2 id="tocS_filesystem_setacl_0">filesystem_setacl_0</h2>

<a id="schemafilesystem_setacl_0"></a>
<a id="schema_filesystem_setacl_0"></a>
<a id="tocSfilesystem_setacl_0"></a>
<a id="tocsfilesystem_setacl_0"></a>

```json
{}

```

filesystem_acl

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|path|string|false|none|none|
|uid|integerÂ¦null|false|none|none|
|gid|integerÂ¦null|false|none|none|
|dacl|[oneOf]|false|none|none|

oneOf

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|Â» *anonymous*|object|false|none|none|

xor

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|Â» *anonymous*|object|false|none|none|

continued

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|nfs41_flags|object|false|none|none|
|Â» autoinherit|boolean|false|none|none|
|Â» protected|boolean|false|none|none|
|acltype|string|false|none|none|
|options|object|false|none|none|
|Â» stripacl|boolean|false|none|none|
|Â» recursive|boolean|false|none|none|
|Â» traverse|boolean|false|none|none|
|Â» canonicalize|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|acltype|NFS4|
|acltype|POSIX1E|
|acltype|RICH|

<h2 id="tocS_filesystem_setperm_0">filesystem_setperm_0</h2>

<a id="schemafilesystem_setperm_0"></a>
<a id="schema_filesystem_setperm_0"></a>
<a id="tocSfilesystem_setperm_0"></a>
<a id="tocsfilesystem_setperm_0"></a>

```json
{}

```

filesystem_permission

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|path|string|false|none|none|
|mode|stringÂ¦null|false|none|none|
|uid|integerÂ¦null|false|none|none|
|gid|integerÂ¦null|false|none|none|
|options|object|false|none|none|
|Â» stripacl|boolean|false|none|none|
|Â» recursive|boolean|false|none|none|
|Â» traverse|boolean|false|none|none|

<h2 id="tocS_filesystem_stat_0">filesystem_stat_0</h2>

<a id="schemafilesystem_stat_0"></a>
<a id="schema_filesystem_stat_0"></a>
<a id="tocSfilesystem_stat_0"></a>
<a id="tocsfilesystem_stat_0"></a>

```json
"string"

```

path

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|path|string|false|none|none|

<h2 id="tocS_filesystem_statfs_0">filesystem_statfs_0</h2>

<a id="schemafilesystem_statfs_0"></a>
<a id="schema_filesystem_statfs_0"></a>
<a id="tocSfilesystem_statfs_0"></a>
<a id="tocsfilesystem_statfs_0"></a>

```json
"string"

```

path

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|path|string|false|none|none|

<h2 id="tocS_ftp_update_0">ftp_update_0</h2>

<a id="schemaftp_update_0"></a>
<a id="schema_ftp_update_0"></a>
<a id="tocSftp_update_0"></a>
<a id="tocsftp_update_0"></a>

```json
{}

```

ftp_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|port|integer|false|none|none|
|clients|integer|false|none|none|
|ipconnections|integer|false|none|none|
|loginattempt|integer|false|none|none|
|timeout|integer|false|none|none|
|rootlogin|boolean|false|none|none|
|onlyanonymous|boolean|false|none|none|
|anonpath|stringÂ¦null|false|none|none|
|onlylocal|boolean|false|none|none|
|banner|string|false|none|none|
|filemask|string|false|none|none|
|dirmask|string|false|none|none|
|fxp|boolean|false|none|none|
|resume|boolean|false|none|none|
|defaultroot|boolean|false|none|none|
|ident|boolean|false|none|none|
|reversedns|boolean|false|none|none|
|masqaddress|string|false|none|none|
|passiveportsmin|integer|false|none|none|
|passiveportsmax|integer|false|none|none|
|localuserbw|integer|false|none|none|
|localuserdlbw|integer|false|none|none|
|anonuserbw|integer|false|none|none|
|anonuserdlbw|integer|false|none|none|
|tls|boolean|false|none|none|
|tls_policy|string|false|none|none|
|tls_opt_allow_client_renegotiations|boolean|false|none|none|
|tls_opt_allow_dot_login|boolean|false|none|none|
|tls_opt_allow_per_user|boolean|false|none|none|
|tls_opt_common_name_required|boolean|false|none|none|
|tls_opt_enable_diags|boolean|false|none|none|
|tls_opt_export_cert_data|boolean|false|none|none|
|tls_opt_no_cert_request|boolean|false|none|none|
|tls_opt_no_empty_fragments|boolean|false|none|none|
|tls_opt_no_session_reuse_required|boolean|false|none|none|
|tls_opt_stdenvvars|boolean|false|none|none|
|tls_opt_dns_name_required|boolean|false|none|none|
|tls_opt_ip_address_required|boolean|false|none|none|
|ssltls_certificate|integerÂ¦null|false|none|none|
|options|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|tls_policy|on|
|tls_policy|off|
|tls_policy|data|
|tls_policy|!data|
|tls_policy|auth|
|tls_policy|ctrl|
|tls_policy|ctrl+data|
|tls_policy|ctrl+!data|
|tls_policy|auth+data|
|tls_policy|auth+!data|

<h2 id="tocS_group_create_0">group_create_0</h2>

<a id="schemagroup_create_0"></a>
<a id="schema_group_create_0"></a>
<a id="tocSgroup_create_0"></a>
<a id="tocsgroup_create_0"></a>

```json
{}

```

group_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|gid|integer|false|none|none|
|name|string|false|none|none|
|smb|boolean|false|none|none|
|sudo|boolean|false|none|none|
|allow_duplicate_gid|boolean|false|none|none|
|users|[integer]|false|none|none|

<h2 id="tocS_group_delete_1">group_delete_1</h2>

<a id="schemagroup_delete_1"></a>
<a id="schema_group_delete_1"></a>
<a id="tocSgroup_delete_1"></a>
<a id="tocsgroup_delete_1"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|delete_users|boolean|false|none|none|

<h2 id="tocS_group_update_1">group_update_1</h2>

<a id="schemagroup_update_1"></a>
<a id="schema_group_update_1"></a>
<a id="tocSgroup_update_1"></a>
<a id="tocsgroup_update_1"></a>

```json
{}

```

group_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|gid|integer|false|none|none|
|name|string|false|none|none|
|smb|boolean|false|none|none|
|sudo|boolean|false|none|none|
|allow_duplicate_gid|boolean|false|none|none|
|users|[integer]|false|none|none|

<h2 id="tocS_group_get_group_obj_0">group_get_group_obj_0</h2>

<a id="schemagroup_get_group_obj_0"></a>
<a id="schema_group_get_group_obj_0"></a>
<a id="tocSgroup_get_group_obj_0"></a>
<a id="tocsgroup_get_group_obj_0"></a>

```json
{}

```

get_group_obj

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|groupname|string|false|none|none|
|gid|integer|false|none|none|

<h2 id="tocS_idmap_create_0">idmap_create_0</h2>

<a id="schemaidmap_create_0"></a>
<a id="schema_idmap_create_0"></a>
<a id="tocSidmap_create_0"></a>
<a id="tocsidmap_create_0"></a>

```json
{}

```

idmap_domain_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|
|dns_domain_name|string|false|none|none|
|range_low|integer|false|none|none|
|range_high|integer|false|none|none|
|idmap_backend|string|false|none|none|
|certificate|integerÂ¦null|false|none|none|
|options|object|false|none|none|
|Â» schema_mode|string|false|none|none|
|Â» unix_primary_group|boolean|false|none|none|
|Â» unix_nss_info|boolean|false|none|none|
|Â» rangesize|integer|false|none|none|
|Â» readonly|boolean|false|none|none|
|Â» ignore_builtin|boolean|false|none|none|
|Â» ldap_base_dn|string|false|none|none|
|Â» ldap_user_dn|string|false|none|none|
|Â» ldap_user_dn_password|string|false|none|none|
|Â» ldap_url|string|false|none|none|
|Â» ssl|string|false|none|none|
|Â» linked_service|string|false|none|none|
|Â» ldap_server|string|false|none|none|
|Â» ldap_realm|boolean|false|none|none|
|Â» bind_path_user|string|false|none|none|
|Â» bind_path_group|string|false|none|none|
|Â» user_cn|boolean|false|none|none|
|Â» cn_realm|string|false|none|none|
|Â» ldap_domain|string|false|none|none|
|Â» sssd_compat|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|idmap_backend|AD|
|idmap_backend|AUTORID|
|idmap_backend|LDAP|
|idmap_backend|NSS|
|idmap_backend|RFC2307|
|idmap_backend|RID|
|idmap_backend|TDB|
|schema_mode|RFC2307|
|schema_mode|SFU|
|schema_mode|SFU20|
|ssl|OFF|
|ssl|ON|
|ssl|START_TLS|
|linked_service|LOCAL_ACCOUNT|
|linked_service|LDAP|
|linked_service|NIS|

<h2 id="tocS_idmap_update_1">idmap_update_1</h2>

<a id="schemaidmap_update_1"></a>
<a id="schema_idmap_update_1"></a>
<a id="tocSidmap_update_1"></a>
<a id="tocsidmap_update_1"></a>

```json
{}

```

idmap_domain_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|
|dns_domain_name|string|false|none|none|
|range_low|integer|false|none|none|
|range_high|integer|false|none|none|
|idmap_backend|string|false|none|none|
|certificate|integerÂ¦null|false|none|none|
|options|object|false|none|none|
|Â» schema_mode|string|false|none|none|
|Â» unix_primary_group|boolean|false|none|none|
|Â» unix_nss_info|boolean|false|none|none|
|Â» rangesize|integer|false|none|none|
|Â» readonly|boolean|false|none|none|
|Â» ignore_builtin|boolean|false|none|none|
|Â» ldap_base_dn|string|false|none|none|
|Â» ldap_user_dn|string|false|none|none|
|Â» ldap_user_dn_password|string|false|none|none|
|Â» ldap_url|string|false|none|none|
|Â» ssl|string|false|none|none|
|Â» linked_service|string|false|none|none|
|Â» ldap_server|string|false|none|none|
|Â» ldap_realm|boolean|false|none|none|
|Â» bind_path_user|string|false|none|none|
|Â» bind_path_group|string|false|none|none|
|Â» user_cn|boolean|false|none|none|
|Â» cn_realm|string|false|none|none|
|Â» ldap_domain|string|false|none|none|
|Â» sssd_compat|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|idmap_backend|AD|
|idmap_backend|AUTORID|
|idmap_backend|LDAP|
|idmap_backend|NSS|
|idmap_backend|RFC2307|
|idmap_backend|RID|
|idmap_backend|TDB|
|schema_mode|RFC2307|
|schema_mode|SFU|
|schema_mode|SFU20|
|ssl|OFF|
|ssl|ON|
|ssl|START_TLS|
|linked_service|LOCAL_ACCOUNT|
|linked_service|LDAP|
|linked_service|NIS|

<h2 id="tocS_idmap_options_choices_0">idmap_options_choices_0</h2>

<a id="schemaidmap_options_choices_0"></a>
<a id="schema_idmap_options_choices_0"></a>
<a id="tocSidmap_options_choices_0"></a>
<a id="tocsidmap_options_choices_0"></a>

```json
"AD"

```

idmap_backend

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|idmap_backend|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|idmap_backend|AD|
|idmap_backend|AUTORID|
|idmap_backend|LDAP|
|idmap_backend|NSS|
|idmap_backend|RFC2307|
|idmap_backend|RID|
|idmap_backend|TDB|

<h2 id="tocS_initshutdownscript_create_0">initshutdownscript_create_0</h2>

<a id="schemainitshutdownscript_create_0"></a>
<a id="schema_initshutdownscript_create_0"></a>
<a id="tocSinitshutdownscript_create_0"></a>
<a id="tocsinitshutdownscript_create_0"></a>

```json
{}

```

init_shutdown_script_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|type|string|false|none|none|
|command|stringÂ¦null|false|none|none|
|script_text|stringÂ¦null|false|none|none|
|script|stringÂ¦null|false|none|none|
|when|string|false|none|none|
|enabled|boolean|false|none|none|
|timeout|integer|false|none|none|
|comment|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|type|COMMAND|
|type|SCRIPT|
|when|PREINIT|
|when|POSTINIT|
|when|SHUTDOWN|

<h2 id="tocS_initshutdownscript_update_1">initshutdownscript_update_1</h2>

<a id="schemainitshutdownscript_update_1"></a>
<a id="schema_initshutdownscript_update_1"></a>
<a id="tocSinitshutdownscript_update_1"></a>
<a id="tocsinitshutdownscript_update_1"></a>

```json
{}

```

init_shutdown_script_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|type|string|false|none|none|
|command|stringÂ¦null|false|none|none|
|script_text|stringÂ¦null|false|none|none|
|script|stringÂ¦null|false|none|none|
|when|string|false|none|none|
|enabled|boolean|false|none|none|
|timeout|integer|false|none|none|
|comment|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|type|COMMAND|
|type|SCRIPT|
|when|PREINIT|
|when|POSTINIT|
|when|SHUTDOWN|

<h2 id="tocS_interface_create_0">interface_create_0</h2>

<a id="schemainterface_create_0"></a>
<a id="schema_interface_create_0"></a>
<a id="tocSinterface_create_0"></a>
<a id="tocsinterface_create_0"></a>

```json
{}

```

interface_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|
|description|stringÂ¦null|false|none|none|
|type|string|false|none|none|
|disable_offload_capabilities|boolean|false|none|none|
|ipv4_dhcp|boolean|false|none|none|
|ipv6_auto|boolean|false|none|none|
|aliases|[object]|false|none|none|
|failover_critical|boolean|false|none|none|
|failover_group|integer|false|none|none|
|failover_vhid|integer|false|none|none|
|failover_aliases|[object]|false|none|none|
|failover_virtual_aliases|[object]|false|none|none|
|bridge_members|[any]|false|none|none|
|lag_protocol|string|false|none|none|
|lag_ports|[string]|false|none|none|
|vlan_parent_interface|string|false|none|none|
|vlan_tag|integer|false|none|none|
|vlan_pcp|integerÂ¦null|false|none|none|
|mtu|integerÂ¦null|false|none|none|
|options|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|type|BRIDGE|
|type|LINK_AGGREGATION|
|type|VLAN|
|lag_protocol|LACP|
|lag_protocol|FAILOVER|
|lag_protocol|LOADBALANCE|
|lag_protocol|ROUNDROBIN|
|lag_protocol|NONE|

<h2 id="tocS_interface_update_1">interface_update_1</h2>

<a id="schemainterface_update_1"></a>
<a id="schema_interface_update_1"></a>
<a id="tocSinterface_update_1"></a>
<a id="tocsinterface_update_1"></a>

```json
{}

```

interface_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|
|description|stringÂ¦null|false|none|none|
|disable_offload_capabilities|boolean|false|none|none|
|ipv4_dhcp|boolean|false|none|none|
|ipv6_auto|boolean|false|none|none|
|aliases|[object]|false|none|none|
|failover_critical|boolean|false|none|none|
|failover_group|integer|false|none|none|
|failover_vhid|integer|false|none|none|
|failover_aliases|[object]|false|none|none|
|failover_virtual_aliases|[object]|false|none|none|
|bridge_members|[any]|false|none|none|
|lag_protocol|string|false|none|none|
|lag_ports|[string]|false|none|none|
|vlan_parent_interface|string|false|none|none|
|vlan_tag|integer|false|none|none|
|vlan_pcp|integerÂ¦null|false|none|none|
|mtu|integerÂ¦null|false|none|none|
|options|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|lag_protocol|LACP|
|lag_protocol|FAILOVER|
|lag_protocol|LOADBALANCE|
|lag_protocol|ROUNDROBIN|
|lag_protocol|NONE|

<h2 id="tocS_interface_bridge_members_choices_0">interface_bridge_members_choices_0</h2>

<a id="schemainterface_bridge_members_choices_0"></a>
<a id="schema_interface_bridge_members_choices_0"></a>
<a id="tocSinterface_bridge_members_choices_0"></a>
<a id="tocsinterface_bridge_members_choices_0"></a>

```json
null

```

id

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|id|stringÂ¦null|false|none|none|

<h2 id="tocS_interface_choices_0">interface_choices_0</h2>

<a id="schemainterface_choices_0"></a>
<a id="schema_interface_choices_0"></a>
<a id="tocSinterface_choices_0"></a>
<a id="tocsinterface_choices_0"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|bridge_members|boolean|false|none|none|
|lag_ports|boolean|false|none|none|
|vlan_parent|boolean|false|none|none|
|exclude|[any]|false|none|none|
|exclude_types|[string]|false|none|none|
|include|[any]|false|none|none|

<h2 id="tocS_interface_commit_0">interface_commit_0</h2>

<a id="schemainterface_commit_0"></a>
<a id="schema_interface_commit_0"></a>
<a id="tocSinterface_commit_0"></a>
<a id="tocsinterface_commit_0"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|rollback|boolean|false|none|none|
|checkin_timeout|integer|false|none|none|

<h2 id="tocS_interface_ip_in_use_0">interface_ip_in_use_0</h2>

<a id="schemainterface_ip_in_use_0"></a>
<a id="schema_interface_ip_in_use_0"></a>
<a id="tocSinterface_ip_in_use_0"></a>
<a id="tocsinterface_ip_in_use_0"></a>

```json
{}

```

ips

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|ipv4|boolean|false|none|none|
|ipv6|boolean|false|none|none|
|ipv6_link_local|boolean|false|none|none|
|loopback|boolean|false|none|none|
|any|boolean|false|none|none|
|static|boolean|false|none|none|

<h2 id="tocS_interface_lag_ports_choices_0">interface_lag_ports_choices_0</h2>

<a id="schemainterface_lag_ports_choices_0"></a>
<a id="schema_interface_lag_ports_choices_0"></a>
<a id="tocSinterface_lag_ports_choices_0"></a>
<a id="tocsinterface_lag_ports_choices_0"></a>

```json
null

```

id

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|id|stringÂ¦null|false|none|none|

<h2 id="tocS_ipmi_update_1">ipmi_update_1</h2>

<a id="schemaipmi_update_1"></a>
<a id="schema_ipmi_update_1"></a>
<a id="tocSipmi_update_1"></a>
<a id="tocsipmi_update_1"></a>

```json
{}

```

ipmi

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|ipaddress|string|false|none|none|
|netmask|string|false|none|none|
|gateway|string|false|none|none|
|password|string|false|none|none|
|dhcp|boolean|false|none|none|
|vlan|integerÂ¦null|false|none|none|

<h2 id="tocS_ipmi_identify_0">ipmi_identify_0</h2>

<a id="schemaipmi_identify_0"></a>
<a id="schema_ipmi_identify_0"></a>
<a id="tocSipmi_identify_0"></a>
<a id="tocsipmi_identify_0"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|seconds|integer|false|none|none|
|force|boolean|false|none|none|

<h2 id="tocS_iscsi_auth_create_0">iscsi_auth_create_0</h2>

<a id="schemaiscsi_auth_create_0"></a>
<a id="schema_iscsi_auth_create_0"></a>
<a id="tocSiscsi_auth_create_0"></a>
<a id="tocsiscsi_auth_create_0"></a>

```json
{}

```

iscsi_auth_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|tag|integer|false|none|none|
|user|string|false|none|none|
|secret|string|false|none|none|
|peeruser|string|false|none|none|
|peersecret|string|false|none|none|

<h2 id="tocS_iscsi_auth_update_1">iscsi_auth_update_1</h2>

<a id="schemaiscsi_auth_update_1"></a>
<a id="schema_iscsi_auth_update_1"></a>
<a id="tocSiscsi_auth_update_1"></a>
<a id="tocsiscsi_auth_update_1"></a>

```json
{}

```

iscsi_auth_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|tag|integer|false|none|none|
|user|string|false|none|none|
|secret|string|false|none|none|
|peeruser|string|false|none|none|
|peersecret|string|false|none|none|

<h2 id="tocS_iscsi_extent_create_0">iscsi_extent_create_0</h2>

<a id="schemaiscsi_extent_create_0"></a>
<a id="schema_iscsi_extent_create_0"></a>
<a id="tocSiscsi_extent_create_0"></a>
<a id="tocsiscsi_extent_create_0"></a>

```json
{}

```

iscsi_extent_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|
|type|string|false|none|none|
|disk|stringÂ¦null|false|none|none|
|serial|stringÂ¦null|false|none|none|
|path|stringÂ¦null|false|none|none|
|filesize|integer|false|none|none|
|blocksize|integer|false|none|none|
|pblocksize|boolean|false|none|none|
|avail_threshold|integerÂ¦null|false|none|none|
|comment|string|false|none|none|
|insecure_tpc|boolean|false|none|none|
|xen|boolean|false|none|none|
|rpm|string|false|none|none|
|ro|boolean|false|none|none|
|enabled|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|type|DISK|
|type|FILE|
|rpm|UNKNOWN|
|rpm|SSD|
|rpm|5400|
|rpm|7200|
|rpm|10000|
|rpm|15000|

<h2 id="tocS_iscsi_extent_delete_1">iscsi_extent_delete_1</h2>

<a id="schemaiscsi_extent_delete_1"></a>
<a id="schema_iscsi_extent_delete_1"></a>
<a id="tocSiscsi_extent_delete_1"></a>
<a id="tocsiscsi_extent_delete_1"></a>

```json
false

```

remove

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|remove|boolean|false|none|none|

<h2 id="tocS_iscsi_extent_delete_2">iscsi_extent_delete_2</h2>

<a id="schemaiscsi_extent_delete_2"></a>
<a id="schema_iscsi_extent_delete_2"></a>
<a id="tocSiscsi_extent_delete_2"></a>
<a id="tocsiscsi_extent_delete_2"></a>

```json
false

```

force

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|force|boolean|false|none|none|

<h2 id="tocS_iscsi_extent_delete">iscsi_extent_delete</h2>

<a id="schemaiscsi_extent_delete"></a>
<a id="schema_iscsi_extent_delete"></a>
<a id="tocSiscsi_extent_delete"></a>
<a id="tocsiscsi_extent_delete"></a>

```json
{
  "remove": false,
  "force": false
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|remove|[iscsi_extent_delete_1](#schemaiscsi_extent_delete_1)|false|none|none|
|force|[iscsi_extent_delete_2](#schemaiscsi_extent_delete_2)|false|none|none|

<h2 id="tocS_iscsi_extent_update_1">iscsi_extent_update_1</h2>

<a id="schemaiscsi_extent_update_1"></a>
<a id="schema_iscsi_extent_update_1"></a>
<a id="tocSiscsi_extent_update_1"></a>
<a id="tocsiscsi_extent_update_1"></a>

```json
{}

```

iscsi_extent_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|
|type|string|false|none|none|
|disk|stringÂ¦null|false|none|none|
|serial|stringÂ¦null|false|none|none|
|path|stringÂ¦null|false|none|none|
|filesize|integer|false|none|none|
|blocksize|integer|false|none|none|
|pblocksize|boolean|false|none|none|
|avail_threshold|integerÂ¦null|false|none|none|
|comment|string|false|none|none|
|insecure_tpc|boolean|false|none|none|
|xen|boolean|false|none|none|
|rpm|string|false|none|none|
|ro|boolean|false|none|none|
|enabled|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|type|DISK|
|type|FILE|
|rpm|UNKNOWN|
|rpm|SSD|
|rpm|5400|
|rpm|7200|
|rpm|10000|
|rpm|15000|

<h2 id="tocS_iscsi_extent_disk_choices_0">iscsi_extent_disk_choices_0</h2>

<a id="schemaiscsi_extent_disk_choices_0"></a>
<a id="schema_iscsi_extent_disk_choices_0"></a>
<a id="tocSiscsi_extent_disk_choices_0"></a>
<a id="tocsiscsi_extent_disk_choices_0"></a>

```json
[]

```

exclude

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|exclude|[any]|false|none|none|

<h2 id="tocS_iscsi_global_update_0">iscsi_global_update_0</h2>

<a id="schemaiscsi_global_update_0"></a>
<a id="schema_iscsi_global_update_0"></a>
<a id="tocSiscsi_global_update_0"></a>
<a id="tocsiscsi_global_update_0"></a>

```json
{}

```

iscsiglobal_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|basename|string|false|none|none|
|isns_servers|[string]|false|none|none|
|pool_avail_threshold|integerÂ¦null|false|none|none|
|alua|boolean|false|none|none|

<h2 id="tocS_iscsi_initiator_create_0">iscsi_initiator_create_0</h2>

<a id="schemaiscsi_initiator_create_0"></a>
<a id="schema_iscsi_initiator_create_0"></a>
<a id="tocSiscsi_initiator_create_0"></a>
<a id="tocsiscsi_initiator_create_0"></a>

```json
{}

```

iscsi_initiator_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|initiators|[any]|false|none|none|
|auth_network|[string]|false|none|none|
|comment|string|false|none|none|

<h2 id="tocS_iscsi_initiator_update_1">iscsi_initiator_update_1</h2>

<a id="schemaiscsi_initiator_update_1"></a>
<a id="schema_iscsi_initiator_update_1"></a>
<a id="tocSiscsi_initiator_update_1"></a>
<a id="tocsiscsi_initiator_update_1"></a>

```json
{}

```

iscsi_initiator_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|initiators|[any]|false|none|none|
|auth_network|[string]|false|none|none|
|comment|string|false|none|none|

<h2 id="tocS_iscsi_portal_create_0">iscsi_portal_create_0</h2>

<a id="schemaiscsi_portal_create_0"></a>
<a id="schema_iscsi_portal_create_0"></a>
<a id="tocSiscsi_portal_create_0"></a>
<a id="tocsiscsi_portal_create_0"></a>

```json
{}

```

iscsiportal_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|comment|string|false|none|none|
|discovery_authmethod|string|false|none|none|
|discovery_authgroup|integerÂ¦null|false|none|none|
|listen|[object]|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|discovery_authmethod|NONE|
|discovery_authmethod|CHAP|
|discovery_authmethod|CHAP_MUTUAL|

<h2 id="tocS_iscsi_portal_update_1">iscsi_portal_update_1</h2>

<a id="schemaiscsi_portal_update_1"></a>
<a id="schema_iscsi_portal_update_1"></a>
<a id="tocSiscsi_portal_update_1"></a>
<a id="tocsiscsi_portal_update_1"></a>

```json
{}

```

iscsiportal_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|comment|string|false|none|none|
|discovery_authmethod|string|false|none|none|
|discovery_authgroup|integerÂ¦null|false|none|none|
|listen|[object]|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|discovery_authmethod|NONE|
|discovery_authmethod|CHAP|
|discovery_authmethod|CHAP_MUTUAL|

<h2 id="tocS_iscsi_target_create_0">iscsi_target_create_0</h2>

<a id="schemaiscsi_target_create_0"></a>
<a id="schema_iscsi_target_create_0"></a>
<a id="tocSiscsi_target_create_0"></a>
<a id="tocsiscsi_target_create_0"></a>

```json
{}

```

iscsi_target_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|
|alias|stringÂ¦null|false|none|none|
|mode|string|false|none|none|
|groups|[object]|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|mode|ISCSI|
|mode|FC|
|mode|BOTH|

<h2 id="tocS_iscsi_target_delete_1">iscsi_target_delete_1</h2>

<a id="schemaiscsi_target_delete_1"></a>
<a id="schema_iscsi_target_delete_1"></a>
<a id="tocSiscsi_target_delete_1"></a>
<a id="tocsiscsi_target_delete_1"></a>

```json
false

```

force

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|force|boolean|false|none|none|

<h2 id="tocS_iscsi_target_update_1">iscsi_target_update_1</h2>

<a id="schemaiscsi_target_update_1"></a>
<a id="schema_iscsi_target_update_1"></a>
<a id="tocSiscsi_target_update_1"></a>
<a id="tocsiscsi_target_update_1"></a>

```json
{}

```

iscsi_target_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|
|alias|stringÂ¦null|false|none|none|
|mode|string|false|none|none|
|groups|[object]|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|mode|ISCSI|
|mode|FC|
|mode|BOTH|

<h2 id="tocS_iscsi_targetextent_create_0">iscsi_targetextent_create_0</h2>

<a id="schemaiscsi_targetextent_create_0"></a>
<a id="schema_iscsi_targetextent_create_0"></a>
<a id="tocSiscsi_targetextent_create_0"></a>
<a id="tocsiscsi_targetextent_create_0"></a>

```json
{}

```

iscsi_targetextent_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|target|integer|false|none|none|
|lunid|integer|false|none|none|
|extent|integer|false|none|none|

<h2 id="tocS_iscsi_targetextent_delete_1">iscsi_targetextent_delete_1</h2>

<a id="schemaiscsi_targetextent_delete_1"></a>
<a id="schema_iscsi_targetextent_delete_1"></a>
<a id="tocSiscsi_targetextent_delete_1"></a>
<a id="tocsiscsi_targetextent_delete_1"></a>

```json
false

```

force

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|force|boolean|false|none|none|

<h2 id="tocS_iscsi_targetextent_update_1">iscsi_targetextent_update_1</h2>

<a id="schemaiscsi_targetextent_update_1"></a>
<a id="schema_iscsi_targetextent_update_1"></a>
<a id="tocSiscsi_targetextent_update_1"></a>
<a id="tocsiscsi_targetextent_update_1"></a>

```json
{}

```

iscsi_targetextent_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|target|integer|false|none|none|
|lunid|integer|false|none|none|
|extent|integer|false|none|none|

<h2 id="tocS_jail_create_0">jail_create_0</h2>

<a id="schemajail_create_0"></a>
<a id="schema_jail_create_0"></a>
<a id="tocSjail_create_0"></a>
<a id="tocsjail_create_0"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|release|string|false|none|none|
|template|string|false|none|none|
|pkglist|[string]|false|none|none|
|uuid|string|false|none|none|
|basejail|boolean|false|none|none|
|empty|boolean|false|none|none|
|short|boolean|false|none|none|
|props|[any]|false|none|none|
|https|boolean|false|none|none|

<h2 id="tocS_jail_delete_1">jail_delete_1</h2>

<a id="schemajail_delete_1"></a>
<a id="schema_jail_delete_1"></a>
<a id="tocSjail_delete_1"></a>
<a id="tocsjail_delete_1"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|force|boolean|false|none|none|

<h2 id="tocS_jail_update_1">jail_update_1</h2>

<a id="schemajail_update_1"></a>
<a id="schema_jail_update_1"></a>
<a id="tocSjail_update_1"></a>
<a id="tocsjail_update_1"></a>

```json
{}

```

jail_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|plugin|boolean|false|none|none|

<h2 id="tocS_jail_activate_0">jail_activate_0</h2>

<a id="schemajail_activate_0"></a>
<a id="schema_jail_activate_0"></a>
<a id="tocSjail_activate_0"></a>
<a id="tocsjail_activate_0"></a>

```json
"string"

```

pool

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|pool|string|false|none|none|

<h2 id="tocS_jail_clean_0">jail_clean_0</h2>

<a id="schemajail_clean_0"></a>
<a id="schema_jail_clean_0"></a>
<a id="tocSjail_clean_0"></a>
<a id="tocsjail_clean_0"></a>

```json
"ALL"

```

ds_type

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|ds_type|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|ds_type|ALL|
|ds_type|JAIL|
|ds_type|TEMPLATE|
|ds_type|RELEASE|

<h2 id="tocS_jail_clone_1">jail_clone_1</h2>

<a id="schemajail_clone_1"></a>
<a id="schema_jail_clone_1"></a>
<a id="tocSjail_clone_1"></a>
<a id="tocsjail_clone_1"></a>

```json
{}

```

clone_jail

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|uuid|string|false|none|none|
|pkglist|[string]|false|none|none|
|thickjail|boolean|false|none|none|
|props|[any]|false|none|none|

<h2 id="tocS_jail_exec_0">jail_exec_0</h2>

<a id="schemajail_exec_0"></a>
<a id="schema_jail_exec_0"></a>
<a id="tocSjail_exec_0"></a>
<a id="tocsjail_exec_0"></a>

```json
"string"

```

jail

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|jail|string|false|none|none|

<h2 id="tocS_jail_exec_1">jail_exec_1</h2>

<a id="schemajail_exec_1"></a>
<a id="schema_jail_exec_1"></a>
<a id="tocSjail_exec_1"></a>
<a id="tocsjail_exec_1"></a>

```json
[
  null
]

```

command

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|command|[any]|false|none|none|

<h2 id="tocS_jail_exec_2">jail_exec_2</h2>

<a id="schemajail_exec_2"></a>
<a id="schema_jail_exec_2"></a>
<a id="tocSjail_exec_2"></a>
<a id="tocsjail_exec_2"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|host_user|string|false|none|none|
|jail_user|string|false|none|none|

<h2 id="tocS_jail_exec">jail_exec</h2>

<a id="schemajail_exec"></a>
<a id="schema_jail_exec"></a>
<a id="tocSjail_exec"></a>
<a id="tocsjail_exec"></a>

```json
{
  "jail": "string",
  "command": [
    null
  ],
  "options": {}
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|jail|[jail_exec_0](#schemajail_exec_0)|false|none|none|
|command|[jail_exec_1](#schemajail_exec_1)|false|none|none|
|options|[jail_exec_2](#schemajail_exec_2)|false|none|none|

<h2 id="tocS_jail_export_0">jail_export_0</h2>

<a id="schemajail_export_0"></a>
<a id="schema_jail_export_0"></a>
<a id="tocSjail_export_0"></a>
<a id="tocsjail_export_0"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|jail|string|false|none|none|
|compression_algorithm|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|compression_algorithm|ZIP|
|compression_algorithm|LZMA|

<h2 id="tocS_jail_fetch_0">jail_fetch_0</h2>

<a id="schemajail_fetch_0"></a>
<a id="schema_jail_fetch_0"></a>
<a id="tocSjail_fetch_0"></a>
<a id="tocsjail_fetch_0"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|release|string|false|none|none|
|server|string|false|none|none|
|user|string|false|none|none|
|password|string|false|none|none|
|name|stringÂ¦null|false|none|none|
|jail_name|string|false|none|none|
|accept|boolean|false|none|none|
|https|boolean|false|none|none|
|props|[any]|false|none|none|
|files|[any]|false|none|none|
|branch|stringÂ¦null|false|none|none|

<h2 id="tocS_jail_fstab_0">jail_fstab_0</h2>

<a id="schemajail_fstab_0"></a>
<a id="schema_jail_fstab_0"></a>
<a id="tocSjail_fstab_0"></a>
<a id="tocsjail_fstab_0"></a>

```json
"string"

```

jail

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|jail|string|false|none|none|

<h2 id="tocS_jail_fstab_1">jail_fstab_1</h2>

<a id="schemajail_fstab_1"></a>
<a id="schema_jail_fstab_1"></a>
<a id="tocSjail_fstab_1"></a>
<a id="tocsjail_fstab_1"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|action|string|false|none|none|
|source|string|false|none|none|
|destination|string|false|none|none|
|fstype|string|false|none|none|
|fsoptions|string|false|none|none|
|dump|string|false|none|none|
|pass|string|false|none|none|
|index|integer|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|action|ADD|
|action|REMOVE|
|action|REPLACE|
|action|LIST|

<h2 id="tocS_jail_fstab">jail_fstab</h2>

<a id="schemajail_fstab"></a>
<a id="schema_jail_fstab"></a>
<a id="tocSjail_fstab"></a>
<a id="tocsjail_fstab"></a>

```json
{
  "jail": "string",
  "options": {}
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|jail|[jail_fstab_0](#schemajail_fstab_0)|false|none|none|
|options|[jail_fstab_1](#schemajail_fstab_1)|false|none|none|

<h2 id="tocS_jail_import_image_0">jail_import_image_0</h2>

<a id="schemajail_import_image_0"></a>
<a id="schema_jail_import_image_0"></a>
<a id="tocSjail_import_image_0"></a>
<a id="tocsjail_import_image_0"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|jail|string|false|none|none|
|path|stringÂ¦null|false|none|none|
|compression_algorithm|stringÂ¦null|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|compression_algorithm|ZIP|
|compression_algorithm|LZMA|
|compression_algorithm|null|

<h2 id="tocS_jail_rc_action_0">jail_rc_action_0</h2>

<a id="schemajail_rc_action_0"></a>
<a id="schema_jail_rc_action_0"></a>
<a id="tocSjail_rc_action_0"></a>
<a id="tocsjail_rc_action_0"></a>

```json
"START"

```

action

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|action|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|action|START|
|action|STOP|
|action|RESTART|

<h2 id="tocS_jail_releases_choices_0">jail_releases_choices_0</h2>

<a id="schemajail_releases_choices_0"></a>
<a id="schema_jail_releases_choices_0"></a>
<a id="tocSjail_releases_choices_0"></a>
<a id="tocsjail_releases_choices_0"></a>

```json
false

```

remote

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|remote|boolean|false|none|none|

<h2 id="tocS_jail_restart_0">jail_restart_0</h2>

<a id="schemajail_restart_0"></a>
<a id="schema_jail_restart_0"></a>
<a id="tocSjail_restart_0"></a>
<a id="tocsjail_restart_0"></a>

```json
"string"

```

jail

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|jail|string|false|none|none|

<h2 id="tocS_jail_start_0">jail_start_0</h2>

<a id="schemajail_start_0"></a>
<a id="schema_jail_start_0"></a>
<a id="tocSjail_start_0"></a>
<a id="tocsjail_start_0"></a>

```json
"string"

```

jail

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|jail|string|false|none|none|

<h2 id="tocS_jail_stop_0">jail_stop_0</h2>

<a id="schemajail_stop_0"></a>
<a id="schema_jail_stop_0"></a>
<a id="tocSjail_stop_0"></a>
<a id="tocsjail_stop_0"></a>

```json
"string"

```

jail

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|jail|string|false|none|none|

<h2 id="tocS_jail_stop_1">jail_stop_1</h2>

<a id="schemajail_stop_1"></a>
<a id="schema_jail_stop_1"></a>
<a id="tocSjail_stop_1"></a>
<a id="tocsjail_stop_1"></a>

```json
false

```

force

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|force|boolean|false|none|none|

<h2 id="tocS_jail_stop">jail_stop</h2>

<a id="schemajail_stop"></a>
<a id="schema_jail_stop"></a>
<a id="tocSjail_stop"></a>
<a id="tocsjail_stop"></a>

```json
{
  "jail": "string",
  "force": false
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|jail|[jail_stop_0](#schemajail_stop_0)|false|none|none|
|force|[jail_stop_1](#schemajail_stop_1)|false|none|none|

<h2 id="tocS_jail_update_defaults_0">jail_update_defaults_0</h2>

<a id="schemajail_update_defaults_0"></a>
<a id="schema_jail_update_defaults_0"></a>
<a id="tocSjail_update_defaults_0"></a>
<a id="tocsjail_update_defaults_0"></a>

```json
{}

```

props

### Properties

*None*

<h2 id="tocS_jail_update_to_latest_patch_0">jail_update_to_latest_patch_0</h2>

<a id="schemajail_update_to_latest_patch_0"></a>
<a id="schema_jail_update_to_latest_patch_0"></a>
<a id="tocSjail_update_to_latest_patch_0"></a>
<a id="tocsjail_update_to_latest_patch_0"></a>

```json
"string"

```

jail

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|jail|string|false|none|none|

<h2 id="tocS_jail_update_to_latest_patch_1">jail_update_to_latest_patch_1</h2>

<a id="schemajail_update_to_latest_patch_1"></a>
<a id="schema_jail_update_to_latest_patch_1"></a>
<a id="tocSjail_update_to_latest_patch_1"></a>
<a id="tocsjail_update_to_latest_patch_1"></a>

```json
false

```

update_pkgs

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|update_pkgs|boolean|false|none|none|

<h2 id="tocS_jail_update_to_latest_patch">jail_update_to_latest_patch</h2>

<a id="schemajail_update_to_latest_patch"></a>
<a id="schema_jail_update_to_latest_patch"></a>
<a id="tocSjail_update_to_latest_patch"></a>
<a id="tocsjail_update_to_latest_patch"></a>

```json
{
  "jail": "string",
  "update_pkgs": false
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|jail|[jail_update_to_latest_patch_0](#schemajail_update_to_latest_patch_0)|false|none|none|
|update_pkgs|[jail_update_to_latest_patch_1](#schemajail_update_to_latest_patch_1)|false|none|none|

<h2 id="tocS_kerberos_update_0">kerberos_update_0</h2>

<a id="schemakerberos_update_0"></a>
<a id="schema_kerberos_update_0"></a>
<a id="tocSkerberos_update_0"></a>
<a id="tocskerberos_update_0"></a>

```json
{}

```

kerberos_settings_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|appdefaults_aux|string|false|none|none|
|libdefaults_aux|string|false|none|none|

<h2 id="tocS_kerberos_keytab_create_0">kerberos_keytab_create_0</h2>

<a id="schemakerberos_keytab_create_0"></a>
<a id="schema_kerberos_keytab_create_0"></a>
<a id="tocSkerberos_keytab_create_0"></a>
<a id="tocskerberos_keytab_create_0"></a>

```json
{}

```

kerberos_keytab_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|file|string|false|none|none|
|name|string|false|none|none|

<h2 id="tocS_kerberos_keytab_update_1">kerberos_keytab_update_1</h2>

<a id="schemakerberos_keytab_update_1"></a>
<a id="schema_kerberos_keytab_update_1"></a>
<a id="tocSkerberos_keytab_update_1"></a>
<a id="tocskerberos_keytab_update_1"></a>

```json
{}

```

kerberos_keytab_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|file|string|false|none|none|
|name|string|false|none|none|

<h2 id="tocS_kerberos_realm_create_0">kerberos_realm_create_0</h2>

<a id="schemakerberos_realm_create_0"></a>
<a id="schema_kerberos_realm_create_0"></a>
<a id="tocSkerberos_realm_create_0"></a>
<a id="tocskerberos_realm_create_0"></a>

```json
{}

```

kerberos_realm_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|realm|string|false|none|none|
|kdc|[any]|false|none|none|
|admin_server|[any]|false|none|none|
|kpasswd_server|[any]|false|none|none|

<h2 id="tocS_kerberos_realm_update_1">kerberos_realm_update_1</h2>

<a id="schemakerberos_realm_update_1"></a>
<a id="schema_kerberos_realm_update_1"></a>
<a id="tocSkerberos_realm_update_1"></a>
<a id="tocskerberos_realm_update_1"></a>

```json
{}

```

kerberos_realm_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|realm|string|false|none|none|
|kdc|[any]|false|none|none|
|admin_server|[any]|false|none|none|
|kpasswd_server|[any]|false|none|none|

<h2 id="tocS_keychaincredential_create_0">keychaincredential_create_0</h2>

<a id="schemakeychaincredential_create_0"></a>
<a id="schema_keychaincredential_create_0"></a>
<a id="tocSkeychaincredential_create_0"></a>
<a id="tocskeychaincredential_create_0"></a>

```json
{}

```

keychain_credential_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|
|type|string|false|none|none|
|attributes|object|false|none|none|

<h2 id="tocS_keychaincredential_delete_1">keychaincredential_delete_1</h2>

<a id="schemakeychaincredential_delete_1"></a>
<a id="schema_keychaincredential_delete_1"></a>
<a id="tocSkeychaincredential_delete_1"></a>
<a id="tocskeychaincredential_delete_1"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|cascade|boolean|false|none|none|

<h2 id="tocS_keychaincredential_update_1">keychaincredential_update_1</h2>

<a id="schemakeychaincredential_update_1"></a>
<a id="schema_keychaincredential_update_1"></a>
<a id="tocSkeychaincredential_update_1"></a>
<a id="tocskeychaincredential_update_1"></a>

```json
{}

```

keychain_credential_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|
|attributes|object|false|none|none|

<h2 id="tocS_keychaincredential_remote_ssh_host_key_scan_0">keychaincredential_remote_ssh_host_key_scan_0</h2>

<a id="schemakeychaincredential_remote_ssh_host_key_scan_0"></a>
<a id="schema_keychaincredential_remote_ssh_host_key_scan_0"></a>
<a id="tocSkeychaincredential_remote_ssh_host_key_scan_0"></a>
<a id="tocskeychaincredential_remote_ssh_host_key_scan_0"></a>

```json
{}

```

keychain_remote_ssh_host_key_scan

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|host|string|false|none|none|
|port|string|false|none|none|
|connect_timeout|integer|false|none|none|

<h2 id="tocS_keychaincredential_remote_ssh_semiautomatic_setup_0">keychaincredential_remote_ssh_semiautomatic_setup_0</h2>

<a id="schemakeychaincredential_remote_ssh_semiautomatic_setup_0"></a>
<a id="schema_keychaincredential_remote_ssh_semiautomatic_setup_0"></a>
<a id="tocSkeychaincredential_remote_ssh_semiautomatic_setup_0"></a>
<a id="tocskeychaincredential_remote_ssh_semiautomatic_setup_0"></a>

```json
{}

```

keychain_remote_ssh_semiautomatic_setup

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|
|url|string|false|none|none|
|token|string|false|none|none|
|password|string|false|none|none|
|username|string|false|none|none|
|private_key|integer|false|none|none|
|cipher|string|false|none|none|
|connect_timeout|integer|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|cipher|STANDARD|
|cipher|FAST|
|cipher|DISABLED|

<h2 id="tocS_keychaincredential_used_by_0">keychaincredential_used_by_0</h2>

<a id="schemakeychaincredential_used_by_0"></a>
<a id="schema_keychaincredential_used_by_0"></a>
<a id="tocSkeychaincredential_used_by_0"></a>
<a id="tocskeychaincredential_used_by_0"></a>

```json
0

```

id

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|id|integer|false|none|none|

<h2 id="tocS_kmip_update_0">kmip_update_0</h2>

<a id="schemakmip_update_0"></a>
<a id="schema_kmip_update_0"></a>
<a id="tocSkmip_update_0"></a>
<a id="tocskmip_update_0"></a>

```json
{}

```

kmip_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|enabled|boolean|false|none|none|
|force_clear|boolean|false|none|none|
|manage_sed_disks|boolean|false|none|none|
|manage_zfs_keys|boolean|false|none|none|
|change_server|boolean|false|none|none|
|validate|boolean|false|none|none|
|certificate|integerÂ¦null|false|none|none|
|certificate_authority|integerÂ¦null|false|none|none|
|port|integer|false|none|none|
|server|string|false|none|none|

<h2 id="tocS_ldap_update_0">ldap_update_0</h2>

<a id="schemaldap_update_0"></a>
<a id="schema_ldap_update_0"></a>
<a id="tocSldap_update_0"></a>
<a id="tocsldap_update_0"></a>

```json
{}

```

ldap_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|hostname|[any]|false|none|none|
|basedn|string|false|none|none|
|binddn|string|false|none|none|
|bindpw|string|false|none|none|
|anonbind|boolean|false|none|none|
|ssl|string|false|none|none|
|certificate|integerÂ¦null|false|none|none|
|validate_certificates|boolean|false|none|none|
|disable_freenas_cache|boolean|false|none|none|
|timeout|integer|false|none|none|
|dns_timeout|integer|false|none|none|
|kerberos_realm|integerÂ¦null|false|none|none|
|kerberos_principal|string|false|none|none|
|has_samba_schema|boolean|false|none|none|
|auxiliary_parameters|string|false|none|none|
|schema|string|false|none|none|
|enable|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|ssl|OFF|
|ssl|ON|
|ssl|START_TLS|
|schema|RFC2307|
|schema|RFC2307BIS|

<h2 id="tocS_lldp_update_0">lldp_update_0</h2>

<a id="schemalldp_update_0"></a>
<a id="schema_lldp_update_0"></a>
<a id="tocSlldp_update_0"></a>
<a id="tocslldp_update_0"></a>

```json
{}

```

lldp_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|intdesc|boolean|false|none|none|
|country|string|false|none|none|
|location|string|false|none|none|

<h2 id="tocS_mail_update_0">mail_update_0</h2>

<a id="schemamail_update_0"></a>
<a id="schema_mail_update_0"></a>
<a id="tocSmail_update_0"></a>
<a id="tocsmail_update_0"></a>

```json
{}

```

mail_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|fromemail|string|false|none|none|
|fromname|string|false|none|none|
|outgoingserver|string|false|none|none|
|port|integer|false|none|none|
|security|string|false|none|none|
|smtp|boolean|false|none|none|
|user|string|false|none|none|
|pass|string|false|none|none|
|oauth|object|false|none|none|
|Â» client_id|string|false|none|none|
|Â» client_secret|string|false|none|none|
|Â» access_token|string|false|none|none|
|Â» refresh_token|string|false|none|none|
|Â» token_uri|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|security|PLAIN|
|security|SSL|
|security|TLS|

<h2 id="tocS_mail_send_0">mail_send_0</h2>

<a id="schemamail_send_0"></a>
<a id="schema_mail_send_0"></a>
<a id="tocSmail_send_0"></a>
<a id="tocsmail_send_0"></a>

```json
{}

```

mail_message

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|subject|string|false|none|none|
|text|string|false|none|none|
|html|stringÂ¦null|false|none|none|
|to|[string]|false|none|none|
|cc|[string]|false|none|none|
|interval|integerÂ¦null|false|none|none|
|channel|stringÂ¦null|false|none|none|
|timeout|integer|false|none|none|
|attachments|boolean|false|none|none|
|queue|boolean|false|none|none|
|extra_headers|object|false|none|none|

<h2 id="tocS_mail_send_1">mail_send_1</h2>

<a id="schemamail_send_1"></a>
<a id="schema_mail_send_1"></a>
<a id="tocSmail_send_1"></a>
<a id="tocsmail_send_1"></a>

```json
{}

```

mail_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|fromemail|string|false|none|none|
|fromname|string|false|none|none|
|outgoingserver|string|false|none|none|
|port|integer|false|none|none|
|security|string|false|none|none|
|smtp|boolean|false|none|none|
|user|string|false|none|none|
|pass|string|false|none|none|
|oauth|object|false|none|none|
|Â» client_id|string|false|none|none|
|Â» client_secret|string|false|none|none|
|Â» access_token|string|false|none|none|
|Â» refresh_token|string|false|none|none|
|Â» token_uri|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|security|PLAIN|
|security|SSL|
|security|TLS|

<h2 id="tocS_mail_send">mail_send</h2>

<a id="schemamail_send"></a>
<a id="schema_mail_send"></a>
<a id="tocSmail_send"></a>
<a id="tocsmail_send"></a>

```json
{
  "mail_message": {},
  "mail_update": {}
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|mail_message|[mail_send_0](#schemamail_send_0)|false|none|none|
|mail_update|[mail_send_1](#schemamail_send_1)|false|none|none|

<h2 id="tocS_network_configuration_update_0">network_configuration_update_0</h2>

<a id="schemanetwork_configuration_update_0"></a>
<a id="schema_network_configuration_update_0"></a>
<a id="tocSnetwork_configuration_update_0"></a>
<a id="tocsnetwork_configuration_update_0"></a>

```json
{}

```

global_configuration_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|hostname|string|false|none|none|
|hostname_b|string|false|none|none|
|hostname_virtual|string|false|none|none|
|domain|string|false|none|none|
|domains|[string]|false|none|none|
|service_announcement|object|false|none|none|
|Â» netbios|boolean|false|none|none|
|Â» mdns|boolean|false|none|none|
|Â» wsd|boolean|false|none|none|
|ipv4gateway|string|false|none|none|
|ipv6gateway|string|false|none|none|
|nameserver1|string|false|none|none|
|nameserver2|string|false|none|none|
|nameserver3|string|false|none|none|
|httpproxy|string|false|none|none|
|netwait_enabled|boolean|false|none|none|
|netwait_ip|[string]|false|none|none|
|hosts|string|false|none|none|

<h2 id="tocS_nfs_update_0">nfs_update_0</h2>

<a id="schemanfs_update_0"></a>
<a id="schema_nfs_update_0"></a>
<a id="tocSnfs_update_0"></a>
<a id="tocsnfs_update_0"></a>

```json
{}

```

nfs_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|servers|integer|false|none|none|
|udp|boolean|false|none|none|
|allow_nonroot|boolean|false|none|none|
|v4|boolean|false|none|none|
|v4_v3owner|boolean|false|none|none|
|v4_krb|boolean|false|none|none|
|v4_domain|string|false|none|none|
|bindip|[string]|false|none|none|
|mountd_port|integerÂ¦null|false|none|none|
|rpcstatd_port|integerÂ¦null|false|none|none|
|rpclockd_port|integerÂ¦null|false|none|none|
|userd_manage_gids|boolean|false|none|none|
|mountd_log|boolean|false|none|none|
|statd_lockd_log|boolean|false|none|none|

<h2 id="tocS_nis_update_0">nis_update_0</h2>

<a id="schemanis_update_0"></a>
<a id="schema_nis_update_0"></a>
<a id="tocSnis_update_0"></a>
<a id="tocsnis_update_0"></a>

```json
{}

```

nis_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|domain|string|false|none|none|
|servers|[any]|false|none|none|
|secure_mode|boolean|false|none|none|
|manycast|boolean|false|none|none|
|enable|boolean|false|none|none|

<h2 id="tocS_openvpn_client_update_0">openvpn_client_update_0</h2>

<a id="schemaopenvpn_client_update_0"></a>
<a id="schema_openvpn_client_update_0"></a>
<a id="tocSopenvpn_client_update_0"></a>
<a id="tocsopenvpn_client_update_0"></a>

```json
{}

```

openvpn_client_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|nobind|boolean|false|none|none|
|tls_crypt_auth_enabled|boolean|false|none|none|
|client_certificate|integer|false|none|none|
|root_ca|integer|false|none|none|
|port|integer|false|none|none|
|additional_parameters|string|false|none|none|
|authentication_algorithm|stringÂ¦null|false|none|none|
|cipher|stringÂ¦null|false|none|none|
|compression|stringÂ¦null|false|none|none|
|device_type|string|false|none|none|
|protocol|string|false|none|none|
|remote|string|false|none|none|
|tls_crypt_auth|stringÂ¦null|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|compression|LZO|
|compression|LZ4|
|device_type|TUN|
|device_type|TAP|
|protocol|UDP|
|protocol|TCP|

<h2 id="tocS_openvpn_server_update_0">openvpn_server_update_0</h2>

<a id="schemaopenvpn_server_update_0"></a>
<a id="schema_openvpn_server_update_0"></a>
<a id="tocSopenvpn_server_update_0"></a>
<a id="tocsopenvpn_server_update_0"></a>

```json
{}

```

openvpn_server_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|tls_crypt_auth_enabled|boolean|false|none|none|
|netmask|integer|false|none|none|
|server_certificate|integer|false|none|none|
|port|integer|false|none|none|
|root_ca|integer|false|none|none|
|server|string|false|none|none|
|additional_parameters|string|false|none|none|
|authentication_algorithm|stringÂ¦null|false|none|none|
|cipher|stringÂ¦null|false|none|none|
|compression|stringÂ¦null|false|none|none|
|device_type|string|false|none|none|
|protocol|string|false|none|none|
|tls_crypt_auth|stringÂ¦null|false|none|none|
|topology|stringÂ¦null|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|compression|LZO|
|compression|LZ4|
|device_type|TUN|
|device_type|TAP|
|protocol|UDP|
|protocol|TCP|
|topology|NET30|
|topology|P2P|
|topology|SUBNET|

<h2 id="tocS_openvpn_server_client_configuration_generation_0">openvpn_server_client_configuration_generation_0</h2>

<a id="schemaopenvpn_server_client_configuration_generation_0"></a>
<a id="schema_openvpn_server_client_configuration_generation_0"></a>
<a id="tocSopenvpn_server_client_configuration_generation_0"></a>
<a id="tocsopenvpn_server_client_configuration_generation_0"></a>

```json
0

```

client_certificate_id

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|client_certificate_id|integer|false|none|none|

<h2 id="tocS_openvpn_server_client_configuration_generation_1">openvpn_server_client_configuration_generation_1</h2>

<a id="schemaopenvpn_server_client_configuration_generation_1"></a>
<a id="schema_openvpn_server_client_configuration_generation_1"></a>
<a id="tocSopenvpn_server_client_configuration_generation_1"></a>
<a id="tocsopenvpn_server_client_configuration_generation_1"></a>

```json
"string"

```

server_address

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|server_address|stringÂ¦null|false|none|none|

<h2 id="tocS_openvpn_server_client_configuration_generation">openvpn_server_client_configuration_generation</h2>

<a id="schemaopenvpn_server_client_configuration_generation"></a>
<a id="schema_openvpn_server_client_configuration_generation"></a>
<a id="tocSopenvpn_server_client_configuration_generation"></a>
<a id="tocsopenvpn_server_client_configuration_generation"></a>

```json
{
  "client_certificate_id": 0,
  "server_address": "string"
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|client_certificate_id|[openvpn_server_client_configuration_generation_0](#schemaopenvpn_server_client_configuration_generation_0)|false|none|none|
|server_address|[openvpn_server_client_configuration_generation_1](#schemaopenvpn_server_client_configuration_generation_1)|false|none|none|

<h2 id="tocS_plugin_create_0">plugin_create_0</h2>

<a id="schemaplugin_create_0"></a>
<a id="schema_plugin_create_0"></a>
<a id="tocSplugin_create_0"></a>
<a id="tocsplugin_create_0"></a>

```json
{}

```

plugin_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|plugin_name|string|false|none|none|
|jail_name|string|false|none|none|
|props|[any]|false|none|none|
|branch|stringÂ¦null|false|none|none|
|plugin_repository|string|false|none|none|

<h2 id="tocS_plugin_update_1">plugin_update_1</h2>

<a id="schemaplugin_update_1"></a>
<a id="schema_plugin_update_1"></a>
<a id="tocSplugin_update_1"></a>
<a id="tocsplugin_update_1"></a>

```json
{}

```

jail_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|plugin|boolean|false|none|none|

<h2 id="tocS_plugin_available_0">plugin_available_0</h2>

<a id="schemaplugin_available_0"></a>
<a id="schema_plugin_available_0"></a>
<a id="tocSplugin_available_0"></a>
<a id="tocsplugin_available_0"></a>

```json
{}

```

available_plugin_options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|cache|boolean|false|none|none|
|plugin_repository|string|false|none|none|
|branch|string|false|none|none|

<h2 id="tocS_plugin_branches_choices_0">plugin_branches_choices_0</h2>

<a id="schemaplugin_branches_choices_0"></a>
<a id="schema_plugin_branches_choices_0"></a>
<a id="tocSplugin_branches_choices_0"></a>
<a id="tocsplugin_branches_choices_0"></a>

```json
null

```

repository

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|repository|stringÂ¦null|false|none|none|

<h2 id="tocS_plugin_defaults_0">plugin_defaults_0</h2>

<a id="schemaplugin_defaults_0"></a>
<a id="schema_plugin_defaults_0"></a>
<a id="tocSplugin_defaults_0"></a>
<a id="tocsplugin_defaults_0"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|refresh|boolean|false|none|none|
|plugin|string|false|none|none|
|branch|stringÂ¦null|false|none|none|
|plugin_repository|string|false|none|none|

<h2 id="tocS_pool_create_0">pool_create_0</h2>

<a id="schemapool_create_0"></a>
<a id="schema_pool_create_0"></a>
<a id="tocSpool_create_0"></a>
<a id="tocspool_create_0"></a>

```json
{}

```

pool_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|
|encryption|boolean|false|none|none|
|deduplication|stringÂ¦null|false|none|none|
|encryption_options|object|false|none|none|
|Â» generate_key|boolean|false|none|none|
|Â» pbkdf2iters|integer|false|none|none|
|Â» algorithm|string|false|none|none|
|Â» passphrase|stringÂ¦null|false|none|none|
|Â» key|stringÂ¦null|false|none|none|
|topology|object|false|none|none|
|Â» data|[object]|false|none|none|
|Â» special|[object]|false|none|none|
|Â» dedup|[object]|false|none|none|
|Â» cache|[object]|false|none|none|
|Â» log|[object]|false|none|none|
|Â» spares|[string]|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|deduplication|null|
|deduplication|ON|
|deduplication|VERIFY|
|deduplication|OFF|
|algorithm|AES-128-CCM|
|algorithm|AES-192-CCM|
|algorithm|AES-256-CCM|
|algorithm|AES-128-GCM|
|algorithm|AES-192-GCM|
|algorithm|AES-256-GCM|

<h2 id="tocS_pool_update_1">pool_update_1</h2>

<a id="schemapool_update_1"></a>
<a id="schema_pool_update_1"></a>
<a id="tocSpool_update_1"></a>
<a id="tocspool_update_1"></a>

```json
{}

```

pool_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|encryption_options|object|false|none|none|
|Â» generate_key|boolean|false|none|none|
|Â» pbkdf2iters|integer|false|none|none|
|Â» algorithm|string|false|none|none|
|Â» passphrase|stringÂ¦null|false|none|none|
|Â» key|stringÂ¦null|false|none|none|
|topology|object|false|none|none|
|Â» data|[object]|false|none|none|
|Â» special|[object]|false|none|none|
|Â» dedup|[object]|false|none|none|
|Â» cache|[object]|false|none|none|
|Â» log|[object]|false|none|none|
|Â» spares|[string]|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|algorithm|AES-128-CCM|
|algorithm|AES-192-CCM|
|algorithm|AES-256-CCM|
|algorithm|AES-128-GCM|
|algorithm|AES-192-GCM|
|algorithm|AES-256-GCM|

<h2 id="tocS_pool_attach_0">pool_attach_0</h2>

<a id="schemapool_attach_0"></a>
<a id="schema_pool_attach_0"></a>
<a id="tocSpool_attach_0"></a>
<a id="tocspool_attach_0"></a>

```json
0

```

oid

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|oid|integer|false|none|none|

<h2 id="tocS_pool_attach_1">pool_attach_1</h2>

<a id="schemapool_attach_1"></a>
<a id="schema_pool_attach_1"></a>
<a id="tocSpool_attach_1"></a>
<a id="tocspool_attach_1"></a>

```json
{}

```

pool_attach

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|target_vdev|string|false|none|none|
|new_disk|string|false|none|none|
|passphrase|string|false|none|none|

<h2 id="tocS_pool_attach">pool_attach</h2>

<a id="schemapool_attach"></a>
<a id="schema_pool_attach"></a>
<a id="tocSpool_attach"></a>
<a id="tocspool_attach"></a>

```json
{
  "oid": 0,
  "pool_attach": {}
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|oid|[pool_attach_0](#schemapool_attach_0)|false|none|none|
|pool_attach|[pool_attach_1](#schemapool_attach_1)|false|none|none|

<h2 id="tocS_pool_attachments">pool_attachments</h2>

<a id="schemapool_attachments"></a>
<a id="schema_pool_attachments"></a>
<a id="tocSpool_attachments"></a>
<a id="tocspool_attachments"></a>

```json
{}

```

### Properties

*None*

<h2 id="tocS_pool_detach_1">pool_detach_1</h2>

<a id="schemapool_detach_1"></a>
<a id="schema_pool_detach_1"></a>
<a id="tocSpool_detach_1"></a>
<a id="tocspool_detach_1"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|label|string|false|none|none|

<h2 id="tocS_pool_download_encryption_key_1">pool_download_encryption_key_1</h2>

<a id="schemapool_download_encryption_key_1"></a>
<a id="schema_pool_download_encryption_key_1"></a>
<a id="tocSpool_download_encryption_key_1"></a>
<a id="tocspool_download_encryption_key_1"></a>

```json
"geli.key"

```

filename

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|filename|string|false|none|none|

<h2 id="tocS_pool_expand_1">pool_expand_1</h2>

<a id="schemapool_expand_1"></a>
<a id="schema_pool_expand_1"></a>
<a id="tocSpool_expand_1"></a>
<a id="tocspool_expand_1"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|geli|object|false|none|none|
|Â» passphrase|string|false|none|none|

<h2 id="tocS_pool_export_1">pool_export_1</h2>

<a id="schemapool_export_1"></a>
<a id="schema_pool_export_1"></a>
<a id="tocSpool_export_1"></a>
<a id="tocspool_export_1"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|cascade|boolean|false|none|none|
|restart_services|boolean|false|none|none|
|destroy|boolean|false|none|none|

<h2 id="tocS_pool_filesystem_choices_0">pool_filesystem_choices_0</h2>

<a id="schemapool_filesystem_choices_0"></a>
<a id="schema_pool_filesystem_choices_0"></a>
<a id="tocSpool_filesystem_choices_0"></a>
<a id="tocspool_filesystem_choices_0"></a>

```json
[
  "FILESYSTEM",
  "VOLUME"
]

```

types

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|types|[string]|false|none|none|

<h2 id="tocS_pool_get_disks">pool_get_disks</h2>

<a id="schemapool_get_disks"></a>
<a id="schema_pool_get_disks"></a>
<a id="tocSpool_get_disks"></a>
<a id="tocspool_get_disks"></a>

```json
{}

```

### Properties

*None*

<h2 id="tocS_pool_import_disk_0">pool_import_disk_0</h2>

<a id="schemapool_import_disk_0"></a>
<a id="schema_pool_import_disk_0"></a>
<a id="tocSpool_import_disk_0"></a>
<a id="tocspool_import_disk_0"></a>

```json
"string"

```

device

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|device|string|false|none|none|

<h2 id="tocS_pool_import_disk_1">pool_import_disk_1</h2>

<a id="schemapool_import_disk_1"></a>
<a id="schema_pool_import_disk_1"></a>
<a id="tocSpool_import_disk_1"></a>
<a id="tocspool_import_disk_1"></a>

```json
"string"

```

fs_type

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|fs_type|string|false|none|none|

<h2 id="tocS_pool_import_disk_2">pool_import_disk_2</h2>

<a id="schemapool_import_disk_2"></a>
<a id="schema_pool_import_disk_2"></a>
<a id="tocSpool_import_disk_2"></a>
<a id="tocspool_import_disk_2"></a>

```json
{}

```

fs_options

### Properties

*None*

<h2 id="tocS_pool_import_disk_3">pool_import_disk_3</h2>

<a id="schemapool_import_disk_3"></a>
<a id="schema_pool_import_disk_3"></a>
<a id="tocSpool_import_disk_3"></a>
<a id="tocspool_import_disk_3"></a>

```json
"string"

```

dst_path

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|dst_path|string|false|none|none|

<h2 id="tocS_pool_import_disk">pool_import_disk</h2>

<a id="schemapool_import_disk"></a>
<a id="schema_pool_import_disk"></a>
<a id="tocSpool_import_disk"></a>
<a id="tocspool_import_disk"></a>

```json
{
  "device": "string",
  "fs_type": "string",
  "fs_options": {},
  "dst_path": "string"
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|device|[pool_import_disk_0](#schemapool_import_disk_0)|false|none|none|
|fs_type|[pool_import_disk_1](#schemapool_import_disk_1)|false|none|none|
|fs_options|[pool_import_disk_2](#schemapool_import_disk_2)|false|none|none|
|dst_path|[pool_import_disk_3](#schemapool_import_disk_3)|false|none|none|

<h2 id="tocS_pool_import_disk_autodetect_fs_type_0">pool_import_disk_autodetect_fs_type_0</h2>

<a id="schemapool_import_disk_autodetect_fs_type_0"></a>
<a id="schema_pool_import_disk_autodetect_fs_type_0"></a>
<a id="tocSpool_import_disk_autodetect_fs_type_0"></a>
<a id="tocspool_import_disk_autodetect_fs_type_0"></a>

```json
"string"

```

device

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|device|string|false|none|none|

<h2 id="tocS_pool_import_pool_0">pool_import_pool_0</h2>

<a id="schemapool_import_pool_0"></a>
<a id="schema_pool_import_pool_0"></a>
<a id="tocSpool_import_pool_0"></a>
<a id="tocspool_import_pool_0"></a>

```json
{}

```

pool_import

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|guid|string|false|none|none|
|name|string|false|none|none|
|passphrase|string|false|none|none|
|enable_attachments|boolean|false|none|none|

<h2 id="tocS_pool_is_upgraded">pool_is_upgraded</h2>

<a id="schemapool_is_upgraded"></a>
<a id="schema_pool_is_upgraded"></a>
<a id="tocSpool_is_upgraded"></a>
<a id="tocspool_is_upgraded"></a>

```json
{}

```

### Properties

*None*

<h2 id="tocS_pool_lock_1">pool_lock_1</h2>

<a id="schemapool_lock_1"></a>
<a id="schema_pool_lock_1"></a>
<a id="tocSpool_lock_1"></a>
<a id="tocspool_lock_1"></a>

```json
"string"

```

passphrase

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|passphrase|string|false|none|none|

<h2 id="tocS_pool_offline_1">pool_offline_1</h2>

<a id="schemapool_offline_1"></a>
<a id="schema_pool_offline_1"></a>
<a id="tocSpool_offline_1"></a>
<a id="tocspool_offline_1"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|label|string|false|none|none|

<h2 id="tocS_pool_online_1">pool_online_1</h2>

<a id="schemapool_online_1"></a>
<a id="schema_pool_online_1"></a>
<a id="tocSpool_online_1"></a>
<a id="tocspool_online_1"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|label|string|false|none|none|

<h2 id="tocS_pool_passphrase_1">pool_passphrase_1</h2>

<a id="schemapool_passphrase_1"></a>
<a id="schema_pool_passphrase_1"></a>
<a id="tocSpool_passphrase_1"></a>
<a id="tocspool_passphrase_1"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|passphrase|stringÂ¦null|false|none|none|
|admin_password|string|false|none|none|

<h2 id="tocS_pool_processes">pool_processes</h2>

<a id="schemapool_processes"></a>
<a id="schema_pool_processes"></a>
<a id="tocSpool_processes"></a>
<a id="tocspool_processes"></a>

```json
{}

```

### Properties

*None*

<h2 id="tocS_pool_recoverykey_rm_1">pool_recoverykey_rm_1</h2>

<a id="schemapool_recoverykey_rm_1"></a>
<a id="schema_pool_recoverykey_rm_1"></a>
<a id="tocSpool_recoverykey_rm_1"></a>
<a id="tocspool_recoverykey_rm_1"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|admin_password|string|false|none|none|

<h2 id="tocS_pool_rekey_1">pool_rekey_1</h2>

<a id="schemapool_rekey_1"></a>
<a id="schema_pool_rekey_1"></a>
<a id="tocSpool_rekey_1"></a>
<a id="tocspool_rekey_1"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|admin_password|string|false|none|none|

<h2 id="tocS_pool_remove_1">pool_remove_1</h2>

<a id="schemapool_remove_1"></a>
<a id="schema_pool_remove_1"></a>
<a id="tocSpool_remove_1"></a>
<a id="tocspool_remove_1"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|label|string|false|none|none|

<h2 id="tocS_pool_replace_1">pool_replace_1</h2>

<a id="schemapool_replace_1"></a>
<a id="schema_pool_replace_1"></a>
<a id="tocSpool_replace_1"></a>
<a id="tocspool_replace_1"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|label|string|false|none|none|
|disk|string|false|none|none|
|force|boolean|false|none|none|
|passphrase|string|false|none|none|

<h2 id="tocS_pool_scrub_1">pool_scrub_1</h2>

<a id="schemapool_scrub_1"></a>
<a id="schema_pool_scrub_1"></a>
<a id="tocSpool_scrub_1"></a>
<a id="tocspool_scrub_1"></a>

```json
"START"

```

action

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|action|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|action|START|
|action|STOP|
|action|PAUSE|

<h2 id="tocS_pool_unlock_1">pool_unlock_1</h2>

<a id="schemapool_unlock_1"></a>
<a id="schema_pool_unlock_1"></a>
<a id="tocSpool_unlock_1"></a>
<a id="tocspool_unlock_1"></a>

```json
{}

```

pool_unlock_options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|passphrase|string|false|none|none|
|recoverykey|boolean|false|none|none|
|services_restart|[any]|false|none|none|

<h2 id="tocS_pool_unlock_services_restart_choices_0">pool_unlock_services_restart_choices_0</h2>

<a id="schemapool_unlock_services_restart_choices_0"></a>
<a id="schema_pool_unlock_services_restart_choices_0"></a>
<a id="tocSpool_unlock_services_restart_choices_0"></a>
<a id="tocspool_unlock_services_restart_choices_0"></a>

```json
0

```

id

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|id|integer|false|none|none|

<h2 id="tocS_pool_upgrade">pool_upgrade</h2>

<a id="schemapool_upgrade"></a>
<a id="schema_pool_upgrade"></a>
<a id="tocSpool_upgrade"></a>
<a id="tocspool_upgrade"></a>

```json
{}

```

### Properties

*None*

<h2 id="tocS_pool_dataset_create_0">pool_dataset_create_0</h2>

<a id="schemapool_dataset_create_0"></a>
<a id="schema_pool_dataset_create_0"></a>
<a id="tocSpool_dataset_create_0"></a>
<a id="tocspool_dataset_create_0"></a>

```json
{}

```

pool_dataset_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|
|type|string|false|none|none|
|volsize|integer|false|none|none|
|volblocksize|string|false|none|none|
|sparse|boolean|false|none|none|
|force_size|boolean|false|none|none|
|comments|string|false|none|none|
|sync|string|false|none|none|
|compression|string|false|none|none|
|atime|string|false|none|none|
|exec|string|false|none|none|
|managedby|string|false|none|none|
|quota|integerÂ¦null|false|none|none|
|quota_warning|integer|false|none|none|
|quota_critical|integer|false|none|none|
|refquota|integerÂ¦null|false|none|none|
|refquota_warning|integer|false|none|none|
|refquota_critical|integer|false|none|none|
|reservation|integer|false|none|none|
|refreservation|integer|false|none|none|
|special_small_block_size|integer|false|none|none|
|copies|integer|false|none|none|
|snapdir|string|false|none|none|
|deduplication|string|false|none|none|
|readonly|string|false|none|none|
|recordsize|string|false|none|none|
|casesensitivity|string|false|none|none|
|aclmode|string|false|none|none|
|acltype|string|false|none|none|
|share_type|string|false|none|none|
|xattr|string|false|none|none|
|encryption_options|object|false|none|none|
|Â» generate_key|boolean|false|none|none|
|Â» pbkdf2iters|integer|false|none|none|
|Â» algorithm|string|false|none|none|
|Â» passphrase|stringÂ¦null|false|none|none|
|Â» key|stringÂ¦null|false|none|none|
|encryption|boolean|false|none|none|
|inherit_encryption|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|type|FILESYSTEM|
|type|VOLUME|
|volblocksize|512|
|volblocksize|1K|
|volblocksize|2K|
|volblocksize|4K|
|volblocksize|8K|
|volblocksize|16K|
|volblocksize|32K|
|volblocksize|64K|
|volblocksize|128K|
|sync|STANDARD|
|sync|ALWAYS|
|sync|DISABLED|
|compression|OFF|
|compression|LZ4|
|compression|GZIP|
|compression|GZIP-1|
|compression|GZIP-9|
|compression|ZLE|
|compression|LZJB|
|atime|ON|
|atime|OFF|
|exec|ON|
|exec|OFF|
|snapdir|VISIBLE|
|snapdir|HIDDEN|
|deduplication|ON|
|deduplication|VERIFY|
|deduplication|OFF|
|readonly|ON|
|readonly|OFF|
|recordsize|512|
|recordsize|1K|
|recordsize|2K|
|recordsize|4K|
|recordsize|8K|
|recordsize|16K|
|recordsize|32K|
|recordsize|64K|
|recordsize|128K|
|recordsize|256K|
|recordsize|512K|
|recordsize|1024K|
|casesensitivity|SENSITIVE|
|casesensitivity|INSENSITIVE|
|casesensitivity|MIXED|
|aclmode|PASSTHROUGH|
|aclmode|RESTRICTED|
|acltype|NOACL|
|acltype|NFS4ACL|
|acltype|POSIXACL|
|share_type|GENERIC|
|share_type|SMB|
|xattr|ON|
|xattr|SA|
|algorithm|AES-128-CCM|
|algorithm|AES-192-CCM|
|algorithm|AES-256-CCM|
|algorithm|AES-128-GCM|
|algorithm|AES-192-GCM|
|algorithm|AES-256-GCM|

<h2 id="tocS_pool_dataset_delete_1">pool_dataset_delete_1</h2>

<a id="schemapool_dataset_delete_1"></a>
<a id="schema_pool_dataset_delete_1"></a>
<a id="tocSpool_dataset_delete_1"></a>
<a id="tocspool_dataset_delete_1"></a>

```json
{}

```

dataset_delete

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|recursive|boolean|false|none|none|
|force|boolean|false|none|none|

<h2 id="tocS_pool_dataset_update_1">pool_dataset_update_1</h2>

<a id="schemapool_dataset_update_1"></a>
<a id="schema_pool_dataset_update_1"></a>
<a id="tocSpool_dataset_update_1"></a>
<a id="tocspool_dataset_update_1"></a>

```json
{}

```

pool_dataset_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|volsize|integer|false|none|none|
|force_size|boolean|false|none|none|
|comments|string|false|none|none|
|sync|string|false|none|none|
|compression|string|false|none|none|
|atime|string|false|none|none|
|exec|string|false|none|none|
|managedby|string|false|none|none|
|quota|integerÂ¦null|false|none|none|
|quota_warning|any|false|none|none|

anyOf

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|Â» *anonymous*|integer|false|none|none|

or

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|Â» *anonymous*|string|false|none|none|

continued

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|quota_critical|any|false|none|none|

anyOf

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|Â» *anonymous*|integer|false|none|none|

or

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|Â» *anonymous*|string|false|none|none|

continued

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|refquota|integerÂ¦null|false|none|none|
|refquota_warning|any|false|none|none|

anyOf

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|Â» *anonymous*|integer|false|none|none|

or

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|Â» *anonymous*|string|false|none|none|

continued

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|refquota_critical|any|false|none|none|

anyOf

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|Â» *anonymous*|integer|false|none|none|

or

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|Â» *anonymous*|string|false|none|none|

continued

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|reservation|integer|false|none|none|
|refreservation|integer|false|none|none|
|special_small_block_size|integer|false|none|none|
|copies|integer|false|none|none|
|snapdir|string|false|none|none|
|deduplication|string|false|none|none|
|readonly|string|false|none|none|
|recordsize|string|false|none|none|
|aclmode|string|false|none|none|
|acltype|string|false|none|none|
|xattr|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|sync|STANDARD|
|sync|ALWAYS|
|sync|DISABLED|
|sync|INHERIT|
|compression|OFF|
|compression|LZ4|
|compression|GZIP|
|compression|GZIP-1|
|compression|GZIP-9|
|compression|ZLE|
|compression|LZJB|
|compression|INHERIT|
|atime|ON|
|atime|OFF|
|atime|INHERIT|
|exec|ON|
|exec|OFF|
|exec|INHERIT|
|*anonymous*|INHERIT|
|*anonymous*|INHERIT|
|*anonymous*|INHERIT|
|*anonymous*|INHERIT|
|snapdir|VISIBLE|
|snapdir|HIDDEN|
|snapdir|INHERIT|
|deduplication|ON|
|deduplication|VERIFY|
|deduplication|OFF|
|deduplication|INHERIT|
|readonly|ON|
|readonly|OFF|
|readonly|INHERIT|
|recordsize|512|
|recordsize|1K|
|recordsize|2K|
|recordsize|4K|
|recordsize|8K|
|recordsize|16K|
|recordsize|32K|
|recordsize|64K|
|recordsize|128K|
|recordsize|256K|
|recordsize|512K|
|recordsize|1024K|
|recordsize|INHERIT|
|aclmode|PASSTHROUGH|
|aclmode|RESTRICTED|
|acltype|NOACL|
|acltype|NFS4ACL|
|acltype|POSIXACL|
|xattr|ON|
|xattr|SA|

<h2 id="tocS_pool_dataset_attachments">pool_dataset_attachments</h2>

<a id="schemapool_dataset_attachments"></a>
<a id="schema_pool_dataset_attachments"></a>
<a id="tocSpool_dataset_attachments"></a>
<a id="tocspool_dataset_attachments"></a>

```json
{}

```

### Properties

*None*

<h2 id="tocS_pool_dataset_change_key_0">pool_dataset_change_key_0</h2>

<a id="schemapool_dataset_change_key_0"></a>
<a id="schema_pool_dataset_change_key_0"></a>
<a id="tocSpool_dataset_change_key_0"></a>
<a id="tocspool_dataset_change_key_0"></a>

```json
"string"

```

id

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|id|string|false|none|none|

<h2 id="tocS_pool_dataset_change_key_1">pool_dataset_change_key_1</h2>

<a id="schemapool_dataset_change_key_1"></a>
<a id="schema_pool_dataset_change_key_1"></a>
<a id="tocSpool_dataset_change_key_1"></a>
<a id="tocspool_dataset_change_key_1"></a>

```json
{}

```

change_key_options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|generate_key|boolean|false|none|none|
|key_file|boolean|false|none|none|
|pbkdf2iters|integer|false|none|none|
|passphrase|stringÂ¦null|false|none|none|
|key|stringÂ¦null|false|none|none|

<h2 id="tocS_pool_dataset_change_key">pool_dataset_change_key</h2>

<a id="schemapool_dataset_change_key"></a>
<a id="schema_pool_dataset_change_key"></a>
<a id="tocSpool_dataset_change_key"></a>
<a id="tocspool_dataset_change_key"></a>

```json
{
  "id": "string",
  "change_key_options": {}
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|id|[pool_dataset_change_key_0](#schemapool_dataset_change_key_0)|false|none|none|
|change_key_options|[pool_dataset_change_key_1](#schemapool_dataset_change_key_1)|false|none|none|

<h2 id="tocS_pool_dataset_encryption_summary_0">pool_dataset_encryption_summary_0</h2>

<a id="schemapool_dataset_encryption_summary_0"></a>
<a id="schema_pool_dataset_encryption_summary_0"></a>
<a id="tocSpool_dataset_encryption_summary_0"></a>
<a id="tocspool_dataset_encryption_summary_0"></a>

```json
"string"

```

id

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|id|string|false|none|none|

<h2 id="tocS_pool_dataset_encryption_summary_1">pool_dataset_encryption_summary_1</h2>

<a id="schemapool_dataset_encryption_summary_1"></a>
<a id="schema_pool_dataset_encryption_summary_1"></a>
<a id="tocSpool_dataset_encryption_summary_1"></a>
<a id="tocspool_dataset_encryption_summary_1"></a>

```json
{}

```

encryption_root_summary_options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|key_file|boolean|false|none|none|
|datasets|[object]|false|none|none|

<h2 id="tocS_pool_dataset_encryption_summary">pool_dataset_encryption_summary</h2>

<a id="schemapool_dataset_encryption_summary"></a>
<a id="schema_pool_dataset_encryption_summary"></a>
<a id="tocSpool_dataset_encryption_summary"></a>
<a id="tocspool_dataset_encryption_summary"></a>

```json
{
  "id": "string",
  "encryption_root_summary_options": {}
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|id|[pool_dataset_encryption_summary_0](#schemapool_dataset_encryption_summary_0)|false|none|none|
|encryption_root_summary_options|[pool_dataset_encryption_summary_1](#schemapool_dataset_encryption_summary_1)|false|none|none|

<h2 id="tocS_pool_dataset_get_quota_1">pool_dataset_get_quota_1</h2>

<a id="schemapool_dataset_get_quota_1"></a>
<a id="schema_pool_dataset_get_quota_1"></a>
<a id="tocSpool_dataset_get_quota_1"></a>
<a id="tocspool_dataset_get_quota_1"></a>

```json
"USER"

```

quota_type

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|quota_type|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|quota_type|USER|
|quota_type|GROUP|
|quota_type|DATASET|

<h2 id="tocS_pool_dataset_get_quota_2">pool_dataset_get_quota_2</h2>

<a id="schemapool_dataset_get_quota_2"></a>
<a id="schema_pool_dataset_get_quota_2"></a>
<a id="tocSpool_dataset_get_quota_2"></a>
<a id="tocspool_dataset_get_quota_2"></a>

```json
[]

```

query-filters

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|query-filters|[any]|false|none|none|

<h2 id="tocS_pool_dataset_get_quota_3">pool_dataset_get_quota_3</h2>

<a id="schemapool_dataset_get_quota_3"></a>
<a id="schema_pool_dataset_get_quota_3"></a>
<a id="tocSpool_dataset_get_quota_3"></a>
<a id="tocspool_dataset_get_quota_3"></a>

```json
{}

```

query-options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|relationships|boolean|false|none|none|
|extend|stringÂ¦null|false|none|none|
|extend_context|stringÂ¦null|false|none|none|
|prefix|stringÂ¦null|false|none|none|
|extra|object|false|none|none|
|order_by|[any]|false|none|none|
|select|[any]|false|none|none|
|count|boolean|false|none|none|
|get|boolean|false|none|none|
|offset|integer|false|none|none|
|limit|integer|false|none|none|

<h2 id="tocS_pool_dataset_get_quota">pool_dataset_get_quota</h2>

<a id="schemapool_dataset_get_quota"></a>
<a id="schema_pool_dataset_get_quota"></a>
<a id="tocSpool_dataset_get_quota"></a>
<a id="tocspool_dataset_get_quota"></a>

```json
{
  "quota_type": "USER",
  "query-filters": [],
  "query-options": {}
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|quota_type|[pool_dataset_get_quota_1](#schemapool_dataset_get_quota_1)|false|none|none|
|query-filters|[pool_dataset_get_quota_2](#schemapool_dataset_get_quota_2)|false|none|none|
|query-options|[pool_dataset_get_quota_3](#schemapool_dataset_get_quota_3)|false|none|none|

<h2 id="tocS_pool_dataset_inherit_parent_encryption_properties_0">pool_dataset_inherit_parent_encryption_properties_0</h2>

<a id="schemapool_dataset_inherit_parent_encryption_properties_0"></a>
<a id="schema_pool_dataset_inherit_parent_encryption_properties_0"></a>
<a id="tocSpool_dataset_inherit_parent_encryption_properties_0"></a>
<a id="tocspool_dataset_inherit_parent_encryption_properties_0"></a>

```json
"string"

```

id

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|id|string|false|none|none|

<h2 id="tocS_pool_dataset_lock_0">pool_dataset_lock_0</h2>

<a id="schemapool_dataset_lock_0"></a>
<a id="schema_pool_dataset_lock_0"></a>
<a id="tocSpool_dataset_lock_0"></a>
<a id="tocspool_dataset_lock_0"></a>

```json
"string"

```

id

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|id|string|false|none|none|

<h2 id="tocS_pool_dataset_lock_1">pool_dataset_lock_1</h2>

<a id="schemapool_dataset_lock_1"></a>
<a id="schema_pool_dataset_lock_1"></a>
<a id="tocSpool_dataset_lock_1"></a>
<a id="tocspool_dataset_lock_1"></a>

```json
{}

```

lock_options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|force_umount|boolean|false|none|none|

<h2 id="tocS_pool_dataset_lock">pool_dataset_lock</h2>

<a id="schemapool_dataset_lock"></a>
<a id="schema_pool_dataset_lock"></a>
<a id="tocSpool_dataset_lock"></a>
<a id="tocspool_dataset_lock"></a>

```json
{
  "id": "string",
  "lock_options": {}
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|id|[pool_dataset_lock_0](#schemapool_dataset_lock_0)|false|none|none|
|lock_options|[pool_dataset_lock_1](#schemapool_dataset_lock_1)|false|none|none|

<h2 id="tocS_pool_dataset_permission_1">pool_dataset_permission_1</h2>

<a id="schemapool_dataset_permission_1"></a>
<a id="schema_pool_dataset_permission_1"></a>
<a id="tocSpool_dataset_permission_1"></a>
<a id="tocspool_dataset_permission_1"></a>

```json
{}

```

pool_dataset_permission

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|user|string|false|none|none|
|group|string|false|none|none|
|mode|stringÂ¦null|false|none|none|
|acl|[object]|false|none|none|
|options|object|false|none|none|
|Â» stripacl|boolean|false|none|none|
|Â» recursive|boolean|false|none|none|
|Â» traverse|boolean|false|none|none|

<h2 id="tocS_pool_dataset_processes">pool_dataset_processes</h2>

<a id="schemapool_dataset_processes"></a>
<a id="schema_pool_dataset_processes"></a>
<a id="tocSpool_dataset_processes"></a>
<a id="tocspool_dataset_processes"></a>

```json
{}

```

### Properties

*None*

<h2 id="tocS_pool_dataset_promote">pool_dataset_promote</h2>

<a id="schemapool_dataset_promote"></a>
<a id="schema_pool_dataset_promote"></a>
<a id="tocSpool_dataset_promote"></a>
<a id="tocspool_dataset_promote"></a>

```json
{}

```

### Properties

*None*

<h2 id="tocS_pool_dataset_recommended_zvol_blocksize_0">pool_dataset_recommended_zvol_blocksize_0</h2>

<a id="schemapool_dataset_recommended_zvol_blocksize_0"></a>
<a id="schema_pool_dataset_recommended_zvol_blocksize_0"></a>
<a id="tocSpool_dataset_recommended_zvol_blocksize_0"></a>
<a id="tocspool_dataset_recommended_zvol_blocksize_0"></a>

```json
"string"

```

pool

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|pool|string|false|none|none|

<h2 id="tocS_pool_dataset_set_quota_1">pool_dataset_set_quota_1</h2>

<a id="schemapool_dataset_set_quota_1"></a>
<a id="schema_pool_dataset_set_quota_1"></a>
<a id="tocSpool_dataset_set_quota_1"></a>
<a id="tocspool_dataset_set_quota_1"></a>

```json
[
  {
    "quota_type": "USER",
    "id": "0",
    "quota_value": 0
  }
]

```

quotas

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|quotas|[object]|false|none|none|

<h2 id="tocS_pool_dataset_unlock_0">pool_dataset_unlock_0</h2>

<a id="schemapool_dataset_unlock_0"></a>
<a id="schema_pool_dataset_unlock_0"></a>
<a id="tocSpool_dataset_unlock_0"></a>
<a id="tocspool_dataset_unlock_0"></a>

```json
"string"

```

id

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|id|string|false|none|none|

<h2 id="tocS_pool_dataset_unlock_1">pool_dataset_unlock_1</h2>

<a id="schemapool_dataset_unlock_1"></a>
<a id="schema_pool_dataset_unlock_1"></a>
<a id="tocSpool_dataset_unlock_1"></a>
<a id="tocspool_dataset_unlock_1"></a>

```json
{}

```

unlock_options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|key_file|boolean|false|none|none|
|recursive|boolean|false|none|none|
|toggle_attachments|boolean|false|none|none|
|datasets|[object]|false|none|none|

<h2 id="tocS_pool_dataset_unlock">pool_dataset_unlock</h2>

<a id="schemapool_dataset_unlock"></a>
<a id="schema_pool_dataset_unlock"></a>
<a id="tocSpool_dataset_unlock"></a>
<a id="tocspool_dataset_unlock"></a>

```json
{
  "id": "string",
  "unlock_options": {}
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|id|[pool_dataset_unlock_0](#schemapool_dataset_unlock_0)|false|none|none|
|unlock_options|[pool_dataset_unlock_1](#schemapool_dataset_unlock_1)|false|none|none|

<h2 id="tocS_pool_dataset_userprop_create_0">pool_dataset_userprop_create_0</h2>

<a id="schemapool_dataset_userprop_create_0"></a>
<a id="schema_pool_dataset_userprop_create_0"></a>
<a id="tocSpool_dataset_userprop_create_0"></a>
<a id="tocspool_dataset_userprop_create_0"></a>

```json
{}

```

dataset_user_prop_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|id|string|false|none|none|
|property|object|false|none|none|
|Â» name|string|false|none|none|
|Â» value|string|false|none|none|

<h2 id="tocS_pool_dataset_userprop_delete_1">pool_dataset_userprop_delete_1</h2>

<a id="schemapool_dataset_userprop_delete_1"></a>
<a id="schema_pool_dataset_userprop_delete_1"></a>
<a id="tocSpool_dataset_userprop_delete_1"></a>
<a id="tocspool_dataset_userprop_delete_1"></a>

```json
{}

```

dataset_user_prop_delete

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|

<h2 id="tocS_pool_dataset_userprop_update_1">pool_dataset_userprop_update_1</h2>

<a id="schemapool_dataset_userprop_update_1"></a>
<a id="schema_pool_dataset_userprop_update_1"></a>
<a id="tocSpool_dataset_userprop_update_1"></a>
<a id="tocspool_dataset_userprop_update_1"></a>

```json
{}

```

dataset_user_prop_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|
|value|string|false|none|none|

<h2 id="tocS_pool_resilver_update_0">pool_resilver_update_0</h2>

<a id="schemapool_resilver_update_0"></a>
<a id="schema_pool_resilver_update_0"></a>
<a id="tocSpool_resilver_update_0"></a>
<a id="tocspool_resilver_update_0"></a>

```json
{}

```

pool_resilver

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|begin|string|false|none|none|
|end|string|false|none|none|
|enabled|boolean|false|none|none|
|weekday|[integer]|false|none|none|

<h2 id="tocS_pool_scrub_create_0">pool_scrub_create_0</h2>

<a id="schemapool_scrub_create_0"></a>
<a id="schema_pool_scrub_create_0"></a>
<a id="tocSpool_scrub_create_0"></a>
<a id="tocspool_scrub_create_0"></a>

```json
{}

```

pool_scrub_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|pool|integer|false|none|none|
|threshold|integer|false|none|none|
|description|string|false|none|none|
|schedule|object|false|none|none|
|Â» minute|string|false|none|none|
|Â» hour|string|false|none|none|
|Â» dom|string|false|none|none|
|Â» month|string|false|none|none|
|Â» dow|string|false|none|none|
|enabled|boolean|false|none|none|

<h2 id="tocS_pool_scrub_update_1">pool_scrub_update_1</h2>

<a id="schemapool_scrub_update_1"></a>
<a id="schema_pool_scrub_update_1"></a>
<a id="tocSpool_scrub_update_1"></a>
<a id="tocspool_scrub_update_1"></a>

```json
{}

```

pool_scrub_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|pool|integer|false|none|none|
|threshold|integer|false|none|none|
|description|string|false|none|none|
|schedule|object|false|none|none|
|Â» minute|string|false|none|none|
|Â» hour|string|false|none|none|
|Â» dom|string|false|none|none|
|Â» month|string|false|none|none|
|Â» dow|string|false|none|none|
|enabled|boolean|false|none|none|

<h2 id="tocS_pool_scrub_run_0">pool_scrub_run_0</h2>

<a id="schemapool_scrub_run_0"></a>
<a id="schema_pool_scrub_run_0"></a>
<a id="tocSpool_scrub_run_0"></a>
<a id="tocspool_scrub_run_0"></a>

```json
"string"

```

name

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|

<h2 id="tocS_pool_scrub_run_1">pool_scrub_run_1</h2>

<a id="schemapool_scrub_run_1"></a>
<a id="schema_pool_scrub_run_1"></a>
<a id="tocSpool_scrub_run_1"></a>
<a id="tocspool_scrub_run_1"></a>

```json
35

```

threshold

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|threshold|integer|false|none|none|

<h2 id="tocS_pool_scrub_run">pool_scrub_run</h2>

<a id="schemapool_scrub_run"></a>
<a id="schema_pool_scrub_run"></a>
<a id="tocSpool_scrub_run"></a>
<a id="tocspool_scrub_run"></a>

```json
{
  "name": "string",
  "threshold": 35
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|[pool_scrub_run_0](#schemapool_scrub_run_0)|false|none|none|
|threshold|[pool_scrub_run_1](#schemapool_scrub_run_1)|false|none|none|

<h2 id="tocS_pool_snapshottask_create_0">pool_snapshottask_create_0</h2>

<a id="schemapool_snapshottask_create_0"></a>
<a id="schema_pool_snapshottask_create_0"></a>
<a id="tocSpool_snapshottask_create_0"></a>
<a id="tocspool_snapshottask_create_0"></a>

```json
{}

```

periodic_snapshot_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|dataset|string|false|none|none|
|recursive|boolean|false|none|none|
|exclude|[string]|false|none|none|
|lifetime_value|integer|false|none|none|
|lifetime_unit|string|false|none|none|
|naming_schema|string|false|none|none|
|schedule|object|false|none|none|
|Â» minute|string|false|none|none|
|Â» hour|string|false|none|none|
|Â» dom|string|false|none|none|
|Â» month|string|false|none|none|
|Â» dow|string|false|none|none|
|Â» begin|string|false|none|none|
|Â» end|string|false|none|none|
|allow_empty|boolean|false|none|none|
|enabled|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|lifetime_unit|HOUR|
|lifetime_unit|DAY|
|lifetime_unit|WEEK|
|lifetime_unit|MONTH|
|lifetime_unit|YEAR|

<h2 id="tocS_pool_snapshottask_update_1">pool_snapshottask_update_1</h2>

<a id="schemapool_snapshottask_update_1"></a>
<a id="schema_pool_snapshottask_update_1"></a>
<a id="tocSpool_snapshottask_update_1"></a>
<a id="tocspool_snapshottask_update_1"></a>

```json
{}

```

periodic_snapshot_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|dataset|string|false|none|none|
|recursive|boolean|false|none|none|
|exclude|[string]|false|none|none|
|lifetime_value|integer|false|none|none|
|lifetime_unit|string|false|none|none|
|naming_schema|string|false|none|none|
|schedule|object|false|none|none|
|Â» minute|string|false|none|none|
|Â» hour|string|false|none|none|
|Â» dom|string|false|none|none|
|Â» month|string|false|none|none|
|Â» dow|string|false|none|none|
|Â» begin|string|false|none|none|
|Â» end|string|false|none|none|
|allow_empty|boolean|false|none|none|
|enabled|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|lifetime_unit|HOUR|
|lifetime_unit|DAY|
|lifetime_unit|WEEK|
|lifetime_unit|MONTH|
|lifetime_unit|YEAR|

<h2 id="tocS_pool_snapshottask_run">pool_snapshottask_run</h2>

<a id="schemapool_snapshottask_run"></a>
<a id="schema_pool_snapshottask_run"></a>
<a id="tocSpool_snapshottask_run"></a>
<a id="tocspool_snapshottask_run"></a>

```json
{}

```

### Properties

*None*

<h2 id="tocS_replication_create_0">replication_create_0</h2>

<a id="schemareplication_create_0"></a>
<a id="schema_replication_create_0"></a>
<a id="tocSreplication_create_0"></a>
<a id="tocsreplication_create_0"></a>

```json
{}

```

replication_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|
|direction|string|false|none|none|
|transport|string|false|none|none|
|ssh_credentials|integerÂ¦null|false|none|none|
|netcat_active_side|stringÂ¦null|false|none|none|
|netcat_active_side_listen_address|stringÂ¦null|false|none|none|
|netcat_active_side_port_min|integerÂ¦null|false|none|none|
|netcat_active_side_port_max|integerÂ¦null|false|none|none|
|netcat_passive_side_connect_address|stringÂ¦null|false|none|none|
|source_datasets|[string]|false|none|none|
|target_dataset|string|false|none|none|
|recursive|boolean|false|none|none|
|exclude|[string]|false|none|none|
|properties|boolean|false|none|none|
|replicate|boolean|false|none|none|
|periodic_snapshot_tasks|[integer]|false|none|none|
|naming_schema|[string]|false|none|none|
|also_include_naming_schema|[string]|false|none|none|
|auto|boolean|false|none|none|
|schedule|object|false|none|none|
|Â» minute|string|false|none|none|
|Â» hour|string|false|none|none|
|Â» dom|string|false|none|none|
|Â» month|string|false|none|none|
|Â» dow|string|false|none|none|
|Â» begin|string|false|none|none|
|Â» end|string|false|none|none|
|restrict_schedule|object|false|none|none|
|Â» minute|string|false|none|none|
|Â» hour|string|false|none|none|
|Â» dom|string|false|none|none|
|Â» month|string|false|none|none|
|Â» dow|string|false|none|none|
|Â» begin|string|false|none|none|
|Â» end|string|false|none|none|
|only_matching_schedule|boolean|false|none|none|
|allow_from_scratch|boolean|false|none|none|
|readonly|string|false|none|none|
|hold_pending_snapshots|boolean|false|none|none|
|retention_policy|string|false|none|none|
|lifetime_value|integerÂ¦null|false|none|none|
|lifetime_unit|stringÂ¦null|false|none|none|
|compression|stringÂ¦null|false|none|none|
|speed_limit|integerÂ¦null|false|none|none|
|large_block|boolean|false|none|none|
|embed|boolean|false|none|none|
|compressed|boolean|false|none|none|
|retries|integer|false|none|none|
|logging_level|stringÂ¦null|false|none|none|
|enabled|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|direction|PUSH|
|direction|PULL|
|transport|SSH|
|transport|SSH+NETCAT|
|transport|LOCAL|
|netcat_active_side|LOCAL|
|netcat_active_side|REMOTE|
|readonly|SET|
|readonly|REQUIRE|
|readonly|IGNORE|
|retention_policy|SOURCE|
|retention_policy|CUSTOM|
|retention_policy|NONE|
|lifetime_unit|HOUR|
|lifetime_unit|DAY|
|lifetime_unit|WEEK|
|lifetime_unit|MONTH|
|lifetime_unit|YEAR|
|compression|LZ4|
|compression|PIGZ|
|compression|PLZIP|
|logging_level|DEBUG|
|logging_level|INFO|
|logging_level|WARNING|
|logging_level|ERROR|

<h2 id="tocS_replication_update_1">replication_update_1</h2>

<a id="schemareplication_update_1"></a>
<a id="schema_replication_update_1"></a>
<a id="tocSreplication_update_1"></a>
<a id="tocsreplication_update_1"></a>

```json
{}

```

replication_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|
|direction|string|false|none|none|
|transport|string|false|none|none|
|ssh_credentials|integerÂ¦null|false|none|none|
|netcat_active_side|stringÂ¦null|false|none|none|
|netcat_active_side_listen_address|stringÂ¦null|false|none|none|
|netcat_active_side_port_min|integerÂ¦null|false|none|none|
|netcat_active_side_port_max|integerÂ¦null|false|none|none|
|netcat_passive_side_connect_address|stringÂ¦null|false|none|none|
|source_datasets|[string]|false|none|none|
|target_dataset|string|false|none|none|
|recursive|boolean|false|none|none|
|exclude|[string]|false|none|none|
|properties|boolean|false|none|none|
|replicate|boolean|false|none|none|
|periodic_snapshot_tasks|[integer]|false|none|none|
|naming_schema|[string]|false|none|none|
|also_include_naming_schema|[string]|false|none|none|
|auto|boolean|false|none|none|
|schedule|object|false|none|none|
|Â» minute|string|false|none|none|
|Â» hour|string|false|none|none|
|Â» dom|string|false|none|none|
|Â» month|string|false|none|none|
|Â» dow|string|false|none|none|
|Â» begin|string|false|none|none|
|Â» end|string|false|none|none|
|restrict_schedule|object|false|none|none|
|Â» minute|string|false|none|none|
|Â» hour|string|false|none|none|
|Â» dom|string|false|none|none|
|Â» month|string|false|none|none|
|Â» dow|string|false|none|none|
|Â» begin|string|false|none|none|
|Â» end|string|false|none|none|
|only_matching_schedule|boolean|false|none|none|
|allow_from_scratch|boolean|false|none|none|
|readonly|string|false|none|none|
|hold_pending_snapshots|boolean|false|none|none|
|retention_policy|string|false|none|none|
|lifetime_value|integerÂ¦null|false|none|none|
|lifetime_unit|stringÂ¦null|false|none|none|
|compression|stringÂ¦null|false|none|none|
|speed_limit|integerÂ¦null|false|none|none|
|large_block|boolean|false|none|none|
|embed|boolean|false|none|none|
|compressed|boolean|false|none|none|
|retries|integer|false|none|none|
|logging_level|stringÂ¦null|false|none|none|
|enabled|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|direction|PUSH|
|direction|PULL|
|transport|SSH|
|transport|SSH+NETCAT|
|transport|LOCAL|
|netcat_active_side|LOCAL|
|netcat_active_side|REMOTE|
|readonly|SET|
|readonly|REQUIRE|
|readonly|IGNORE|
|retention_policy|SOURCE|
|retention_policy|CUSTOM|
|retention_policy|NONE|
|lifetime_unit|HOUR|
|lifetime_unit|DAY|
|lifetime_unit|WEEK|
|lifetime_unit|MONTH|
|lifetime_unit|YEAR|
|compression|LZ4|
|compression|PIGZ|
|compression|PLZIP|
|logging_level|DEBUG|
|logging_level|INFO|
|logging_level|WARNING|
|logging_level|ERROR|

<h2 id="tocS_replication_count_eligible_manual_snapshots_0">replication_count_eligible_manual_snapshots_0</h2>

<a id="schemareplication_count_eligible_manual_snapshots_0"></a>
<a id="schema_replication_count_eligible_manual_snapshots_0"></a>
<a id="tocSreplication_count_eligible_manual_snapshots_0"></a>
<a id="tocsreplication_count_eligible_manual_snapshots_0"></a>

```json
[
  "string"
]

```

datasets

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|datasets|[string]|false|none|none|

<h2 id="tocS_replication_count_eligible_manual_snapshots_1">replication_count_eligible_manual_snapshots_1</h2>

<a id="schemareplication_count_eligible_manual_snapshots_1"></a>
<a id="schema_replication_count_eligible_manual_snapshots_1"></a>
<a id="tocSreplication_count_eligible_manual_snapshots_1"></a>
<a id="tocsreplication_count_eligible_manual_snapshots_1"></a>

```json
[
  "string"
]

```

naming_schema

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|naming_schema|[string]|false|none|none|

<h2 id="tocS_replication_count_eligible_manual_snapshots_2">replication_count_eligible_manual_snapshots_2</h2>

<a id="schemareplication_count_eligible_manual_snapshots_2"></a>
<a id="schema_replication_count_eligible_manual_snapshots_2"></a>
<a id="tocSreplication_count_eligible_manual_snapshots_2"></a>
<a id="tocsreplication_count_eligible_manual_snapshots_2"></a>

```json
"SSH"

```

transport

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|transport|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|transport|SSH|
|transport|SSH+NETCAT|
|transport|LOCAL|

<h2 id="tocS_replication_count_eligible_manual_snapshots_3">replication_count_eligible_manual_snapshots_3</h2>

<a id="schemareplication_count_eligible_manual_snapshots_3"></a>
<a id="schema_replication_count_eligible_manual_snapshots_3"></a>
<a id="tocSreplication_count_eligible_manual_snapshots_3"></a>
<a id="tocsreplication_count_eligible_manual_snapshots_3"></a>

```json
null

```

ssh_credentials

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|ssh_credentials|integerÂ¦null|false|none|none|

<h2 id="tocS_replication_count_eligible_manual_snapshots">replication_count_eligible_manual_snapshots</h2>

<a id="schemareplication_count_eligible_manual_snapshots"></a>
<a id="schema_replication_count_eligible_manual_snapshots"></a>
<a id="tocSreplication_count_eligible_manual_snapshots"></a>
<a id="tocsreplication_count_eligible_manual_snapshots"></a>

```json
{
  "datasets": [
    "string"
  ],
  "naming_schema": [
    "string"
  ],
  "transport": "SSH",
  "ssh_credentials": null
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|datasets|[replication_count_eligible_manual_snapshots_0](#schemareplication_count_eligible_manual_snapshots_0)|false|none|none|
|naming_schema|[replication_count_eligible_manual_snapshots_1](#schemareplication_count_eligible_manual_snapshots_1)|false|none|none|
|transport|[replication_count_eligible_manual_snapshots_2](#schemareplication_count_eligible_manual_snapshots_2)|false|none|none|
|ssh_credentials|[replication_count_eligible_manual_snapshots_3](#schemareplication_count_eligible_manual_snapshots_3)|false|none|none|

<h2 id="tocS_replication_create_dataset_0">replication_create_dataset_0</h2>

<a id="schemareplication_create_dataset_0"></a>
<a id="schema_replication_create_dataset_0"></a>
<a id="tocSreplication_create_dataset_0"></a>
<a id="tocsreplication_create_dataset_0"></a>

```json
"string"

```

dataset

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|dataset|string|false|none|none|

<h2 id="tocS_replication_create_dataset_1">replication_create_dataset_1</h2>

<a id="schemareplication_create_dataset_1"></a>
<a id="schema_replication_create_dataset_1"></a>
<a id="tocSreplication_create_dataset_1"></a>
<a id="tocsreplication_create_dataset_1"></a>

```json
"SSH"

```

transport

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|transport|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|transport|SSH|
|transport|SSH+NETCAT|
|transport|LOCAL|

<h2 id="tocS_replication_create_dataset_2">replication_create_dataset_2</h2>

<a id="schemareplication_create_dataset_2"></a>
<a id="schema_replication_create_dataset_2"></a>
<a id="tocSreplication_create_dataset_2"></a>
<a id="tocsreplication_create_dataset_2"></a>

```json
null

```

ssh_credentials

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|ssh_credentials|integerÂ¦null|false|none|none|

<h2 id="tocS_replication_create_dataset">replication_create_dataset</h2>

<a id="schemareplication_create_dataset"></a>
<a id="schema_replication_create_dataset"></a>
<a id="tocSreplication_create_dataset"></a>
<a id="tocsreplication_create_dataset"></a>

```json
{
  "dataset": "string",
  "transport": "SSH",
  "ssh_credentials": null
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|dataset|[replication_create_dataset_0](#schemareplication_create_dataset_0)|false|none|none|
|transport|[replication_create_dataset_1](#schemareplication_create_dataset_1)|false|none|none|
|ssh_credentials|[replication_create_dataset_2](#schemareplication_create_dataset_2)|false|none|none|

<h2 id="tocS_replication_list_datasets_0">replication_list_datasets_0</h2>

<a id="schemareplication_list_datasets_0"></a>
<a id="schema_replication_list_datasets_0"></a>
<a id="tocSreplication_list_datasets_0"></a>
<a id="tocsreplication_list_datasets_0"></a>

```json
"SSH"

```

transport

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|transport|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|transport|SSH|
|transport|SSH+NETCAT|
|transport|LOCAL|

<h2 id="tocS_replication_list_datasets_1">replication_list_datasets_1</h2>

<a id="schemareplication_list_datasets_1"></a>
<a id="schema_replication_list_datasets_1"></a>
<a id="tocSreplication_list_datasets_1"></a>
<a id="tocsreplication_list_datasets_1"></a>

```json
null

```

ssh_credentials

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|ssh_credentials|integerÂ¦null|false|none|none|

<h2 id="tocS_replication_list_datasets">replication_list_datasets</h2>

<a id="schemareplication_list_datasets"></a>
<a id="schema_replication_list_datasets"></a>
<a id="tocSreplication_list_datasets"></a>
<a id="tocsreplication_list_datasets"></a>

```json
{
  "transport": "SSH",
  "ssh_credentials": null
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|transport|[replication_list_datasets_0](#schemareplication_list_datasets_0)|false|none|none|
|ssh_credentials|[replication_list_datasets_1](#schemareplication_list_datasets_1)|false|none|none|

<h2 id="tocS_replication_restore_1">replication_restore_1</h2>

<a id="schemareplication_restore_1"></a>
<a id="schema_replication_restore_1"></a>
<a id="tocSreplication_restore_1"></a>
<a id="tocsreplication_restore_1"></a>

```json
{}

```

replication_restore

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|
|target_dataset|string|false|none|none|

<h2 id="tocS_replication_run">replication_run</h2>

<a id="schemareplication_run"></a>
<a id="schema_replication_run"></a>
<a id="tocSreplication_run"></a>
<a id="tocsreplication_run"></a>

```json
{}

```

### Properties

*None*

<h2 id="tocS_replication_target_unmatched_snapshots_0">replication_target_unmatched_snapshots_0</h2>

<a id="schemareplication_target_unmatched_snapshots_0"></a>
<a id="schema_replication_target_unmatched_snapshots_0"></a>
<a id="tocSreplication_target_unmatched_snapshots_0"></a>
<a id="tocsreplication_target_unmatched_snapshots_0"></a>

```json
"PUSH"

```

direction

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|direction|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|direction|PUSH|
|direction|PULL|

<h2 id="tocS_replication_target_unmatched_snapshots_1">replication_target_unmatched_snapshots_1</h2>

<a id="schemareplication_target_unmatched_snapshots_1"></a>
<a id="schema_replication_target_unmatched_snapshots_1"></a>
<a id="tocSreplication_target_unmatched_snapshots_1"></a>
<a id="tocsreplication_target_unmatched_snapshots_1"></a>

```json
[
  "string"
]

```

source_datasets

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|source_datasets|[string]|false|none|none|

<h2 id="tocS_replication_target_unmatched_snapshots_2">replication_target_unmatched_snapshots_2</h2>

<a id="schemareplication_target_unmatched_snapshots_2"></a>
<a id="schema_replication_target_unmatched_snapshots_2"></a>
<a id="tocSreplication_target_unmatched_snapshots_2"></a>
<a id="tocsreplication_target_unmatched_snapshots_2"></a>

```json
"string"

```

target_dataset

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|target_dataset|string|false|none|none|

<h2 id="tocS_replication_target_unmatched_snapshots_3">replication_target_unmatched_snapshots_3</h2>

<a id="schemareplication_target_unmatched_snapshots_3"></a>
<a id="schema_replication_target_unmatched_snapshots_3"></a>
<a id="tocSreplication_target_unmatched_snapshots_3"></a>
<a id="tocsreplication_target_unmatched_snapshots_3"></a>

```json
"SSH"

```

transport

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|transport|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|transport|SSH|
|transport|SSH+NETCAT|
|transport|LOCAL|
|transport|LEGACY|

<h2 id="tocS_replication_target_unmatched_snapshots_4">replication_target_unmatched_snapshots_4</h2>

<a id="schemareplication_target_unmatched_snapshots_4"></a>
<a id="schema_replication_target_unmatched_snapshots_4"></a>
<a id="tocSreplication_target_unmatched_snapshots_4"></a>
<a id="tocsreplication_target_unmatched_snapshots_4"></a>

```json
null

```

ssh_credentials

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|ssh_credentials|integerÂ¦null|false|none|none|

<h2 id="tocS_replication_target_unmatched_snapshots">replication_target_unmatched_snapshots</h2>

<a id="schemareplication_target_unmatched_snapshots"></a>
<a id="schema_replication_target_unmatched_snapshots"></a>
<a id="tocSreplication_target_unmatched_snapshots"></a>
<a id="tocsreplication_target_unmatched_snapshots"></a>

```json
{
  "direction": "PUSH",
  "source_datasets": [
    "string"
  ],
  "target_dataset": "string",
  "transport": "SSH",
  "ssh_credentials": null
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|direction|[replication_target_unmatched_snapshots_0](#schemareplication_target_unmatched_snapshots_0)|false|none|none|
|source_datasets|[replication_target_unmatched_snapshots_1](#schemareplication_target_unmatched_snapshots_1)|false|none|none|
|target_dataset|[replication_target_unmatched_snapshots_2](#schemareplication_target_unmatched_snapshots_2)|false|none|none|
|transport|[replication_target_unmatched_snapshots_3](#schemareplication_target_unmatched_snapshots_3)|false|none|none|
|ssh_credentials|[replication_target_unmatched_snapshots_4](#schemareplication_target_unmatched_snapshots_4)|false|none|none|

<h2 id="tocS_reporting_update_0">reporting_update_0</h2>

<a id="schemareporting_update_0"></a>
<a id="schema_reporting_update_0"></a>
<a id="tocSreporting_update_0"></a>
<a id="tocsreporting_update_0"></a>

```json
{}

```

reporting_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|cpu_in_percentage|boolean|false|none|none|
|graphite|string|false|none|none|
|graphite_separateinstances|boolean|false|none|none|
|graph_age|integer|false|none|none|
|graph_points|integer|false|none|none|
|confirm_rrd_destroy|boolean|false|none|none|

<h2 id="tocS_reporting_get_data_0">reporting_get_data_0</h2>

<a id="schemareporting_get_data_0"></a>
<a id="schema_reporting_get_data_0"></a>
<a id="tocSreporting_get_data_0"></a>
<a id="tocsreporting_get_data_0"></a>

```json
[
  {}
]

```

graphs

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|graphs|[object]|false|none|none|

<h2 id="tocS_reporting_get_data_1">reporting_get_data_1</h2>

<a id="schemareporting_get_data_1"></a>
<a id="schema_reporting_get_data_1"></a>
<a id="tocSreporting_get_data_1"></a>
<a id="tocsreporting_get_data_1"></a>

```json
{}

```

reporting_query

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|unit|string|false|none|none|
|page|integer|false|none|none|
|start|string|false|none|none|
|end|string|false|none|none|
|aggregate|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|unit|HOUR|
|unit|DAY|
|unit|WEEK|
|unit|MONTH|
|unit|YEAR|

<h2 id="tocS_reporting_get_data">reporting_get_data</h2>

<a id="schemareporting_get_data"></a>
<a id="schema_reporting_get_data"></a>
<a id="tocSreporting_get_data"></a>
<a id="tocsreporting_get_data"></a>

```json
{
  "graphs": [
    {}
  ],
  "reporting_query": {}
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|graphs|[reporting_get_data_0](#schemareporting_get_data_0)|false|none|none|
|reporting_query|[reporting_get_data_1](#schemareporting_get_data_1)|false|none|none|

<h2 id="tocS_route_ipv4gw_reachable_0">route_ipv4gw_reachable_0</h2>

<a id="schemaroute_ipv4gw_reachable_0"></a>
<a id="schema_route_ipv4gw_reachable_0"></a>
<a id="tocSroute_ipv4gw_reachable_0"></a>
<a id="tocsroute_ipv4gw_reachable_0"></a>

```json
"string"

```

ipv4_gateway

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|ipv4_gateway|string|false|none|none|

<h2 id="tocS_rsyncd_update_0">rsyncd_update_0</h2>

<a id="schemarsyncd_update_0"></a>
<a id="schema_rsyncd_update_0"></a>
<a id="tocSrsyncd_update_0"></a>
<a id="tocsrsyncd_update_0"></a>

```json
{}

```

rsyncd_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|port|integer|false|none|none|
|auxiliary|string|false|none|none|

<h2 id="tocS_rsyncmod_create_0">rsyncmod_create_0</h2>

<a id="schemarsyncmod_create_0"></a>
<a id="schema_rsyncmod_create_0"></a>
<a id="tocSrsyncmod_create_0"></a>
<a id="tocsrsyncmod_create_0"></a>

```json
{}

```

rsyncmod_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|enabled|boolean|false|none|none|
|name|string|false|none|none|
|comment|string|false|none|none|
|path|string|false|none|none|
|mode|string|false|none|none|
|maxconn|integer|false|none|none|
|user|string|false|none|none|
|group|string|false|none|none|
|hostsallow|[string]|false|none|none|
|hostsdeny|[string]|false|none|none|
|auxiliary|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|mode|RO|
|mode|RW|
|mode|WO|

<h2 id="tocS_rsyncmod_update_1">rsyncmod_update_1</h2>

<a id="schemarsyncmod_update_1"></a>
<a id="schema_rsyncmod_update_1"></a>
<a id="tocSrsyncmod_update_1"></a>
<a id="tocsrsyncmod_update_1"></a>

```json
{}

```

rsyncmod_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|enabled|boolean|false|none|none|
|name|string|false|none|none|
|comment|string|false|none|none|
|path|string|false|none|none|
|mode|string|false|none|none|
|maxconn|integer|false|none|none|
|user|string|false|none|none|
|group|string|false|none|none|
|hostsallow|[string]|false|none|none|
|hostsdeny|[string]|false|none|none|
|auxiliary|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|mode|RO|
|mode|RW|
|mode|WO|

<h2 id="tocS_rsynctask_create_0">rsynctask_create_0</h2>

<a id="schemarsynctask_create_0"></a>
<a id="schema_rsynctask_create_0"></a>
<a id="tocSrsynctask_create_0"></a>
<a id="tocsrsynctask_create_0"></a>

```json
{}

```

rsync_task_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|path|string|false|none|none|
|user|string|false|none|none|
|remotehost|string|false|none|none|
|remoteport|integer|false|none|none|
|mode|string|false|none|none|
|remotemodule|string|false|none|none|
|remotepath|string|false|none|none|
|validate_rpath|boolean|false|none|none|
|direction|string|false|none|none|
|desc|string|false|none|none|
|schedule|object|false|none|none|
|Â» minute|string|false|none|none|
|Â» hour|string|false|none|none|
|Â» dom|string|false|none|none|
|Â» month|string|false|none|none|
|Â» dow|string|false|none|none|
|recursive|boolean|false|none|none|
|times|boolean|false|none|none|
|compress|boolean|false|none|none|
|archive|boolean|false|none|none|
|delete|boolean|false|none|none|
|quiet|boolean|false|none|none|
|preserveperm|boolean|false|none|none|
|preserveattr|boolean|false|none|none|
|delayupdates|boolean|false|none|none|
|extra|[string]|false|none|none|
|enabled|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|mode|MODULE|
|mode|SSH|
|direction|PULL|
|direction|PUSH|

<h2 id="tocS_rsynctask_update_1">rsynctask_update_1</h2>

<a id="schemarsynctask_update_1"></a>
<a id="schema_rsynctask_update_1"></a>
<a id="tocSrsynctask_update_1"></a>
<a id="tocsrsynctask_update_1"></a>

```json
{}

```

rsync_task_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|path|string|false|none|none|
|user|string|false|none|none|
|remotehost|string|false|none|none|
|remoteport|integer|false|none|none|
|mode|string|false|none|none|
|remotemodule|string|false|none|none|
|remotepath|string|false|none|none|
|validate_rpath|boolean|false|none|none|
|direction|string|false|none|none|
|desc|string|false|none|none|
|schedule|object|false|none|none|
|Â» minute|string|false|none|none|
|Â» hour|string|false|none|none|
|Â» dom|string|false|none|none|
|Â» month|string|false|none|none|
|Â» dow|string|false|none|none|
|recursive|boolean|false|none|none|
|times|boolean|false|none|none|
|compress|boolean|false|none|none|
|archive|boolean|false|none|none|
|delete|boolean|false|none|none|
|quiet|boolean|false|none|none|
|preserveperm|boolean|false|none|none|
|preserveattr|boolean|false|none|none|
|delayupdates|boolean|false|none|none|
|extra|[string]|false|none|none|
|enabled|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|mode|MODULE|
|mode|SSH|
|direction|PULL|
|direction|PUSH|

<h2 id="tocS_rsynctask_run">rsynctask_run</h2>

<a id="schemarsynctask_run"></a>
<a id="schema_rsynctask_run"></a>
<a id="tocSrsynctask_run"></a>
<a id="tocsrsynctask_run"></a>

```json
{}

```

### Properties

*None*

<h2 id="tocS_s3_update_0">s3_update_0</h2>

<a id="schemas3_update_0"></a>
<a id="schema_s3_update_0"></a>
<a id="tocSs3_update_0"></a>
<a id="tocss3_update_0"></a>

```json
{}

```

s3_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|bindip|string|false|none|none|
|bindport|integer|false|none|none|
|access_key|string|false|none|none|
|secret_key|string|false|none|none|
|browser|boolean|false|none|none|
|storage_path|string|false|none|none|
|certificate|integerÂ¦null|false|none|none|

<h2 id="tocS_service_update_1">service_update_1</h2>

<a id="schemaservice_update_1"></a>
<a id="schema_service_update_1"></a>
<a id="tocSservice_update_1"></a>
<a id="tocsservice_update_1"></a>

```json
{}

```

service-update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|enable|boolean|false|none|none|

<h2 id="tocS_service_reload_0">service_reload_0</h2>

<a id="schemaservice_reload_0"></a>
<a id="schema_service_reload_0"></a>
<a id="tocSservice_reload_0"></a>
<a id="tocsservice_reload_0"></a>

```json
"string"

```

service

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|service|string|false|none|none|

<h2 id="tocS_service_reload_1">service_reload_1</h2>

<a id="schemaservice_reload_1"></a>
<a id="schema_service_reload_1"></a>
<a id="tocSservice_reload_1"></a>
<a id="tocsservice_reload_1"></a>

```json
{}

```

service-control

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|ha_propagate|boolean|false|none|none|

<h2 id="tocS_service_reload">service_reload</h2>

<a id="schemaservice_reload"></a>
<a id="schema_service_reload"></a>
<a id="tocSservice_reload"></a>
<a id="tocsservice_reload"></a>

```json
{
  "service": "string",
  "service-control": {}
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|service|[service_reload_0](#schemaservice_reload_0)|false|none|none|
|service-control|[service_reload_1](#schemaservice_reload_1)|false|none|none|

<h2 id="tocS_service_restart_0">service_restart_0</h2>

<a id="schemaservice_restart_0"></a>
<a id="schema_service_restart_0"></a>
<a id="tocSservice_restart_0"></a>
<a id="tocsservice_restart_0"></a>

```json
"string"

```

service

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|service|string|false|none|none|

<h2 id="tocS_service_restart_1">service_restart_1</h2>

<a id="schemaservice_restart_1"></a>
<a id="schema_service_restart_1"></a>
<a id="tocSservice_restart_1"></a>
<a id="tocsservice_restart_1"></a>

```json
{}

```

service-control

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|ha_propagate|boolean|false|none|none|

<h2 id="tocS_service_restart">service_restart</h2>

<a id="schemaservice_restart"></a>
<a id="schema_service_restart"></a>
<a id="tocSservice_restart"></a>
<a id="tocsservice_restart"></a>

```json
{
  "service": "string",
  "service-control": {}
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|service|[service_restart_0](#schemaservice_restart_0)|false|none|none|
|service-control|[service_restart_1](#schemaservice_restart_1)|false|none|none|

<h2 id="tocS_service_start_0">service_start_0</h2>

<a id="schemaservice_start_0"></a>
<a id="schema_service_start_0"></a>
<a id="tocSservice_start_0"></a>
<a id="tocsservice_start_0"></a>

```json
"string"

```

service

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|service|string|false|none|none|

<h2 id="tocS_service_start_1">service_start_1</h2>

<a id="schemaservice_start_1"></a>
<a id="schema_service_start_1"></a>
<a id="tocSservice_start_1"></a>
<a id="tocsservice_start_1"></a>

```json
{}

```

service-control

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|ha_propagate|boolean|false|none|none|

<h2 id="tocS_service_start">service_start</h2>

<a id="schemaservice_start"></a>
<a id="schema_service_start"></a>
<a id="tocSservice_start"></a>
<a id="tocsservice_start"></a>

```json
{
  "service": "string",
  "service-control": {}
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|service|[service_start_0](#schemaservice_start_0)|false|none|none|
|service-control|[service_start_1](#schemaservice_start_1)|false|none|none|

<h2 id="tocS_service_stop_0">service_stop_0</h2>

<a id="schemaservice_stop_0"></a>
<a id="schema_service_stop_0"></a>
<a id="tocSservice_stop_0"></a>
<a id="tocsservice_stop_0"></a>

```json
"string"

```

service

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|service|string|false|none|none|

<h2 id="tocS_service_stop_1">service_stop_1</h2>

<a id="schemaservice_stop_1"></a>
<a id="schema_service_stop_1"></a>
<a id="tocSservice_stop_1"></a>
<a id="tocsservice_stop_1"></a>

```json
{}

```

service-control

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|ha_propagate|boolean|false|none|none|

<h2 id="tocS_service_stop">service_stop</h2>

<a id="schemaservice_stop"></a>
<a id="schema_service_stop"></a>
<a id="tocSservice_stop"></a>
<a id="tocsservice_stop"></a>

```json
{
  "service": "string",
  "service-control": {}
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|service|[service_stop_0](#schemaservice_stop_0)|false|none|none|
|service-control|[service_stop_1](#schemaservice_stop_1)|false|none|none|

<h2 id="tocS_service_terminate_process_0">service_terminate_process_0</h2>

<a id="schemaservice_terminate_process_0"></a>
<a id="schema_service_terminate_process_0"></a>
<a id="tocSservice_terminate_process_0"></a>
<a id="tocsservice_terminate_process_0"></a>

```json
0

```

pid

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|pid|integer|false|none|none|

<h2 id="tocS_service_terminate_process_1">service_terminate_process_1</h2>

<a id="schemaservice_terminate_process_1"></a>
<a id="schema_service_terminate_process_1"></a>
<a id="tocSservice_terminate_process_1"></a>
<a id="tocsservice_terminate_process_1"></a>

```json
10

```

timeout

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|timeout|integer|false|none|none|

<h2 id="tocS_service_terminate_process">service_terminate_process</h2>

<a id="schemaservice_terminate_process"></a>
<a id="schema_service_terminate_process"></a>
<a id="tocSservice_terminate_process"></a>
<a id="tocsservice_terminate_process"></a>

```json
{
  "pid": 0,
  "timeout": 10
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|pid|[service_terminate_process_0](#schemaservice_terminate_process_0)|false|none|none|
|timeout|[service_terminate_process_1](#schemaservice_terminate_process_1)|false|none|none|

<h2 id="tocS_sharing_afp_create_0">sharing_afp_create_0</h2>

<a id="schemasharing_afp_create_0"></a>
<a id="schema_sharing_afp_create_0"></a>
<a id="tocSsharing_afp_create_0"></a>
<a id="tocssharing_afp_create_0"></a>

```json
{}

```

sharingafp_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|path|string|false|none|none|
|home|boolean|false|none|none|
|name|string|false|none|none|
|comment|string|false|none|none|
|allow|[any]|false|none|none|
|deny|[any]|false|none|none|
|ro|[any]|false|none|none|
|rw|[any]|false|none|none|
|timemachine|boolean|false|none|none|
|timemachine_quota|integer|false|none|none|
|nodev|boolean|false|none|none|
|nostat|boolean|false|none|none|
|upriv|boolean|false|none|none|
|fperm|string|false|none|none|
|dperm|string|false|none|none|
|umask|string|false|none|none|
|hostsallow|[any]|false|none|none|
|hostsdeny|[any]|false|none|none|
|vuid|stringÂ¦null|false|none|none|
|auxparams|string|false|none|none|
|enabled|boolean|false|none|none|

<h2 id="tocS_sharing_afp_update_1">sharing_afp_update_1</h2>

<a id="schemasharing_afp_update_1"></a>
<a id="schema_sharing_afp_update_1"></a>
<a id="tocSsharing_afp_update_1"></a>
<a id="tocssharing_afp_update_1"></a>

```json
{}

```

sharingafp_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|path|string|false|none|none|
|home|boolean|false|none|none|
|name|string|false|none|none|
|comment|string|false|none|none|
|allow|[any]|false|none|none|
|deny|[any]|false|none|none|
|ro|[any]|false|none|none|
|rw|[any]|false|none|none|
|timemachine|boolean|false|none|none|
|timemachine_quota|integer|false|none|none|
|nodev|boolean|false|none|none|
|nostat|boolean|false|none|none|
|upriv|boolean|false|none|none|
|fperm|string|false|none|none|
|dperm|string|false|none|none|
|umask|string|false|none|none|
|hostsallow|[any]|false|none|none|
|hostsdeny|[any]|false|none|none|
|vuid|stringÂ¦null|false|none|none|
|auxparams|string|false|none|none|
|enabled|boolean|false|none|none|

<h2 id="tocS_sharing_nfs_create_0">sharing_nfs_create_0</h2>

<a id="schemasharing_nfs_create_0"></a>
<a id="schema_sharing_nfs_create_0"></a>
<a id="tocSsharing_nfs_create_0"></a>
<a id="tocssharing_nfs_create_0"></a>

```json
{}

```

sharingnfs_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|paths|[string]|false|none|none|
|comment|string|false|none|none|
|networks|[string]|false|none|none|
|hosts|[string]|false|none|none|
|alldirs|boolean|false|none|none|
|ro|boolean|false|none|none|
|quiet|boolean|false|none|none|
|maproot_user|stringÂ¦null|false|none|none|
|maproot_group|stringÂ¦null|false|none|none|
|mapall_user|stringÂ¦null|false|none|none|
|mapall_group|stringÂ¦null|false|none|none|
|security|[string]|false|none|none|
|enabled|boolean|false|none|none|

<h2 id="tocS_sharing_nfs_update_1">sharing_nfs_update_1</h2>

<a id="schemasharing_nfs_update_1"></a>
<a id="schema_sharing_nfs_update_1"></a>
<a id="tocSsharing_nfs_update_1"></a>
<a id="tocssharing_nfs_update_1"></a>

```json
{}

```

sharingnfs_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|paths|[string]|false|none|none|
|comment|string|false|none|none|
|networks|[string]|false|none|none|
|hosts|[string]|false|none|none|
|alldirs|boolean|false|none|none|
|ro|boolean|false|none|none|
|quiet|boolean|false|none|none|
|maproot_user|stringÂ¦null|false|none|none|
|maproot_group|stringÂ¦null|false|none|none|
|mapall_user|stringÂ¦null|false|none|none|
|mapall_group|stringÂ¦null|false|none|none|
|security|[string]|false|none|none|
|enabled|boolean|false|none|none|

<h2 id="tocS_sharing_smb_create_0">sharing_smb_create_0</h2>

<a id="schemasharing_smb_create_0"></a>
<a id="schema_sharing_smb_create_0"></a>
<a id="tocSsharing_smb_create_0"></a>
<a id="tocssharing_smb_create_0"></a>

```json
{}

```

sharingsmb_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|purpose|string|false|none|none|
|path|string|false|none|none|
|path_suffix|string|false|none|none|
|home|boolean|false|none|none|
|name|string|false|none|none|
|comment|string|false|none|none|
|ro|boolean|false|none|none|
|browsable|boolean|false|none|none|
|timemachine|boolean|false|none|none|
|recyclebin|boolean|false|none|none|
|guestok|boolean|false|none|none|
|abe|boolean|false|none|none|
|hostsallow|[any]|false|none|none|
|hostsdeny|[any]|false|none|none|
|aapl_name_mangling|boolean|false|none|none|
|acl|boolean|false|none|none|
|durablehandle|boolean|false|none|none|
|shadowcopy|boolean|false|none|none|
|streams|boolean|false|none|none|
|fsrvp|boolean|false|none|none|
|auxsmbconf|string|false|none|none|
|enabled|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|purpose|NO_PRESET|
|purpose|DEFAULT_SHARE|
|purpose|ENHANCED_TIMEMACHINE|
|purpose|MULTI_PROTOCOL_AFP|
|purpose|MULTI_PROTOCOL_NFS|
|purpose|PRIVATE_DATASETS|
|purpose|WORM_DROPBOX|

<h2 id="tocS_sharing_smb_update_1">sharing_smb_update_1</h2>

<a id="schemasharing_smb_update_1"></a>
<a id="schema_sharing_smb_update_1"></a>
<a id="tocSsharing_smb_update_1"></a>
<a id="tocssharing_smb_update_1"></a>

```json
{}

```

sharingsmb_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|purpose|string|false|none|none|
|path|string|false|none|none|
|path_suffix|string|false|none|none|
|home|boolean|false|none|none|
|name|string|false|none|none|
|comment|string|false|none|none|
|ro|boolean|false|none|none|
|browsable|boolean|false|none|none|
|timemachine|boolean|false|none|none|
|recyclebin|boolean|false|none|none|
|guestok|boolean|false|none|none|
|abe|boolean|false|none|none|
|hostsallow|[any]|false|none|none|
|hostsdeny|[any]|false|none|none|
|aapl_name_mangling|boolean|false|none|none|
|acl|boolean|false|none|none|
|durablehandle|boolean|false|none|none|
|shadowcopy|boolean|false|none|none|
|streams|boolean|false|none|none|
|fsrvp|boolean|false|none|none|
|auxsmbconf|string|false|none|none|
|enabled|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|purpose|NO_PRESET|
|purpose|DEFAULT_SHARE|
|purpose|ENHANCED_TIMEMACHINE|
|purpose|MULTI_PROTOCOL_AFP|
|purpose|MULTI_PROTOCOL_NFS|
|purpose|PRIVATE_DATASETS|
|purpose|WORM_DROPBOX|

<h2 id="tocS_sharing_webdav_create_0">sharing_webdav_create_0</h2>

<a id="schemasharing_webdav_create_0"></a>
<a id="schema_sharing_webdav_create_0"></a>
<a id="tocSsharing_webdav_create_0"></a>
<a id="tocssharing_webdav_create_0"></a>

```json
{}

```

webdav_share_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|perm|boolean|false|none|none|
|ro|boolean|false|none|none|
|comment|string|false|none|none|
|name|string|false|none|none|
|path|string|false|none|none|
|enabled|boolean|false|none|none|

<h2 id="tocS_sharing_webdav_update_1">sharing_webdav_update_1</h2>

<a id="schemasharing_webdav_update_1"></a>
<a id="schema_sharing_webdav_update_1"></a>
<a id="tocSsharing_webdav_update_1"></a>
<a id="tocssharing_webdav_update_1"></a>

```json
{}

```

webdav_share_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|perm|boolean|false|none|none|
|ro|boolean|false|none|none|
|comment|string|false|none|none|
|name|string|false|none|none|
|path|string|false|none|none|
|enabled|boolean|false|none|none|

<h2 id="tocS_smart_update_0">smart_update_0</h2>

<a id="schemasmart_update_0"></a>
<a id="schema_smart_update_0"></a>
<a id="tocSsmart_update_0"></a>
<a id="tocssmart_update_0"></a>

```json
{}

```

smart_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|interval|integer|false|none|none|
|powermode|string|false|none|none|
|difference|integer|false|none|none|
|informational|integer|false|none|none|
|critical|integer|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|powermode|NEVER|
|powermode|SLEEP|
|powermode|STANDBY|
|powermode|IDLE|

<h2 id="tocS_smart_test_create_0">smart_test_create_0</h2>

<a id="schemasmart_test_create_0"></a>
<a id="schema_smart_test_create_0"></a>
<a id="tocSsmart_test_create_0"></a>
<a id="tocssmart_test_create_0"></a>

```json
{}

```

smart_task_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|schedule|object|false|none|none|
|Â» hour|string|false|none|none|
|Â» dom|string|false|none|none|
|Â» month|string|false|none|none|
|Â» dow|string|false|none|none|
|desc|string|false|none|none|
|all_disks|boolean|false|none|none|
|disks|[string]|false|none|none|
|type|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|type|LONG|
|type|SHORT|
|type|CONVEYANCE|
|type|OFFLINE|

<h2 id="tocS_smart_test_update_1">smart_test_update_1</h2>

<a id="schemasmart_test_update_1"></a>
<a id="schema_smart_test_update_1"></a>
<a id="tocSsmart_test_update_1"></a>
<a id="tocssmart_test_update_1"></a>

```json
{}

```

smart_task_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|schedule|object|false|none|none|
|Â» hour|string|false|none|none|
|Â» dom|string|false|none|none|
|Â» month|string|false|none|none|
|Â» dow|string|false|none|none|
|desc|string|false|none|none|
|all_disks|boolean|false|none|none|
|disks|[string]|false|none|none|
|type|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|type|LONG|
|type|SHORT|
|type|CONVEYANCE|
|type|OFFLINE|

<h2 id="tocS_smart_test_manual_test_0">smart_test_manual_test_0</h2>

<a id="schemasmart_test_manual_test_0"></a>
<a id="schema_smart_test_manual_test_0"></a>
<a id="tocSsmart_test_manual_test_0"></a>
<a id="tocssmart_test_manual_test_0"></a>

```json
[
  {}
]

```

disks

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|disks|[object]|false|none|none|

<h2 id="tocS_smb_update_0">smb_update_0</h2>

<a id="schemasmb_update_0"></a>
<a id="schema_smb_update_0"></a>
<a id="tocSsmb_update_0"></a>
<a id="tocssmb_update_0"></a>

```json
{}

```

smb_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|netbiosname|string|false|none|none|
|netbiosname_b|string|false|none|none|
|netbiosalias|[string]|false|none|none|
|workgroup|string|false|none|none|
|description|string|false|none|none|
|enable_smb1|boolean|false|none|none|
|unixcharset|string|false|none|none|
|loglevel|string|false|none|none|
|syslog|boolean|false|none|none|
|aapl_extensions|boolean|false|none|none|
|localmaster|boolean|false|none|none|
|guest|string|false|none|none|
|admin_group|stringÂ¦null|false|none|none|
|filemask|string|false|none|none|
|dirmask|string|false|none|none|
|ntlmv1_auth|boolean|false|none|none|
|bindip|[string]|false|none|none|
|smb_options|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|loglevel|NONE|
|loglevel|MINIMUM|
|loglevel|NORMAL|
|loglevel|FULL|
|loglevel|DEBUG|

<h2 id="tocS_smb_get_remote_acl_0">smb_get_remote_acl_0</h2>

<a id="schemasmb_get_remote_acl_0"></a>
<a id="schema_smb_get_remote_acl_0"></a>
<a id="tocSsmb_get_remote_acl_0"></a>
<a id="tocssmb_get_remote_acl_0"></a>

```json
{}

```

get_remote_acl

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|server|string|false|none|none|
|share|string|false|none|none|
|path|string|false|none|none|
|username|string|false|none|none|
|password|string|false|none|none|
|options|object|false|none|none|
|Â» use_kerberos|boolean|false|none|none|
|Â» output_format|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|output_format|SMB|
|output_format|LOCAL|

<h2 id="tocS_smb_status_0">smb_status_0</h2>

<a id="schemasmb_status_0"></a>
<a id="schema_smb_status_0"></a>
<a id="tocSsmb_status_0"></a>
<a id="tocssmb_status_0"></a>

```json
"ALL"

```

info_level

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|info_level|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|info_level|AUTH_LOG|
|info_level|ALL|
|info_level|SESSIONS|
|info_level|SHARES|
|info_level|LOCKS|
|info_level|BYTERANGE|
|info_level|NOTIFICATIONS|

<h2 id="tocS_smb_status_1">smb_status_1</h2>

<a id="schemasmb_status_1"></a>
<a id="schema_smb_status_1"></a>
<a id="tocSsmb_status_1"></a>
<a id="tocssmb_status_1"></a>

```json
[]

```

query-filters

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|query-filters|[any]|false|none|none|

<h2 id="tocS_smb_status_2">smb_status_2</h2>

<a id="schemasmb_status_2"></a>
<a id="schema_smb_status_2"></a>
<a id="tocSsmb_status_2"></a>
<a id="tocssmb_status_2"></a>

```json
{}

```

query-options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|relationships|boolean|false|none|none|
|extend|stringÂ¦null|false|none|none|
|extend_context|stringÂ¦null|false|none|none|
|prefix|stringÂ¦null|false|none|none|
|extra|object|false|none|none|
|order_by|[any]|false|none|none|
|select|[any]|false|none|none|
|count|boolean|false|none|none|
|get|boolean|false|none|none|
|offset|integer|false|none|none|
|limit|integer|false|none|none|

<h2 id="tocS_smb_status_3">smb_status_3</h2>

<a id="schemasmb_status_3"></a>
<a id="schema_smb_status_3"></a>
<a id="tocSsmb_status_3"></a>
<a id="tocssmb_status_3"></a>

```json
{}

```

status_options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|verbose|boolean|false|none|none|
|fast|boolean|false|none|none|
|restrict_user|string|false|none|none|

<h2 id="tocS_smb_status">smb_status</h2>

<a id="schemasmb_status"></a>
<a id="schema_smb_status"></a>
<a id="tocSsmb_status"></a>
<a id="tocssmb_status"></a>

```json
{
  "info_level": "ALL",
  "query-filters": [],
  "query-options": {},
  "status_options": {}
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|info_level|[smb_status_0](#schemasmb_status_0)|false|none|none|
|query-filters|[smb_status_1](#schemasmb_status_1)|false|none|none|
|query-options|[smb_status_2](#schemasmb_status_2)|false|none|none|
|status_options|[smb_status_3](#schemasmb_status_3)|false|none|none|

<h2 id="tocS_smb_sharesec_create_0">smb_sharesec_create_0</h2>

<a id="schemasmb_sharesec_create_0"></a>
<a id="schema_smb_sharesec_create_0"></a>
<a id="tocSsmb_sharesec_create_0"></a>
<a id="tocssmb_sharesec_create_0"></a>

```json
{}

```

smbsharesec_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|share_name|string|false|none|none|
|share_acl|[object]|false|none|none|

<h2 id="tocS_smb_sharesec_update_1">smb_sharesec_update_1</h2>

<a id="schemasmb_sharesec_update_1"></a>
<a id="schema_smb_sharesec_update_1"></a>
<a id="tocSsmb_sharesec_update_1"></a>
<a id="tocssmb_sharesec_update_1"></a>

```json
{}

```

smbsharesec_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|share_acl|[object]|false|none|none|

<h2 id="tocS_smb_sharesec_getacl_0">smb_sharesec_getacl_0</h2>

<a id="schemasmb_sharesec_getacl_0"></a>
<a id="schema_smb_sharesec_getacl_0"></a>
<a id="tocSsmb_sharesec_getacl_0"></a>
<a id="tocssmb_sharesec_getacl_0"></a>

```json
"string"

```

share_name

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|share_name|string|false|none|none|

<h2 id="tocS_smb_sharesec_getacl_1">smb_sharesec_getacl_1</h2>

<a id="schemasmb_sharesec_getacl_1"></a>
<a id="schema_smb_sharesec_getacl_1"></a>
<a id="tocSsmb_sharesec_getacl_1"></a>
<a id="tocssmb_sharesec_getacl_1"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|resolve_sids|boolean|false|none|none|

<h2 id="tocS_smb_sharesec_getacl">smb_sharesec_getacl</h2>

<a id="schemasmb_sharesec_getacl"></a>
<a id="schema_smb_sharesec_getacl"></a>
<a id="tocSsmb_sharesec_getacl"></a>
<a id="tocssmb_sharesec_getacl"></a>

```json
{
  "share_name": "string",
  "options": {}
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|share_name|[smb_sharesec_getacl_0](#schemasmb_sharesec_getacl_0)|false|none|none|
|options|[smb_sharesec_getacl_1](#schemasmb_sharesec_getacl_1)|false|none|none|

<h2 id="tocS_snmp_update_0">snmp_update_0</h2>

<a id="schemasnmp_update_0"></a>
<a id="schema_snmp_update_0"></a>
<a id="tocSsnmp_update_0"></a>
<a id="tocssnmp_update_0"></a>

```json
{}

```

snmp_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|location|string|false|none|none|
|contact|string|false|none|none|
|traps|boolean|false|none|none|
|v3|boolean|false|none|none|
|community|string|false|none|none|
|v3_username|string|false|none|none|
|v3_authtype|string|false|none|none|
|v3_password|string|false|none|none|
|v3_privproto|stringÂ¦null|false|none|none|
|v3_privpassphrase|string|false|none|none|
|loglevel|integer|false|none|none|
|options|string|false|none|none|
|zilstat|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|v3_authtype||
|v3_authtype|MD5|
|v3_authtype|SHA|
|v3_privproto|null|
|v3_privproto|AES|
|v3_privproto|DES|

<h2 id="tocS_ssh_update_0">ssh_update_0</h2>

<a id="schemassh_update_0"></a>
<a id="schema_ssh_update_0"></a>
<a id="tocSssh_update_0"></a>
<a id="tocsssh_update_0"></a>

```json
{}

```

ssh_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|bindiface|[string]|false|none|none|
|tcpport|integer|false|none|none|
|rootlogin|boolean|false|none|none|
|passwordauth|boolean|false|none|none|
|kerberosauth|boolean|false|none|none|
|tcpfwd|boolean|false|none|none|
|compression|boolean|false|none|none|
|sftp_log_level|string|false|none|none|
|sftp_log_facility|string|false|none|none|
|weak_ciphers|[string]|false|none|none|
|options|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|sftp_log_level||
|sftp_log_level|QUIET|
|sftp_log_level|FATAL|
|sftp_log_level|ERROR|
|sftp_log_level|INFO|
|sftp_log_level|VERBOSE|
|sftp_log_level|DEBUG|
|sftp_log_level|DEBUG2|
|sftp_log_level|DEBUG3|
|sftp_log_facility||
|sftp_log_facility|DAEMON|
|sftp_log_facility|USER|
|sftp_log_facility|AUTH|
|sftp_log_facility|LOCAL0|
|sftp_log_facility|LOCAL1|
|sftp_log_facility|LOCAL2|
|sftp_log_facility|LOCAL3|
|sftp_log_facility|LOCAL4|
|sftp_log_facility|LOCAL5|
|sftp_log_facility|LOCAL6|
|sftp_log_facility|LOCAL7|

<h2 id="tocS_staticroute_create_0">staticroute_create_0</h2>

<a id="schemastaticroute_create_0"></a>
<a id="schema_staticroute_create_0"></a>
<a id="tocSstaticroute_create_0"></a>
<a id="tocsstaticroute_create_0"></a>

```json
{}

```

staticroute_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|destination|string|false|none|none|
|gateway|string|false|none|none|
|description|string|false|none|none|

<h2 id="tocS_staticroute_update_1">staticroute_update_1</h2>

<a id="schemastaticroute_update_1"></a>
<a id="schema_staticroute_update_1"></a>
<a id="tocSstaticroute_update_1"></a>
<a id="tocsstaticroute_update_1"></a>

```json
{}

```

staticroute_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|destination|string|false|none|none|
|gateway|string|false|none|none|
|description|string|false|none|none|

<h2 id="tocS_stats_get_data_0">stats_get_data_0</h2>

<a id="schemastats_get_data_0"></a>
<a id="schema_stats_get_data_0"></a>
<a id="tocSstats_get_data_0"></a>
<a id="tocsstats_get_data_0"></a>

```json
[
  {}
]

```

stats_list

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|stats_list|[object]|false|none|none|

<h2 id="tocS_stats_get_data_1">stats_get_data_1</h2>

<a id="schemastats_get_data_1"></a>
<a id="schema_stats_get_data_1"></a>
<a id="tocSstats_get_data_1"></a>
<a id="tocsstats_get_data_1"></a>

```json
{}

```

stats-filter

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|step|integer|false|none|none|
|start|string|false|none|none|
|end|string|false|none|none|

<h2 id="tocS_stats_get_data">stats_get_data</h2>

<a id="schemastats_get_data"></a>
<a id="schema_stats_get_data"></a>
<a id="tocSstats_get_data"></a>
<a id="tocsstats_get_data"></a>

```json
{
  "stats_list": [
    {}
  ],
  "stats-filter": {}
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|stats_list|[stats_get_data_0](#schemastats_get_data_0)|false|none|none|
|stats-filter|[stats_get_data_1](#schemastats_get_data_1)|false|none|none|

<h2 id="tocS_stats_get_dataset_info_0">stats_get_dataset_info_0</h2>

<a id="schemastats_get_dataset_info_0"></a>
<a id="schema_stats_get_dataset_info_0"></a>
<a id="tocSstats_get_dataset_info_0"></a>
<a id="tocsstats_get_dataset_info_0"></a>

```json
"string"

```

source

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|source|string|false|none|none|

<h2 id="tocS_stats_get_dataset_info_1">stats_get_dataset_info_1</h2>

<a id="schemastats_get_dataset_info_1"></a>
<a id="schema_stats_get_dataset_info_1"></a>
<a id="tocSstats_get_dataset_info_1"></a>
<a id="tocsstats_get_dataset_info_1"></a>

```json
"string"

```

type

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|type|string|false|none|none|

<h2 id="tocS_stats_get_dataset_info">stats_get_dataset_info</h2>

<a id="schemastats_get_dataset_info"></a>
<a id="schema_stats_get_dataset_info"></a>
<a id="tocSstats_get_dataset_info"></a>
<a id="tocsstats_get_dataset_info"></a>

```json
{
  "source": "string",
  "type": "string"
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|source|[stats_get_dataset_info_0](#schemastats_get_dataset_info_0)|false|none|none|
|type|[stats_get_dataset_info_1](#schemastats_get_dataset_info_1)|false|none|none|

<h2 id="tocS_support_update_0">support_update_0</h2>

<a id="schemasupport_update_0"></a>
<a id="schema_support_update_0"></a>
<a id="tocSsupport_update_0"></a>
<a id="tocssupport_update_0"></a>

```json
{}

```

support_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|enabled|booleanÂ¦null|false|none|none|
|name|string|false|none|none|
|title|string|false|none|none|
|email|string|false|none|none|
|phone|string|false|none|none|
|secondary_name|string|false|none|none|
|secondary_title|string|false|none|none|
|secondary_email|string|false|none|none|
|secondary_phone|string|false|none|none|

<h2 id="tocS_support_fetch_categories_0">support_fetch_categories_0</h2>

<a id="schemasupport_fetch_categories_0"></a>
<a id="schema_support_fetch_categories_0"></a>
<a id="tocSsupport_fetch_categories_0"></a>
<a id="tocssupport_fetch_categories_0"></a>

```json
"string"

```

username

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|username|string|false|none|none|

<h2 id="tocS_support_fetch_categories_1">support_fetch_categories_1</h2>

<a id="schemasupport_fetch_categories_1"></a>
<a id="schema_support_fetch_categories_1"></a>
<a id="tocSsupport_fetch_categories_1"></a>
<a id="tocssupport_fetch_categories_1"></a>

```json
"string"

```

password

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|password|string|false|none|none|

<h2 id="tocS_support_fetch_categories">support_fetch_categories</h2>

<a id="schemasupport_fetch_categories"></a>
<a id="schema_support_fetch_categories"></a>
<a id="tocSsupport_fetch_categories"></a>
<a id="tocssupport_fetch_categories"></a>

```json
{
  "username": "string",
  "password": "string"
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|username|[support_fetch_categories_0](#schemasupport_fetch_categories_0)|false|none|none|
|password|[support_fetch_categories_1](#schemasupport_fetch_categories_1)|false|none|none|

<h2 id="tocS_support_new_ticket_0">support_new_ticket_0</h2>

<a id="schemasupport_new_ticket_0"></a>
<a id="schema_support_new_ticket_0"></a>
<a id="tocSsupport_new_ticket_0"></a>
<a id="tocssupport_new_ticket_0"></a>

```json
{}

```

new_ticket

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|title|string|false|none|none|
|body|string|false|none|none|
|category|string|false|none|none|
|attach_debug|boolean|false|none|none|
|username|string|false|none|none|
|password|string|false|none|none|
|type|string|false|none|none|
|criticality|string|false|none|none|
|environment|string|false|none|none|
|phone|string|false|none|none|
|name|string|false|none|none|
|email|string|false|none|none|
|cc|[string]|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|type|BUG|
|type|FEATURE|

<h2 id="tocS_system_feature_enabled_0">system_feature_enabled_0</h2>

<a id="schemasystem_feature_enabled_0"></a>
<a id="schema_system_feature_enabled_0"></a>
<a id="tocSsystem_feature_enabled_0"></a>
<a id="tocssystem_feature_enabled_0"></a>

```json
"DEDUP"

```

feature

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|feature|string|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|feature|DEDUP|
|feature|FIBRECHANNEL|
|feature|JAILS|
|feature|VM|

<h2 id="tocS_system_license_update_0">system_license_update_0</h2>

<a id="schemasystem_license_update_0"></a>
<a id="schema_system_license_update_0"></a>
<a id="tocSsystem_license_update_0"></a>
<a id="tocssystem_license_update_0"></a>

```json
"string"

```

license

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|license|string|false|none|none|

<h2 id="tocS_system_reboot_0">system_reboot_0</h2>

<a id="schemasystem_reboot_0"></a>
<a id="schema_system_reboot_0"></a>
<a id="tocSsystem_reboot_0"></a>
<a id="tocssystem_reboot_0"></a>

```json
{}

```

system-reboot

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|delay|integer|false|none|none|

<h2 id="tocS_system_shutdown_0">system_shutdown_0</h2>

<a id="schemasystem_shutdown_0"></a>
<a id="schema_system_shutdown_0"></a>
<a id="tocSsystem_shutdown_0"></a>
<a id="tocssystem_shutdown_0"></a>

```json
{}

```

system-shutdown

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|delay|integer|false|none|none|

<h2 id="tocS_system_advanced_update_0">system_advanced_update_0</h2>

<a id="schemasystem_advanced_update_0"></a>
<a id="schema_system_advanced_update_0"></a>
<a id="tocSsystem_advanced_update_0"></a>
<a id="tocssystem_advanced_update_0"></a>

```json
{}

```

system_advanced_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|advancedmode|boolean|false|none|none|
|autotune|boolean|false|none|none|
|boot_scrub|integer|false|none|none|
|consolemenu|boolean|false|none|none|
|consolemsg|boolean|false|none|none|
|debugkernel|boolean|false|none|none|
|fqdn_syslog|boolean|false|none|none|
|motd|string|false|none|none|
|powerdaemon|boolean|false|none|none|
|serialconsole|boolean|false|none|none|
|serialport|string|false|none|none|
|serialspeed|string|false|none|none|
|swapondrive|integer|false|none|none|
|overprovision|integerÂ¦null|false|none|none|
|traceback|boolean|false|none|none|
|uploadcrash|boolean|false|none|none|
|anonstats|boolean|false|none|none|
|sed_user|string|false|none|none|
|sed_passwd|string|false|none|none|
|sysloglevel|string|false|none|none|
|syslogserver|string|false|none|none|
|syslog_transport|string|false|none|none|
|syslog_tls_certificate|integerÂ¦null|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|serialspeed|9600|
|serialspeed|19200|
|serialspeed|38400|
|serialspeed|57600|
|serialspeed|115200|
|sed_user|USER|
|sed_user|MASTER|
|sysloglevel|F_EMERG|
|sysloglevel|F_ALERT|
|sysloglevel|F_CRIT|
|sysloglevel|F_ERR|
|sysloglevel|F_WARNING|
|sysloglevel|F_NOTICE|
|sysloglevel|F_INFO|
|sysloglevel|F_DEBUG|
|sysloglevel|F_IS_DEBUG|
|syslog_transport|UDP|
|syslog_transport|TCP|
|syslog_transport|TLS|

<h2 id="tocS_system_general_update_0">system_general_update_0</h2>

<a id="schemasystem_general_update_0"></a>
<a id="schema_system_general_update_0"></a>
<a id="tocSsystem_general_update_0"></a>
<a id="tocssystem_general_update_0"></a>

```json
{}

```

general_settings

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|ui_certificate|integerÂ¦null|false|none|none|
|ui_httpsport|integer|false|none|none|
|ui_httpsredirect|boolean|false|none|none|
|ui_httpsprotocols|[string]|false|none|none|
|ui_port|integer|false|none|none|
|ui_address|[string]|false|none|none|
|ui_v6address|[string]|false|none|none|
|kbdmap|string|false|none|none|
|language|string|false|none|none|
|sysloglevel|string|false|none|none|
|syslogserver|string|false|none|none|
|timezone|string|false|none|none|
|crash_reporting|booleanÂ¦null|false|none|none|
|usage_collection|booleanÂ¦null|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|sysloglevel|F_EMERG|
|sysloglevel|F_ALERT|
|sysloglevel|F_CRIT|
|sysloglevel|F_ERR|
|sysloglevel|F_WARNING|
|sysloglevel|F_NOTICE|
|sysloglevel|F_INFO|
|sysloglevel|F_DEBUG|
|sysloglevel|F_IS_DEBUG|

<h2 id="tocS_system_ntpserver_create_0">system_ntpserver_create_0</h2>

<a id="schemasystem_ntpserver_create_0"></a>
<a id="schema_system_ntpserver_create_0"></a>
<a id="tocSsystem_ntpserver_create_0"></a>
<a id="tocssystem_ntpserver_create_0"></a>

```json
{}

```

ntp_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|address|string|false|none|none|
|burst|boolean|false|none|none|
|iburst|boolean|false|none|none|
|prefer|boolean|false|none|none|
|minpoll|integer|false|none|none|
|maxpoll|integer|false|none|none|
|force|boolean|false|none|none|

<h2 id="tocS_system_ntpserver_update_1">system_ntpserver_update_1</h2>

<a id="schemasystem_ntpserver_update_1"></a>
<a id="schema_system_ntpserver_update_1"></a>
<a id="tocSsystem_ntpserver_update_1"></a>
<a id="tocssystem_ntpserver_update_1"></a>

```json
{}

```

ntp_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|address|string|false|none|none|
|burst|boolean|false|none|none|
|iburst|boolean|false|none|none|
|prefer|boolean|false|none|none|
|minpoll|integer|false|none|none|
|maxpoll|integer|false|none|none|
|force|boolean|false|none|none|

<h2 id="tocS_systemdataset_update_0">systemdataset_update_0</h2>

<a id="schemasystemdataset_update_0"></a>
<a id="schema_systemdataset_update_0"></a>
<a id="tocSsystemdataset_update_0"></a>
<a id="tocssystemdataset_update_0"></a>

```json
{}

```

sysdataset_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|pool|stringÂ¦null|false|none|none|
|pool_exclude|stringÂ¦null|false|none|none|
|syslog|boolean|false|none|none|

<h2 id="tocS_tftp_update_0">tftp_update_0</h2>

<a id="schematftp_update_0"></a>
<a id="schema_tftp_update_0"></a>
<a id="tocStftp_update_0"></a>
<a id="tocstftp_update_0"></a>

```json
{}

```

tftp_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|newfiles|boolean|false|none|none|
|directory|string|false|none|none|
|host|string|false|none|none|
|port|integer|false|none|none|
|options|string|false|none|none|
|umask|string|false|none|none|
|username|string|false|none|none|

<h2 id="tocS_truecommand_update_0">truecommand_update_0</h2>

<a id="schematruecommand_update_0"></a>
<a id="schema_truecommand_update_0"></a>
<a id="tocStruecommand_update_0"></a>
<a id="tocstruecommand_update_0"></a>

```json
{}

```

truecommand_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|enabled|boolean|false|none|none|
|api_key|stringÂ¦null|false|none|none|

<h2 id="tocS_truenas_set_production_0">truenas_set_production_0</h2>

<a id="schematruenas_set_production_0"></a>
<a id="schema_truenas_set_production_0"></a>
<a id="tocStruenas_set_production_0"></a>
<a id="tocstruenas_set_production_0"></a>

```json
true

```

production

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|production|boolean|false|none|none|

<h2 id="tocS_truenas_set_production_1">truenas_set_production_1</h2>

<a id="schematruenas_set_production_1"></a>
<a id="schema_truenas_set_production_1"></a>
<a id="tocStruenas_set_production_1"></a>
<a id="tocstruenas_set_production_1"></a>

```json
false

```

attach_debug

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|attach_debug|boolean|false|none|none|

<h2 id="tocS_truenas_set_production">truenas_set_production</h2>

<a id="schematruenas_set_production"></a>
<a id="schema_truenas_set_production"></a>
<a id="tocStruenas_set_production"></a>
<a id="tocstruenas_set_production"></a>

```json
{
  "production": true,
  "attach_debug": false
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|production|[truenas_set_production_0](#schematruenas_set_production_0)|false|none|none|
|attach_debug|[truenas_set_production_1](#schematruenas_set_production_1)|false|none|none|

<h2 id="tocS_truenas_update_customer_information_0">truenas_update_customer_information_0</h2>

<a id="schematruenas_update_customer_information_0"></a>
<a id="schema_truenas_update_customer_information_0"></a>
<a id="tocStruenas_update_customer_information_0"></a>
<a id="tocstruenas_update_customer_information_0"></a>

```json
{}

```

customer_information_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|company|string|false|none|none|
|administrative_user|object|false|none|none|
|Â» first_name|string|false|none|none|
|Â» last_name|string|false|none|none|
|Â» title|string|false|none|none|
|Â» office_phone|string|false|none|none|
|Â» mobile_phone|string|false|none|none|
|Â» primary_email|string|false|none|none|
|Â» secondary_email|string|false|none|none|
|Â» address|string|false|none|none|
|Â» city|string|false|none|none|
|Â» state|string|false|none|none|
|Â» zip|string|false|none|none|
|Â» country|string|false|none|none|
|technical_user|object|false|none|none|
|Â» first_name|string|false|none|none|
|Â» last_name|string|false|none|none|
|Â» title|string|false|none|none|
|Â» office_phone|string|false|none|none|
|Â» mobile_phone|string|false|none|none|
|Â» primary_email|string|false|none|none|
|Â» secondary_email|string|false|none|none|
|Â» address|string|false|none|none|
|Â» city|string|false|none|none|
|Â» state|string|false|none|none|
|Â» zip|string|false|none|none|
|Â» country|string|false|none|none|
|reseller|object|false|none|none|
|Â» company|string|false|none|none|
|Â» first_name|string|false|none|none|
|Â» last_name|string|false|none|none|
|Â» title|string|false|none|none|
|Â» office_phone|string|false|none|none|
|Â» mobile_phone|string|false|none|none|
|physical_location|object|false|none|none|
|Â» address|string|false|none|none|
|Â» city|string|false|none|none|
|Â» state|string|false|none|none|
|Â» zip|string|false|none|none|
|Â» country|string|false|none|none|
|Â» contact_name|string|false|none|none|
|Â» contact_phone_number|string|false|none|none|
|Â» contact_email|string|false|none|none|
|primary_use_case|string|false|none|none|
|other_primary_use_case|string|false|none|none|

<h2 id="tocS_tunable_create_0">tunable_create_0</h2>

<a id="schematunable_create_0"></a>
<a id="schema_tunable_create_0"></a>
<a id="tocStunable_create_0"></a>
<a id="tocstunable_create_0"></a>

```json
{}

```

tunable_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|var|string|false|none|none|
|value|string|false|none|none|
|type|string|false|none|none|
|comment|string|false|none|none|
|enabled|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|type|SYSCTL|
|type|LOADER|
|type|RC|

<h2 id="tocS_tunable_update_1">tunable_update_1</h2>

<a id="schematunable_update_1"></a>
<a id="schema_tunable_update_1"></a>
<a id="tocStunable_update_1"></a>
<a id="tocstunable_update_1"></a>

```json
{}

```

tunable_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|var|string|false|none|none|
|value|string|false|none|none|
|type|string|false|none|none|
|comment|string|false|none|none|
|enabled|boolean|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|type|SYSCTL|
|type|LOADER|
|type|RC|

<h2 id="tocS_update_check_available_0">update_check_available_0</h2>

<a id="schemaupdate_check_available_0"></a>
<a id="schema_update_check_available_0"></a>
<a id="tocSupdate_check_available_0"></a>
<a id="tocsupdate_check_available_0"></a>

```json
{}

```

update-check-available

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|train|string|false|none|none|

<h2 id="tocS_update_get_pending_0">update_get_pending_0</h2>

<a id="schemaupdate_get_pending_0"></a>
<a id="schema_update_get_pending_0"></a>
<a id="tocSupdate_get_pending_0"></a>
<a id="tocsupdate_get_pending_0"></a>

```json
null

```

path

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|path|stringÂ¦null|false|none|none|

<h2 id="tocS_update_manual_0">update_manual_0</h2>

<a id="schemaupdate_manual_0"></a>
<a id="schema_update_manual_0"></a>
<a id="tocSupdate_manual_0"></a>
<a id="tocsupdate_manual_0"></a>

```json
"string"

```

path

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|path|string|false|none|none|

<h2 id="tocS_update_set_auto_download_0">update_set_auto_download_0</h2>

<a id="schemaupdate_set_auto_download_0"></a>
<a id="schema_update_set_auto_download_0"></a>
<a id="tocSupdate_set_auto_download_0"></a>
<a id="tocsupdate_set_auto_download_0"></a>

```json
true

```

autocheck

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|autocheck|boolean|false|none|none|

<h2 id="tocS_update_set_train_0">update_set_train_0</h2>

<a id="schemaupdate_set_train_0"></a>
<a id="schema_update_set_train_0"></a>
<a id="tocSupdate_set_train_0"></a>
<a id="tocsupdate_set_train_0"></a>

```json
"string"

```

train

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|train|string|false|none|none|

<h2 id="tocS_update_update_0">update_update_0</h2>

<a id="schemaupdate_update_0"></a>
<a id="schema_update_update_0"></a>
<a id="tocSupdate_update_0"></a>
<a id="tocsupdate_update_0"></a>

```json
{}

```

update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|train|string|false|none|none|
|reboot|boolean|false|none|none|

<h2 id="tocS_ups_update_0">ups_update_0</h2>

<a id="schemaups_update_0"></a>
<a id="schema_ups_update_0"></a>
<a id="tocSups_update_0"></a>
<a id="tocsups_update_0"></a>

```json
{}

```

ups_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|emailnotify|boolean|false|none|none|
|powerdown|boolean|false|none|none|
|rmonitor|boolean|false|none|none|
|nocommwarntime|integerÂ¦null|false|none|none|
|remoteport|integer|false|none|none|
|shutdowntimer|integer|false|none|none|
|hostsync|integer|false|none|none|
|description|string|false|none|none|
|driver|string|false|none|none|
|extrausers|string|false|none|none|
|identifier|string|false|none|none|
|mode|string|false|none|none|
|monpwd|string|false|none|none|
|monuser|string|false|none|none|
|options|string|false|none|none|
|optionsupsd|string|false|none|none|
|port|string|false|none|none|
|remotehost|string|false|none|none|
|shutdown|string|false|none|none|
|shutdowncmd|stringÂ¦null|false|none|none|
|subject|string|false|none|none|
|toemail|[string]|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|mode|MASTER|
|mode|SLAVE|
|shutdown|LOWBATT|
|shutdown|BATT|

<h2 id="tocS_user_create_0">user_create_0</h2>

<a id="schemauser_create_0"></a>
<a id="schema_user_create_0"></a>
<a id="tocSuser_create_0"></a>
<a id="tocsuser_create_0"></a>

```json
{}

```

user_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|uid|integer|false|none|none|
|username|string|false|none|none|
|group|integer|false|none|none|
|group_create|boolean|false|none|none|
|home|string|false|none|none|
|home_mode|string|false|none|none|
|shell|string|false|none|none|
|full_name|string|false|none|none|
|email|stringÂ¦null|false|none|none|
|password|string|false|none|none|
|password_disabled|boolean|false|none|none|
|locked|boolean|false|none|none|
|microsoft_account|boolean|false|none|none|
|smb|boolean|false|none|none|
|sudo|boolean|false|none|none|
|sshpubkey|stringÂ¦null|false|none|none|
|groups|[any]|false|none|none|
|attributes|object|false|none|none|

<h2 id="tocS_user_delete_1">user_delete_1</h2>

<a id="schemauser_delete_1"></a>
<a id="schema_user_delete_1"></a>
<a id="tocSuser_delete_1"></a>
<a id="tocsuser_delete_1"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|delete_group|boolean|false|none|none|

<h2 id="tocS_user_update_1">user_update_1</h2>

<a id="schemauser_update_1"></a>
<a id="schema_user_update_1"></a>
<a id="tocSuser_update_1"></a>
<a id="tocsuser_update_1"></a>

```json
{}

```

user_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|uid|integer|false|none|none|
|username|string|false|none|none|
|group|integer|false|none|none|
|home|string|false|none|none|
|home_mode|string|false|none|none|
|shell|string|false|none|none|
|full_name|string|false|none|none|
|email|stringÂ¦null|false|none|none|
|password|string|false|none|none|
|password_disabled|boolean|false|none|none|
|locked|boolean|false|none|none|
|microsoft_account|boolean|false|none|none|
|smb|boolean|false|none|none|
|sudo|boolean|false|none|none|
|sshpubkey|stringÂ¦null|false|none|none|
|groups|[any]|false|none|none|
|attributes|object|false|none|none|

<h2 id="tocS_user_get_user_obj_0">user_get_user_obj_0</h2>

<a id="schemauser_get_user_obj_0"></a>
<a id="schema_user_get_user_obj_0"></a>
<a id="tocSuser_get_user_obj_0"></a>
<a id="tocsuser_get_user_obj_0"></a>

```json
{}

```

get_user_obj

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|username|string|false|none|none|
|uid|integer|false|none|none|

<h2 id="tocS_user_pop_attribute_1">user_pop_attribute_1</h2>

<a id="schemauser_pop_attribute_1"></a>
<a id="schema_user_pop_attribute_1"></a>
<a id="tocSuser_pop_attribute_1"></a>
<a id="tocsuser_pop_attribute_1"></a>

```json
"string"

```

key

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|key|string|false|none|none|

<h2 id="tocS_user_set_attribute_1">user_set_attribute_1</h2>

<a id="schemauser_set_attribute_1"></a>
<a id="schema_user_set_attribute_1"></a>
<a id="tocSuser_set_attribute_1"></a>
<a id="tocsuser_set_attribute_1"></a>

```json
"string"

```

key

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|key|string|false|none|none|

<h2 id="tocS_user_set_attribute_2">user_set_attribute_2</h2>

<a id="schemauser_set_attribute_2"></a>
<a id="schema_user_set_attribute_2"></a>
<a id="tocSuser_set_attribute_2"></a>
<a id="tocsuser_set_attribute_2"></a>

```json
"string"

```

value

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|value|any|false|none|none|

anyOf

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|*anonymous*|string|false|none|none|

or

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|*anonymous*|integer|false|none|none|

or

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|*anonymous*|boolean|false|none|none|

or

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|*anonymous*|object|false|none|none|

or

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|*anonymous*|array|false|none|none|

<h2 id="tocS_user_set_attribute">user_set_attribute</h2>

<a id="schemauser_set_attribute"></a>
<a id="schema_user_set_attribute"></a>
<a id="tocSuser_set_attribute"></a>
<a id="tocsuser_set_attribute"></a>

```json
{
  "key": "string",
  "value": "string"
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|key|[user_set_attribute_1](#schemauser_set_attribute_1)|false|none|none|
|value|[user_set_attribute_2](#schemauser_set_attribute_2)|false|none|none|

<h2 id="tocS_user_shell_choices_0">user_shell_choices_0</h2>

<a id="schemauser_shell_choices_0"></a>
<a id="schema_user_shell_choices_0"></a>
<a id="tocSuser_shell_choices_0"></a>
<a id="tocsuser_shell_choices_0"></a>

```json
null

```

user_id

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|user_id|integerÂ¦null|false|none|none|

<h2 id="tocS_vm_create_0">vm_create_0</h2>

<a id="schemavm_create_0"></a>
<a id="schema_vm_create_0"></a>
<a id="tocSvm_create_0"></a>
<a id="tocsvm_create_0"></a>

```json
{}

```

vm_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|
|description|string|false|none|none|
|vcpus|integer|false|none|none|
|cores|integer|false|none|none|
|threads|integer|false|none|none|
|memory|integer|false|none|none|
|bootloader|string|false|none|none|
|grubconfig|stringÂ¦null|false|none|none|
|devices|[object]|false|none|none|
|autostart|boolean|false|none|none|
|time|string|false|none|none|
|shutdown_timeout|integer|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|bootloader|UEFI|
|bootloader|UEFI_CSM|
|bootloader|GRUB|
|time|LOCAL|
|time|UTC|

<h2 id="tocS_vm_delete_1">vm_delete_1</h2>

<a id="schemavm_delete_1"></a>
<a id="schema_vm_delete_1"></a>
<a id="tocSvm_delete_1"></a>
<a id="tocsvm_delete_1"></a>

```json
{}

```

vm_delete

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|zvols|boolean|false|none|none|
|force|boolean|false|none|none|

<h2 id="tocS_vm_update_1">vm_update_1</h2>

<a id="schemavm_update_1"></a>
<a id="schema_vm_update_1"></a>
<a id="tocSvm_update_1"></a>
<a id="tocsvm_update_1"></a>

```json
{}

```

vm_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|
|description|string|false|none|none|
|vcpus|integer|false|none|none|
|cores|integer|false|none|none|
|threads|integer|false|none|none|
|memory|integer|false|none|none|
|bootloader|string|false|none|none|
|grubconfig|stringÂ¦null|false|none|none|
|devices|[object]|false|none|none|
|autostart|boolean|false|none|none|
|time|string|false|none|none|
|shutdown_timeout|integer|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|bootloader|UEFI|
|bootloader|UEFI_CSM|
|bootloader|GRUB|
|time|LOCAL|
|time|UTC|

<h2 id="tocS_vm_clone_1">vm_clone_1</h2>

<a id="schemavm_clone_1"></a>
<a id="schema_vm_clone_1"></a>
<a id="tocSvm_clone_1"></a>
<a id="tocsvm_clone_1"></a>

```json
null

```

name

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|name|string|false|none|none|

<h2 id="tocS_vm_get_attached_iface_0">vm_get_attached_iface_0</h2>

<a id="schemavm_get_attached_iface_0"></a>
<a id="schema_vm_get_attached_iface_0"></a>
<a id="tocSvm_get_attached_iface_0"></a>
<a id="tocsvm_get_attached_iface_0"></a>

```json
0

```

id

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|id|integer|false|none|none|

<h2 id="tocS_vm_get_available_memory_0">vm_get_available_memory_0</h2>

<a id="schemavm_get_available_memory_0"></a>
<a id="schema_vm_get_available_memory_0"></a>
<a id="tocSvm_get_available_memory_0"></a>
<a id="tocsvm_get_available_memory_0"></a>

```json
false

```

overcommit

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|overcommit|boolean|false|none|none|

<h2 id="tocS_vm_get_console_0">vm_get_console_0</h2>

<a id="schemavm_get_console_0"></a>
<a id="schema_vm_get_console_0"></a>
<a id="tocSvm_get_console_0"></a>
<a id="tocsvm_get_console_0"></a>

```json
0

```

id

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|id|integer|false|none|none|

<h2 id="tocS_vm_get_vnc_0">vm_get_vnc_0</h2>

<a id="schemavm_get_vnc_0"></a>
<a id="schema_vm_get_vnc_0"></a>
<a id="tocSvm_get_vnc_0"></a>
<a id="tocsvm_get_vnc_0"></a>

```json
0

```

id

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|id|integer|false|none|none|

<h2 id="tocS_vm_poweroff">vm_poweroff</h2>

<a id="schemavm_poweroff"></a>
<a id="schema_vm_poweroff"></a>
<a id="tocSvm_poweroff"></a>
<a id="tocsvm_poweroff"></a>

```json
{}

```

### Properties

*None*

<h2 id="tocS_vm_restart">vm_restart</h2>

<a id="schemavm_restart"></a>
<a id="schema_vm_restart"></a>
<a id="tocSvm_restart"></a>
<a id="tocsvm_restart"></a>

```json
{}

```

### Properties

*None*

<h2 id="tocS_vm_start_1">vm_start_1</h2>

<a id="schemavm_start_1"></a>
<a id="schema_vm_start_1"></a>
<a id="tocSvm_start_1"></a>
<a id="tocsvm_start_1"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|overcommit|boolean|false|none|none|

<h2 id="tocS_vm_status">vm_status</h2>

<a id="schemavm_status"></a>
<a id="schema_vm_status"></a>
<a id="tocSvm_status"></a>
<a id="tocsvm_status"></a>

```json
{}

```

### Properties

*None*

<h2 id="tocS_vm_stop_1">vm_stop_1</h2>

<a id="schemavm_stop_1"></a>
<a id="schema_vm_stop_1"></a>
<a id="tocSvm_stop_1"></a>
<a id="tocsvm_stop_1"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|force|boolean|false|none|none|
|force_after_timeout|boolean|false|none|none|

<h2 id="tocS_vm_device_create_0">vm_device_create_0</h2>

<a id="schemavm_device_create_0"></a>
<a id="schema_vm_device_create_0"></a>
<a id="tocSvm_device_create_0"></a>
<a id="tocsvm_device_create_0"></a>

```json
{}

```

vmdevice_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|dtype|string|false|none|none|
|vm|integer|false|none|none|
|attributes|object|false|none|none|
|order|integerÂ¦null|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|dtype|NIC|
|dtype|DISK|
|dtype|CDROM|
|dtype|PCI|
|dtype|VNC|
|dtype|RAW|

<h2 id="tocS_vm_device_delete_1">vm_device_delete_1</h2>

<a id="schemavm_device_delete_1"></a>
<a id="schema_vm_device_delete_1"></a>
<a id="tocSvm_device_delete_1"></a>
<a id="tocsvm_device_delete_1"></a>

```json
{}

```

vm_device_delete

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|zvol|boolean|false|none|none|
|raw_file|boolean|false|none|none|

<h2 id="tocS_vm_device_update_1">vm_device_update_1</h2>

<a id="schemavm_device_update_1"></a>
<a id="schema_vm_device_update_1"></a>
<a id="tocSvm_device_update_1"></a>
<a id="tocsvm_device_update_1"></a>

```json
{}

```

vmdevice_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|dtype|string|false|none|none|
|vm|integer|false|none|none|
|attributes|object|false|none|none|
|order|integerÂ¦null|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|dtype|NIC|
|dtype|DISK|
|dtype|CDROM|
|dtype|PCI|
|dtype|VNC|
|dtype|RAW|

<h2 id="tocS_vmware_create_0">vmware_create_0</h2>

<a id="schemavmware_create_0"></a>
<a id="schema_vmware_create_0"></a>
<a id="tocSvmware_create_0"></a>
<a id="tocsvmware_create_0"></a>

```json
{}

```

vmware_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|datastore|string|false|none|none|
|filesystem|string|false|none|none|
|hostname|string|false|none|none|
|password|string|false|none|none|
|username|string|false|none|none|

<h2 id="tocS_vmware_update_1">vmware_update_1</h2>

<a id="schemavmware_update_1"></a>
<a id="schema_vmware_update_1"></a>
<a id="tocSvmware_update_1"></a>
<a id="tocsvmware_update_1"></a>

```json
{}

```

vmware_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|datastore|string|false|none|none|
|filesystem|string|false|none|none|
|hostname|string|false|none|none|
|password|string|false|none|none|
|username|string|false|none|none|

<h2 id="tocS_vmware_dataset_has_vms_0">vmware_dataset_has_vms_0</h2>

<a id="schemavmware_dataset_has_vms_0"></a>
<a id="schema_vmware_dataset_has_vms_0"></a>
<a id="tocSvmware_dataset_has_vms_0"></a>
<a id="tocsvmware_dataset_has_vms_0"></a>

```json
"string"

```

dataset

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|dataset|string|false|none|none|

<h2 id="tocS_vmware_dataset_has_vms_1">vmware_dataset_has_vms_1</h2>

<a id="schemavmware_dataset_has_vms_1"></a>
<a id="schema_vmware_dataset_has_vms_1"></a>
<a id="tocSvmware_dataset_has_vms_1"></a>
<a id="tocsvmware_dataset_has_vms_1"></a>

```json
true

```

recursive

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|recursive|boolean|false|none|none|

<h2 id="tocS_vmware_dataset_has_vms">vmware_dataset_has_vms</h2>

<a id="schemavmware_dataset_has_vms"></a>
<a id="schema_vmware_dataset_has_vms"></a>
<a id="tocSvmware_dataset_has_vms"></a>
<a id="tocsvmware_dataset_has_vms"></a>

```json
{
  "dataset": "string",
  "recursive": true
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|dataset|[vmware_dataset_has_vms_0](#schemavmware_dataset_has_vms_0)|false|none|none|
|recursive|[vmware_dataset_has_vms_1](#schemavmware_dataset_has_vms_1)|false|none|none|

<h2 id="tocS_vmware_get_datastores_0">vmware_get_datastores_0</h2>

<a id="schemavmware_get_datastores_0"></a>
<a id="schema_vmware_get_datastores_0"></a>
<a id="tocSvmware_get_datastores_0"></a>
<a id="tocsvmware_get_datastores_0"></a>

```json
{}

```

vmware-creds

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|hostname|string|false|none|none|
|username|string|false|none|none|
|password|string|false|none|none|

<h2 id="tocS_vmware_get_virtual_machines_0">vmware_get_virtual_machines_0</h2>

<a id="schemavmware_get_virtual_machines_0"></a>
<a id="schema_vmware_get_virtual_machines_0"></a>
<a id="tocSvmware_get_virtual_machines_0"></a>
<a id="tocsvmware_get_virtual_machines_0"></a>

```json
0

```

pk

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|pk|integer|false|none|none|

<h2 id="tocS_vmware_match_datastores_with_datasets_0">vmware_match_datastores_with_datasets_0</h2>

<a id="schemavmware_match_datastores_with_datasets_0"></a>
<a id="schema_vmware_match_datastores_with_datasets_0"></a>
<a id="tocSvmware_match_datastores_with_datasets_0"></a>
<a id="tocsvmware_match_datastores_with_datasets_0"></a>

```json
{}

```

vmware-creds

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|hostname|string|false|none|none|
|username|string|false|none|none|
|password|string|false|none|none|

<h2 id="tocS_webdav_update_0">webdav_update_0</h2>

<a id="schemawebdav_update_0"></a>
<a id="schema_webdav_update_0"></a>
<a id="tocSwebdav_update_0"></a>
<a id="tocswebdav_update_0"></a>

```json
{}

```

webdav_update

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|protocol|string|false|none|none|
|tcpport|integer|false|none|none|
|tcpportssl|integer|false|none|none|
|password|string|false|none|none|
|htauth|string|false|none|none|
|certssl|integerÂ¦null|false|none|none|

#### Enumerated Values

|Property|Value|
|---|---|
|protocol|HTTP|
|protocol|HTTPS|
|protocol|HTTPHTTPS|
|htauth|NONE|
|htauth|BASIC|
|htauth|DIGEST|

<h2 id="tocS_zfs_snapshot_create_0">zfs_snapshot_create_0</h2>

<a id="schemazfs_snapshot_create_0"></a>
<a id="schema_zfs_snapshot_create_0"></a>
<a id="tocSzfs_snapshot_create_0"></a>
<a id="tocszfs_snapshot_create_0"></a>

```json
{}

```

snapshot_create

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|dataset|string|false|none|none|
|name|string|false|none|none|
|naming_schema|string|false|none|none|
|recursive|boolean|false|none|none|
|vmware_sync|boolean|false|none|none|
|properties|object|false|none|none|

<h2 id="tocS_zfs_snapshot_delete_1">zfs_snapshot_delete_1</h2>

<a id="schemazfs_snapshot_delete_1"></a>
<a id="schema_zfs_snapshot_delete_1"></a>
<a id="tocSzfs_snapshot_delete_1"></a>
<a id="tocszfs_snapshot_delete_1"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|defer|boolean|false|none|none|

<h2 id="tocS_zfs_snapshot_clone_0">zfs_snapshot_clone_0</h2>

<a id="schemazfs_snapshot_clone_0"></a>
<a id="schema_zfs_snapshot_clone_0"></a>
<a id="tocSzfs_snapshot_clone_0"></a>
<a id="tocszfs_snapshot_clone_0"></a>

```json
{}

```

snapshot_clone

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|snapshot|string|false|none|none|
|dataset_dst|string|false|none|none|

<h2 id="tocS_zfs_snapshot_remove_0">zfs_snapshot_remove_0</h2>

<a id="schemazfs_snapshot_remove_0"></a>
<a id="schema_zfs_snapshot_remove_0"></a>
<a id="tocSzfs_snapshot_remove_0"></a>
<a id="tocszfs_snapshot_remove_0"></a>

```json
{}

```

snapshot_remove

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|dataset|string|false|none|none|
|name|string|false|none|none|
|defer_delete|boolean|false|none|none|

<h2 id="tocS_zfs_snapshot_rollback_0">zfs_snapshot_rollback_0</h2>

<a id="schemazfs_snapshot_rollback_0"></a>
<a id="schema_zfs_snapshot_rollback_0"></a>
<a id="tocSzfs_snapshot_rollback_0"></a>
<a id="tocszfs_snapshot_rollback_0"></a>

```json
"string"

```

id

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|id|string|false|none|none|

<h2 id="tocS_zfs_snapshot_rollback_1">zfs_snapshot_rollback_1</h2>

<a id="schemazfs_snapshot_rollback_1"></a>
<a id="schema_zfs_snapshot_rollback_1"></a>
<a id="tocSzfs_snapshot_rollback_1"></a>
<a id="tocszfs_snapshot_rollback_1"></a>

```json
{}

```

options

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|recursive|boolean|false|none|none|
|recursive_clones|boolean|false|none|none|
|force|boolean|false|none|none|

<h2 id="tocS_zfs_snapshot_rollback">zfs_snapshot_rollback</h2>

<a id="schemazfs_snapshot_rollback"></a>
<a id="schema_zfs_snapshot_rollback"></a>
<a id="tocSzfs_snapshot_rollback"></a>
<a id="tocszfs_snapshot_rollback"></a>

```json
{
  "id": "string",
  "options": {}
}

```

### Properties

|Name|Type|Required|Restrictions|Description|
|---|---|---|---|---|
|id|[zfs_snapshot_rollback_0](#schemazfs_snapshot_rollback_0)|false|none|none|
|options|[zfs_snapshot_rollback_1](#schemazfs_snapshot_rollback_1)|false|none|none|

